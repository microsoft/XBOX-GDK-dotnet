# <a id="GDK_Net_PlayFab_Party_PartyThreadId"></a> Enum PartyThreadId

Namespace: [GDK.Net.PlayFab.Party](GDK.Net.PlayFab.Party.md)  
Assembly: GDK.Net.dll  

Identifies one of the Party library's internal threads.

```csharp
public enum PartyThreadId : uint
```

## Fields

`Audio = 0` 

The thread that performs audio processing.



`Networking = 1` 

The thread that performs networking.



