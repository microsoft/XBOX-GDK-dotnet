# <a id="GDK_Net_Users_UserPrivilegeDenyReason"></a> Enum UserPrivilegeDenyReason

Namespace: [GDK.Net.Users](GDK.Net.Users.md)  
Assembly: GDK.Net.dll  

Mirrors <code>XUserPrivilegeDenyReason</code>.

```csharp
public enum UserPrivilegeDenyReason : uint
```

## Fields

`Banned = 3` 

The account is banned from using the privilege.



`None = 0` 

The privilege is not denied.



`PurchaseRequired = 1` 

A subscription purchase is required before the privilege is granted.



`Restricted = 2` 

The privilege is restricted, typically by parental controls or account settings.



`Unknown = 4294967295` 

The reason could not be determined.



