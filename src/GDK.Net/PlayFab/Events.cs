// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFEvents
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Threading;
using System.Threading.Tasks;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>The PlayFab Events service (<c>PFEvents.h</c>).</summary>
public static unsafe class Events
{
    /// <summary>Calls <c>PFEventsWriteEventsAsync</c>.</summary>
    public static Task<EventsWriteEventsResponse> WriteEventsAsync(
        PlayFabEntity entity,
        EventsWriteEventsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFEventsWriteEventsRequest* nativeRequest = arena.Alloc<PFEventsWriteEventsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFEventsWriteEventsAsync(context, (PFEventsWriteEventsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out EventsWriteEventsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFEventsWriteEventsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFEventsWriteEventsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFEventsWriteEventsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = EventsWriteEventsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFEventsWriteTelemetryEventsAsync</c>.</summary>
    public static Task<EventsWriteEventsResponse> WriteTelemetryEventsAsync(
        PlayFabEntity entity,
        EventsWriteEventsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFEventsWriteEventsRequest* nativeRequest = arena.Alloc<PFEventsWriteEventsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFEventsWriteTelemetryEventsAsync(context, (PFEventsWriteEventsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out EventsWriteEventsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFEventsWriteTelemetryEventsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFEventsWriteEventsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFEventsWriteTelemetryEventsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = EventsWriteEventsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }
}
