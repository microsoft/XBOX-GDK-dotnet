// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFTitleDataManagement
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Threading;
using System.Threading.Tasks;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>The PlayFab TitleDataManagement service (<c>PFTitleDataManagement.h</c>).</summary>
public static unsafe class TitleDataManagement
{
    /// <summary>Calls <c>PFTitleDataManagementClientGetPublisherDataAsync</c>.</summary>
    public static Task<TitleDataManagementGetPublisherDataResult> ClientGetPublisherDataAsync(
        PlayFabEntity entity,
        TitleDataManagementGetPublisherDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementGetPublisherDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementGetPublisherDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementClientGetPublisherDataAsync(context, (PFTitleDataManagementGetPublisherDataRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetPublisherDataResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFTitleDataManagementClientGetPublisherDataGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFTitleDataManagementGetPublisherDataResult* result;
                        nuint used;
                        hr = NativePlayFab.PFTitleDataManagementClientGetPublisherDataGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = TitleDataManagementGetPublisherDataResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementClientGetTimeAsync</c>.</summary>
    public static Task<TitleDataManagementGetTimeResult> ClientGetTimeAsync(
        PlayFabEntity entity,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementClientGetTimeAsync(context, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetTimeResult value) =>
                {
                    value = null!;
                    PFTitleDataManagementGetTimeResult native = default;
                    int hr = NativePlayFab.PFTitleDataManagementClientGetTimeGetResult((XAsyncBlock*)block, &native);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    value = TitleDataManagementGetTimeResult.FromNative(&native);
                    return HResult.SOk;
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementClientGetTitleDataAsync</c>.</summary>
    public static Task<TitleDataManagementGetTitleDataResult> ClientGetTitleDataAsync(
        PlayFabEntity entity,
        TitleDataManagementGetTitleDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementGetTitleDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementGetTitleDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementClientGetTitleDataAsync(context, (PFTitleDataManagementGetTitleDataRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetTitleDataResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFTitleDataManagementClientGetTitleDataGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFTitleDataManagementGetTitleDataResult* result;
                        nuint used;
                        hr = NativePlayFab.PFTitleDataManagementClientGetTitleDataGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = TitleDataManagementGetTitleDataResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementClientGetTitleNewsAsync</c>.</summary>
    public static Task<TitleDataManagementGetTitleNewsResult> ClientGetTitleNewsAsync(
        PlayFabEntity entity,
        TitleDataManagementGetTitleNewsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementGetTitleNewsRequest* nativeRequest = arena.Alloc<PFTitleDataManagementGetTitleNewsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementClientGetTitleNewsAsync(context, (PFTitleDataManagementGetTitleNewsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetTitleNewsResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFTitleDataManagementClientGetTitleNewsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFTitleDataManagementGetTitleNewsResult* result;
                        nuint used;
                        hr = NativePlayFab.PFTitleDataManagementClientGetTitleNewsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = TitleDataManagementGetTitleNewsResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerGetPublisherDataAsync</c>.</summary>
    public static Task<TitleDataManagementGetPublisherDataResult> ServerGetPublisherDataAsync(
        PlayFabEntity entity,
        TitleDataManagementGetPublisherDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementGetPublisherDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementGetPublisherDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerGetPublisherDataAsync(context, (PFTitleDataManagementGetPublisherDataRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetPublisherDataResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFTitleDataManagementServerGetPublisherDataGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFTitleDataManagementGetPublisherDataResult* result;
                        nuint used;
                        hr = NativePlayFab.PFTitleDataManagementServerGetPublisherDataGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = TitleDataManagementGetPublisherDataResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerGetTimeAsync</c>.</summary>
    public static Task<TitleDataManagementGetTimeResult> ServerGetTimeAsync(
        PlayFabEntity entity,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerGetTimeAsync(context, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetTimeResult value) =>
                {
                    value = null!;
                    PFTitleDataManagementGetTimeResult native = default;
                    int hr = NativePlayFab.PFTitleDataManagementServerGetTimeGetResult((XAsyncBlock*)block, &native);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    value = TitleDataManagementGetTimeResult.FromNative(&native);
                    return HResult.SOk;
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerGetTitleDataAsync</c>.</summary>
    public static Task<TitleDataManagementGetTitleDataResult> ServerGetTitleDataAsync(
        PlayFabEntity entity,
        TitleDataManagementGetTitleDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementGetTitleDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementGetTitleDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerGetTitleDataAsync(context, (PFTitleDataManagementGetTitleDataRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetTitleDataResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFTitleDataManagementServerGetTitleDataGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFTitleDataManagementGetTitleDataResult* result;
                        nuint used;
                        hr = NativePlayFab.PFTitleDataManagementServerGetTitleDataGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = TitleDataManagementGetTitleDataResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerGetTitleInternalDataAsync</c>.</summary>
    public static Task<TitleDataManagementGetTitleDataResult> ServerGetTitleInternalDataAsync(
        PlayFabEntity entity,
        TitleDataManagementGetTitleDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementGetTitleDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementGetTitleDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerGetTitleInternalDataAsync(context, (PFTitleDataManagementGetTitleDataRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetTitleDataResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFTitleDataManagementServerGetTitleInternalDataGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFTitleDataManagementGetTitleDataResult* result;
                        nuint used;
                        hr = NativePlayFab.PFTitleDataManagementServerGetTitleInternalDataGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = TitleDataManagementGetTitleDataResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerGetTitleNewsAsync</c>.</summary>
    public static Task<TitleDataManagementGetTitleNewsResult> ServerGetTitleNewsAsync(
        PlayFabEntity entity,
        TitleDataManagementGetTitleNewsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementGetTitleNewsRequest* nativeRequest = arena.Alloc<PFTitleDataManagementGetTitleNewsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerGetTitleNewsAsync(context, (PFTitleDataManagementGetTitleNewsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out TitleDataManagementGetTitleNewsResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFTitleDataManagementServerGetTitleNewsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFTitleDataManagementGetTitleNewsResult* result;
                        nuint used;
                        hr = NativePlayFab.PFTitleDataManagementServerGetTitleNewsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = TitleDataManagementGetTitleNewsResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerSetPublisherDataAsync</c>.</summary>
    public static Task ServerSetPublisherDataAsync(
        PlayFabEntity entity,
        TitleDataManagementSetPublisherDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementSetPublisherDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementSetPublisherDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerSetPublisherDataAsync(context, (PFTitleDataManagementSetPublisherDataRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerSetTitleDataAsync</c>.</summary>
    public static Task ServerSetTitleDataAsync(
        PlayFabEntity entity,
        TitleDataManagementSetTitleDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementSetTitleDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementSetTitleDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerSetTitleDataAsync(context, (PFTitleDataManagementSetTitleDataRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFTitleDataManagementServerSetTitleInternalDataAsync</c>.</summary>
    public static Task ServerSetTitleInternalDataAsync(
        PlayFabEntity entity,
        TitleDataManagementSetTitleDataRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFTitleDataManagementSetTitleDataRequest* nativeRequest = arena.Alloc<PFTitleDataManagementSetTitleDataRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFTitleDataManagementServerSetTitleInternalDataAsync(context, (PFTitleDataManagementSetTitleDataRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }
}
