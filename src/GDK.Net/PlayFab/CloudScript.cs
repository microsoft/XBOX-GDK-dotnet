// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFCloudScript
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Threading;
using System.Threading.Tasks;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>The PlayFab CloudScript service (<c>PFCloudScript.h</c>).</summary>
public static unsafe class CloudScript
{
    /// <summary>Calls <c>PFCloudScriptClientExecuteCloudScriptAsync</c>.</summary>
    public static Task<CloudScriptExecuteCloudScriptResult> ClientExecuteCloudScriptAsync(
        PlayFabEntity entity,
        CloudScriptExecuteCloudScriptRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFCloudScriptExecuteCloudScriptRequest* nativeRequest = arena.Alloc<PFCloudScriptExecuteCloudScriptRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFCloudScriptClientExecuteCloudScriptAsync(context, (PFCloudScriptExecuteCloudScriptRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out CloudScriptExecuteCloudScriptResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFCloudScriptClientExecuteCloudScriptGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFCloudScriptExecuteCloudScriptResult* result;
                        nuint used;
                        hr = NativePlayFab.PFCloudScriptClientExecuteCloudScriptGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = CloudScriptExecuteCloudScriptResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFCloudScriptExecuteEntityCloudScriptAsync</c>.</summary>
    public static Task<CloudScriptExecuteCloudScriptResult> ExecuteEntityCloudScriptAsync(
        PlayFabEntity entity,
        CloudScriptExecuteEntityCloudScriptRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFCloudScriptExecuteEntityCloudScriptRequest* nativeRequest = arena.Alloc<PFCloudScriptExecuteEntityCloudScriptRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFCloudScriptExecuteEntityCloudScriptAsync(context, (PFCloudScriptExecuteEntityCloudScriptRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out CloudScriptExecuteCloudScriptResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFCloudScriptExecuteEntityCloudScriptGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFCloudScriptExecuteCloudScriptResult* result;
                        nuint used;
                        hr = NativePlayFab.PFCloudScriptExecuteEntityCloudScriptGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = CloudScriptExecuteCloudScriptResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFCloudScriptExecuteFunctionAsync</c>.</summary>
    public static Task<CloudScriptExecuteFunctionResult> ExecuteFunctionAsync(
        PlayFabEntity entity,
        CloudScriptExecuteFunctionRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFCloudScriptExecuteFunctionRequest* nativeRequest = arena.Alloc<PFCloudScriptExecuteFunctionRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFCloudScriptExecuteFunctionAsync(context, (PFCloudScriptExecuteFunctionRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out CloudScriptExecuteFunctionResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFCloudScriptExecuteFunctionGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFCloudScriptExecuteFunctionResult* result;
                        nuint used;
                        hr = NativePlayFab.PFCloudScriptExecuteFunctionGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = CloudScriptExecuteFunctionResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFCloudScriptServerExecuteCloudScriptAsync</c>.</summary>
    public static Task<CloudScriptExecuteCloudScriptResult> ServerExecuteCloudScriptAsync(
        PlayFabEntity entity,
        CloudScriptExecuteCloudScriptServerRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFCloudScriptExecuteCloudScriptServerRequest* nativeRequest = arena.Alloc<PFCloudScriptExecuteCloudScriptServerRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFCloudScriptServerExecuteCloudScriptAsync(context, (PFCloudScriptExecuteCloudScriptServerRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out CloudScriptExecuteCloudScriptResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFCloudScriptServerExecuteCloudScriptGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFCloudScriptExecuteCloudScriptResult* result;
                        nuint used;
                        hr = NativePlayFab.PFCloudScriptServerExecuteCloudScriptGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = CloudScriptExecuteCloudScriptResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }
}
