// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFGroups
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFGroupsAcceptGroupApplicationRequest</c>.</summary>
public sealed class GroupsAcceptGroupApplicationRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsAcceptGroupApplicationRequest FromNative(PFGroupsAcceptGroupApplicationRequest* source)
    {
        var value = new GroupsAcceptGroupApplicationRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsAcceptGroupApplicationRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
    }
}

/// <summary>Projects <c>PFGroupsAcceptGroupInvitationRequest</c>.</summary>
public sealed class GroupsAcceptGroupInvitationRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsAcceptGroupInvitationRequest FromNative(PFGroupsAcceptGroupInvitationRequest* source)
    {
        var value = new GroupsAcceptGroupInvitationRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsAcceptGroupInvitationRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
    }
}

/// <summary>Projects <c>PFGroupsAddMembersRequest</c>.</summary>
public sealed class GroupsAddMembersRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>Members</c>.</summary>
    public IReadOnlyList<EntityKey>? Members { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    internal static unsafe GroupsAddMembersRequest FromNative(PFGroupsAddMembersRequest* source)
    {
        var value = new GroupsAddMembersRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        if (source->Members is not null && source->MembersCount != 0)
        {
            var list3 = new List<EntityKey>((int)source->MembersCount);
            for (uint i3 = 0; i3 < source->MembersCount; i3++)
            {
                list3.Add(EntityKey.FromNative(source->Members[i3]));
            }

            value.Members = list3;
        }
        value.RoleId = Utf8.ToString(source->RoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsAddMembersRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
        target->MembersCount = (uint)(Members is null ? 0 : Members.Count);
        if (Members is not null && Members.Count > 0)
        {
            PFEntityKey** array3 = arena.PointerArray<PFEntityKey>(Members.Count);
            for (int i3 = 0; i3 < Members.Count; i3++)
            {
                PFEntityKey* item3 = arena.Alloc<PFEntityKey>(1);
                Members[i3].WriteTo(item3, arena);
                array3[i3] = item3;
            }

            target->Members = array3;
        }
        target->RoleId = arena.String(RoleId);
    }
}

/// <summary>Projects <c>PFGroupsApplyToGroupRequest</c>.</summary>
public sealed class GroupsApplyToGroupRequest
{
    /// <summary><c>AutoAcceptOutstandingInvite</c>.</summary>
    public bool? AutoAcceptOutstandingInvite { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsApplyToGroupRequest FromNative(PFGroupsApplyToGroupRequest* source)
    {
        var value = new GroupsApplyToGroupRequest();
        value.AutoAcceptOutstandingInvite = source->AutoAcceptOutstandingInvite is null ? null : (bool?)(*source->AutoAcceptOutstandingInvite != 0);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsApplyToGroupRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->AutoAcceptOutstandingInvite = arena.Value(AutoAcceptOutstandingInvite is null ? null : (AutoAcceptOutstandingInvite.Value ? (byte?)1 : (byte?)0));
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        if (Group is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native4, arena);
            target->Group = native4;
        }
    }
}

/// <summary>Projects <c>PFGroupsApplyToGroupResponse</c>.</summary>
public sealed class GroupsApplyToGroupResponse
{
    /// <summary><c>Entity</c>.</summary>
    public GroupsEntityWithLineage? Entity { get; set; }

    /// <summary><c>Expires</c>.</summary>
    public DateTimeOffset Expires { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsApplyToGroupResponse FromNative(PFGroupsApplyToGroupResponse* source)
    {
        var value = new GroupsApplyToGroupResponse();
        value.Entity = source->Entity is null ? null : GroupsEntityWithLineage.FromNative(source->Entity);
        value.Expires = PlayFabTime.ToDateTimeOffset(source->Expires);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsApplyToGroupResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFGroupsEntityWithLineage* native0 = arena.Alloc<PFGroupsEntityWithLineage>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->Expires = PlayFabTime.ToUnixTime(Expires);
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsBlockEntityRequest</c>.</summary>
public sealed class GroupsBlockEntityRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsBlockEntityRequest FromNative(PFGroupsBlockEntityRequest* source)
    {
        var value = new GroupsBlockEntityRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsBlockEntityRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
    }
}

/// <summary>Projects <c>PFGroupsChangeMemberRoleRequest</c>.</summary>
public sealed class GroupsChangeMemberRoleRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DestinationRoleId</c>.</summary>
    public string? DestinationRoleId { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>Members</c>.</summary>
    public IReadOnlyList<EntityKey>? Members { get; set; }

    /// <summary><c>OriginRoleId</c>.</summary>
    public string? OriginRoleId { get; set; }

    internal static unsafe GroupsChangeMemberRoleRequest FromNative(PFGroupsChangeMemberRoleRequest* source)
    {
        var value = new GroupsChangeMemberRoleRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.DestinationRoleId = Utf8.ToString(source->DestinationRoleId);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        if (source->Members is not null && source->MembersCount != 0)
        {
            var list4 = new List<EntityKey>((int)source->MembersCount);
            for (uint i4 = 0; i4 < source->MembersCount; i4++)
            {
                list4.Add(EntityKey.FromNative(source->Members[i4]));
            }

            value.Members = list4;
        }
        value.OriginRoleId = Utf8.ToString(source->OriginRoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsChangeMemberRoleRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DestinationRoleId = arena.String(DestinationRoleId);
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
        target->MembersCount = (uint)(Members is null ? 0 : Members.Count);
        if (Members is not null && Members.Count > 0)
        {
            PFEntityKey** array4 = arena.PointerArray<PFEntityKey>(Members.Count);
            for (int i4 = 0; i4 < Members.Count; i4++)
            {
                PFEntityKey* item4 = arena.Alloc<PFEntityKey>(1);
                Members[i4].WriteTo(item4, arena);
                array4[i4] = item4;
            }

            target->Members = array4;
        }
        target->OriginRoleId = arena.String(OriginRoleId);
    }
}

/// <summary>Projects <c>PFGroupsCreateGroupRequest</c>.</summary>
public sealed class GroupsCreateGroupRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>GroupName</c>.</summary>
    public string? GroupName { get; set; }

    internal static unsafe GroupsCreateGroupRequest FromNative(PFGroupsCreateGroupRequest* source)
    {
        var value = new GroupsCreateGroupRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.GroupName = Utf8.ToString(source->GroupName);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsCreateGroupRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->GroupName = arena.String(GroupName);
    }
}

/// <summary>Projects <c>PFGroupsCreateGroupResponse</c>.</summary>
public sealed class GroupsCreateGroupResponse
{
    /// <summary><c>AdminRoleId</c>.</summary>
    public string? AdminRoleId { get; set; }

    /// <summary><c>Created</c>.</summary>
    public DateTimeOffset Created { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>GroupName</c>.</summary>
    public string? GroupName { get; set; }

    /// <summary><c>MemberRoleId</c>.</summary>
    public string? MemberRoleId { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>Roles</c>.</summary>
    public IReadOnlyDictionary<string, string>? Roles { get; set; }

    internal static unsafe GroupsCreateGroupResponse FromNative(PFGroupsCreateGroupResponse* source)
    {
        var value = new GroupsCreateGroupResponse();
        value.AdminRoleId = Utf8.ToString(source->AdminRoleId);
        value.Created = PlayFabTime.ToDateTimeOffset(source->Created);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.GroupName = Utf8.ToString(source->GroupName);
        value.MemberRoleId = Utf8.ToString(source->MemberRoleId);
        value.ProfileVersion = source->ProfileVersion;
        if (source->Roles is not null && source->RolesCount != 0)
        {
            var map6 = new Dictionary<string, string>((int)source->RolesCount, StringComparer.Ordinal);
            for (uint i6 = 0; i6 < source->RolesCount; i6++)
            {
                map6[Utf8.ToString(source->Roles[i6].Key) ?? string.Empty] = Utf8.ToString(source->Roles[i6].Value) ?? string.Empty;
            }

            value.Roles = map6;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsCreateGroupResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->AdminRoleId = arena.String(AdminRoleId);
        target->Created = PlayFabTime.ToUnixTime(Created);
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
        target->GroupName = arena.String(GroupName);
        target->MemberRoleId = arena.String(MemberRoleId);
        target->ProfileVersion = ProfileVersion;
        target->RolesCount = (uint)(Roles is null ? 0 : Roles.Count);
        if (Roles is not null && Roles.Count > 0)
        {
            PFStringDictionaryEntry* entries6 = arena.Alloc<PFStringDictionaryEntry>(Roles.Count);
            int next6 = 0;
            foreach (KeyValuePair<string, string> pair6 in Roles)
            {
                entries6[next6].Key = arena.String(pair6.Key);
                entries6[next6].Value = arena.String(pair6.Value);
                next6++;
            }

            target->Roles = entries6;
        }
    }
}

/// <summary>Projects <c>PFGroupsCreateGroupRoleRequest</c>.</summary>
public sealed class GroupsCreateGroupRoleRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    /// <summary><c>RoleName</c>.</summary>
    public string? RoleName { get; set; }

    internal static unsafe GroupsCreateGroupRoleRequest FromNative(PFGroupsCreateGroupRoleRequest* source)
    {
        var value = new GroupsCreateGroupRoleRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.RoleId = Utf8.ToString(source->RoleId);
        value.RoleName = Utf8.ToString(source->RoleName);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsCreateGroupRoleRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
        target->RoleId = arena.String(RoleId);
        target->RoleName = arena.String(RoleName);
    }
}

/// <summary>Projects <c>PFGroupsCreateGroupRoleResponse</c>.</summary>
public sealed class GroupsCreateGroupRoleResponse
{
    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    /// <summary><c>RoleName</c>.</summary>
    public string? RoleName { get; set; }

    internal static unsafe GroupsCreateGroupRoleResponse FromNative(PFGroupsCreateGroupRoleResponse* source)
    {
        var value = new GroupsCreateGroupRoleResponse();
        value.ProfileVersion = source->ProfileVersion;
        value.RoleId = Utf8.ToString(source->RoleId);
        value.RoleName = Utf8.ToString(source->RoleName);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsCreateGroupRoleResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ProfileVersion = ProfileVersion;
        target->RoleId = arena.String(RoleId);
        target->RoleName = arena.String(RoleName);
    }
}

/// <summary>Projects <c>PFGroupsDeleteGroupRequest</c>.</summary>
public sealed class GroupsDeleteGroupRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsDeleteGroupRequest FromNative(PFGroupsDeleteGroupRequest* source)
    {
        var value = new GroupsDeleteGroupRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsDeleteGroupRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsDeleteRoleRequest</c>.</summary>
public sealed class GroupsDeleteRoleRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    internal static unsafe GroupsDeleteRoleRequest FromNative(PFGroupsDeleteRoleRequest* source)
    {
        var value = new GroupsDeleteRoleRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.RoleId = Utf8.ToString(source->RoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsDeleteRoleRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
        target->RoleId = arena.String(RoleId);
    }
}

/// <summary>Projects <c>PFGroupsEntityMemberRole</c>.</summary>
public sealed class GroupsEntityMemberRole
{
    /// <summary><c>Members</c>.</summary>
    public IReadOnlyList<GroupsEntityWithLineage>? Members { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    /// <summary><c>RoleName</c>.</summary>
    public string? RoleName { get; set; }

    internal static unsafe GroupsEntityMemberRole FromNative(PFGroupsEntityMemberRole* source)
    {
        var value = new GroupsEntityMemberRole();
        if (source->Members is not null && source->MembersCount != 0)
        {
            var list0 = new List<GroupsEntityWithLineage>((int)source->MembersCount);
            for (uint i0 = 0; i0 < source->MembersCount; i0++)
            {
                list0.Add(GroupsEntityWithLineage.FromNative(source->Members[i0]));
            }

            value.Members = list0;
        }
        value.RoleId = Utf8.ToString(source->RoleId);
        value.RoleName = Utf8.ToString(source->RoleName);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsEntityMemberRole* target, PlayFabArena arena)
    {
        *target = default;
        target->MembersCount = (uint)(Members is null ? 0 : Members.Count);
        if (Members is not null && Members.Count > 0)
        {
            PFGroupsEntityWithLineage** array0 = arena.PointerArray<PFGroupsEntityWithLineage>(Members.Count);
            for (int i0 = 0; i0 < Members.Count; i0++)
            {
                PFGroupsEntityWithLineage* item0 = arena.Alloc<PFGroupsEntityWithLineage>(1);
                Members[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Members = array0;
        }
        target->RoleId = arena.String(RoleId);
        target->RoleName = arena.String(RoleName);
    }
}

/// <summary>Projects <c>PFGroupsEntityWithLineage</c>.</summary>
public sealed class GroupsEntityWithLineage
{
    /// <summary><c>Key</c>.</summary>
    public EntityKey? Key { get; set; }

    /// <summary><c>Lineage</c>.</summary>
    public IReadOnlyDictionary<string, EntityKey>? Lineage { get; set; }

    internal static unsafe GroupsEntityWithLineage FromNative(PFGroupsEntityWithLineage* source)
    {
        var value = new GroupsEntityWithLineage();
        value.Key = source->Key is null ? null : EntityKey.FromNative(source->Key);
        if (source->Lineage is not null && source->LineageCount != 0)
        {
            var map1 = new Dictionary<string, EntityKey>((int)source->LineageCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->LineageCount; i1++)
            {
                map1[Utf8.ToString(source->Lineage[i1].Key) ?? string.Empty] = EntityKey.FromNative(source->Lineage[i1].Value);
            }

            value.Lineage = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsEntityWithLineage* target, PlayFabArena arena)
    {
        *target = default;
        if (Key is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Key.WriteTo(native0, arena);
            target->Key = native0;
        }
        target->LineageCount = (uint)(Lineage is null ? 0 : Lineage.Count);
        if (Lineage is not null && Lineage.Count > 0)
        {
            PFEntityKeyDictionaryEntry* entries1 = arena.Alloc<PFEntityKeyDictionaryEntry>(Lineage.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, EntityKey> pair1 in Lineage)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                if (pair1.Value is not null)
                {
                    PFEntityKey* item1 = arena.Alloc<PFEntityKey>(1);
                    pair1.Value.WriteTo(item1, arena);
                    entries1[next1].Value = item1;
                }
                next1++;
            }

            target->Lineage = entries1;
        }
    }
}

/// <summary>Projects <c>PFGroupsGetGroupRequest</c>.</summary>
public sealed class GroupsGetGroupRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>GroupName</c>.</summary>
    public string? GroupName { get; set; }

    internal static unsafe GroupsGetGroupRequest FromNative(PFGroupsGetGroupRequest* source)
    {
        var value = new GroupsGetGroupRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.GroupName = Utf8.ToString(source->GroupName);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsGetGroupRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
        target->GroupName = arena.String(GroupName);
    }
}

/// <summary>Projects <c>PFGroupsGetGroupResponse</c>.</summary>
public sealed class GroupsGetGroupResponse
{
    /// <summary><c>AdminRoleId</c>.</summary>
    public string? AdminRoleId { get; set; }

    /// <summary><c>Created</c>.</summary>
    public DateTimeOffset Created { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>GroupName</c>.</summary>
    public string? GroupName { get; set; }

    /// <summary><c>MemberRoleId</c>.</summary>
    public string? MemberRoleId { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>Roles</c>.</summary>
    public IReadOnlyDictionary<string, string>? Roles { get; set; }

    internal static unsafe GroupsGetGroupResponse FromNative(PFGroupsGetGroupResponse* source)
    {
        var value = new GroupsGetGroupResponse();
        value.AdminRoleId = Utf8.ToString(source->AdminRoleId);
        value.Created = PlayFabTime.ToDateTimeOffset(source->Created);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.GroupName = Utf8.ToString(source->GroupName);
        value.MemberRoleId = Utf8.ToString(source->MemberRoleId);
        value.ProfileVersion = source->ProfileVersion;
        if (source->Roles is not null && source->RolesCount != 0)
        {
            var map6 = new Dictionary<string, string>((int)source->RolesCount, StringComparer.Ordinal);
            for (uint i6 = 0; i6 < source->RolesCount; i6++)
            {
                map6[Utf8.ToString(source->Roles[i6].Key) ?? string.Empty] = Utf8.ToString(source->Roles[i6].Value) ?? string.Empty;
            }

            value.Roles = map6;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsGetGroupResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->AdminRoleId = arena.String(AdminRoleId);
        target->Created = PlayFabTime.ToUnixTime(Created);
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
        target->GroupName = arena.String(GroupName);
        target->MemberRoleId = arena.String(MemberRoleId);
        target->ProfileVersion = ProfileVersion;
        target->RolesCount = (uint)(Roles is null ? 0 : Roles.Count);
        if (Roles is not null && Roles.Count > 0)
        {
            PFStringDictionaryEntry* entries6 = arena.Alloc<PFStringDictionaryEntry>(Roles.Count);
            int next6 = 0;
            foreach (KeyValuePair<string, string> pair6 in Roles)
            {
                entries6[next6].Key = arena.String(pair6.Key);
                entries6[next6].Value = arena.String(pair6.Value);
                next6++;
            }

            target->Roles = entries6;
        }
    }
}

/// <summary>Projects <c>PFGroupsGroupApplication</c>.</summary>
public sealed class GroupsGroupApplication
{
    /// <summary><c>Entity</c>.</summary>
    public GroupsEntityWithLineage? Entity { get; set; }

    /// <summary><c>Expires</c>.</summary>
    public DateTimeOffset Expires { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsGroupApplication FromNative(PFGroupsGroupApplication* source)
    {
        var value = new GroupsGroupApplication();
        value.Entity = source->Entity is null ? null : GroupsEntityWithLineage.FromNative(source->Entity);
        value.Expires = PlayFabTime.ToDateTimeOffset(source->Expires);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsGroupApplication* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFGroupsEntityWithLineage* native0 = arena.Alloc<PFGroupsEntityWithLineage>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->Expires = PlayFabTime.ToUnixTime(Expires);
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsGroupBlock</c>.</summary>
public sealed class GroupsGroupBlock
{
    /// <summary><c>Entity</c>.</summary>
    public GroupsEntityWithLineage? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsGroupBlock FromNative(PFGroupsGroupBlock* source)
    {
        var value = new GroupsGroupBlock();
        value.Entity = source->Entity is null ? null : GroupsEntityWithLineage.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsGroupBlock* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFGroupsEntityWithLineage* native0 = arena.Alloc<PFGroupsEntityWithLineage>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        if (Group is not null)
        {
            PFEntityKey* native1 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native1, arena);
            target->Group = native1;
        }
    }
}

/// <summary>Projects <c>PFGroupsGroupInvitation</c>.</summary>
public sealed class GroupsGroupInvitation
{
    /// <summary><c>Expires</c>.</summary>
    public DateTimeOffset Expires { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>InvitedByEntity</c>.</summary>
    public GroupsEntityWithLineage? InvitedByEntity { get; set; }

    /// <summary><c>InvitedEntity</c>.</summary>
    public GroupsEntityWithLineage? InvitedEntity { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    internal static unsafe GroupsGroupInvitation FromNative(PFGroupsGroupInvitation* source)
    {
        var value = new GroupsGroupInvitation();
        value.Expires = PlayFabTime.ToDateTimeOffset(source->Expires);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.InvitedByEntity = source->InvitedByEntity is null ? null : GroupsEntityWithLineage.FromNative(source->InvitedByEntity);
        value.InvitedEntity = source->InvitedEntity is null ? null : GroupsEntityWithLineage.FromNative(source->InvitedEntity);
        value.RoleId = Utf8.ToString(source->RoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsGroupInvitation* target, PlayFabArena arena)
    {
        *target = default;
        target->Expires = PlayFabTime.ToUnixTime(Expires);
        if (Group is not null)
        {
            PFEntityKey* native1 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native1, arena);
            target->Group = native1;
        }
        if (InvitedByEntity is not null)
        {
            PFGroupsEntityWithLineage* native2 = arena.Alloc<PFGroupsEntityWithLineage>(1);
            InvitedByEntity.WriteTo(native2, arena);
            target->InvitedByEntity = native2;
        }
        if (InvitedEntity is not null)
        {
            PFGroupsEntityWithLineage* native3 = arena.Alloc<PFGroupsEntityWithLineage>(1);
            InvitedEntity.WriteTo(native3, arena);
            target->InvitedEntity = native3;
        }
        target->RoleId = arena.String(RoleId);
    }
}

/// <summary>Projects <c>PFGroupsGroupRole</c>.</summary>
public sealed class GroupsGroupRole
{
    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    /// <summary><c>RoleName</c>.</summary>
    public string? RoleName { get; set; }

    internal static unsafe GroupsGroupRole FromNative(PFGroupsGroupRole* source)
    {
        var value = new GroupsGroupRole();
        value.RoleId = Utf8.ToString(source->RoleId);
        value.RoleName = Utf8.ToString(source->RoleName);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsGroupRole* target, PlayFabArena arena)
    {
        *target = default;
        target->RoleId = arena.String(RoleId);
        target->RoleName = arena.String(RoleName);
    }
}

/// <summary>Projects <c>PFGroupsGroupWithRoles</c>.</summary>
public sealed class GroupsGroupWithRoles
{
    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>GroupName</c>.</summary>
    public string? GroupName { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>Roles</c>.</summary>
    public IReadOnlyList<GroupsGroupRole>? Roles { get; set; }

    internal static unsafe GroupsGroupWithRoles FromNative(PFGroupsGroupWithRoles* source)
    {
        var value = new GroupsGroupWithRoles();
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.GroupName = Utf8.ToString(source->GroupName);
        value.ProfileVersion = source->ProfileVersion;
        if (source->Roles is not null && source->RolesCount != 0)
        {
            var list3 = new List<GroupsGroupRole>((int)source->RolesCount);
            for (uint i3 = 0; i3 < source->RolesCount; i3++)
            {
                list3.Add(GroupsGroupRole.FromNative(source->Roles[i3]));
            }

            value.Roles = list3;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsGroupWithRoles* target, PlayFabArena arena)
    {
        *target = default;
        if (Group is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native0, arena);
            target->Group = native0;
        }
        target->GroupName = arena.String(GroupName);
        target->ProfileVersion = ProfileVersion;
        target->RolesCount = (uint)(Roles is null ? 0 : Roles.Count);
        if (Roles is not null && Roles.Count > 0)
        {
            PFGroupsGroupRole** array3 = arena.PointerArray<PFGroupsGroupRole>(Roles.Count);
            for (int i3 = 0; i3 < Roles.Count; i3++)
            {
                PFGroupsGroupRole* item3 = arena.Alloc<PFGroupsGroupRole>(1);
                Roles[i3].WriteTo(item3, arena);
                array3[i3] = item3;
            }

            target->Roles = array3;
        }
    }
}

/// <summary>Projects <c>PFGroupsInviteToGroupRequest</c>.</summary>
public sealed class GroupsInviteToGroupRequest
{
    /// <summary><c>AutoAcceptOutstandingApplication</c>.</summary>
    public bool? AutoAcceptOutstandingApplication { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    internal static unsafe GroupsInviteToGroupRequest FromNative(PFGroupsInviteToGroupRequest* source)
    {
        var value = new GroupsInviteToGroupRequest();
        value.AutoAcceptOutstandingApplication = source->AutoAcceptOutstandingApplication is null ? null : (bool?)(*source->AutoAcceptOutstandingApplication != 0);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.RoleId = Utf8.ToString(source->RoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsInviteToGroupRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->AutoAcceptOutstandingApplication = arena.Value(AutoAcceptOutstandingApplication is null ? null : (AutoAcceptOutstandingApplication.Value ? (byte?)1 : (byte?)0));
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        if (Group is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native4, arena);
            target->Group = native4;
        }
        target->RoleId = arena.String(RoleId);
    }
}

/// <summary>Projects <c>PFGroupsInviteToGroupResponse</c>.</summary>
public sealed class GroupsInviteToGroupResponse
{
    /// <summary><c>Expires</c>.</summary>
    public DateTimeOffset Expires { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>InvitedByEntity</c>.</summary>
    public GroupsEntityWithLineage? InvitedByEntity { get; set; }

    /// <summary><c>InvitedEntity</c>.</summary>
    public GroupsEntityWithLineage? InvitedEntity { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    internal static unsafe GroupsInviteToGroupResponse FromNative(PFGroupsInviteToGroupResponse* source)
    {
        var value = new GroupsInviteToGroupResponse();
        value.Expires = PlayFabTime.ToDateTimeOffset(source->Expires);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.InvitedByEntity = source->InvitedByEntity is null ? null : GroupsEntityWithLineage.FromNative(source->InvitedByEntity);
        value.InvitedEntity = source->InvitedEntity is null ? null : GroupsEntityWithLineage.FromNative(source->InvitedEntity);
        value.RoleId = Utf8.ToString(source->RoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsInviteToGroupResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->Expires = PlayFabTime.ToUnixTime(Expires);
        if (Group is not null)
        {
            PFEntityKey* native1 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native1, arena);
            target->Group = native1;
        }
        if (InvitedByEntity is not null)
        {
            PFGroupsEntityWithLineage* native2 = arena.Alloc<PFGroupsEntityWithLineage>(1);
            InvitedByEntity.WriteTo(native2, arena);
            target->InvitedByEntity = native2;
        }
        if (InvitedEntity is not null)
        {
            PFGroupsEntityWithLineage* native3 = arena.Alloc<PFGroupsEntityWithLineage>(1);
            InvitedEntity.WriteTo(native3, arena);
            target->InvitedEntity = native3;
        }
        target->RoleId = arena.String(RoleId);
    }
}

/// <summary>Projects <c>PFGroupsIsMemberRequest</c>.</summary>
public sealed class GroupsIsMemberRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    internal static unsafe GroupsIsMemberRequest FromNative(PFGroupsIsMemberRequest* source)
    {
        var value = new GroupsIsMemberRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.RoleId = Utf8.ToString(source->RoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsIsMemberRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
        target->RoleId = arena.String(RoleId);
    }
}

/// <summary>Projects <c>PFGroupsIsMemberResponse</c>.</summary>
public sealed class GroupsIsMemberResponse
{
    /// <summary><c>IsMember</c>.</summary>
    public bool IsMember { get; set; }

    internal static unsafe GroupsIsMemberResponse FromNative(PFGroupsIsMemberResponse* source)
    {
        var value = new GroupsIsMemberResponse();
        value.IsMember = source->IsMember != 0;
        return value;
    }

    internal unsafe void WriteTo(PFGroupsIsMemberResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->IsMember = IsMember ? (byte)1 : (byte)0;
    }
}

/// <summary>Projects <c>PFGroupsListGroupApplicationsRequest</c>.</summary>
public sealed class GroupsListGroupApplicationsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsListGroupApplicationsRequest FromNative(PFGroupsListGroupApplicationsRequest* source)
    {
        var value = new GroupsListGroupApplicationsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupApplicationsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsListGroupApplicationsResponse</c>.</summary>
public sealed class GroupsListGroupApplicationsResponse
{
    /// <summary><c>Applications</c>.</summary>
    public IReadOnlyList<GroupsGroupApplication>? Applications { get; set; }

    internal static unsafe GroupsListGroupApplicationsResponse FromNative(PFGroupsListGroupApplicationsResponse* source)
    {
        var value = new GroupsListGroupApplicationsResponse();
        if (source->Applications is not null && source->ApplicationsCount != 0)
        {
            var list0 = new List<GroupsGroupApplication>((int)source->ApplicationsCount);
            for (uint i0 = 0; i0 < source->ApplicationsCount; i0++)
            {
                list0.Add(GroupsGroupApplication.FromNative(source->Applications[i0]));
            }

            value.Applications = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupApplicationsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ApplicationsCount = (uint)(Applications is null ? 0 : Applications.Count);
        if (Applications is not null && Applications.Count > 0)
        {
            PFGroupsGroupApplication** array0 = arena.PointerArray<PFGroupsGroupApplication>(Applications.Count);
            for (int i0 = 0; i0 < Applications.Count; i0++)
            {
                PFGroupsGroupApplication* item0 = arena.Alloc<PFGroupsGroupApplication>(1);
                Applications[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Applications = array0;
        }
    }
}

/// <summary>Projects <c>PFGroupsListGroupBlocksRequest</c>.</summary>
public sealed class GroupsListGroupBlocksRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsListGroupBlocksRequest FromNative(PFGroupsListGroupBlocksRequest* source)
    {
        var value = new GroupsListGroupBlocksRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupBlocksRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsListGroupBlocksResponse</c>.</summary>
public sealed class GroupsListGroupBlocksResponse
{
    /// <summary><c>BlockedEntities</c>.</summary>
    public IReadOnlyList<GroupsGroupBlock>? BlockedEntities { get; set; }

    internal static unsafe GroupsListGroupBlocksResponse FromNative(PFGroupsListGroupBlocksResponse* source)
    {
        var value = new GroupsListGroupBlocksResponse();
        if (source->BlockedEntities is not null && source->BlockedEntitiesCount != 0)
        {
            var list0 = new List<GroupsGroupBlock>((int)source->BlockedEntitiesCount);
            for (uint i0 = 0; i0 < source->BlockedEntitiesCount; i0++)
            {
                list0.Add(GroupsGroupBlock.FromNative(source->BlockedEntities[i0]));
            }

            value.BlockedEntities = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupBlocksResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->BlockedEntitiesCount = (uint)(BlockedEntities is null ? 0 : BlockedEntities.Count);
        if (BlockedEntities is not null && BlockedEntities.Count > 0)
        {
            PFGroupsGroupBlock** array0 = arena.PointerArray<PFGroupsGroupBlock>(BlockedEntities.Count);
            for (int i0 = 0; i0 < BlockedEntities.Count; i0++)
            {
                PFGroupsGroupBlock* item0 = arena.Alloc<PFGroupsGroupBlock>(1);
                BlockedEntities[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->BlockedEntities = array0;
        }
    }
}

/// <summary>Projects <c>PFGroupsListGroupInvitationsRequest</c>.</summary>
public sealed class GroupsListGroupInvitationsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsListGroupInvitationsRequest FromNative(PFGroupsListGroupInvitationsRequest* source)
    {
        var value = new GroupsListGroupInvitationsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupInvitationsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsListGroupInvitationsResponse</c>.</summary>
public sealed class GroupsListGroupInvitationsResponse
{
    /// <summary><c>Invitations</c>.</summary>
    public IReadOnlyList<GroupsGroupInvitation>? Invitations { get; set; }

    internal static unsafe GroupsListGroupInvitationsResponse FromNative(PFGroupsListGroupInvitationsResponse* source)
    {
        var value = new GroupsListGroupInvitationsResponse();
        if (source->Invitations is not null && source->InvitationsCount != 0)
        {
            var list0 = new List<GroupsGroupInvitation>((int)source->InvitationsCount);
            for (uint i0 = 0; i0 < source->InvitationsCount; i0++)
            {
                list0.Add(GroupsGroupInvitation.FromNative(source->Invitations[i0]));
            }

            value.Invitations = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupInvitationsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->InvitationsCount = (uint)(Invitations is null ? 0 : Invitations.Count);
        if (Invitations is not null && Invitations.Count > 0)
        {
            PFGroupsGroupInvitation** array0 = arena.PointerArray<PFGroupsGroupInvitation>(Invitations.Count);
            for (int i0 = 0; i0 < Invitations.Count; i0++)
            {
                PFGroupsGroupInvitation* item0 = arena.Alloc<PFGroupsGroupInvitation>(1);
                Invitations[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Invitations = array0;
        }
    }
}

/// <summary>Projects <c>PFGroupsListGroupMembersRequest</c>.</summary>
public sealed class GroupsListGroupMembersRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsListGroupMembersRequest FromNative(PFGroupsListGroupMembersRequest* source)
    {
        var value = new GroupsListGroupMembersRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupMembersRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsListGroupMembersResponse</c>.</summary>
public sealed class GroupsListGroupMembersResponse
{
    /// <summary><c>Members</c>.</summary>
    public IReadOnlyList<GroupsEntityMemberRole>? Members { get; set; }

    internal static unsafe GroupsListGroupMembersResponse FromNative(PFGroupsListGroupMembersResponse* source)
    {
        var value = new GroupsListGroupMembersResponse();
        if (source->Members is not null && source->MembersCount != 0)
        {
            var list0 = new List<GroupsEntityMemberRole>((int)source->MembersCount);
            for (uint i0 = 0; i0 < source->MembersCount; i0++)
            {
                list0.Add(GroupsEntityMemberRole.FromNative(source->Members[i0]));
            }

            value.Members = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListGroupMembersResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->MembersCount = (uint)(Members is null ? 0 : Members.Count);
        if (Members is not null && Members.Count > 0)
        {
            PFGroupsEntityMemberRole** array0 = arena.PointerArray<PFGroupsEntityMemberRole>(Members.Count);
            for (int i0 = 0; i0 < Members.Count; i0++)
            {
                PFGroupsEntityMemberRole* item0 = arena.Alloc<PFGroupsEntityMemberRole>(1);
                Members[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Members = array0;
        }
    }
}

/// <summary>Projects <c>PFGroupsListMembershipOpportunitiesRequest</c>.</summary>
public sealed class GroupsListMembershipOpportunitiesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    internal static unsafe GroupsListMembershipOpportunitiesRequest FromNative(PFGroupsListMembershipOpportunitiesRequest* source)
    {
        var value = new GroupsListMembershipOpportunitiesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListMembershipOpportunitiesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsListMembershipOpportunitiesResponse</c>.</summary>
public sealed class GroupsListMembershipOpportunitiesResponse
{
    /// <summary><c>Applications</c>.</summary>
    public IReadOnlyList<GroupsGroupApplication>? Applications { get; set; }

    /// <summary><c>Invitations</c>.</summary>
    public IReadOnlyList<GroupsGroupInvitation>? Invitations { get; set; }

    internal static unsafe GroupsListMembershipOpportunitiesResponse FromNative(PFGroupsListMembershipOpportunitiesResponse* source)
    {
        var value = new GroupsListMembershipOpportunitiesResponse();
        if (source->Applications is not null && source->ApplicationsCount != 0)
        {
            var list0 = new List<GroupsGroupApplication>((int)source->ApplicationsCount);
            for (uint i0 = 0; i0 < source->ApplicationsCount; i0++)
            {
                list0.Add(GroupsGroupApplication.FromNative(source->Applications[i0]));
            }

            value.Applications = list0;
        }
        if (source->Invitations is not null && source->InvitationsCount != 0)
        {
            var list2 = new List<GroupsGroupInvitation>((int)source->InvitationsCount);
            for (uint i2 = 0; i2 < source->InvitationsCount; i2++)
            {
                list2.Add(GroupsGroupInvitation.FromNative(source->Invitations[i2]));
            }

            value.Invitations = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListMembershipOpportunitiesResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ApplicationsCount = (uint)(Applications is null ? 0 : Applications.Count);
        if (Applications is not null && Applications.Count > 0)
        {
            PFGroupsGroupApplication** array0 = arena.PointerArray<PFGroupsGroupApplication>(Applications.Count);
            for (int i0 = 0; i0 < Applications.Count; i0++)
            {
                PFGroupsGroupApplication* item0 = arena.Alloc<PFGroupsGroupApplication>(1);
                Applications[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Applications = array0;
        }
        target->InvitationsCount = (uint)(Invitations is null ? 0 : Invitations.Count);
        if (Invitations is not null && Invitations.Count > 0)
        {
            PFGroupsGroupInvitation** array2 = arena.PointerArray<PFGroupsGroupInvitation>(Invitations.Count);
            for (int i2 = 0; i2 < Invitations.Count; i2++)
            {
                PFGroupsGroupInvitation* item2 = arena.Alloc<PFGroupsGroupInvitation>(1);
                Invitations[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Invitations = array2;
        }
    }
}

/// <summary>Projects <c>PFGroupsListMembershipRequest</c>.</summary>
public sealed class GroupsListMembershipRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    internal static unsafe GroupsListMembershipRequest FromNative(PFGroupsListMembershipRequest* source)
    {
        var value = new GroupsListMembershipRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListMembershipRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
    }
}

/// <summary>Projects <c>PFGroupsListMembershipResponse</c>.</summary>
public sealed class GroupsListMembershipResponse
{
    /// <summary><c>Groups</c>.</summary>
    public IReadOnlyList<GroupsGroupWithRoles>? Groups { get; set; }

    internal static unsafe GroupsListMembershipResponse FromNative(PFGroupsListMembershipResponse* source)
    {
        var value = new GroupsListMembershipResponse();
        if (source->Groups is not null && source->GroupsCount != 0)
        {
            var list0 = new List<GroupsGroupWithRoles>((int)source->GroupsCount);
            for (uint i0 = 0; i0 < source->GroupsCount; i0++)
            {
                list0.Add(GroupsGroupWithRoles.FromNative(source->Groups[i0]));
            }

            value.Groups = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFGroupsListMembershipResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->GroupsCount = (uint)(Groups is null ? 0 : Groups.Count);
        if (Groups is not null && Groups.Count > 0)
        {
            PFGroupsGroupWithRoles** array0 = arena.PointerArray<PFGroupsGroupWithRoles>(Groups.Count);
            for (int i0 = 0; i0 < Groups.Count; i0++)
            {
                PFGroupsGroupWithRoles* item0 = arena.Alloc<PFGroupsGroupWithRoles>(1);
                Groups[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Groups = array0;
        }
    }
}

/// <summary>Projects <c>PFGroupsRemoveGroupApplicationRequest</c>.</summary>
public sealed class GroupsRemoveGroupApplicationRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsRemoveGroupApplicationRequest FromNative(PFGroupsRemoveGroupApplicationRequest* source)
    {
        var value = new GroupsRemoveGroupApplicationRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsRemoveGroupApplicationRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
    }
}

/// <summary>Projects <c>PFGroupsRemoveGroupInvitationRequest</c>.</summary>
public sealed class GroupsRemoveGroupInvitationRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsRemoveGroupInvitationRequest FromNative(PFGroupsRemoveGroupInvitationRequest* source)
    {
        var value = new GroupsRemoveGroupInvitationRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsRemoveGroupInvitationRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
    }
}

/// <summary>Projects <c>PFGroupsRemoveMembersRequest</c>.</summary>
public sealed class GroupsRemoveMembersRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>Members</c>.</summary>
    public IReadOnlyList<EntityKey>? Members { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    internal static unsafe GroupsRemoveMembersRequest FromNative(PFGroupsRemoveMembersRequest* source)
    {
        var value = new GroupsRemoveMembersRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        if (source->Members is not null && source->MembersCount != 0)
        {
            var list3 = new List<EntityKey>((int)source->MembersCount);
            for (uint i3 = 0; i3 < source->MembersCount; i3++)
            {
                list3.Add(EntityKey.FromNative(source->Members[i3]));
            }

            value.Members = list3;
        }
        value.RoleId = Utf8.ToString(source->RoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsRemoveMembersRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Group is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native2, arena);
            target->Group = native2;
        }
        target->MembersCount = (uint)(Members is null ? 0 : Members.Count);
        if (Members is not null && Members.Count > 0)
        {
            PFEntityKey** array3 = arena.PointerArray<PFEntityKey>(Members.Count);
            for (int i3 = 0; i3 < Members.Count; i3++)
            {
                PFEntityKey* item3 = arena.Alloc<PFEntityKey>(1);
                Members[i3].WriteTo(item3, arena);
                array3[i3] = item3;
            }

            target->Members = array3;
        }
        target->RoleId = arena.String(RoleId);
    }
}

/// <summary>Projects <c>PFGroupsUnblockEntityRequest</c>.</summary>
public sealed class GroupsUnblockEntityRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    internal static unsafe GroupsUnblockEntityRequest FromNative(PFGroupsUnblockEntityRequest* source)
    {
        var value = new GroupsUnblockEntityRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsUnblockEntityRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
    }
}

/// <summary>Projects <c>PFGroupsUpdateGroupRequest</c>.</summary>
public sealed class GroupsUpdateGroupRequest
{
    /// <summary><c>AdminRoleId</c>.</summary>
    public string? AdminRoleId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ExpectedProfileVersion</c>.</summary>
    public int? ExpectedProfileVersion { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>GroupName</c>.</summary>
    public string? GroupName { get; set; }

    /// <summary><c>MemberRoleId</c>.</summary>
    public string? MemberRoleId { get; set; }

    internal static unsafe GroupsUpdateGroupRequest FromNative(PFGroupsUpdateGroupRequest* source)
    {
        var value = new GroupsUpdateGroupRequest();
        value.AdminRoleId = Utf8.ToString(source->AdminRoleId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.ExpectedProfileVersion = source->ExpectedProfileVersion is null ? null : (int?)(*source->ExpectedProfileVersion);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.GroupName = Utf8.ToString(source->GroupName);
        value.MemberRoleId = Utf8.ToString(source->MemberRoleId);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsUpdateGroupRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->AdminRoleId = arena.String(AdminRoleId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->ExpectedProfileVersion = arena.Value(ExpectedProfileVersion);
        if (Group is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native4, arena);
            target->Group = native4;
        }
        target->GroupName = arena.String(GroupName);
        target->MemberRoleId = arena.String(MemberRoleId);
    }
}

/// <summary>Projects <c>PFGroupsUpdateGroupResponse</c>.</summary>
public sealed class GroupsUpdateGroupResponse
{
    /// <summary><c>OperationReason</c>.</summary>
    public string? OperationReason { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>SetResult</c>.</summary>
    public OperationTypes? SetResult { get; set; }

    internal static unsafe GroupsUpdateGroupResponse FromNative(PFGroupsUpdateGroupResponse* source)
    {
        var value = new GroupsUpdateGroupResponse();
        value.OperationReason = Utf8.ToString(source->OperationReason);
        value.ProfileVersion = source->ProfileVersion;
        value.SetResult = source->SetResult is null ? null : (OperationTypes?)(*source->SetResult);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsUpdateGroupResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->OperationReason = arena.String(OperationReason);
        target->ProfileVersion = ProfileVersion;
        target->SetResult = arena.Value((PFOperationTypes?)SetResult);
    }
}

/// <summary>Projects <c>PFGroupsUpdateGroupRoleRequest</c>.</summary>
public sealed class GroupsUpdateGroupRoleRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ExpectedProfileVersion</c>.</summary>
    public int? ExpectedProfileVersion { get; set; }

    /// <summary><c>Group</c>.</summary>
    public EntityKey? Group { get; set; }

    /// <summary><c>RoleId</c>.</summary>
    public string? RoleId { get; set; }

    /// <summary><c>RoleName</c>.</summary>
    public string? RoleName { get; set; }

    internal static unsafe GroupsUpdateGroupRoleRequest FromNative(PFGroupsUpdateGroupRoleRequest* source)
    {
        var value = new GroupsUpdateGroupRoleRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ExpectedProfileVersion = source->ExpectedProfileVersion is null ? null : (int?)(*source->ExpectedProfileVersion);
        value.Group = source->Group is null ? null : EntityKey.FromNative(source->Group);
        value.RoleId = Utf8.ToString(source->RoleId);
        value.RoleName = Utf8.ToString(source->RoleName);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsUpdateGroupRoleRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ExpectedProfileVersion = arena.Value(ExpectedProfileVersion);
        if (Group is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Group.WriteTo(native3, arena);
            target->Group = native3;
        }
        target->RoleId = arena.String(RoleId);
        target->RoleName = arena.String(RoleName);
    }
}

/// <summary>Projects <c>PFGroupsUpdateGroupRoleResponse</c>.</summary>
public sealed class GroupsUpdateGroupRoleResponse
{
    /// <summary><c>OperationReason</c>.</summary>
    public string? OperationReason { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>SetResult</c>.</summary>
    public OperationTypes? SetResult { get; set; }

    internal static unsafe GroupsUpdateGroupRoleResponse FromNative(PFGroupsUpdateGroupRoleResponse* source)
    {
        var value = new GroupsUpdateGroupRoleResponse();
        value.OperationReason = Utf8.ToString(source->OperationReason);
        value.ProfileVersion = source->ProfileVersion;
        value.SetResult = source->SetResult is null ? null : (OperationTypes?)(*source->SetResult);
        return value;
    }

    internal unsafe void WriteTo(PFGroupsUpdateGroupRoleResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->OperationReason = arena.String(OperationReason);
        target->ProfileVersion = ProfileVersion;
        target->SetResult = arena.Value((PFOperationTypes?)SetResult);
    }
}
