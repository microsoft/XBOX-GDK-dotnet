// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFTitleDataManagement
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFTitleDataManagementGetPublisherDataRequest</c>.</summary>
public sealed class TitleDataManagementGetPublisherDataRequest
{
    /// <summary><c>Keys</c>.</summary>
    public IReadOnlyList<string>? Keys { get; set; }

    internal static unsafe TitleDataManagementGetPublisherDataRequest FromNative(PFTitleDataManagementGetPublisherDataRequest* source)
    {
        var value = new TitleDataManagementGetPublisherDataRequest();
        if (source->Keys is not null && source->KeysCount != 0)
        {
            var list0 = new List<string>((int)source->KeysCount);
            for (uint i0 = 0; i0 < source->KeysCount; i0++)
            {
                list0.Add(Utf8.ToString(source->Keys[i0]) ?? string.Empty);
            }

            value.Keys = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementGetPublisherDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->KeysCount = (uint)(Keys is null ? 0 : Keys.Count);
        target->Keys = arena.StringArray(Keys);
    }
}

/// <summary>Projects <c>PFTitleDataManagementGetPublisherDataResult</c>.</summary>
public sealed class TitleDataManagementGetPublisherDataResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyDictionary<string, string>? Data { get; set; }

    internal static unsafe TitleDataManagementGetPublisherDataResult FromNative(PFTitleDataManagementGetPublisherDataResult* source)
    {
        var value = new TitleDataManagementGetPublisherDataResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->DataCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                map0[Utf8.ToString(source->Data[i0].Key) ?? string.Empty] = Utf8.ToString(source->Data[i0].Value) ?? string.Empty;
            }

            value.Data = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementGetPublisherDataResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(Data.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in Data)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->Data = entries0;
        }
    }
}

/// <summary>Projects <c>PFTitleDataManagementGetTimeResult</c>.</summary>
public sealed class TitleDataManagementGetTimeResult
{
    /// <summary><c>Time</c>.</summary>
    public DateTimeOffset Time { get; set; }

    internal static unsafe TitleDataManagementGetTimeResult FromNative(PFTitleDataManagementGetTimeResult* source)
    {
        var value = new TitleDataManagementGetTimeResult();
        value.Time = PlayFabTime.ToDateTimeOffset(source->Time);
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementGetTimeResult* target, PlayFabArena arena)
    {
        *target = default;
        target->Time = PlayFabTime.ToUnixTime(Time);
    }
}

/// <summary>Projects <c>PFTitleDataManagementGetTitleDataRequest</c>.</summary>
public sealed class TitleDataManagementGetTitleDataRequest
{
    /// <summary><c>Keys</c>.</summary>
    public IReadOnlyList<string>? Keys { get; set; }

    /// <summary><c>OverrideLabel</c>.</summary>
    public string? OverrideLabel { get; set; }

    internal static unsafe TitleDataManagementGetTitleDataRequest FromNative(PFTitleDataManagementGetTitleDataRequest* source)
    {
        var value = new TitleDataManagementGetTitleDataRequest();
        if (source->Keys is not null && source->KeysCount != 0)
        {
            var list0 = new List<string>((int)source->KeysCount);
            for (uint i0 = 0; i0 < source->KeysCount; i0++)
            {
                list0.Add(Utf8.ToString(source->Keys[i0]) ?? string.Empty);
            }

            value.Keys = list0;
        }
        value.OverrideLabel = Utf8.ToString(source->OverrideLabel);
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementGetTitleDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->KeysCount = (uint)(Keys is null ? 0 : Keys.Count);
        target->Keys = arena.StringArray(Keys);
        target->OverrideLabel = arena.String(OverrideLabel);
    }
}

/// <summary>Projects <c>PFTitleDataManagementGetTitleDataResult</c>.</summary>
public sealed class TitleDataManagementGetTitleDataResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyDictionary<string, string>? Data { get; set; }

    internal static unsafe TitleDataManagementGetTitleDataResult FromNative(PFTitleDataManagementGetTitleDataResult* source)
    {
        var value = new TitleDataManagementGetTitleDataResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->DataCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                map0[Utf8.ToString(source->Data[i0].Key) ?? string.Empty] = Utf8.ToString(source->Data[i0].Value) ?? string.Empty;
            }

            value.Data = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementGetTitleDataResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(Data.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in Data)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->Data = entries0;
        }
    }
}

/// <summary>Projects <c>PFTitleDataManagementGetTitleNewsRequest</c>.</summary>
public sealed class TitleDataManagementGetTitleNewsRequest
{
    /// <summary><c>Count</c>.</summary>
    public int? Count { get; set; }

    internal static unsafe TitleDataManagementGetTitleNewsRequest FromNative(PFTitleDataManagementGetTitleNewsRequest* source)
    {
        var value = new TitleDataManagementGetTitleNewsRequest();
        value.Count = source->Count is null ? null : (int?)(*source->Count);
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementGetTitleNewsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Count = arena.Value(Count);
    }
}

/// <summary>Projects <c>PFTitleDataManagementGetTitleNewsResult</c>.</summary>
public sealed class TitleDataManagementGetTitleNewsResult
{
    /// <summary><c>News</c>.</summary>
    public IReadOnlyList<TitleDataManagementTitleNewsItem>? News { get; set; }

    internal static unsafe TitleDataManagementGetTitleNewsResult FromNative(PFTitleDataManagementGetTitleNewsResult* source)
    {
        var value = new TitleDataManagementGetTitleNewsResult();
        if (source->News is not null && source->NewsCount != 0)
        {
            var list0 = new List<TitleDataManagementTitleNewsItem>((int)source->NewsCount);
            for (uint i0 = 0; i0 < source->NewsCount; i0++)
            {
                list0.Add(TitleDataManagementTitleNewsItem.FromNative(source->News[i0]));
            }

            value.News = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementGetTitleNewsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->NewsCount = (uint)(News is null ? 0 : News.Count);
        if (News is not null && News.Count > 0)
        {
            PFTitleDataManagementTitleNewsItem** array0 = arena.PointerArray<PFTitleDataManagementTitleNewsItem>(News.Count);
            for (int i0 = 0; i0 < News.Count; i0++)
            {
                PFTitleDataManagementTitleNewsItem* item0 = arena.Alloc<PFTitleDataManagementTitleNewsItem>(1);
                News[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->News = array0;
        }
    }
}

/// <summary>Projects <c>PFTitleDataManagementSetPublisherDataRequest</c>.</summary>
public sealed class TitleDataManagementSetPublisherDataRequest
{
    /// <summary><c>Key</c>.</summary>
    public string? Key { get; set; }

    /// <summary><c>Value</c>.</summary>
    public string? Value { get; set; }

    internal static unsafe TitleDataManagementSetPublisherDataRequest FromNative(PFTitleDataManagementSetPublisherDataRequest* source)
    {
        var value = new TitleDataManagementSetPublisherDataRequest();
        value.Key = Utf8.ToString(source->Key);
        value.Value = Utf8.ToString(source->Value);
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementSetPublisherDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Key = arena.String(Key);
        target->Value = arena.String(Value);
    }
}

/// <summary>Projects <c>PFTitleDataManagementSetTitleDataRequest</c>.</summary>
public sealed class TitleDataManagementSetTitleDataRequest
{
    /// <summary><c>Key</c>.</summary>
    public string? Key { get; set; }

    /// <summary><c>Value</c>.</summary>
    public string? Value { get; set; }

    internal static unsafe TitleDataManagementSetTitleDataRequest FromNative(PFTitleDataManagementSetTitleDataRequest* source)
    {
        var value = new TitleDataManagementSetTitleDataRequest();
        value.Key = Utf8.ToString(source->Key);
        value.Value = Utf8.ToString(source->Value);
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementSetTitleDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Key = arena.String(Key);
        target->Value = arena.String(Value);
    }
}

/// <summary>Projects <c>PFTitleDataManagementTitleNewsItem</c>.</summary>
public sealed class TitleDataManagementTitleNewsItem
{
    /// <summary><c>Body</c>.</summary>
    public string? Body { get; set; }

    /// <summary><c>NewsId</c>.</summary>
    public string? NewsId { get; set; }

    /// <summary><c>Timestamp</c>.</summary>
    public DateTimeOffset Timestamp { get; set; }

    /// <summary><c>Title</c>.</summary>
    public string? Title { get; set; }

    internal static unsafe TitleDataManagementTitleNewsItem FromNative(PFTitleDataManagementTitleNewsItem* source)
    {
        var value = new TitleDataManagementTitleNewsItem();
        value.Body = Utf8.ToString(source->Body);
        value.NewsId = Utf8.ToString(source->NewsId);
        value.Timestamp = PlayFabTime.ToDateTimeOffset(source->Timestamp);
        value.Title = Utf8.ToString(source->Title);
        return value;
    }

    internal unsafe void WriteTo(PFTitleDataManagementTitleNewsItem* target, PlayFabArena arena)
    {
        *target = default;
        target->Body = arena.String(Body);
        target->NewsId = arena.String(NewsId);
        target->Timestamp = PlayFabTime.ToUnixTime(Timestamp);
        target->Title = arena.String(Title);
    }
}
