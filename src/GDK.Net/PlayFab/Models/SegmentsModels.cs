// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFSegments
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFSegmentsChurnRiskLevel</c>.</summary>
public enum SegmentsChurnRiskLevel : uint
{
    /// <summary>Projects <c>PFSegmentsChurnRiskLevel::NoData</c>.</summary>
    NoData = 0,
    /// <summary>Projects <c>PFSegmentsChurnRiskLevel::LowRisk</c>.</summary>
    LowRisk = 1,
    /// <summary>Projects <c>PFSegmentsChurnRiskLevel::MediumRisk</c>.</summary>
    MediumRisk = 2,
    /// <summary>Projects <c>PFSegmentsChurnRiskLevel::HighRisk</c>.</summary>
    HighRisk = 3,
}

/// <summary>Projects <c>PFSegmentsAdCampaignAttribution</c>.</summary>
public sealed class SegmentsAdCampaignAttribution
{
    /// <summary><c>AttributedAt</c>.</summary>
    public DateTimeOffset AttributedAt { get; set; }

    /// <summary><c>CampaignId</c>.</summary>
    public string? CampaignId { get; set; }

    /// <summary><c>Platform</c>.</summary>
    public string? Platform { get; set; }

    internal static unsafe SegmentsAdCampaignAttribution FromNative(PFSegmentsAdCampaignAttribution* source)
    {
        var value = new SegmentsAdCampaignAttribution();
        value.AttributedAt = PlayFabTime.ToDateTimeOffset(source->AttributedAt);
        value.CampaignId = Utf8.ToString(source->CampaignId);
        value.Platform = Utf8.ToString(source->Platform);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsAdCampaignAttribution* target, PlayFabArena arena)
    {
        *target = default;
        target->AttributedAt = PlayFabTime.ToUnixTime(AttributedAt);
        target->CampaignId = arena.String(CampaignId);
        target->Platform = arena.String(Platform);
    }
}

/// <summary>Projects <c>PFSegmentsAddPlayerTagRequest</c>.</summary>
public sealed class SegmentsAddPlayerTagRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>TagName</c>.</summary>
    public string? TagName { get; set; }

    internal static unsafe SegmentsAddPlayerTagRequest FromNative(PFSegmentsAddPlayerTagRequest* source)
    {
        var value = new SegmentsAddPlayerTagRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.TagName = Utf8.ToString(source->TagName);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsAddPlayerTagRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
        target->TagName = arena.String(TagName);
    }
}

/// <summary>Projects <c>PFSegmentsContactEmailInfo</c>.</summary>
public sealed class SegmentsContactEmailInfo
{
    /// <summary><c>EmailAddress</c>.</summary>
    public string? EmailAddress { get; set; }

    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    /// <summary><c>VerificationStatus</c>.</summary>
    public EmailVerificationStatus? VerificationStatus { get; set; }

    internal static unsafe SegmentsContactEmailInfo FromNative(PFSegmentsContactEmailInfo* source)
    {
        var value = new SegmentsContactEmailInfo();
        value.EmailAddress = Utf8.ToString(source->EmailAddress);
        value.Name = Utf8.ToString(source->Name);
        value.VerificationStatus = source->VerificationStatus is null ? null : (EmailVerificationStatus?)(*source->VerificationStatus);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsContactEmailInfo* target, PlayFabArena arena)
    {
        *target = default;
        target->EmailAddress = arena.String(EmailAddress);
        target->Name = arena.String(Name);
        target->VerificationStatus = arena.Value((PFEmailVerificationStatus?)VerificationStatus);
    }
}

/// <summary>Projects <c>PFSegmentsGetAllSegmentsResult</c>.</summary>
public sealed class SegmentsGetAllSegmentsResult
{
    /// <summary><c>Segments</c>.</summary>
    public IReadOnlyList<SegmentsGetSegmentResult>? Segments { get; set; }

    internal static unsafe SegmentsGetAllSegmentsResult FromNative(PFSegmentsGetAllSegmentsResult* source)
    {
        var value = new SegmentsGetAllSegmentsResult();
        if (source->Segments is not null && source->SegmentsCount != 0)
        {
            var list0 = new List<SegmentsGetSegmentResult>((int)source->SegmentsCount);
            for (uint i0 = 0; i0 < source->SegmentsCount; i0++)
            {
                list0.Add(SegmentsGetSegmentResult.FromNative(source->Segments[i0]));
            }

            value.Segments = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetAllSegmentsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->SegmentsCount = (uint)(Segments is null ? 0 : Segments.Count);
        if (Segments is not null && Segments.Count > 0)
        {
            PFSegmentsGetSegmentResult** array0 = arena.PointerArray<PFSegmentsGetSegmentResult>(Segments.Count);
            for (int i0 = 0; i0 < Segments.Count; i0++)
            {
                PFSegmentsGetSegmentResult* item0 = arena.Alloc<PFSegmentsGetSegmentResult>(1);
                Segments[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Segments = array0;
        }
    }
}

/// <summary>Projects <c>PFSegmentsGetPlayerSegmentsResult</c>.</summary>
public sealed class SegmentsGetPlayerSegmentsResult
{
    /// <summary><c>Segments</c>.</summary>
    public IReadOnlyList<SegmentsGetSegmentResult>? Segments { get; set; }

    internal static unsafe SegmentsGetPlayerSegmentsResult FromNative(PFSegmentsGetPlayerSegmentsResult* source)
    {
        var value = new SegmentsGetPlayerSegmentsResult();
        if (source->Segments is not null && source->SegmentsCount != 0)
        {
            var list0 = new List<SegmentsGetSegmentResult>((int)source->SegmentsCount);
            for (uint i0 = 0; i0 < source->SegmentsCount; i0++)
            {
                list0.Add(SegmentsGetSegmentResult.FromNative(source->Segments[i0]));
            }

            value.Segments = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetPlayerSegmentsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->SegmentsCount = (uint)(Segments is null ? 0 : Segments.Count);
        if (Segments is not null && Segments.Count > 0)
        {
            PFSegmentsGetSegmentResult** array0 = arena.PointerArray<PFSegmentsGetSegmentResult>(Segments.Count);
            for (int i0 = 0; i0 < Segments.Count; i0++)
            {
                PFSegmentsGetSegmentResult* item0 = arena.Alloc<PFSegmentsGetSegmentResult>(1);
                Segments[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Segments = array0;
        }
    }
}

/// <summary>Projects <c>PFSegmentsGetPlayerTagsRequest</c>.</summary>
public sealed class SegmentsGetPlayerTagsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayfabNamespace</c>.</summary>
    public string? PlayfabNamespace { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe SegmentsGetPlayerTagsRequest FromNative(PFSegmentsGetPlayerTagsRequest* source)
    {
        var value = new SegmentsGetPlayerTagsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayfabNamespace = Utf8.ToString(source->PlayfabNamespace);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetPlayerTagsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayfabNamespace = arena.String(PlayfabNamespace);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFSegmentsGetPlayerTagsResult</c>.</summary>
public sealed class SegmentsGetPlayerTagsResult
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    internal static unsafe SegmentsGetPlayerTagsResult FromNative(PFSegmentsGetPlayerTagsResult* source)
    {
        var value = new SegmentsGetPlayerTagsResult();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list1 = new List<string>((int)source->TagsCount);
            for (uint i1 = 0; i1 < source->TagsCount; i1++)
            {
                list1.Add(Utf8.ToString(source->Tags[i1]) ?? string.Empty);
            }

            value.Tags = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetPlayerTagsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
    }
}

/// <summary>Projects <c>PFSegmentsGetPlayersInSegmentRequest</c>.</summary>
public sealed class SegmentsGetPlayersInSegmentRequest
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>GetProfilesAsync</c>.</summary>
    public bool? GetProfilesAsync { get; set; }

    /// <summary><c>MaxBatchSize</c>.</summary>
    public uint? MaxBatchSize { get; set; }

    /// <summary><c>SecondsToLive</c>.</summary>
    public uint? SecondsToLive { get; set; }

    /// <summary><c>SegmentId</c>.</summary>
    public string? SegmentId { get; set; }

    internal static unsafe SegmentsGetPlayersInSegmentRequest FromNative(PFSegmentsGetPlayersInSegmentRequest* source)
    {
        var value = new SegmentsGetPlayersInSegmentRequest();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.GetProfilesAsync = source->GetProfilesAsync is null ? null : (bool?)(*source->GetProfilesAsync != 0);
        value.MaxBatchSize = source->MaxBatchSize is null ? null : (uint?)(*source->MaxBatchSize);
        value.SecondsToLive = source->SecondsToLive is null ? null : (uint?)(*source->SecondsToLive);
        value.SegmentId = Utf8.ToString(source->SegmentId);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetPlayersInSegmentRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->GetProfilesAsync = arena.Value(GetProfilesAsync is null ? null : (GetProfilesAsync.Value ? (byte?)1 : (byte?)0));
        target->MaxBatchSize = arena.Value(MaxBatchSize);
        target->SecondsToLive = arena.Value(SecondsToLive);
        target->SegmentId = arena.String(SegmentId);
    }
}

/// <summary>Projects <c>PFSegmentsGetPlayersInSegmentResult</c>.</summary>
public sealed class SegmentsGetPlayersInSegmentResult
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>PlayerProfiles</c>.</summary>
    public IReadOnlyList<SegmentsPlayerProfile>? PlayerProfiles { get; set; }

    /// <summary><c>ProfilesInSegment</c>.</summary>
    public int ProfilesInSegment { get; set; }

    internal static unsafe SegmentsGetPlayersInSegmentResult FromNative(PFSegmentsGetPlayersInSegmentResult* source)
    {
        var value = new SegmentsGetPlayersInSegmentResult();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        if (source->PlayerProfiles is not null && source->PlayerProfilesCount != 0)
        {
            var list1 = new List<SegmentsPlayerProfile>((int)source->PlayerProfilesCount);
            for (uint i1 = 0; i1 < source->PlayerProfilesCount; i1++)
            {
                list1.Add(SegmentsPlayerProfile.FromNative(source->PlayerProfiles[i1]));
            }

            value.PlayerProfiles = list1;
        }
        value.ProfilesInSegment = source->ProfilesInSegment;
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetPlayersInSegmentResult* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->PlayerProfilesCount = (uint)(PlayerProfiles is null ? 0 : PlayerProfiles.Count);
        if (PlayerProfiles is not null && PlayerProfiles.Count > 0)
        {
            PFSegmentsPlayerProfile** array1 = arena.PointerArray<PFSegmentsPlayerProfile>(PlayerProfiles.Count);
            for (int i1 = 0; i1 < PlayerProfiles.Count; i1++)
            {
                PFSegmentsPlayerProfile* item1 = arena.Alloc<PFSegmentsPlayerProfile>(1);
                PlayerProfiles[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->PlayerProfiles = array1;
        }
        target->ProfilesInSegment = ProfilesInSegment;
    }
}

/// <summary>Projects <c>PFSegmentsGetPlayersSegmentsRequest</c>.</summary>
public sealed class SegmentsGetPlayersSegmentsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe SegmentsGetPlayersSegmentsRequest FromNative(PFSegmentsGetPlayersSegmentsRequest* source)
    {
        var value = new SegmentsGetPlayersSegmentsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetPlayersSegmentsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFSegmentsGetSegmentResult</c>.</summary>
public sealed class SegmentsGetSegmentResult
{
    /// <summary><c>ABTestParent</c>.</summary>
    public string? ABTestParent { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    internal static unsafe SegmentsGetSegmentResult FromNative(PFSegmentsGetSegmentResult* source)
    {
        var value = new SegmentsGetSegmentResult();
        value.ABTestParent = Utf8.ToString(source->ABTestParent);
        value.Id = Utf8.ToString(source->Id);
        value.Name = Utf8.ToString(source->Name);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsGetSegmentResult* target, PlayFabArena arena)
    {
        *target = default;
        target->ABTestParent = arena.String(ABTestParent);
        target->Id = arena.String(Id);
        target->Name = arena.String(Name);
    }
}

/// <summary>Projects <c>PFSegmentsPlayerLinkedAccount</c>.</summary>
public sealed class SegmentsPlayerLinkedAccount
{
    /// <summary><c>Email</c>.</summary>
    public string? Email { get; set; }

    /// <summary><c>Platform</c>.</summary>
    public LoginIdentityProvider? Platform { get; set; }

    /// <summary><c>PlatformUserId</c>.</summary>
    public string? PlatformUserId { get; set; }

    /// <summary><c>Username</c>.</summary>
    public string? Username { get; set; }

    internal static unsafe SegmentsPlayerLinkedAccount FromNative(PFSegmentsPlayerLinkedAccount* source)
    {
        var value = new SegmentsPlayerLinkedAccount();
        value.Email = Utf8.ToString(source->Email);
        value.Platform = source->Platform is null ? null : (LoginIdentityProvider?)(*source->Platform);
        value.PlatformUserId = Utf8.ToString(source->PlatformUserId);
        value.Username = Utf8.ToString(source->Username);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsPlayerLinkedAccount* target, PlayFabArena arena)
    {
        *target = default;
        target->Email = arena.String(Email);
        target->Platform = arena.Value((PFLoginIdentityProvider?)Platform);
        target->PlatformUserId = arena.String(PlatformUserId);
        target->Username = arena.String(Username);
    }
}

/// <summary>Projects <c>PFSegmentsPlayerLocation</c>.</summary>
public sealed class SegmentsPlayerLocation
{
    /// <summary><c>City</c>.</summary>
    public string? City { get; set; }

    /// <summary><c>ContinentCode</c>.</summary>
    public ContinentCode ContinentCode { get; set; }

    /// <summary><c>CountryCode</c>.</summary>
    public CountryCode CountryCode { get; set; }

    /// <summary><c>Latitude</c>.</summary>
    public double? Latitude { get; set; }

    /// <summary><c>Longitude</c>.</summary>
    public double? Longitude { get; set; }

    internal static unsafe SegmentsPlayerLocation FromNative(PFSegmentsPlayerLocation* source)
    {
        var value = new SegmentsPlayerLocation();
        value.City = Utf8.ToString(source->City);
        value.ContinentCode = (ContinentCode)source->ContinentCode;
        value.CountryCode = (CountryCode)source->CountryCode;
        value.Latitude = source->Latitude is null ? null : (double?)(*source->Latitude);
        value.Longitude = source->Longitude is null ? null : (double?)(*source->Longitude);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsPlayerLocation* target, PlayFabArena arena)
    {
        *target = default;
        target->City = arena.String(City);
        target->ContinentCode = (PFContinentCode)ContinentCode;
        target->CountryCode = (PFCountryCode)CountryCode;
        target->Latitude = arena.Value(Latitude);
        target->Longitude = arena.Value(Longitude);
    }
}

/// <summary>Projects <c>PFSegmentsPlayerProfile</c>.</summary>
public sealed class SegmentsPlayerProfile
{
    /// <summary><c>AdCampaignAttributions</c>.</summary>
    public IReadOnlyList<SegmentsAdCampaignAttribution>? AdCampaignAttributions { get; set; }

    /// <summary><c>AvatarUrl</c>.</summary>
    public string? AvatarUrl { get; set; }

    /// <summary><c>BannedUntil</c>.</summary>
    public DateTimeOffset? BannedUntil { get; set; }

    /// <summary><c>ChurnPrediction</c>.</summary>
    public SegmentsChurnRiskLevel? ChurnPrediction { get; set; }

    /// <summary><c>ContactEmailAddresses</c>.</summary>
    public IReadOnlyList<SegmentsContactEmailInfo>? ContactEmailAddresses { get; set; }

    /// <summary><c>Created</c>.</summary>
    public DateTimeOffset? Created { get; set; }

    /// <summary><c>CustomProperties</c>.</summary>
    public string? CustomProperties { get; set; }

    /// <summary><c>DisplayName</c>.</summary>
    public string? DisplayName { get; set; }

    /// <summary><c>LastLogin</c>.</summary>
    public DateTimeOffset? LastLogin { get; set; }

    /// <summary><c>LinkedAccounts</c>.</summary>
    public IReadOnlyList<SegmentsPlayerLinkedAccount>? LinkedAccounts { get; set; }

    /// <summary><c>Locations</c>.</summary>
    public IReadOnlyDictionary<string, SegmentsPlayerLocation>? Locations { get; set; }

    /// <summary><c>Origination</c>.</summary>
    public LoginIdentityProvider? Origination { get; set; }

    /// <summary><c>PlayerExperimentVariants</c>.</summary>
    public IReadOnlyList<string>? PlayerExperimentVariants { get; set; }

    /// <summary><c>PlayerId</c>.</summary>
    public string? PlayerId { get; set; }

    /// <summary><c>PlayerStatistics</c>.</summary>
    public IReadOnlyList<SegmentsPlayerStatistic>? PlayerStatistics { get; set; }

    /// <summary><c>PublisherId</c>.</summary>
    public string? PublisherId { get; set; }

    /// <summary><c>PushNotificationRegistrations</c>.</summary>
    public IReadOnlyList<SegmentsPushNotificationRegistration>? PushNotificationRegistrations { get; set; }

    /// <summary><c>Statistics</c>.</summary>
    public IReadOnlyDictionary<string, int>? Statistics { get; set; }

    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    /// <summary><c>TitleId</c>.</summary>
    public string? TitleId { get; set; }

    /// <summary><c>TotalValueToDateInUSD</c>.</summary>
    public uint? TotalValueToDateInUSD { get; set; }

    /// <summary><c>ValuesToDate</c>.</summary>
    public IReadOnlyDictionary<string, uint>? ValuesToDate { get; set; }

    /// <summary><c>VirtualCurrencyBalances</c>.</summary>
    public IReadOnlyDictionary<string, int>? VirtualCurrencyBalances { get; set; }

    internal static unsafe SegmentsPlayerProfile FromNative(PFSegmentsPlayerProfile* source)
    {
        var value = new SegmentsPlayerProfile();
        if (source->AdCampaignAttributions is not null && source->AdCampaignAttributionsCount != 0)
        {
            var list0 = new List<SegmentsAdCampaignAttribution>((int)source->AdCampaignAttributionsCount);
            for (uint i0 = 0; i0 < source->AdCampaignAttributionsCount; i0++)
            {
                list0.Add(SegmentsAdCampaignAttribution.FromNative(source->AdCampaignAttributions[i0]));
            }

            value.AdCampaignAttributions = list0;
        }
        value.AvatarUrl = Utf8.ToString(source->AvatarUrl);
        value.BannedUntil = source->BannedUntil is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->BannedUntil);
        value.ChurnPrediction = source->ChurnPrediction is null ? null : (SegmentsChurnRiskLevel?)(*source->ChurnPrediction);
        if (source->ContactEmailAddresses is not null && source->ContactEmailAddressesCount != 0)
        {
            var list5 = new List<SegmentsContactEmailInfo>((int)source->ContactEmailAddressesCount);
            for (uint i5 = 0; i5 < source->ContactEmailAddressesCount; i5++)
            {
                list5.Add(SegmentsContactEmailInfo.FromNative(source->ContactEmailAddresses[i5]));
            }

            value.ContactEmailAddresses = list5;
        }
        value.Created = source->Created is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->Created);
        value.CustomProperties = Utf8.ToString(source->CustomProperties.StringValue);
        value.DisplayName = Utf8.ToString(source->DisplayName);
        value.LastLogin = source->LastLogin is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->LastLogin);
        if (source->LinkedAccounts is not null && source->LinkedAccountsCount != 0)
        {
            var list11 = new List<SegmentsPlayerLinkedAccount>((int)source->LinkedAccountsCount);
            for (uint i11 = 0; i11 < source->LinkedAccountsCount; i11++)
            {
                list11.Add(SegmentsPlayerLinkedAccount.FromNative(source->LinkedAccounts[i11]));
            }

            value.LinkedAccounts = list11;
        }
        if (source->Locations is not null && source->LocationsCount != 0)
        {
            var map13 = new Dictionary<string, SegmentsPlayerLocation>((int)source->LocationsCount, StringComparer.Ordinal);
            for (uint i13 = 0; i13 < source->LocationsCount; i13++)
            {
                map13[Utf8.ToString(source->Locations[i13].Key) ?? string.Empty] = SegmentsPlayerLocation.FromNative(source->Locations[i13].Value);
            }

            value.Locations = map13;
        }
        value.Origination = source->Origination is null ? null : (LoginIdentityProvider?)(*source->Origination);
        if (source->PlayerExperimentVariants is not null && source->PlayerExperimentVariantsCount != 0)
        {
            var list16 = new List<string>((int)source->PlayerExperimentVariantsCount);
            for (uint i16 = 0; i16 < source->PlayerExperimentVariantsCount; i16++)
            {
                list16.Add(Utf8.ToString(source->PlayerExperimentVariants[i16]) ?? string.Empty);
            }

            value.PlayerExperimentVariants = list16;
        }
        value.PlayerId = Utf8.ToString(source->PlayerId);
        if (source->PlayerStatistics is not null && source->PlayerStatisticsCount != 0)
        {
            var list19 = new List<SegmentsPlayerStatistic>((int)source->PlayerStatisticsCount);
            for (uint i19 = 0; i19 < source->PlayerStatisticsCount; i19++)
            {
                list19.Add(SegmentsPlayerStatistic.FromNative(source->PlayerStatistics[i19]));
            }

            value.PlayerStatistics = list19;
        }
        value.PublisherId = Utf8.ToString(source->PublisherId);
        if (source->PushNotificationRegistrations is not null && source->PushNotificationRegistrationsCount != 0)
        {
            var list22 = new List<SegmentsPushNotificationRegistration>((int)source->PushNotificationRegistrationsCount);
            for (uint i22 = 0; i22 < source->PushNotificationRegistrationsCount; i22++)
            {
                list22.Add(SegmentsPushNotificationRegistration.FromNative(source->PushNotificationRegistrations[i22]));
            }

            value.PushNotificationRegistrations = list22;
        }
        if (source->Statistics is not null && source->StatisticsCount != 0)
        {
            var map24 = new Dictionary<string, int>((int)source->StatisticsCount, StringComparer.Ordinal);
            for (uint i24 = 0; i24 < source->StatisticsCount; i24++)
            {
                map24[Utf8.ToString(source->Statistics[i24].Key) ?? string.Empty] = source->Statistics[i24].Value;
            }

            value.Statistics = map24;
        }
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list26 = new List<string>((int)source->TagsCount);
            for (uint i26 = 0; i26 < source->TagsCount; i26++)
            {
                list26.Add(Utf8.ToString(source->Tags[i26]) ?? string.Empty);
            }

            value.Tags = list26;
        }
        value.TitleId = Utf8.ToString(source->TitleId);
        value.TotalValueToDateInUSD = source->TotalValueToDateInUSD is null ? null : (uint?)(*source->TotalValueToDateInUSD);
        if (source->ValuesToDate is not null && source->ValuesToDateCount != 0)
        {
            var map30 = new Dictionary<string, uint>((int)source->ValuesToDateCount, StringComparer.Ordinal);
            for (uint i30 = 0; i30 < source->ValuesToDateCount; i30++)
            {
                map30[Utf8.ToString(source->ValuesToDate[i30].Key) ?? string.Empty] = source->ValuesToDate[i30].Value;
            }

            value.ValuesToDate = map30;
        }
        if (source->VirtualCurrencyBalances is not null && source->VirtualCurrencyBalancesCount != 0)
        {
            var map32 = new Dictionary<string, int>((int)source->VirtualCurrencyBalancesCount, StringComparer.Ordinal);
            for (uint i32 = 0; i32 < source->VirtualCurrencyBalancesCount; i32++)
            {
                map32[Utf8.ToString(source->VirtualCurrencyBalances[i32].Key) ?? string.Empty] = source->VirtualCurrencyBalances[i32].Value;
            }

            value.VirtualCurrencyBalances = map32;
        }
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsPlayerProfile* target, PlayFabArena arena)
    {
        *target = default;
        target->AdCampaignAttributionsCount = (uint)(AdCampaignAttributions is null ? 0 : AdCampaignAttributions.Count);
        if (AdCampaignAttributions is not null && AdCampaignAttributions.Count > 0)
        {
            PFSegmentsAdCampaignAttribution** array0 = arena.PointerArray<PFSegmentsAdCampaignAttribution>(AdCampaignAttributions.Count);
            for (int i0 = 0; i0 < AdCampaignAttributions.Count; i0++)
            {
                PFSegmentsAdCampaignAttribution* item0 = arena.Alloc<PFSegmentsAdCampaignAttribution>(1);
                AdCampaignAttributions[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->AdCampaignAttributions = array0;
        }
        target->AvatarUrl = arena.String(AvatarUrl);
        target->BannedUntil = arena.Value(BannedUntil is null ? null : (long?)PlayFabTime.ToUnixTime(BannedUntil.Value));
        target->ChurnPrediction = arena.Value((PFSegmentsChurnRiskLevel?)ChurnPrediction);
        target->ContactEmailAddressesCount = (uint)(ContactEmailAddresses is null ? 0 : ContactEmailAddresses.Count);
        if (ContactEmailAddresses is not null && ContactEmailAddresses.Count > 0)
        {
            PFSegmentsContactEmailInfo** array5 = arena.PointerArray<PFSegmentsContactEmailInfo>(ContactEmailAddresses.Count);
            for (int i5 = 0; i5 < ContactEmailAddresses.Count; i5++)
            {
                PFSegmentsContactEmailInfo* item5 = arena.Alloc<PFSegmentsContactEmailInfo>(1);
                ContactEmailAddresses[i5].WriteTo(item5, arena);
                array5[i5] = item5;
            }

            target->ContactEmailAddresses = array5;
        }
        target->Created = arena.Value(Created is null ? null : (long?)PlayFabTime.ToUnixTime(Created.Value));
        target->CustomProperties.StringValue = arena.String(CustomProperties);
        target->DisplayName = arena.String(DisplayName);
        target->LastLogin = arena.Value(LastLogin is null ? null : (long?)PlayFabTime.ToUnixTime(LastLogin.Value));
        target->LinkedAccountsCount = (uint)(LinkedAccounts is null ? 0 : LinkedAccounts.Count);
        if (LinkedAccounts is not null && LinkedAccounts.Count > 0)
        {
            PFSegmentsPlayerLinkedAccount** array11 = arena.PointerArray<PFSegmentsPlayerLinkedAccount>(LinkedAccounts.Count);
            for (int i11 = 0; i11 < LinkedAccounts.Count; i11++)
            {
                PFSegmentsPlayerLinkedAccount* item11 = arena.Alloc<PFSegmentsPlayerLinkedAccount>(1);
                LinkedAccounts[i11].WriteTo(item11, arena);
                array11[i11] = item11;
            }

            target->LinkedAccounts = array11;
        }
        target->LocationsCount = (uint)(Locations is null ? 0 : Locations.Count);
        if (Locations is not null && Locations.Count > 0)
        {
            PFSegmentsPlayerLocationDictionaryEntry* entries13 = arena.Alloc<PFSegmentsPlayerLocationDictionaryEntry>(Locations.Count);
            int next13 = 0;
            foreach (KeyValuePair<string, SegmentsPlayerLocation> pair13 in Locations)
            {
                entries13[next13].Key = arena.String(pair13.Key);
                if (pair13.Value is not null)
                {
                    PFSegmentsPlayerLocation* item13 = arena.Alloc<PFSegmentsPlayerLocation>(1);
                    pair13.Value.WriteTo(item13, arena);
                    entries13[next13].Value = item13;
                }
                next13++;
            }

            target->Locations = entries13;
        }
        target->Origination = arena.Value((PFLoginIdentityProvider?)Origination);
        target->PlayerExperimentVariantsCount = (uint)(PlayerExperimentVariants is null ? 0 : PlayerExperimentVariants.Count);
        target->PlayerExperimentVariants = arena.StringArray(PlayerExperimentVariants);
        target->PlayerId = arena.String(PlayerId);
        target->PlayerStatisticsCount = (uint)(PlayerStatistics is null ? 0 : PlayerStatistics.Count);
        if (PlayerStatistics is not null && PlayerStatistics.Count > 0)
        {
            PFSegmentsPlayerStatistic** array19 = arena.PointerArray<PFSegmentsPlayerStatistic>(PlayerStatistics.Count);
            for (int i19 = 0; i19 < PlayerStatistics.Count; i19++)
            {
                PFSegmentsPlayerStatistic* item19 = arena.Alloc<PFSegmentsPlayerStatistic>(1);
                PlayerStatistics[i19].WriteTo(item19, arena);
                array19[i19] = item19;
            }

            target->PlayerStatistics = array19;
        }
        target->PublisherId = arena.String(PublisherId);
        target->PushNotificationRegistrationsCount = (uint)(PushNotificationRegistrations is null ? 0 : PushNotificationRegistrations.Count);
        if (PushNotificationRegistrations is not null && PushNotificationRegistrations.Count > 0)
        {
            PFSegmentsPushNotificationRegistration** array22 = arena.PointerArray<PFSegmentsPushNotificationRegistration>(PushNotificationRegistrations.Count);
            for (int i22 = 0; i22 < PushNotificationRegistrations.Count; i22++)
            {
                PFSegmentsPushNotificationRegistration* item22 = arena.Alloc<PFSegmentsPushNotificationRegistration>(1);
                PushNotificationRegistrations[i22].WriteTo(item22, arena);
                array22[i22] = item22;
            }

            target->PushNotificationRegistrations = array22;
        }
        target->StatisticsCount = (uint)(Statistics is null ? 0 : Statistics.Count);
        if (Statistics is not null && Statistics.Count > 0)
        {
            PFInt32DictionaryEntry* entries24 = arena.Alloc<PFInt32DictionaryEntry>(Statistics.Count);
            int next24 = 0;
            foreach (KeyValuePair<string, int> pair24 in Statistics)
            {
                entries24[next24].Key = arena.String(pair24.Key);
                entries24[next24].Value = pair24.Value;
                next24++;
            }

            target->Statistics = entries24;
        }
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
        target->TitleId = arena.String(TitleId);
        target->TotalValueToDateInUSD = arena.Value(TotalValueToDateInUSD);
        target->ValuesToDateCount = (uint)(ValuesToDate is null ? 0 : ValuesToDate.Count);
        if (ValuesToDate is not null && ValuesToDate.Count > 0)
        {
            PFUint32DictionaryEntry* entries30 = arena.Alloc<PFUint32DictionaryEntry>(ValuesToDate.Count);
            int next30 = 0;
            foreach (KeyValuePair<string, uint> pair30 in ValuesToDate)
            {
                entries30[next30].Key = arena.String(pair30.Key);
                entries30[next30].Value = pair30.Value;
                next30++;
            }

            target->ValuesToDate = entries30;
        }
        target->VirtualCurrencyBalancesCount = (uint)(VirtualCurrencyBalances is null ? 0 : VirtualCurrencyBalances.Count);
        if (VirtualCurrencyBalances is not null && VirtualCurrencyBalances.Count > 0)
        {
            PFInt32DictionaryEntry* entries32 = arena.Alloc<PFInt32DictionaryEntry>(VirtualCurrencyBalances.Count);
            int next32 = 0;
            foreach (KeyValuePair<string, int> pair32 in VirtualCurrencyBalances)
            {
                entries32[next32].Key = arena.String(pair32.Key);
                entries32[next32].Value = pair32.Value;
                next32++;
            }

            target->VirtualCurrencyBalances = entries32;
        }
    }
}

/// <summary>Projects <c>PFSegmentsPlayerStatistic</c>.</summary>
public sealed class SegmentsPlayerStatistic
{
    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    /// <summary><c>StatisticValue</c>.</summary>
    public int StatisticValue { get; set; }

    /// <summary><c>StatisticVersion</c>.</summary>
    public int StatisticVersion { get; set; }

    internal static unsafe SegmentsPlayerStatistic FromNative(PFSegmentsPlayerStatistic* source)
    {
        var value = new SegmentsPlayerStatistic();
        value.Id = Utf8.ToString(source->Id);
        value.Name = Utf8.ToString(source->Name);
        value.StatisticValue = source->StatisticValue;
        value.StatisticVersion = source->StatisticVersion;
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsPlayerStatistic* target, PlayFabArena arena)
    {
        *target = default;
        target->Id = arena.String(Id);
        target->Name = arena.String(Name);
        target->StatisticValue = StatisticValue;
        target->StatisticVersion = StatisticVersion;
    }
}

/// <summary>Projects <c>PFSegmentsPushNotificationRegistration</c>.</summary>
public sealed class SegmentsPushNotificationRegistration
{
    /// <summary><c>NotificationEndpointARN</c>.</summary>
    public string? NotificationEndpointARN { get; set; }

    /// <summary><c>Platform</c>.</summary>
    public PushNotificationPlatform? Platform { get; set; }

    internal static unsafe SegmentsPushNotificationRegistration FromNative(PFSegmentsPushNotificationRegistration* source)
    {
        var value = new SegmentsPushNotificationRegistration();
        value.NotificationEndpointARN = Utf8.ToString(source->NotificationEndpointARN);
        value.Platform = source->Platform is null ? null : (PushNotificationPlatform?)(*source->Platform);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsPushNotificationRegistration* target, PlayFabArena arena)
    {
        *target = default;
        target->NotificationEndpointARN = arena.String(NotificationEndpointARN);
        target->Platform = arena.Value((PFPushNotificationPlatform?)Platform);
    }
}

/// <summary>Projects <c>PFSegmentsRemovePlayerTagRequest</c>.</summary>
public sealed class SegmentsRemovePlayerTagRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>TagName</c>.</summary>
    public string? TagName { get; set; }

    internal static unsafe SegmentsRemovePlayerTagRequest FromNative(PFSegmentsRemovePlayerTagRequest* source)
    {
        var value = new SegmentsRemovePlayerTagRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.TagName = Utf8.ToString(source->TagName);
        return value;
    }

    internal unsafe void WriteTo(PFSegmentsRemovePlayerTagRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
        target->TagName = arena.String(TagName);
    }
}
