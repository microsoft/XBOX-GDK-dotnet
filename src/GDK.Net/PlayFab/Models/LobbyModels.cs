// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFLobby
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFLobbyInviteListenerStatus</c>.</summary>
public enum LobbyInviteListenerStatus : uint
{
    /// <summary>Projects <c>PFLobbyInviteListenerStatus::NotListening</c>.</summary>
    NotListening = 0,
    /// <summary>Projects <c>PFLobbyInviteListenerStatus::Listening</c>.</summary>
    Listening = 1,
    /// <summary>Projects <c>PFLobbyInviteListenerStatus::NotAuthorized</c>.</summary>
    NotAuthorized = 2,
}

/// <summary>Projects <c>PFLobbyServerConnectionStatus</c>.</summary>
public enum LobbyServerConnectionStatus : uint
{
    /// <summary>Projects <c>PFLobbyServerConnectionStatus::NotConnected</c>.</summary>
    NotConnected = 0,
    /// <summary>Projects <c>PFLobbyServerConnectionStatus::Connected</c>.</summary>
    Connected = 1,
}

/// <summary>Projects <c>PFLobbyMemberConnectionStatus</c>.</summary>
public enum LobbyMemberConnectionStatus : uint
{
    /// <summary>Projects <c>PFLobbyMemberConnectionStatus::NotConnected</c>.</summary>
    NotConnected = 0,
    /// <summary>Projects <c>PFLobbyMemberConnectionStatus::Connected</c>.</summary>
    Connected = 1,
}

/// <summary>Projects <c>PFLobbyDisconnectingReason</c>.</summary>
public enum LobbyDisconnectingReason : uint
{
    /// <summary>Projects <c>PFLobbyDisconnectingReason::NoLocalMembers</c>.</summary>
    NoLocalMembers = 0,
    /// <summary>Projects <c>PFLobbyDisconnectingReason::LobbyDeleted</c>.</summary>
    LobbyDeleted = 1,
    /// <summary>Projects <c>PFLobbyDisconnectingReason::ConnectionInterruption</c>.</summary>
    ConnectionInterruption = 2,
    /// <summary>Projects <c>PFLobbyDisconnectingReason::LobbyServerLeft</c>.</summary>
    LobbyServerLeft = 3,
}

/// <summary>Projects <c>PFLobbyMemberRemovedReason</c>.</summary>
public enum LobbyMemberRemovedReason : uint
{
    /// <summary>Projects <c>PFLobbyMemberRemovedReason::LocalUserLeftLobby</c>.</summary>
    LocalUserLeftLobby = 0,
    /// <summary>Projects <c>PFLobbyMemberRemovedReason::LocalUserForciblyRemoved</c>.</summary>
    LocalUserForciblyRemoved = 1,
    /// <summary>Projects <c>PFLobbyMemberRemovedReason::RemoteUserLeftLobby</c>.</summary>
    RemoteUserLeftLobby = 2,
}

/// <summary>Projects <c>PFLobbyStateChangeType</c>.</summary>
public enum LobbyStateChangeType : uint
{
    /// <summary>Projects <c>PFLobbyStateChangeType::CreateAndJoinLobbyCompleted</c>.</summary>
    CreateAndJoinLobbyCompleted = 0,
    /// <summary>Projects <c>PFLobbyStateChangeType::JoinLobbyCompleted</c>.</summary>
    JoinLobbyCompleted = 1,
    /// <summary>Projects <c>PFLobbyStateChangeType::MemberAdded</c>.</summary>
    MemberAdded = 2,
    /// <summary>Projects <c>PFLobbyStateChangeType::AddMemberCompleted</c>.</summary>
    AddMemberCompleted = 3,
    /// <summary>Projects <c>PFLobbyStateChangeType::MemberRemoved</c>.</summary>
    MemberRemoved = 4,
    /// <summary>Projects <c>PFLobbyStateChangeType::ForceRemoveMemberCompleted</c>.</summary>
    ForceRemoveMemberCompleted = 5,
    /// <summary>Projects <c>PFLobbyStateChangeType::LeaveLobbyCompleted</c>.</summary>
    LeaveLobbyCompleted = 6,
    /// <summary>Projects <c>PFLobbyStateChangeType::Updated</c>.</summary>
    Updated = 7,
    /// <summary>Projects <c>PFLobbyStateChangeType::PostUpdateCompleted</c>.</summary>
    PostUpdateCompleted = 8,
    /// <summary>Projects <c>PFLobbyStateChangeType::Disconnecting</c>.</summary>
    Disconnecting = 9,
    /// <summary>Projects <c>PFLobbyStateChangeType::Disconnected</c>.</summary>
    Disconnected = 10,
    /// <summary>Projects <c>PFLobbyStateChangeType::JoinArrangedLobbyCompleted</c>.</summary>
    JoinArrangedLobbyCompleted = 11,
    /// <summary>Projects <c>PFLobbyStateChangeType::FindLobbiesCompleted</c>.</summary>
    FindLobbiesCompleted = 12,
    /// <summary>Projects <c>PFLobbyStateChangeType::InviteReceived</c>.</summary>
    InviteReceived = 13,
    /// <summary>Projects <c>PFLobbyStateChangeType::InviteListenerStatusChanged</c>.</summary>
    InviteListenerStatusChanged = 14,
    /// <summary>Projects <c>PFLobbyStateChangeType::SendInviteCompleted</c>.</summary>
    SendInviteCompleted = 15,
    /// <summary>Projects <c>PFLobbyStateChangeType::CreateAndClaimServerLobbyCompleted</c>.</summary>
    CreateAndClaimServerLobbyCompleted = 16,
    /// <summary>Projects <c>PFLobbyStateChangeType::ClaimServerLobbyCompleted</c>.</summary>
    ClaimServerLobbyCompleted = 17,
    /// <summary>Projects <c>PFLobbyStateChangeType::ServerPostUpdateCompleted</c>.</summary>
    ServerPostUpdateCompleted = 18,
    /// <summary>Projects <c>PFLobbyStateChangeType::ServerDeleteLobbyCompleted</c>.</summary>
    ServerDeleteLobbyCompleted = 19,
    /// <summary>Projects <c>PFLobbyStateChangeType::JoinLobbyAsServerCompleted</c>.</summary>
    JoinLobbyAsServerCompleted = 20,
    /// <summary>Projects <c>PFLobbyStateChangeType::ServerPostUpdateAsServerCompleted</c>.</summary>
    ServerPostUpdateAsServerCompleted = 21,
    /// <summary>Projects <c>PFLobbyStateChangeType::ServerLeaveLobbyAsServerCompleted</c>.</summary>
    ServerLeaveLobbyAsServerCompleted = 22,
    /// <summary>Projects <c>PFLobbyStateChangeType::ConnectToLobbyCompleted</c>.</summary>
    ConnectToLobbyCompleted = 23,
}

/// <summary>Projects <c>PFLobbyOwnerMigrationPolicy</c>.</summary>
public enum LobbyOwnerMigrationPolicy : uint
{
    /// <summary>Projects <c>PFLobbyOwnerMigrationPolicy::Automatic</c>.</summary>
    Automatic = 0,
    /// <summary>Projects <c>PFLobbyOwnerMigrationPolicy::Manual</c>.</summary>
    Manual = 1,
    /// <summary>Projects <c>PFLobbyOwnerMigrationPolicy::None</c>.</summary>
    None = 2,
    /// <summary>Projects <c>PFLobbyOwnerMigrationPolicy::Server</c>.</summary>
    Server = 3,
}

/// <summary>Projects <c>PFLobbyMembershipLock</c>.</summary>
public enum LobbyMembershipLock : uint
{
    /// <summary>Projects <c>PFLobbyMembershipLock::Unlocked</c>.</summary>
    Unlocked = 0,
    /// <summary>Projects <c>PFLobbyMembershipLock::Locked</c>.</summary>
    Locked = 1,
}

/// <summary>Projects <c>PFLobbyAccessPolicy</c>.</summary>
public enum LobbyAccessPolicy : uint
{
    /// <summary>Projects <c>PFLobbyAccessPolicy::Public</c>.</summary>
    Public = 0,
    /// <summary>Projects <c>PFLobbyAccessPolicy::Friends</c>.</summary>
    Friends = 1,
    /// <summary>Projects <c>PFLobbyAccessPolicy::Private</c>.</summary>
    Private = 2,
}

/// <summary>Projects <c>PFLobbyArrangedJoinConfiguration</c>.</summary>
public sealed class LobbyArrangedJoinConfiguration
{
    /// <summary><c>MaxMemberCount</c>.</summary>
    public uint MaxMemberCount { get; set; }

    /// <summary><c>OwnerMigrationPolicy</c>.</summary>
    public LobbyOwnerMigrationPolicy OwnerMigrationPolicy { get; set; }

    /// <summary><c>AccessPolicy</c>.</summary>
    public LobbyAccessPolicy AccessPolicy { get; set; }

    /// <summary>The <c>MemberPropertyKeys</c> / <c>MemberPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? MemberProperties { get; set; }

    /// <summary><c>RestrictInvitesToLobbyOwner</c>.</summary>
    public bool RestrictInvitesToLobbyOwner { get; set; }

    internal static unsafe LobbyArrangedJoinConfiguration FromNative(PFLobbyArrangedJoinConfiguration* source)
    {
        var value = new LobbyArrangedJoinConfiguration();
        value.MaxMemberCount = source->MaxMemberCount;
        value.OwnerMigrationPolicy = (LobbyOwnerMigrationPolicy)source->OwnerMigrationPolicy;
        value.AccessPolicy = (LobbyAccessPolicy)source->AccessPolicy;
        if (source->MemberPropertyKeys is not null && source->MemberPropertyValues is not null && source->MemberPropertyCount != 0)
        {
            var map4 = new Dictionary<string, string>((int)source->MemberPropertyCount, StringComparer.Ordinal);
            for (uint i4 = 0; i4 < source->MemberPropertyCount; i4++)
            {
                string? key4 = Utf8.ToString(source->MemberPropertyKeys[i4]);
                if (key4 is not null)
                {
                    map4[key4] = Utf8.ToString(source->MemberPropertyValues[i4]) ?? string.Empty;
                }
            }

            value.MemberProperties = map4;
        }
        value.RestrictInvitesToLobbyOwner = source->RestrictInvitesToLobbyOwner != 0;
        return value;
    }

    internal unsafe void WriteTo(PFLobbyArrangedJoinConfiguration* target, PlayFabArena arena)
    {
        *target = default;
        target->MaxMemberCount = MaxMemberCount;
        target->OwnerMigrationPolicy = (PFLobbyOwnerMigrationPolicy)OwnerMigrationPolicy;
        target->AccessPolicy = (PFLobbyAccessPolicy)AccessPolicy;
        target->MemberPropertyCount = (uint)(MemberProperties is null ? 0 : MemberProperties.Count);
        if (MemberProperties is not null && MemberProperties.Count > 0)
        {
            byte** keys4 = arena.PointerArray<byte>(MemberProperties.Count);
            byte** values4 = arena.PointerArray<byte>(MemberProperties.Count);
            int next4 = 0;
            foreach (KeyValuePair<string, string> pair4 in MemberProperties)
            {
                keys4[next4] = arena.String(pair4.Key);
                values4[next4] = arena.String(pair4.Value);
                next4++;
            }

            target->MemberPropertyKeys = keys4;
            target->MemberPropertyValues = values4;
        }
        target->RestrictInvitesToLobbyOwner = RestrictInvitesToLobbyOwner ? (byte)1 : (byte)0;
    }
}

/// <summary>Projects <c>PFLobbyCreateConfiguration</c>.</summary>
public sealed class LobbyCreateConfiguration
{
    /// <summary><c>MaxMemberCount</c>.</summary>
    public uint MaxMemberCount { get; set; }

    /// <summary><c>OwnerMigrationPolicy</c>.</summary>
    public LobbyOwnerMigrationPolicy OwnerMigrationPolicy { get; set; }

    /// <summary><c>AccessPolicy</c>.</summary>
    public LobbyAccessPolicy AccessPolicy { get; set; }

    /// <summary>The <c>SearchPropertyKeys</c> / <c>SearchPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? SearchProperties { get; set; }

    /// <summary>The <c>LobbyPropertyKeys</c> / <c>LobbyPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? LobbyProperties { get; set; }

    /// <summary><c>RestrictInvitesToLobbyOwner</c>.</summary>
    public bool RestrictInvitesToLobbyOwner { get; set; }

    internal static unsafe LobbyCreateConfiguration FromNative(PFLobbyCreateConfiguration* source)
    {
        var value = new LobbyCreateConfiguration();
        value.MaxMemberCount = source->MaxMemberCount;
        value.OwnerMigrationPolicy = (LobbyOwnerMigrationPolicy)source->OwnerMigrationPolicy;
        value.AccessPolicy = (LobbyAccessPolicy)source->AccessPolicy;
        if (source->SearchPropertyKeys is not null && source->SearchPropertyValues is not null && source->SearchPropertyCount != 0)
        {
            var map4 = new Dictionary<string, string>((int)source->SearchPropertyCount, StringComparer.Ordinal);
            for (uint i4 = 0; i4 < source->SearchPropertyCount; i4++)
            {
                string? key4 = Utf8.ToString(source->SearchPropertyKeys[i4]);
                if (key4 is not null)
                {
                    map4[key4] = Utf8.ToString(source->SearchPropertyValues[i4]) ?? string.Empty;
                }
            }

            value.SearchProperties = map4;
        }
        if (source->LobbyPropertyKeys is not null && source->LobbyPropertyValues is not null && source->LobbyPropertyCount != 0)
        {
            var map7 = new Dictionary<string, string>((int)source->LobbyPropertyCount, StringComparer.Ordinal);
            for (uint i7 = 0; i7 < source->LobbyPropertyCount; i7++)
            {
                string? key7 = Utf8.ToString(source->LobbyPropertyKeys[i7]);
                if (key7 is not null)
                {
                    map7[key7] = Utf8.ToString(source->LobbyPropertyValues[i7]) ?? string.Empty;
                }
            }

            value.LobbyProperties = map7;
        }
        value.RestrictInvitesToLobbyOwner = source->RestrictInvitesToLobbyOwner != 0;
        return value;
    }

    internal unsafe void WriteTo(PFLobbyCreateConfiguration* target, PlayFabArena arena)
    {
        *target = default;
        target->MaxMemberCount = MaxMemberCount;
        target->OwnerMigrationPolicy = (PFLobbyOwnerMigrationPolicy)OwnerMigrationPolicy;
        target->AccessPolicy = (PFLobbyAccessPolicy)AccessPolicy;
        target->SearchPropertyCount = (uint)(SearchProperties is null ? 0 : SearchProperties.Count);
        if (SearchProperties is not null && SearchProperties.Count > 0)
        {
            byte** keys4 = arena.PointerArray<byte>(SearchProperties.Count);
            byte** values4 = arena.PointerArray<byte>(SearchProperties.Count);
            int next4 = 0;
            foreach (KeyValuePair<string, string> pair4 in SearchProperties)
            {
                keys4[next4] = arena.String(pair4.Key);
                values4[next4] = arena.String(pair4.Value);
                next4++;
            }

            target->SearchPropertyKeys = keys4;
            target->SearchPropertyValues = values4;
        }
        target->LobbyPropertyCount = (uint)(LobbyProperties is null ? 0 : LobbyProperties.Count);
        if (LobbyProperties is not null && LobbyProperties.Count > 0)
        {
            byte** keys7 = arena.PointerArray<byte>(LobbyProperties.Count);
            byte** values7 = arena.PointerArray<byte>(LobbyProperties.Count);
            int next7 = 0;
            foreach (KeyValuePair<string, string> pair7 in LobbyProperties)
            {
                keys7[next7] = arena.String(pair7.Key);
                values7[next7] = arena.String(pair7.Value);
                next7++;
            }

            target->LobbyPropertyKeys = keys7;
            target->LobbyPropertyValues = values7;
        }
        target->RestrictInvitesToLobbyOwner = RestrictInvitesToLobbyOwner ? (byte)1 : (byte)0;
    }
}

/// <summary>Projects <c>PFLobbyDataUpdate</c>.</summary>
public sealed class LobbyDataUpdate
{
    /// <summary><c>NewOwner</c>.</summary>
    public EntityKey? NewOwner { get; set; }

    /// <summary><c>MaxMemberCount</c>.</summary>
    public uint? MaxMemberCount { get; set; }

    /// <summary><c>AccessPolicy</c>.</summary>
    public LobbyAccessPolicy? AccessPolicy { get; set; }

    /// <summary><c>MembershipLock</c>.</summary>
    public LobbyMembershipLock? MembershipLock { get; set; }

    /// <summary>The <c>SearchPropertyKeys</c> / <c>SearchPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? SearchProperties { get; set; }

    /// <summary>The <c>LobbyPropertyKeys</c> / <c>LobbyPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? LobbyProperties { get; set; }

    /// <summary><c>RestrictInvitesToLobbyOwner</c>.</summary>
    public bool? RestrictInvitesToLobbyOwner { get; set; }

    internal static unsafe LobbyDataUpdate FromNative(PFLobbyDataUpdate* source)
    {
        var value = new LobbyDataUpdate();
        value.NewOwner = source->NewOwner is null ? null : EntityKey.FromNative(source->NewOwner);
        value.MaxMemberCount = source->MaxMemberCount is null ? null : (uint?)(*source->MaxMemberCount);
        value.AccessPolicy = source->AccessPolicy is null ? null : (LobbyAccessPolicy?)(*source->AccessPolicy);
        value.MembershipLock = source->MembershipLock is null ? null : (LobbyMembershipLock?)(*source->MembershipLock);
        if (source->SearchPropertyKeys is not null && source->SearchPropertyValues is not null && source->SearchPropertyCount != 0)
        {
            var map5 = new Dictionary<string, string>((int)source->SearchPropertyCount, StringComparer.Ordinal);
            for (uint i5 = 0; i5 < source->SearchPropertyCount; i5++)
            {
                string? key5 = Utf8.ToString(source->SearchPropertyKeys[i5]);
                if (key5 is not null)
                {
                    map5[key5] = Utf8.ToString(source->SearchPropertyValues[i5]) ?? string.Empty;
                }
            }

            value.SearchProperties = map5;
        }
        if (source->LobbyPropertyKeys is not null && source->LobbyPropertyValues is not null && source->LobbyPropertyCount != 0)
        {
            var map8 = new Dictionary<string, string>((int)source->LobbyPropertyCount, StringComparer.Ordinal);
            for (uint i8 = 0; i8 < source->LobbyPropertyCount; i8++)
            {
                string? key8 = Utf8.ToString(source->LobbyPropertyKeys[i8]);
                if (key8 is not null)
                {
                    map8[key8] = Utf8.ToString(source->LobbyPropertyValues[i8]) ?? string.Empty;
                }
            }

            value.LobbyProperties = map8;
        }
        value.RestrictInvitesToLobbyOwner = source->RestrictInvitesToLobbyOwner is null ? null : (bool?)(*source->RestrictInvitesToLobbyOwner != 0);
        return value;
    }

    internal unsafe void WriteTo(PFLobbyDataUpdate* target, PlayFabArena arena)
    {
        *target = default;
        if (NewOwner is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            NewOwner.WriteTo(native0, arena);
            target->NewOwner = native0;
        }
        target->MaxMemberCount = arena.Value(MaxMemberCount);
        target->AccessPolicy = arena.Value((PFLobbyAccessPolicy?)AccessPolicy);
        target->MembershipLock = arena.Value((PFLobbyMembershipLock?)MembershipLock);
        target->SearchPropertyCount = (uint)(SearchProperties is null ? 0 : SearchProperties.Count);
        if (SearchProperties is not null && SearchProperties.Count > 0)
        {
            byte** keys5 = arena.PointerArray<byte>(SearchProperties.Count);
            byte** values5 = arena.PointerArray<byte>(SearchProperties.Count);
            int next5 = 0;
            foreach (KeyValuePair<string, string> pair5 in SearchProperties)
            {
                keys5[next5] = arena.String(pair5.Key);
                values5[next5] = arena.String(pair5.Value);
                next5++;
            }

            target->SearchPropertyKeys = keys5;
            target->SearchPropertyValues = values5;
        }
        target->LobbyPropertyCount = (uint)(LobbyProperties is null ? 0 : LobbyProperties.Count);
        if (LobbyProperties is not null && LobbyProperties.Count > 0)
        {
            byte** keys8 = arena.PointerArray<byte>(LobbyProperties.Count);
            byte** values8 = arena.PointerArray<byte>(LobbyProperties.Count);
            int next8 = 0;
            foreach (KeyValuePair<string, string> pair8 in LobbyProperties)
            {
                keys8[next8] = arena.String(pair8.Key);
                values8[next8] = arena.String(pair8.Value);
                next8++;
            }

            target->LobbyPropertyKeys = keys8;
            target->LobbyPropertyValues = values8;
        }
        target->RestrictInvitesToLobbyOwner = arena.Value(RestrictInvitesToLobbyOwner is null ? null : (RestrictInvitesToLobbyOwner.Value ? (byte?)1 : (byte?)0));
    }
}

/// <summary>Projects <c>PFLobbyJoinConfiguration</c>.</summary>
public sealed class LobbyJoinConfiguration
{
    /// <summary>The <c>MemberPropertyKeys</c> / <c>MemberPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? MemberProperties { get; set; }

    internal static unsafe LobbyJoinConfiguration FromNative(PFLobbyJoinConfiguration* source)
    {
        var value = new LobbyJoinConfiguration();
        if (source->MemberPropertyKeys is not null && source->MemberPropertyValues is not null && source->MemberPropertyCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->MemberPropertyCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->MemberPropertyCount; i1++)
            {
                string? key1 = Utf8.ToString(source->MemberPropertyKeys[i1]);
                if (key1 is not null)
                {
                    map1[key1] = Utf8.ToString(source->MemberPropertyValues[i1]) ?? string.Empty;
                }
            }

            value.MemberProperties = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFLobbyJoinConfiguration* target, PlayFabArena arena)
    {
        *target = default;
        target->MemberPropertyCount = (uint)(MemberProperties is null ? 0 : MemberProperties.Count);
        if (MemberProperties is not null && MemberProperties.Count > 0)
        {
            byte** keys1 = arena.PointerArray<byte>(MemberProperties.Count);
            byte** values1 = arena.PointerArray<byte>(MemberProperties.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in MemberProperties)
            {
                keys1[next1] = arena.String(pair1.Key);
                values1[next1] = arena.String(pair1.Value);
                next1++;
            }

            target->MemberPropertyKeys = keys1;
            target->MemberPropertyValues = values1;
        }
    }
}

/// <summary>Projects <c>PFLobbyMemberDataUpdate</c>.</summary>
public sealed class LobbyMemberDataUpdate
{
    /// <summary>The <c>MemberPropertyKeys</c> / <c>MemberPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? MemberProperties { get; set; }

    internal static unsafe LobbyMemberDataUpdate FromNative(PFLobbyMemberDataUpdate* source)
    {
        var value = new LobbyMemberDataUpdate();
        if (source->MemberPropertyKeys is not null && source->MemberPropertyValues is not null && source->MemberPropertyCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->MemberPropertyCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->MemberPropertyCount; i1++)
            {
                string? key1 = Utf8.ToString(source->MemberPropertyKeys[i1]);
                if (key1 is not null)
                {
                    map1[key1] = Utf8.ToString(source->MemberPropertyValues[i1]) ?? string.Empty;
                }
            }

            value.MemberProperties = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFLobbyMemberDataUpdate* target, PlayFabArena arena)
    {
        *target = default;
        target->MemberPropertyCount = (uint)(MemberProperties is null ? 0 : MemberProperties.Count);
        if (MemberProperties is not null && MemberProperties.Count > 0)
        {
            byte** keys1 = arena.PointerArray<byte>(MemberProperties.Count);
            byte** values1 = arena.PointerArray<byte>(MemberProperties.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in MemberProperties)
            {
                keys1[next1] = arena.String(pair1.Key);
                values1[next1] = arena.String(pair1.Value);
                next1++;
            }

            target->MemberPropertyKeys = keys1;
            target->MemberPropertyValues = values1;
        }
    }
}

/// <summary>Projects <c>PFLobbyMemberUpdateSummary</c>.</summary>
public sealed class LobbyMemberUpdateSummary
{
    /// <summary><c>Member</c>.</summary>
    public EntityKey Member { get; set; } = new();

    /// <summary><c>ConnectionStatusUpdated</c>.</summary>
    public bool ConnectionStatusUpdated { get; set; }

    /// <summary><c>UpdatedMemberPropertyKeys</c>.</summary>
    public IReadOnlyList<string>? UpdatedMemberPropertyKeys { get; set; }

    internal static unsafe LobbyMemberUpdateSummary FromNative(PFLobbyMemberUpdateSummary* source)
    {
        var value = new LobbyMemberUpdateSummary();
        value.Member = EntityKey.FromNative(&source->Member);
        value.ConnectionStatusUpdated = source->ConnectionStatusUpdated != 0;
        if (source->UpdatedMemberPropertyKeys is not null && source->UpdatedMemberPropertyCount != 0)
        {
            var list3 = new List<string>((int)source->UpdatedMemberPropertyCount);
            for (uint i3 = 0; i3 < source->UpdatedMemberPropertyCount; i3++)
            {
                list3.Add(Utf8.ToString(source->UpdatedMemberPropertyKeys[i3]) ?? string.Empty);
            }

            value.UpdatedMemberPropertyKeys = list3;
        }
        return value;
    }

    internal unsafe void WriteTo(PFLobbyMemberUpdateSummary* target, PlayFabArena arena)
    {
        *target = default;
        Member.WriteTo(&target->Member, arena);
        target->ConnectionStatusUpdated = ConnectionStatusUpdated ? (byte)1 : (byte)0;
        target->UpdatedMemberPropertyCount = (uint)(UpdatedMemberPropertyKeys is null ? 0 : UpdatedMemberPropertyKeys.Count);
        target->UpdatedMemberPropertyKeys = arena.StringArray(UpdatedMemberPropertyKeys);
    }
}

/// <summary>Projects <c>PFLobbySearchConfiguration</c>.</summary>
public sealed class LobbySearchConfiguration
{
    /// <summary><c>FriendsFilter</c>.</summary>
    public LobbySearchFriendsFilter? FriendsFilter { get; set; }

    /// <summary><c>FilterString</c>.</summary>
    public string? FilterString { get; set; }

    /// <summary><c>SortString</c>.</summary>
    public string? SortString { get; set; }

    /// <summary><c>ClientSearchResultCount</c>.</summary>
    public uint? ClientSearchResultCount { get; set; }

    internal static unsafe LobbySearchConfiguration FromNative(PFLobbySearchConfiguration* source)
    {
        var value = new LobbySearchConfiguration();
        value.FriendsFilter = source->FriendsFilter is null ? null : LobbySearchFriendsFilter.FromNative(source->FriendsFilter);
        value.FilterString = Utf8.ToString(source->FilterString);
        value.SortString = Utf8.ToString(source->SortString);
        value.ClientSearchResultCount = source->ClientSearchResultCount is null ? null : (uint?)(*source->ClientSearchResultCount);
        return value;
    }

    internal unsafe void WriteTo(PFLobbySearchConfiguration* target, PlayFabArena arena)
    {
        *target = default;
        if (FriendsFilter is not null)
        {
            PFLobbySearchFriendsFilter* native0 = arena.Alloc<PFLobbySearchFriendsFilter>(1);
            FriendsFilter.WriteTo(native0, arena);
            target->FriendsFilter = native0;
        }
        target->FilterString = arena.String(FilterString);
        target->SortString = arena.String(SortString);
        target->ClientSearchResultCount = arena.Value(ClientSearchResultCount);
    }
}

/// <summary>Projects <c>PFLobbySearchFriendsFilter</c>.</summary>
public sealed class LobbySearchFriendsFilter
{
    /// <summary><c>IncludeSteamFriends</c>.</summary>
    public bool IncludeSteamFriends { get; set; }

    /// <summary><c>IncludeFacebookFriends</c>.</summary>
    public bool IncludeFacebookFriends { get; set; }

    /// <summary><c>IncludeXboxFriendsToken</c>.</summary>
    public string? IncludeXboxFriendsToken { get; set; }

    internal static unsafe LobbySearchFriendsFilter FromNative(PFLobbySearchFriendsFilter* source)
    {
        var value = new LobbySearchFriendsFilter();
        value.IncludeSteamFriends = source->IncludeSteamFriends != 0;
        value.IncludeFacebookFriends = source->IncludeFacebookFriends != 0;
        value.IncludeXboxFriendsToken = Utf8.ToString(source->IncludeXboxFriendsToken);
        return value;
    }

    internal unsafe void WriteTo(PFLobbySearchFriendsFilter* target, PlayFabArena arena)
    {
        *target = default;
        target->IncludeSteamFriends = IncludeSteamFriends ? (byte)1 : (byte)0;
        target->IncludeFacebookFriends = IncludeFacebookFriends ? (byte)1 : (byte)0;
        target->IncludeXboxFriendsToken = arena.String(IncludeXboxFriendsToken);
    }
}

/// <summary>Projects <c>PFLobbySearchResult</c>.</summary>
public sealed class LobbySearchResult
{
    /// <summary><c>LobbyId</c>.</summary>
    public string? LobbyId { get; set; }

    /// <summary><c>ConnectionString</c>.</summary>
    public string? ConnectionString { get; set; }

    /// <summary><c>OwnerEntity</c>.</summary>
    public EntityKey? OwnerEntity { get; set; }

    /// <summary><c>MaxMemberCount</c>.</summary>
    public uint MaxMemberCount { get; set; }

    /// <summary><c>CurrentMemberCount</c>.</summary>
    public uint CurrentMemberCount { get; set; }

    /// <summary>The <c>SearchPropertyKeys</c> / <c>SearchPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? SearchProperties { get; set; }

    /// <summary><c>Friends</c>.</summary>
    public IReadOnlyList<EntityKey>? Friends { get; set; }

    /// <summary><c>MembershipLock</c>.</summary>
    public LobbyMembershipLock MembershipLock { get; set; }

    internal static unsafe LobbySearchResult FromNative(PFLobbySearchResult* source)
    {
        var value = new LobbySearchResult();
        value.LobbyId = Utf8.ToString(source->LobbyId);
        value.ConnectionString = Utf8.ToString(source->ConnectionString);
        value.OwnerEntity = source->OwnerEntity is null ? null : EntityKey.FromNative(source->OwnerEntity);
        value.MaxMemberCount = source->MaxMemberCount;
        value.CurrentMemberCount = source->CurrentMemberCount;
        if (source->SearchPropertyKeys is not null && source->SearchPropertyValues is not null && source->SearchPropertyCount != 0)
        {
            var map6 = new Dictionary<string, string>((int)source->SearchPropertyCount, StringComparer.Ordinal);
            for (uint i6 = 0; i6 < source->SearchPropertyCount; i6++)
            {
                string? key6 = Utf8.ToString(source->SearchPropertyKeys[i6]);
                if (key6 is not null)
                {
                    map6[key6] = Utf8.ToString(source->SearchPropertyValues[i6]) ?? string.Empty;
                }
            }

            value.SearchProperties = map6;
        }
        if (source->Friends is not null && source->FriendCount != 0)
        {
            var list9 = new List<EntityKey>((int)source->FriendCount);
            for (uint i9 = 0; i9 < source->FriendCount; i9++)
            {
                list9.Add(EntityKey.FromNative(&source->Friends[i9]));
            }

            value.Friends = list9;
        }
        value.MembershipLock = (LobbyMembershipLock)source->MembershipLock;
        return value;
    }

    internal unsafe void WriteTo(PFLobbySearchResult* target, PlayFabArena arena)
    {
        *target = default;
        target->LobbyId = arena.String(LobbyId);
        target->ConnectionString = arena.String(ConnectionString);
        if (OwnerEntity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            OwnerEntity.WriteTo(native2, arena);
            target->OwnerEntity = native2;
        }
        target->MaxMemberCount = MaxMemberCount;
        target->CurrentMemberCount = CurrentMemberCount;
        target->SearchPropertyCount = (uint)(SearchProperties is null ? 0 : SearchProperties.Count);
        if (SearchProperties is not null && SearchProperties.Count > 0)
        {
            byte** keys6 = arena.PointerArray<byte>(SearchProperties.Count);
            byte** values6 = arena.PointerArray<byte>(SearchProperties.Count);
            int next6 = 0;
            foreach (KeyValuePair<string, string> pair6 in SearchProperties)
            {
                keys6[next6] = arena.String(pair6.Key);
                values6[next6] = arena.String(pair6.Value);
                next6++;
            }

            target->SearchPropertyKeys = keys6;
            target->SearchPropertyValues = values6;
        }
        target->FriendCount = (uint)(Friends is null ? 0 : Friends.Count);
        if (Friends is not null && Friends.Count > 0)
        {
            PFEntityKey* array9 = arena.Alloc<PFEntityKey>(Friends.Count);
            for (int i9 = 0; i9 < Friends.Count; i9++)
            {
                Friends[i9].WriteTo(&array9[i9], arena);
            }

            target->Friends = array9;
        }
        target->MembershipLock = (PFLobbyMembershipLock)MembershipLock;
    }
}

/// <summary>Projects <c>PFLobbyServerDataUpdate</c>.</summary>
public sealed class LobbyServerDataUpdate
{
    /// <summary><c>NewServer</c>.</summary>
    public EntityKey? NewServer { get; set; }

    /// <summary>The <c>ServerPropertyKeys</c> / <c>ServerPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? ServerProperties { get; set; }

    internal static unsafe LobbyServerDataUpdate FromNative(PFLobbyServerDataUpdate* source)
    {
        var value = new LobbyServerDataUpdate();
        value.NewServer = source->NewServer is null ? null : EntityKey.FromNative(source->NewServer);
        if (source->ServerPropertyKeys is not null && source->ServerPropertyValues is not null && source->ServerPropertyCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->ServerPropertyCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->ServerPropertyCount; i2++)
            {
                string? key2 = Utf8.ToString(source->ServerPropertyKeys[i2]);
                if (key2 is not null)
                {
                    map2[key2] = Utf8.ToString(source->ServerPropertyValues[i2]) ?? string.Empty;
                }
            }

            value.ServerProperties = map2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFLobbyServerDataUpdate* target, PlayFabArena arena)
    {
        *target = default;
        if (NewServer is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            NewServer.WriteTo(native0, arena);
            target->NewServer = native0;
        }
        target->ServerPropertyCount = (uint)(ServerProperties is null ? 0 : ServerProperties.Count);
        if (ServerProperties is not null && ServerProperties.Count > 0)
        {
            byte** keys2 = arena.PointerArray<byte>(ServerProperties.Count);
            byte** values2 = arena.PointerArray<byte>(ServerProperties.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in ServerProperties)
            {
                keys2[next2] = arena.String(pair2.Key);
                values2[next2] = arena.String(pair2.Value);
                next2++;
            }

            target->ServerPropertyKeys = keys2;
            target->ServerPropertyValues = values2;
        }
    }
}

/// <summary>Projects <c>PFLobbyServerJoinConfiguration</c>.</summary>
public sealed class LobbyServerJoinConfiguration
{
    /// <summary>The <c>ServerPropertyKeys</c> / <c>ServerPropertyValues</c> pairs.</summary>
    public IReadOnlyDictionary<string, string>? ServerProperties { get; set; }

    internal static unsafe LobbyServerJoinConfiguration FromNative(PFLobbyServerJoinConfiguration* source)
    {
        var value = new LobbyServerJoinConfiguration();
        if (source->ServerPropertyKeys is not null && source->ServerPropertyValues is not null && source->ServerPropertyCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->ServerPropertyCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->ServerPropertyCount; i1++)
            {
                string? key1 = Utf8.ToString(source->ServerPropertyKeys[i1]);
                if (key1 is not null)
                {
                    map1[key1] = Utf8.ToString(source->ServerPropertyValues[i1]) ?? string.Empty;
                }
            }

            value.ServerProperties = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFLobbyServerJoinConfiguration* target, PlayFabArena arena)
    {
        *target = default;
        target->ServerPropertyCount = (uint)(ServerProperties is null ? 0 : ServerProperties.Count);
        if (ServerProperties is not null && ServerProperties.Count > 0)
        {
            byte** keys1 = arena.PointerArray<byte>(ServerProperties.Count);
            byte** values1 = arena.PointerArray<byte>(ServerProperties.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in ServerProperties)
            {
                keys1[next1] = arena.String(pair1.Key);
                values1[next1] = arena.String(pair1.Value);
                next1++;
            }

            target->ServerPropertyKeys = keys1;
            target->ServerPropertyValues = values1;
        }
    }
}
