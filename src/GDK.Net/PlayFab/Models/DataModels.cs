// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFData
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFDataAbortFileUploadsRequest</c>.</summary>
public sealed class DataAbortFileUploadsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>FileNames</c>.</summary>
    public IReadOnlyList<string>? FileNames { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int? ProfileVersion { get; set; }

    internal static unsafe DataAbortFileUploadsRequest FromNative(PFDataAbortFileUploadsRequest* source)
    {
        var value = new DataAbortFileUploadsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->FileNames is not null && source->FileNamesCount != 0)
        {
            var list3 = new List<string>((int)source->FileNamesCount);
            for (uint i3 = 0; i3 < source->FileNamesCount; i3++)
            {
                list3.Add(Utf8.ToString(source->FileNames[i3]) ?? string.Empty);
            }

            value.FileNames = list3;
        }
        value.ProfileVersion = source->ProfileVersion is null ? null : (int?)(*source->ProfileVersion);
        return value;
    }

    internal unsafe void WriteTo(PFDataAbortFileUploadsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->FileNamesCount = (uint)(FileNames is null ? 0 : FileNames.Count);
        target->FileNames = arena.StringArray(FileNames);
        target->ProfileVersion = arena.Value(ProfileVersion);
    }
}

/// <summary>Projects <c>PFDataAbortFileUploadsResponse</c>.</summary>
public sealed class DataAbortFileUploadsResponse
{
    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    internal static unsafe DataAbortFileUploadsResponse FromNative(PFDataAbortFileUploadsResponse* source)
    {
        var value = new DataAbortFileUploadsResponse();
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ProfileVersion = source->ProfileVersion;
        return value;
    }

    internal unsafe void WriteTo(PFDataAbortFileUploadsResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->ProfileVersion = ProfileVersion;
    }
}

/// <summary>Projects <c>PFDataDeleteFilesRequest</c>.</summary>
public sealed class DataDeleteFilesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>FileNames</c>.</summary>
    public IReadOnlyList<string>? FileNames { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int? ProfileVersion { get; set; }

    internal static unsafe DataDeleteFilesRequest FromNative(PFDataDeleteFilesRequest* source)
    {
        var value = new DataDeleteFilesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->FileNames is not null && source->FileNamesCount != 0)
        {
            var list3 = new List<string>((int)source->FileNamesCount);
            for (uint i3 = 0; i3 < source->FileNamesCount; i3++)
            {
                list3.Add(Utf8.ToString(source->FileNames[i3]) ?? string.Empty);
            }

            value.FileNames = list3;
        }
        value.ProfileVersion = source->ProfileVersion is null ? null : (int?)(*source->ProfileVersion);
        return value;
    }

    internal unsafe void WriteTo(PFDataDeleteFilesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->FileNamesCount = (uint)(FileNames is null ? 0 : FileNames.Count);
        target->FileNames = arena.StringArray(FileNames);
        target->ProfileVersion = arena.Value(ProfileVersion);
    }
}

/// <summary>Projects <c>PFDataDeleteFilesResponse</c>.</summary>
public sealed class DataDeleteFilesResponse
{
    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    internal static unsafe DataDeleteFilesResponse FromNative(PFDataDeleteFilesResponse* source)
    {
        var value = new DataDeleteFilesResponse();
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ProfileVersion = source->ProfileVersion;
        return value;
    }

    internal unsafe void WriteTo(PFDataDeleteFilesResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->ProfileVersion = ProfileVersion;
    }
}

/// <summary>Projects <c>PFDataFinalizeFileUploadsRequest</c>.</summary>
public sealed class DataFinalizeFileUploadsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>FileNames</c>.</summary>
    public IReadOnlyList<string>? FileNames { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    internal static unsafe DataFinalizeFileUploadsRequest FromNative(PFDataFinalizeFileUploadsRequest* source)
    {
        var value = new DataFinalizeFileUploadsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->FileNames is not null && source->FileNamesCount != 0)
        {
            var list3 = new List<string>((int)source->FileNamesCount);
            for (uint i3 = 0; i3 < source->FileNamesCount; i3++)
            {
                list3.Add(Utf8.ToString(source->FileNames[i3]) ?? string.Empty);
            }

            value.FileNames = list3;
        }
        value.ProfileVersion = source->ProfileVersion;
        return value;
    }

    internal unsafe void WriteTo(PFDataFinalizeFileUploadsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->FileNamesCount = (uint)(FileNames is null ? 0 : FileNames.Count);
        target->FileNames = arena.StringArray(FileNames);
        target->ProfileVersion = ProfileVersion;
    }
}

/// <summary>Projects <c>PFDataFinalizeFileUploadsResponse</c>.</summary>
public sealed class DataFinalizeFileUploadsResponse
{
    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Metadata</c>.</summary>
    public IReadOnlyDictionary<string, DataGetFileMetadata>? Metadata { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    internal static unsafe DataFinalizeFileUploadsResponse FromNative(PFDataFinalizeFileUploadsResponse* source)
    {
        var value = new DataFinalizeFileUploadsResponse();
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->Metadata is not null && source->MetadataCount != 0)
        {
            var map1 = new Dictionary<string, DataGetFileMetadata>((int)source->MetadataCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->MetadataCount; i1++)
            {
                map1[Utf8.ToString(source->Metadata[i1].Key) ?? string.Empty] = DataGetFileMetadata.FromNative(source->Metadata[i1].Value);
            }

            value.Metadata = map1;
        }
        value.ProfileVersion = source->ProfileVersion;
        return value;
    }

    internal unsafe void WriteTo(PFDataFinalizeFileUploadsResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->MetadataCount = (uint)(Metadata is null ? 0 : Metadata.Count);
        if (Metadata is not null && Metadata.Count > 0)
        {
            PFDataGetFileMetadataDictionaryEntry* entries1 = arena.Alloc<PFDataGetFileMetadataDictionaryEntry>(Metadata.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, DataGetFileMetadata> pair1 in Metadata)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                if (pair1.Value is not null)
                {
                    PFDataGetFileMetadata* item1 = arena.Alloc<PFDataGetFileMetadata>(1);
                    pair1.Value.WriteTo(item1, arena);
                    entries1[next1].Value = item1;
                }
                next1++;
            }

            target->Metadata = entries1;
        }
        target->ProfileVersion = ProfileVersion;
    }
}

/// <summary>Projects <c>PFDataGetFileMetadata</c>.</summary>
public sealed class DataGetFileMetadata
{
    /// <summary><c>Checksum</c>.</summary>
    public string? Checksum { get; set; }

    /// <summary><c>DownloadUrl</c>.</summary>
    public string? DownloadUrl { get; set; }

    /// <summary><c>FileName</c>.</summary>
    public string? FileName { get; set; }

    /// <summary><c>LastModified</c>.</summary>
    public DateTimeOffset LastModified { get; set; }

    /// <summary><c>Size</c>.</summary>
    public int Size { get; set; }

    internal static unsafe DataGetFileMetadata FromNative(PFDataGetFileMetadata* source)
    {
        var value = new DataGetFileMetadata();
        value.Checksum = Utf8.ToString(source->Checksum);
        value.DownloadUrl = Utf8.ToString(source->DownloadUrl);
        value.FileName = Utf8.ToString(source->FileName);
        value.LastModified = PlayFabTime.ToDateTimeOffset(source->LastModified);
        value.Size = source->Size;
        return value;
    }

    internal unsafe void WriteTo(PFDataGetFileMetadata* target, PlayFabArena arena)
    {
        *target = default;
        target->Checksum = arena.String(Checksum);
        target->DownloadUrl = arena.String(DownloadUrl);
        target->FileName = arena.String(FileName);
        target->LastModified = PlayFabTime.ToUnixTime(LastModified);
        target->Size = Size;
    }
}

/// <summary>Projects <c>PFDataGetFilesRequest</c>.</summary>
public sealed class DataGetFilesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    internal static unsafe DataGetFilesRequest FromNative(PFDataGetFilesRequest* source)
    {
        var value = new DataGetFilesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFDataGetFilesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
    }
}

/// <summary>Projects <c>PFDataGetFilesResponse</c>.</summary>
public sealed class DataGetFilesResponse
{
    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Metadata</c>.</summary>
    public IReadOnlyDictionary<string, DataGetFileMetadata>? Metadata { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    internal static unsafe DataGetFilesResponse FromNative(PFDataGetFilesResponse* source)
    {
        var value = new DataGetFilesResponse();
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->Metadata is not null && source->MetadataCount != 0)
        {
            var map1 = new Dictionary<string, DataGetFileMetadata>((int)source->MetadataCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->MetadataCount; i1++)
            {
                map1[Utf8.ToString(source->Metadata[i1].Key) ?? string.Empty] = DataGetFileMetadata.FromNative(source->Metadata[i1].Value);
            }

            value.Metadata = map1;
        }
        value.ProfileVersion = source->ProfileVersion;
        return value;
    }

    internal unsafe void WriteTo(PFDataGetFilesResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->MetadataCount = (uint)(Metadata is null ? 0 : Metadata.Count);
        if (Metadata is not null && Metadata.Count > 0)
        {
            PFDataGetFileMetadataDictionaryEntry* entries1 = arena.Alloc<PFDataGetFileMetadataDictionaryEntry>(Metadata.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, DataGetFileMetadata> pair1 in Metadata)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                if (pair1.Value is not null)
                {
                    PFDataGetFileMetadata* item1 = arena.Alloc<PFDataGetFileMetadata>(1);
                    pair1.Value.WriteTo(item1, arena);
                    entries1[next1].Value = item1;
                }
                next1++;
            }

            target->Metadata = entries1;
        }
        target->ProfileVersion = ProfileVersion;
    }
}

/// <summary>Projects <c>PFDataGetObjectsRequest</c>.</summary>
public sealed class DataGetObjectsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>EscapeObject</c>.</summary>
    public bool? EscapeObject { get; set; }

    internal static unsafe DataGetObjectsRequest FromNative(PFDataGetObjectsRequest* source)
    {
        var value = new DataGetObjectsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.EscapeObject = source->EscapeObject is null ? null : (bool?)(*source->EscapeObject != 0);
        return value;
    }

    internal unsafe void WriteTo(PFDataGetObjectsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->EscapeObject = arena.Value(EscapeObject is null ? null : (EscapeObject.Value ? (byte?)1 : (byte?)0));
    }
}

/// <summary>Projects <c>PFDataGetObjectsResponse</c>.</summary>
public sealed class DataGetObjectsResponse
{
    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Objects</c>.</summary>
    public IReadOnlyDictionary<string, DataObjectResult>? Objects { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    internal static unsafe DataGetObjectsResponse FromNative(PFDataGetObjectsResponse* source)
    {
        var value = new DataGetObjectsResponse();
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->Objects is not null && source->ObjectsCount != 0)
        {
            var map1 = new Dictionary<string, DataObjectResult>((int)source->ObjectsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->ObjectsCount; i1++)
            {
                map1[Utf8.ToString(source->Objects[i1].Key) ?? string.Empty] = DataObjectResult.FromNative(source->Objects[i1].Value);
            }

            value.Objects = map1;
        }
        value.ProfileVersion = source->ProfileVersion;
        return value;
    }

    internal unsafe void WriteTo(PFDataGetObjectsResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->ObjectsCount = (uint)(Objects is null ? 0 : Objects.Count);
        if (Objects is not null && Objects.Count > 0)
        {
            PFDataObjectResultDictionaryEntry* entries1 = arena.Alloc<PFDataObjectResultDictionaryEntry>(Objects.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, DataObjectResult> pair1 in Objects)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                if (pair1.Value is not null)
                {
                    PFDataObjectResult* item1 = arena.Alloc<PFDataObjectResult>(1);
                    pair1.Value.WriteTo(item1, arena);
                    entries1[next1].Value = item1;
                }
                next1++;
            }

            target->Objects = entries1;
        }
        target->ProfileVersion = ProfileVersion;
    }
}

/// <summary>Projects <c>PFDataInitiateFileUploadMetadata</c>.</summary>
public sealed class DataInitiateFileUploadMetadata
{
    /// <summary><c>FileName</c>.</summary>
    public string? FileName { get; set; }

    /// <summary><c>UploadUrl</c>.</summary>
    public string? UploadUrl { get; set; }

    internal static unsafe DataInitiateFileUploadMetadata FromNative(PFDataInitiateFileUploadMetadata* source)
    {
        var value = new DataInitiateFileUploadMetadata();
        value.FileName = Utf8.ToString(source->FileName);
        value.UploadUrl = Utf8.ToString(source->UploadUrl);
        return value;
    }

    internal unsafe void WriteTo(PFDataInitiateFileUploadMetadata* target, PlayFabArena arena)
    {
        *target = default;
        target->FileName = arena.String(FileName);
        target->UploadUrl = arena.String(UploadUrl);
    }
}

/// <summary>Projects <c>PFDataInitiateFileUploadsRequest</c>.</summary>
public sealed class DataInitiateFileUploadsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>FileNames</c>.</summary>
    public IReadOnlyList<string>? FileNames { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int? ProfileVersion { get; set; }

    internal static unsafe DataInitiateFileUploadsRequest FromNative(PFDataInitiateFileUploadsRequest* source)
    {
        var value = new DataInitiateFileUploadsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->FileNames is not null && source->FileNamesCount != 0)
        {
            var list3 = new List<string>((int)source->FileNamesCount);
            for (uint i3 = 0; i3 < source->FileNamesCount; i3++)
            {
                list3.Add(Utf8.ToString(source->FileNames[i3]) ?? string.Empty);
            }

            value.FileNames = list3;
        }
        value.ProfileVersion = source->ProfileVersion is null ? null : (int?)(*source->ProfileVersion);
        return value;
    }

    internal unsafe void WriteTo(PFDataInitiateFileUploadsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->FileNamesCount = (uint)(FileNames is null ? 0 : FileNames.Count);
        target->FileNames = arena.StringArray(FileNames);
        target->ProfileVersion = arena.Value(ProfileVersion);
    }
}

/// <summary>Projects <c>PFDataInitiateFileUploadsResponse</c>.</summary>
public sealed class DataInitiateFileUploadsResponse
{
    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>UploadDetails</c>.</summary>
    public IReadOnlyList<DataInitiateFileUploadMetadata>? UploadDetails { get; set; }

    internal static unsafe DataInitiateFileUploadsResponse FromNative(PFDataInitiateFileUploadsResponse* source)
    {
        var value = new DataInitiateFileUploadsResponse();
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ProfileVersion = source->ProfileVersion;
        if (source->UploadDetails is not null && source->UploadDetailsCount != 0)
        {
            var list2 = new List<DataInitiateFileUploadMetadata>((int)source->UploadDetailsCount);
            for (uint i2 = 0; i2 < source->UploadDetailsCount; i2++)
            {
                list2.Add(DataInitiateFileUploadMetadata.FromNative(source->UploadDetails[i2]));
            }

            value.UploadDetails = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFDataInitiateFileUploadsResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Entity is not null)
        {
            PFEntityKey* native0 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native0, arena);
            target->Entity = native0;
        }
        target->ProfileVersion = ProfileVersion;
        target->UploadDetailsCount = (uint)(UploadDetails is null ? 0 : UploadDetails.Count);
        if (UploadDetails is not null && UploadDetails.Count > 0)
        {
            PFDataInitiateFileUploadMetadata** array2 = arena.PointerArray<PFDataInitiateFileUploadMetadata>(UploadDetails.Count);
            for (int i2 = 0; i2 < UploadDetails.Count; i2++)
            {
                PFDataInitiateFileUploadMetadata* item2 = arena.Alloc<PFDataInitiateFileUploadMetadata>(1);
                UploadDetails[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->UploadDetails = array2;
        }
    }
}

/// <summary>Projects <c>PFDataObjectResult</c>.</summary>
public sealed class DataObjectResult
{
    /// <summary><c>DataObject</c>.</summary>
    public string? DataObject { get; set; }

    /// <summary><c>EscapedDataObject</c>.</summary>
    public string? EscapedDataObject { get; set; }

    /// <summary><c>ObjectName</c>.</summary>
    public string? ObjectName { get; set; }

    internal static unsafe DataObjectResult FromNative(PFDataObjectResult* source)
    {
        var value = new DataObjectResult();
        value.DataObject = Utf8.ToString(source->DataObject.StringValue);
        value.EscapedDataObject = Utf8.ToString(source->EscapedDataObject);
        value.ObjectName = Utf8.ToString(source->ObjectName);
        return value;
    }

    internal unsafe void WriteTo(PFDataObjectResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataObject.StringValue = arena.String(DataObject);
        target->EscapedDataObject = arena.String(EscapedDataObject);
        target->ObjectName = arena.String(ObjectName);
    }
}

/// <summary>Projects <c>PFDataSetObject</c>.</summary>
public sealed class DataSetObject
{
    /// <summary><c>DataObject</c>.</summary>
    public string? DataObject { get; set; }

    /// <summary><c>DeleteObject</c>.</summary>
    public bool? DeleteObject { get; set; }

    /// <summary><c>EscapedDataObject</c>.</summary>
    public string? EscapedDataObject { get; set; }

    /// <summary><c>ObjectName</c>.</summary>
    public string? ObjectName { get; set; }

    internal static unsafe DataSetObject FromNative(PFDataSetObject* source)
    {
        var value = new DataSetObject();
        value.DataObject = Utf8.ToString(source->DataObject.StringValue);
        value.DeleteObject = source->DeleteObject is null ? null : (bool?)(*source->DeleteObject != 0);
        value.EscapedDataObject = Utf8.ToString(source->EscapedDataObject);
        value.ObjectName = Utf8.ToString(source->ObjectName);
        return value;
    }

    internal unsafe void WriteTo(PFDataSetObject* target, PlayFabArena arena)
    {
        *target = default;
        target->DataObject.StringValue = arena.String(DataObject);
        target->DeleteObject = arena.Value(DeleteObject is null ? null : (DeleteObject.Value ? (byte?)1 : (byte?)0));
        target->EscapedDataObject = arena.String(EscapedDataObject);
        target->ObjectName = arena.String(ObjectName);
    }
}

/// <summary>Projects <c>PFDataSetObjectInfo</c>.</summary>
public sealed class DataSetObjectInfo
{
    /// <summary><c>ObjectName</c>.</summary>
    public string? ObjectName { get; set; }

    /// <summary><c>OperationReason</c>.</summary>
    public string? OperationReason { get; set; }

    /// <summary><c>SetResult</c>.</summary>
    public OperationTypes? SetResult { get; set; }

    internal static unsafe DataSetObjectInfo FromNative(PFDataSetObjectInfo* source)
    {
        var value = new DataSetObjectInfo();
        value.ObjectName = Utf8.ToString(source->ObjectName);
        value.OperationReason = Utf8.ToString(source->OperationReason);
        value.SetResult = source->SetResult is null ? null : (OperationTypes?)(*source->SetResult);
        return value;
    }

    internal unsafe void WriteTo(PFDataSetObjectInfo* target, PlayFabArena arena)
    {
        *target = default;
        target->ObjectName = arena.String(ObjectName);
        target->OperationReason = arena.String(OperationReason);
        target->SetResult = arena.Value((PFOperationTypes?)SetResult);
    }
}

/// <summary>Projects <c>PFDataSetObjectsRequest</c>.</summary>
public sealed class DataSetObjectsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ExpectedProfileVersion</c>.</summary>
    public int? ExpectedProfileVersion { get; set; }

    /// <summary><c>Objects</c>.</summary>
    public IReadOnlyList<DataSetObject>? Objects { get; set; }

    internal static unsafe DataSetObjectsRequest FromNative(PFDataSetObjectsRequest* source)
    {
        var value = new DataSetObjectsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ExpectedProfileVersion = source->ExpectedProfileVersion is null ? null : (int?)(*source->ExpectedProfileVersion);
        if (source->Objects is not null && source->ObjectsCount != 0)
        {
            var list4 = new List<DataSetObject>((int)source->ObjectsCount);
            for (uint i4 = 0; i4 < source->ObjectsCount; i4++)
            {
                list4.Add(DataSetObject.FromNative(source->Objects[i4]));
            }

            value.Objects = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFDataSetObjectsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->ExpectedProfileVersion = arena.Value(ExpectedProfileVersion);
        target->ObjectsCount = (uint)(Objects is null ? 0 : Objects.Count);
        if (Objects is not null && Objects.Count > 0)
        {
            PFDataSetObject** array4 = arena.PointerArray<PFDataSetObject>(Objects.Count);
            for (int i4 = 0; i4 < Objects.Count; i4++)
            {
                PFDataSetObject* item4 = arena.Alloc<PFDataSetObject>(1);
                Objects[i4].WriteTo(item4, arena);
                array4[i4] = item4;
            }

            target->Objects = array4;
        }
    }
}

/// <summary>Projects <c>PFDataSetObjectsResponse</c>.</summary>
public sealed class DataSetObjectsResponse
{
    /// <summary><c>ProfileVersion</c>.</summary>
    public int ProfileVersion { get; set; }

    /// <summary><c>SetResults</c>.</summary>
    public IReadOnlyList<DataSetObjectInfo>? SetResults { get; set; }

    internal static unsafe DataSetObjectsResponse FromNative(PFDataSetObjectsResponse* source)
    {
        var value = new DataSetObjectsResponse();
        value.ProfileVersion = source->ProfileVersion;
        if (source->SetResults is not null && source->SetResultsCount != 0)
        {
            var list1 = new List<DataSetObjectInfo>((int)source->SetResultsCount);
            for (uint i1 = 0; i1 < source->SetResultsCount; i1++)
            {
                list1.Add(DataSetObjectInfo.FromNative(source->SetResults[i1]));
            }

            value.SetResults = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFDataSetObjectsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ProfileVersion = ProfileVersion;
        target->SetResultsCount = (uint)(SetResults is null ? 0 : SetResults.Count);
        if (SetResults is not null && SetResults.Count > 0)
        {
            PFDataSetObjectInfo** array1 = arena.PointerArray<PFDataSetObjectInfo>(SetResults.Count);
            for (int i1 = 0; i1 < SetResults.Count; i1++)
            {
                PFDataSetObjectInfo* item1 = arena.Alloc<PFDataSetObjectInfo>(1);
                SetResults[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->SetResults = array1;
        }
    }
}
