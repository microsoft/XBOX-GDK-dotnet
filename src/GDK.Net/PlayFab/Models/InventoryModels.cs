// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFInventory
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFInventoryAddInventoryItemsOperation</c>.</summary>
public sealed class InventoryAddInventoryItemsOperation
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>DurationInSeconds</c>.</summary>
    public double? DurationInSeconds { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    /// <summary><c>NewStackValues</c>.</summary>
    public InventoryInitialValues? NewStackValues { get; set; }

    internal static unsafe InventoryAddInventoryItemsOperation FromNative(PFInventoryAddInventoryItemsOperation* source)
    {
        var value = new InventoryAddInventoryItemsOperation();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.DurationInSeconds = source->DurationInSeconds is null ? null : (double?)(*source->DurationInSeconds);
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        value.NewStackValues = source->NewStackValues is null ? null : InventoryInitialValues.FromNative(source->NewStackValues);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryAddInventoryItemsOperation* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->DurationInSeconds = arena.Value(DurationInSeconds);
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native2 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native2, arena);
            target->Item = native2;
        }
        if (NewStackValues is not null)
        {
            PFInventoryInitialValues* native3 = arena.Alloc<PFInventoryInitialValues>(1);
            NewStackValues.WriteTo(native3, arena);
            target->NewStackValues = native3;
        }
    }
}

/// <summary>Projects <c>PFInventoryAddInventoryItemsRequest</c>.</summary>
public sealed class InventoryAddInventoryItemsRequest
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DurationInSeconds</c>.</summary>
    public double? DurationInSeconds { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    /// <summary><c>NewStackValues</c>.</summary>
    public InventoryInitialValues? NewStackValues { get; set; }

    internal static unsafe InventoryAddInventoryItemsRequest FromNative(PFInventoryAddInventoryItemsRequest* source)
    {
        var value = new InventoryAddInventoryItemsRequest();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.DurationInSeconds = source->DurationInSeconds is null ? null : (double?)(*source->DurationInSeconds);
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        value.NewStackValues = source->NewStackValues is null ? null : InventoryInitialValues.FromNative(source->NewStackValues);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryAddInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        target->DurationInSeconds = arena.Value(DurationInSeconds);
        if (Entity is not null)
        {
            PFEntityKey* native5 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native5, arena);
            target->Entity = native5;
        }
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native8 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native8, arena);
            target->Item = native8;
        }
        if (NewStackValues is not null)
        {
            PFInventoryInitialValues* native9 = arena.Alloc<PFInventoryInitialValues>(1);
            NewStackValues.WriteTo(native9, arena);
            target->NewStackValues = native9;
        }
    }
}

/// <summary>Projects <c>PFInventoryAddInventoryItemsResponse</c>.</summary>
public sealed class InventoryAddInventoryItemsResponse
{
    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryAddInventoryItemsResponse FromNative(PFInventoryAddInventoryItemsResponse* source)
    {
        var value = new InventoryAddInventoryItemsResponse();
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list2 = new List<string>((int)source->TransactionIdsCount);
            for (uint i2 = 0; i2 < source->TransactionIdsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->TransactionIds[i2]) ?? string.Empty);
            }

            value.TransactionIds = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryAddInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryAlternateId</c>.</summary>
public sealed class InventoryAlternateId
{
    /// <summary><c>Type</c>.</summary>
    public string? Type { get; set; }

    /// <summary><c>Value</c>.</summary>
    public string? Value { get; set; }

    internal static unsafe InventoryAlternateId FromNative(PFInventoryAlternateId* source)
    {
        var value = new InventoryAlternateId();
        value.Type = Utf8.ToString(source->Type);
        value.Value = Utf8.ToString(source->Value);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryAlternateId* target, PlayFabArena arena)
    {
        *target = default;
        target->Type = arena.String(Type);
        target->Value = arena.String(Value);
    }
}

/// <summary>Projects <c>PFInventoryDeleteInventoryCollectionRequest</c>.</summary>
public sealed class InventoryDeleteInventoryCollectionRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    internal static unsafe InventoryDeleteInventoryCollectionRequest FromNative(PFInventoryDeleteInventoryCollectionRequest* source)
    {
        var value = new InventoryDeleteInventoryCollectionRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryDeleteInventoryCollectionRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->ETag = arena.String(ETag);
    }
}

/// <summary>Projects <c>PFInventoryDeleteInventoryItemsOperation</c>.</summary>
public sealed class InventoryDeleteInventoryItemsOperation
{
    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    internal static unsafe InventoryDeleteInventoryItemsOperation FromNative(PFInventoryDeleteInventoryItemsOperation* source)
    {
        var value = new InventoryDeleteInventoryItemsOperation();
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryDeleteInventoryItemsOperation* target, PlayFabArena arena)
    {
        *target = default;
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native0 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native0, arena);
            target->Item = native0;
        }
    }
}

/// <summary>Projects <c>PFInventoryDeleteInventoryItemsRequest</c>.</summary>
public sealed class InventoryDeleteInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    internal static unsafe InventoryDeleteInventoryItemsRequest FromNative(PFInventoryDeleteInventoryItemsRequest* source)
    {
        var value = new InventoryDeleteInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryDeleteInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native6 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native6, arena);
            target->Item = native6;
        }
    }
}

/// <summary>Projects <c>PFInventoryDeleteInventoryItemsResponse</c>.</summary>
public sealed class InventoryDeleteInventoryItemsResponse
{
    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryDeleteInventoryItemsResponse FromNative(PFInventoryDeleteInventoryItemsResponse* source)
    {
        var value = new InventoryDeleteInventoryItemsResponse();
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list2 = new List<string>((int)source->TransactionIdsCount);
            for (uint i2 = 0; i2 < source->TransactionIdsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->TransactionIds[i2]) ?? string.Empty);
            }

            value.TransactionIds = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryDeleteInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryExecuteInventoryOperationsRequest</c>.</summary>
public sealed class InventoryExecuteInventoryOperationsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>Operations</c>.</summary>
    public IReadOnlyList<InventoryInventoryOperation>? Operations { get; set; }

    internal static unsafe InventoryExecuteInventoryOperationsRequest FromNative(PFInventoryExecuteInventoryOperationsRequest* source)
    {
        var value = new InventoryExecuteInventoryOperationsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->Operations is not null && source->OperationsCount != 0)
        {
            var list6 = new List<InventoryInventoryOperation>((int)source->OperationsCount);
            for (uint i6 = 0; i6 < source->OperationsCount; i6++)
            {
                list6.Add(InventoryInventoryOperation.FromNative(source->Operations[i6]));
            }

            value.Operations = list6;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryExecuteInventoryOperationsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->OperationsCount = (uint)(Operations is null ? 0 : Operations.Count);
        if (Operations is not null && Operations.Count > 0)
        {
            PFInventoryInventoryOperation** array6 = arena.PointerArray<PFInventoryInventoryOperation>(Operations.Count);
            for (int i6 = 0; i6 < Operations.Count; i6++)
            {
                PFInventoryInventoryOperation* item6 = arena.Alloc<PFInventoryInventoryOperation>(1);
                Operations[i6].WriteTo(item6, arena);
                array6[i6] = item6;
            }

            target->Operations = array6;
        }
    }
}

/// <summary>Projects <c>PFInventoryExecuteInventoryOperationsResponse</c>.</summary>
public sealed class InventoryExecuteInventoryOperationsResponse
{
    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryExecuteInventoryOperationsResponse FromNative(PFInventoryExecuteInventoryOperationsResponse* source)
    {
        var value = new InventoryExecuteInventoryOperationsResponse();
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list2 = new List<string>((int)source->TransactionIdsCount);
            for (uint i2 = 0; i2 < source->TransactionIdsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->TransactionIds[i2]) ?? string.Empty);
            }

            value.TransactionIds = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryExecuteInventoryOperationsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryExecuteTransferOperationsRequest</c>.</summary>
public sealed class InventoryExecuteTransferOperationsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>GivingCollectionId</c>.</summary>
    public string? GivingCollectionId { get; set; }

    /// <summary><c>GivingEntity</c>.</summary>
    public EntityKey? GivingEntity { get; set; }

    /// <summary><c>GivingETag</c>.</summary>
    public string? GivingETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>Operations</c>.</summary>
    public IReadOnlyList<InventoryTransferInventoryItemsOperation>? Operations { get; set; }

    /// <summary><c>ReceivingCollectionId</c>.</summary>
    public string? ReceivingCollectionId { get; set; }

    /// <summary><c>ReceivingEntity</c>.</summary>
    public EntityKey? ReceivingEntity { get; set; }

    internal static unsafe InventoryExecuteTransferOperationsRequest FromNative(PFInventoryExecuteTransferOperationsRequest* source)
    {
        var value = new InventoryExecuteTransferOperationsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.GivingCollectionId = Utf8.ToString(source->GivingCollectionId);
        value.GivingEntity = source->GivingEntity is null ? null : EntityKey.FromNative(source->GivingEntity);
        value.GivingETag = Utf8.ToString(source->GivingETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->Operations is not null && source->OperationsCount != 0)
        {
            var list6 = new List<InventoryTransferInventoryItemsOperation>((int)source->OperationsCount);
            for (uint i6 = 0; i6 < source->OperationsCount; i6++)
            {
                list6.Add(InventoryTransferInventoryItemsOperation.FromNative(source->Operations[i6]));
            }

            value.Operations = list6;
        }
        value.ReceivingCollectionId = Utf8.ToString(source->ReceivingCollectionId);
        value.ReceivingEntity = source->ReceivingEntity is null ? null : EntityKey.FromNative(source->ReceivingEntity);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryExecuteTransferOperationsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->GivingCollectionId = arena.String(GivingCollectionId);
        if (GivingEntity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            GivingEntity.WriteTo(native3, arena);
            target->GivingEntity = native3;
        }
        target->GivingETag = arena.String(GivingETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->OperationsCount = (uint)(Operations is null ? 0 : Operations.Count);
        if (Operations is not null && Operations.Count > 0)
        {
            PFInventoryTransferInventoryItemsOperation** array6 = arena.PointerArray<PFInventoryTransferInventoryItemsOperation>(Operations.Count);
            for (int i6 = 0; i6 < Operations.Count; i6++)
            {
                PFInventoryTransferInventoryItemsOperation* item6 = arena.Alloc<PFInventoryTransferInventoryItemsOperation>(1);
                Operations[i6].WriteTo(item6, arena);
                array6[i6] = item6;
            }

            target->Operations = array6;
        }
        target->ReceivingCollectionId = arena.String(ReceivingCollectionId);
        if (ReceivingEntity is not null)
        {
            PFEntityKey* native9 = arena.Alloc<PFEntityKey>(1);
            ReceivingEntity.WriteTo(native9, arena);
            target->ReceivingEntity = native9;
        }
    }
}

/// <summary>Projects <c>PFInventoryExecuteTransferOperationsResponse</c>.</summary>
public sealed class InventoryExecuteTransferOperationsResponse
{
    /// <summary><c>GivingETag</c>.</summary>
    public string? GivingETag { get; set; }

    /// <summary><c>GivingTransactionIds</c>.</summary>
    public IReadOnlyList<string>? GivingTransactionIds { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>OperationStatus</c>.</summary>
    public string? OperationStatus { get; set; }

    /// <summary><c>OperationToken</c>.</summary>
    public string? OperationToken { get; set; }

    /// <summary><c>ReceivingETag</c>.</summary>
    public string? ReceivingETag { get; set; }

    /// <summary><c>ReceivingTransactionIds</c>.</summary>
    public IReadOnlyList<string>? ReceivingTransactionIds { get; set; }

    internal static unsafe InventoryExecuteTransferOperationsResponse FromNative(PFInventoryExecuteTransferOperationsResponse* source)
    {
        var value = new InventoryExecuteTransferOperationsResponse();
        value.GivingETag = Utf8.ToString(source->GivingETag);
        if (source->GivingTransactionIds is not null && source->GivingTransactionIdsCount != 0)
        {
            var list1 = new List<string>((int)source->GivingTransactionIdsCount);
            for (uint i1 = 0; i1 < source->GivingTransactionIdsCount; i1++)
            {
                list1.Add(Utf8.ToString(source->GivingTransactionIds[i1]) ?? string.Empty);
            }

            value.GivingTransactionIds = list1;
        }
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.OperationStatus = Utf8.ToString(source->OperationStatus);
        value.OperationToken = Utf8.ToString(source->OperationToken);
        value.ReceivingETag = Utf8.ToString(source->ReceivingETag);
        if (source->ReceivingTransactionIds is not null && source->ReceivingTransactionIdsCount != 0)
        {
            var list7 = new List<string>((int)source->ReceivingTransactionIdsCount);
            for (uint i7 = 0; i7 < source->ReceivingTransactionIdsCount; i7++)
            {
                list7.Add(Utf8.ToString(source->ReceivingTransactionIds[i7]) ?? string.Empty);
            }

            value.ReceivingTransactionIds = list7;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryExecuteTransferOperationsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->GivingETag = arena.String(GivingETag);
        target->GivingTransactionIdsCount = (uint)(GivingTransactionIds is null ? 0 : GivingTransactionIds.Count);
        target->GivingTransactionIds = arena.StringArray(GivingTransactionIds);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->OperationStatus = arena.String(OperationStatus);
        target->OperationToken = arena.String(OperationToken);
        target->ReceivingETag = arena.String(ReceivingETag);
        target->ReceivingTransactionIdsCount = (uint)(ReceivingTransactionIds is null ? 0 : ReceivingTransactionIds.Count);
        target->ReceivingTransactionIds = arena.StringArray(ReceivingTransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryGetInventoryCollectionIdsRequest</c>.</summary>
public sealed class InventoryGetInventoryCollectionIdsRequest
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    internal static unsafe InventoryGetInventoryCollectionIdsRequest FromNative(PFInventoryGetInventoryCollectionIdsRequest* source)
    {
        var value = new InventoryGetInventoryCollectionIdsRequest();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count;
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetInventoryCollectionIdsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = Count;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        if (Entity is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native4, arena);
            target->Entity = native4;
        }
    }
}

/// <summary>Projects <c>PFInventoryGetInventoryCollectionIdsResponse</c>.</summary>
public sealed class InventoryGetInventoryCollectionIdsResponse
{
    /// <summary><c>CollectionIds</c>.</summary>
    public IReadOnlyList<string>? CollectionIds { get; set; }

    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    internal static unsafe InventoryGetInventoryCollectionIdsResponse FromNative(PFInventoryGetInventoryCollectionIdsResponse* source)
    {
        var value = new InventoryGetInventoryCollectionIdsResponse();
        if (source->CollectionIds is not null && source->CollectionIdsCount != 0)
        {
            var list0 = new List<string>((int)source->CollectionIdsCount);
            for (uint i0 = 0; i0 < source->CollectionIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->CollectionIds[i0]) ?? string.Empty);
            }

            value.CollectionIds = list0;
        }
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetInventoryCollectionIdsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionIdsCount = (uint)(CollectionIds is null ? 0 : CollectionIds.Count);
        target->CollectionIds = arena.StringArray(CollectionIds);
        target->ContinuationToken = arena.String(ContinuationToken);
    }
}

/// <summary>Projects <c>PFInventoryGetInventoryItemsRequest</c>.</summary>
public sealed class InventoryGetInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Filter</c>.</summary>
    public string? Filter { get; set; }

    internal static unsafe InventoryGetInventoryItemsRequest FromNative(PFInventoryGetInventoryItemsRequest* source)
    {
        var value = new InventoryGetInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count;
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map3 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i3 = 0; i3 < source->CustomTagsCount; i3++)
            {
                map3[Utf8.ToString(source->CustomTags[i3].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i3].Value) ?? string.Empty;
            }

            value.CustomTags = map3;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Filter = Utf8.ToString(source->Filter);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = Count;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries3 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next3 = 0;
            foreach (KeyValuePair<string, string> pair3 in CustomTags)
            {
                entries3[next3].Key = arena.String(pair3.Key);
                entries3[next3].Value = arena.String(pair3.Value);
                next3++;
            }

            target->CustomTags = entries3;
        }
        if (Entity is not null)
        {
            PFEntityKey* native5 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native5, arena);
            target->Entity = native5;
        }
        target->Filter = arena.String(Filter);
    }
}

/// <summary>Projects <c>PFInventoryGetInventoryItemsResponse</c>.</summary>
public sealed class InventoryGetInventoryItemsResponse
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>Items</c>.</summary>
    public IReadOnlyList<InventoryInventoryItem>? Items { get; set; }

    internal static unsafe InventoryGetInventoryItemsResponse FromNative(PFInventoryGetInventoryItemsResponse* source)
    {
        var value = new InventoryGetInventoryItemsResponse();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.ETag = Utf8.ToString(source->ETag);
        if (source->Items is not null && source->ItemsCount != 0)
        {
            var list2 = new List<InventoryInventoryItem>((int)source->ItemsCount);
            for (uint i2 = 0; i2 < source->ItemsCount; i2++)
            {
                list2.Add(InventoryInventoryItem.FromNative(source->Items[i2]));
            }

            value.Items = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->ETag = arena.String(ETag);
        target->ItemsCount = (uint)(Items is null ? 0 : Items.Count);
        if (Items is not null && Items.Count > 0)
        {
            PFInventoryInventoryItem** array2 = arena.PointerArray<PFInventoryInventoryItem>(Items.Count);
            for (int i2 = 0; i2 < Items.Count; i2++)
            {
                PFInventoryInventoryItem* item2 = arena.Alloc<PFInventoryInventoryItem>(1);
                Items[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Items = array2;
        }
    }
}

/// <summary>Projects <c>PFInventoryGetInventoryOperationStatusRequest</c>.</summary>
public sealed class InventoryGetInventoryOperationStatusRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>OperationToken</c>.</summary>
    public string? OperationToken { get; set; }

    internal static unsafe InventoryGetInventoryOperationStatusRequest FromNative(PFInventoryGetInventoryOperationStatusRequest* source)
    {
        var value = new InventoryGetInventoryOperationStatusRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.OperationToken = Utf8.ToString(source->OperationToken);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetInventoryOperationStatusRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->OperationToken = arena.String(OperationToken);
    }
}

/// <summary>Projects <c>PFInventoryGetInventoryOperationStatusResponse</c>.</summary>
public sealed class InventoryGetInventoryOperationStatusResponse
{
    /// <summary><c>OperationStatus</c>.</summary>
    public string? OperationStatus { get; set; }

    internal static unsafe InventoryGetInventoryOperationStatusResponse FromNative(PFInventoryGetInventoryOperationStatusResponse* source)
    {
        var value = new InventoryGetInventoryOperationStatusResponse();
        value.OperationStatus = Utf8.ToString(source->OperationStatus);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetInventoryOperationStatusResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->OperationStatus = arena.String(OperationStatus);
    }
}

/// <summary>Projects <c>PFInventoryGetTransactionHistoryRequest</c>.</summary>
public sealed class InventoryGetTransactionHistoryRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Filter</c>.</summary>
    public string? Filter { get; set; }

    /// <summary><c>OrderBy</c>.</summary>
    public string? OrderBy { get; set; }

    internal static unsafe InventoryGetTransactionHistoryRequest FromNative(PFInventoryGetTransactionHistoryRequest* source)
    {
        var value = new InventoryGetTransactionHistoryRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count;
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map3 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i3 = 0; i3 < source->CustomTagsCount; i3++)
            {
                map3[Utf8.ToString(source->CustomTags[i3].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i3].Value) ?? string.Empty;
            }

            value.CustomTags = map3;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Filter = Utf8.ToString(source->Filter);
        value.OrderBy = Utf8.ToString(source->OrderBy);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetTransactionHistoryRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = Count;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries3 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next3 = 0;
            foreach (KeyValuePair<string, string> pair3 in CustomTags)
            {
                entries3[next3].Key = arena.String(pair3.Key);
                entries3[next3].Value = arena.String(pair3.Value);
                next3++;
            }

            target->CustomTags = entries3;
        }
        if (Entity is not null)
        {
            PFEntityKey* native5 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native5, arena);
            target->Entity = native5;
        }
        target->Filter = arena.String(Filter);
        target->OrderBy = arena.String(OrderBy);
    }
}

/// <summary>Projects <c>PFInventoryGetTransactionHistoryResponse</c>.</summary>
public sealed class InventoryGetTransactionHistoryResponse
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Transactions</c>.</summary>
    public IReadOnlyList<InventoryTransaction>? Transactions { get; set; }

    internal static unsafe InventoryGetTransactionHistoryResponse FromNative(PFInventoryGetTransactionHistoryResponse* source)
    {
        var value = new InventoryGetTransactionHistoryResponse();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        if (source->Transactions is not null && source->TransactionsCount != 0)
        {
            var list1 = new List<InventoryTransaction>((int)source->TransactionsCount);
            for (uint i1 = 0; i1 < source->TransactionsCount; i1++)
            {
                list1.Add(InventoryTransaction.FromNative(source->Transactions[i1]));
            }

            value.Transactions = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGetTransactionHistoryResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->TransactionsCount = (uint)(Transactions is null ? 0 : Transactions.Count);
        if (Transactions is not null && Transactions.Count > 0)
        {
            PFInventoryTransaction** array1 = arena.PointerArray<PFInventoryTransaction>(Transactions.Count);
            for (int i1 = 0; i1 < Transactions.Count; i1++)
            {
                PFInventoryTransaction* item1 = arena.Alloc<PFInventoryTransaction>(1);
                Transactions[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->Transactions = array1;
        }
    }
}

/// <summary>Projects <c>PFInventoryGooglePlayProductPurchase</c>.</summary>
public sealed class InventoryGooglePlayProductPurchase
{
    /// <summary><c>ProductId</c>.</summary>
    public string? ProductId { get; set; }

    /// <summary><c>Token</c>.</summary>
    public string? Token { get; set; }

    internal static unsafe InventoryGooglePlayProductPurchase FromNative(PFInventoryGooglePlayProductPurchase* source)
    {
        var value = new InventoryGooglePlayProductPurchase();
        value.ProductId = Utf8.ToString(source->ProductId);
        value.Token = Utf8.ToString(source->Token);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryGooglePlayProductPurchase* target, PlayFabArena arena)
    {
        *target = default;
        target->ProductId = arena.String(ProductId);
        target->Token = arena.String(Token);
    }
}

/// <summary>Projects <c>PFInventoryInitialValues</c>.</summary>
public sealed class InventoryInitialValues
{
    /// <summary><c>DisplayProperties</c>.</summary>
    public string? DisplayProperties { get; set; }

    internal static unsafe InventoryInitialValues FromNative(PFInventoryInitialValues* source)
    {
        var value = new InventoryInitialValues();
        value.DisplayProperties = Utf8.ToString(source->DisplayProperties.StringValue);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryInitialValues* target, PlayFabArena arena)
    {
        *target = default;
        target->DisplayProperties.StringValue = arena.String(DisplayProperties);
    }
}

/// <summary>Projects <c>PFInventoryInventoryItem</c>.</summary>
public sealed class InventoryInventoryItem
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>DisplayProperties</c>.</summary>
    public string? DisplayProperties { get; set; }

    /// <summary><c>ExpirationDate</c>.</summary>
    public DateTimeOffset? ExpirationDate { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>StackId</c>.</summary>
    public string? StackId { get; set; }

    /// <summary><c>StartDate</c>.</summary>
    public DateTimeOffset? StartDate { get; set; }

    /// <summary><c>Type</c>.</summary>
    public string? Type { get; set; }

    internal static unsafe InventoryInventoryItem FromNative(PFInventoryInventoryItem* source)
    {
        var value = new InventoryInventoryItem();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.DisplayProperties = Utf8.ToString(source->DisplayProperties.StringValue);
        value.ExpirationDate = source->ExpirationDate is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->ExpirationDate);
        value.Id = Utf8.ToString(source->Id);
        value.StackId = Utf8.ToString(source->StackId);
        value.StartDate = source->StartDate is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->StartDate);
        value.Type = Utf8.ToString(source->Type);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryInventoryItem* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->DisplayProperties.StringValue = arena.String(DisplayProperties);
        target->ExpirationDate = arena.Value(ExpirationDate is null ? null : (long?)PlayFabTime.ToUnixTime(ExpirationDate.Value));
        target->Id = arena.String(Id);
        target->StackId = arena.String(StackId);
        target->StartDate = arena.Value(StartDate is null ? null : (long?)PlayFabTime.ToUnixTime(StartDate.Value));
        target->Type = arena.String(Type);
    }
}

/// <summary>Projects <c>PFInventoryInventoryItemReference</c>.</summary>
public sealed class InventoryInventoryItemReference
{
    /// <summary><c>AlternateId</c>.</summary>
    public InventoryAlternateId? AlternateId { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>StackId</c>.</summary>
    public string? StackId { get; set; }

    internal static unsafe InventoryInventoryItemReference FromNative(PFInventoryInventoryItemReference* source)
    {
        var value = new InventoryInventoryItemReference();
        value.AlternateId = source->AlternateId is null ? null : InventoryAlternateId.FromNative(source->AlternateId);
        value.Id = Utf8.ToString(source->Id);
        value.StackId = Utf8.ToString(source->StackId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryInventoryItemReference* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFInventoryAlternateId* native0 = arena.Alloc<PFInventoryAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->Id = arena.String(Id);
        target->StackId = arena.String(StackId);
    }
}

/// <summary>Projects <c>PFInventoryInventoryOperation</c>.</summary>
public sealed class InventoryInventoryOperation
{
    /// <summary><c>Add</c>.</summary>
    public InventoryAddInventoryItemsOperation? Add { get; set; }

    /// <summary><c>DeleteOp</c>.</summary>
    public InventoryDeleteInventoryItemsOperation? DeleteOp { get; set; }

    /// <summary><c>Purchase</c>.</summary>
    public InventoryPurchaseInventoryItemsOperation? Purchase { get; set; }

    /// <summary><c>Subtract</c>.</summary>
    public InventorySubtractInventoryItemsOperation? Subtract { get; set; }

    /// <summary><c>Transfer</c>.</summary>
    public InventoryTransferInventoryItemsOperation? Transfer { get; set; }

    /// <summary><c>Update</c>.</summary>
    public InventoryUpdateInventoryItemsOperation? Update { get; set; }

    internal static unsafe InventoryInventoryOperation FromNative(PFInventoryInventoryOperation* source)
    {
        var value = new InventoryInventoryOperation();
        value.Add = source->Add is null ? null : InventoryAddInventoryItemsOperation.FromNative(source->Add);
        value.DeleteOp = source->DeleteOp is null ? null : InventoryDeleteInventoryItemsOperation.FromNative(source->DeleteOp);
        value.Purchase = source->Purchase is null ? null : InventoryPurchaseInventoryItemsOperation.FromNative(source->Purchase);
        value.Subtract = source->Subtract is null ? null : InventorySubtractInventoryItemsOperation.FromNative(source->Subtract);
        value.Transfer = source->Transfer is null ? null : InventoryTransferInventoryItemsOperation.FromNative(source->Transfer);
        value.Update = source->Update is null ? null : InventoryUpdateInventoryItemsOperation.FromNative(source->Update);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryInventoryOperation* target, PlayFabArena arena)
    {
        *target = default;
        if (Add is not null)
        {
            PFInventoryAddInventoryItemsOperation* native0 = arena.Alloc<PFInventoryAddInventoryItemsOperation>(1);
            Add.WriteTo(native0, arena);
            target->Add = native0;
        }
        if (DeleteOp is not null)
        {
            PFInventoryDeleteInventoryItemsOperation* native1 = arena.Alloc<PFInventoryDeleteInventoryItemsOperation>(1);
            DeleteOp.WriteTo(native1, arena);
            target->DeleteOp = native1;
        }
        if (Purchase is not null)
        {
            PFInventoryPurchaseInventoryItemsOperation* native2 = arena.Alloc<PFInventoryPurchaseInventoryItemsOperation>(1);
            Purchase.WriteTo(native2, arena);
            target->Purchase = native2;
        }
        if (Subtract is not null)
        {
            PFInventorySubtractInventoryItemsOperation* native3 = arena.Alloc<PFInventorySubtractInventoryItemsOperation>(1);
            Subtract.WriteTo(native3, arena);
            target->Subtract = native3;
        }
        if (Transfer is not null)
        {
            PFInventoryTransferInventoryItemsOperation* native4 = arena.Alloc<PFInventoryTransferInventoryItemsOperation>(1);
            Transfer.WriteTo(native4, arena);
            target->Transfer = native4;
        }
        if (Update is not null)
        {
            PFInventoryUpdateInventoryItemsOperation* native5 = arena.Alloc<PFInventoryUpdateInventoryItemsOperation>(1);
            Update.WriteTo(native5, arena);
            target->Update = native5;
        }
    }
}

/// <summary>Projects <c>PFInventoryPurchaseInventoryItemsOperation</c>.</summary>
public sealed class InventoryPurchaseInventoryItemsOperation
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>DeleteEmptyStacks</c>.</summary>
    public bool DeleteEmptyStacks { get; set; }

    /// <summary><c>DurationInSeconds</c>.</summary>
    public double? DurationInSeconds { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    /// <summary><c>NewStackValues</c>.</summary>
    public InventoryInitialValues? NewStackValues { get; set; }

    /// <summary><c>PriceAmounts</c>.</summary>
    public IReadOnlyList<InventoryPurchasePriceAmount>? PriceAmounts { get; set; }

    /// <summary><c>StoreId</c>.</summary>
    public string? StoreId { get; set; }

    internal static unsafe InventoryPurchaseInventoryItemsOperation FromNative(PFInventoryPurchaseInventoryItemsOperation* source)
    {
        var value = new InventoryPurchaseInventoryItemsOperation();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.DeleteEmptyStacks = source->DeleteEmptyStacks != 0;
        value.DurationInSeconds = source->DurationInSeconds is null ? null : (double?)(*source->DurationInSeconds);
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        value.NewStackValues = source->NewStackValues is null ? null : InventoryInitialValues.FromNative(source->NewStackValues);
        if (source->PriceAmounts is not null && source->PriceAmountsCount != 0)
        {
            var list5 = new List<InventoryPurchasePriceAmount>((int)source->PriceAmountsCount);
            for (uint i5 = 0; i5 < source->PriceAmountsCount; i5++)
            {
                list5.Add(InventoryPurchasePriceAmount.FromNative(source->PriceAmounts[i5]));
            }

            value.PriceAmounts = list5;
        }
        value.StoreId = Utf8.ToString(source->StoreId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryPurchaseInventoryItemsOperation* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->DeleteEmptyStacks = DeleteEmptyStacks ? (byte)1 : (byte)0;
        target->DurationInSeconds = arena.Value(DurationInSeconds);
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native3 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native3, arena);
            target->Item = native3;
        }
        if (NewStackValues is not null)
        {
            PFInventoryInitialValues* native4 = arena.Alloc<PFInventoryInitialValues>(1);
            NewStackValues.WriteTo(native4, arena);
            target->NewStackValues = native4;
        }
        target->PriceAmountsCount = (uint)(PriceAmounts is null ? 0 : PriceAmounts.Count);
        if (PriceAmounts is not null && PriceAmounts.Count > 0)
        {
            PFInventoryPurchasePriceAmount** array5 = arena.PointerArray<PFInventoryPurchasePriceAmount>(PriceAmounts.Count);
            for (int i5 = 0; i5 < PriceAmounts.Count; i5++)
            {
                PFInventoryPurchasePriceAmount* item5 = arena.Alloc<PFInventoryPurchasePriceAmount>(1);
                PriceAmounts[i5].WriteTo(item5, arena);
                array5[i5] = item5;
            }

            target->PriceAmounts = array5;
        }
        target->StoreId = arena.String(StoreId);
    }
}

/// <summary>Projects <c>PFInventoryPurchaseInventoryItemsRequest</c>.</summary>
public sealed class InventoryPurchaseInventoryItemsRequest
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DeleteEmptyStacks</c>.</summary>
    public bool DeleteEmptyStacks { get; set; }

    /// <summary><c>DurationInSeconds</c>.</summary>
    public double? DurationInSeconds { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    /// <summary><c>NewStackValues</c>.</summary>
    public InventoryInitialValues? NewStackValues { get; set; }

    /// <summary><c>PriceAmounts</c>.</summary>
    public IReadOnlyList<InventoryPurchasePriceAmount>? PriceAmounts { get; set; }

    /// <summary><c>StoreId</c>.</summary>
    public string? StoreId { get; set; }

    internal static unsafe InventoryPurchaseInventoryItemsRequest FromNative(PFInventoryPurchaseInventoryItemsRequest* source)
    {
        var value = new InventoryPurchaseInventoryItemsRequest();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.DeleteEmptyStacks = source->DeleteEmptyStacks != 0;
        value.DurationInSeconds = source->DurationInSeconds is null ? null : (double?)(*source->DurationInSeconds);
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        value.NewStackValues = source->NewStackValues is null ? null : InventoryInitialValues.FromNative(source->NewStackValues);
        if (source->PriceAmounts is not null && source->PriceAmountsCount != 0)
        {
            var list11 = new List<InventoryPurchasePriceAmount>((int)source->PriceAmountsCount);
            for (uint i11 = 0; i11 < source->PriceAmountsCount; i11++)
            {
                list11.Add(InventoryPurchasePriceAmount.FromNative(source->PriceAmounts[i11]));
            }

            value.PriceAmounts = list11;
        }
        value.StoreId = Utf8.ToString(source->StoreId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryPurchaseInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        target->DeleteEmptyStacks = DeleteEmptyStacks ? (byte)1 : (byte)0;
        target->DurationInSeconds = arena.Value(DurationInSeconds);
        if (Entity is not null)
        {
            PFEntityKey* native6 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native6, arena);
            target->Entity = native6;
        }
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native9 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native9, arena);
            target->Item = native9;
        }
        if (NewStackValues is not null)
        {
            PFInventoryInitialValues* native10 = arena.Alloc<PFInventoryInitialValues>(1);
            NewStackValues.WriteTo(native10, arena);
            target->NewStackValues = native10;
        }
        target->PriceAmountsCount = (uint)(PriceAmounts is null ? 0 : PriceAmounts.Count);
        if (PriceAmounts is not null && PriceAmounts.Count > 0)
        {
            PFInventoryPurchasePriceAmount** array11 = arena.PointerArray<PFInventoryPurchasePriceAmount>(PriceAmounts.Count);
            for (int i11 = 0; i11 < PriceAmounts.Count; i11++)
            {
                PFInventoryPurchasePriceAmount* item11 = arena.Alloc<PFInventoryPurchasePriceAmount>(1);
                PriceAmounts[i11].WriteTo(item11, arena);
                array11[i11] = item11;
            }

            target->PriceAmounts = array11;
        }
        target->StoreId = arena.String(StoreId);
    }
}

/// <summary>Projects <c>PFInventoryPurchaseInventoryItemsResponse</c>.</summary>
public sealed class InventoryPurchaseInventoryItemsResponse
{
    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryPurchaseInventoryItemsResponse FromNative(PFInventoryPurchaseInventoryItemsResponse* source)
    {
        var value = new InventoryPurchaseInventoryItemsResponse();
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list2 = new List<string>((int)source->TransactionIdsCount);
            for (uint i2 = 0; i2 < source->TransactionIdsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->TransactionIds[i2]) ?? string.Empty);
            }

            value.TransactionIds = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryPurchaseInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryPurchasePriceAmount</c>.</summary>
public sealed class InventoryPurchasePriceAmount
{
    /// <summary><c>Amount</c>.</summary>
    public int Amount { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>StackId</c>.</summary>
    public string? StackId { get; set; }

    internal static unsafe InventoryPurchasePriceAmount FromNative(PFInventoryPurchasePriceAmount* source)
    {
        var value = new InventoryPurchasePriceAmount();
        value.Amount = source->Amount;
        value.ItemId = Utf8.ToString(source->ItemId);
        value.StackId = Utf8.ToString(source->StackId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryPurchasePriceAmount* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = Amount;
        target->ItemId = arena.String(ItemId);
        target->StackId = arena.String(StackId);
    }
}

/// <summary>Projects <c>PFInventoryRedeemAppleAppStoreInventoryItemsRequest</c>.</summary>
public sealed class InventoryRedeemAppleAppStoreInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Receipt</c>.</summary>
    public string? Receipt { get; set; }

    internal static unsafe InventoryRedeemAppleAppStoreInventoryItemsRequest FromNative(PFInventoryRedeemAppleAppStoreInventoryItemsRequest* source)
    {
        var value = new InventoryRedeemAppleAppStoreInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Receipt = Utf8.ToString(source->Receipt);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemAppleAppStoreInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->Receipt = arena.String(Receipt);
    }
}

/// <summary>Projects <c>PFInventoryRedeemAppleAppStoreInventoryItemsResponse</c>.</summary>
public sealed class InventoryRedeemAppleAppStoreInventoryItemsResponse
{
    /// <summary><c>Failed</c>.</summary>
    public IReadOnlyList<InventoryRedemptionFailure>? Failed { get; set; }

    /// <summary><c>Succeeded</c>.</summary>
    public IReadOnlyList<InventoryRedemptionSuccess>? Succeeded { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryRedeemAppleAppStoreInventoryItemsResponse FromNative(PFInventoryRedeemAppleAppStoreInventoryItemsResponse* source)
    {
        var value = new InventoryRedeemAppleAppStoreInventoryItemsResponse();
        if (source->Failed is not null && source->FailedCount != 0)
        {
            var list0 = new List<InventoryRedemptionFailure>((int)source->FailedCount);
            for (uint i0 = 0; i0 < source->FailedCount; i0++)
            {
                list0.Add(InventoryRedemptionFailure.FromNative(source->Failed[i0]));
            }

            value.Failed = list0;
        }
        if (source->Succeeded is not null && source->SucceededCount != 0)
        {
            var list2 = new List<InventoryRedemptionSuccess>((int)source->SucceededCount);
            for (uint i2 = 0; i2 < source->SucceededCount; i2++)
            {
                list2.Add(InventoryRedemptionSuccess.FromNative(source->Succeeded[i2]));
            }

            value.Succeeded = list2;
        }
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list4 = new List<string>((int)source->TransactionIdsCount);
            for (uint i4 = 0; i4 < source->TransactionIdsCount; i4++)
            {
                list4.Add(Utf8.ToString(source->TransactionIds[i4]) ?? string.Empty);
            }

            value.TransactionIds = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemAppleAppStoreInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->FailedCount = (uint)(Failed is null ? 0 : Failed.Count);
        if (Failed is not null && Failed.Count > 0)
        {
            PFInventoryRedemptionFailure** array0 = arena.PointerArray<PFInventoryRedemptionFailure>(Failed.Count);
            for (int i0 = 0; i0 < Failed.Count; i0++)
            {
                PFInventoryRedemptionFailure* item0 = arena.Alloc<PFInventoryRedemptionFailure>(1);
                Failed[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Failed = array0;
        }
        target->SucceededCount = (uint)(Succeeded is null ? 0 : Succeeded.Count);
        if (Succeeded is not null && Succeeded.Count > 0)
        {
            PFInventoryRedemptionSuccess** array2 = arena.PointerArray<PFInventoryRedemptionSuccess>(Succeeded.Count);
            for (int i2 = 0; i2 < Succeeded.Count; i2++)
            {
                PFInventoryRedemptionSuccess* item2 = arena.Alloc<PFInventoryRedemptionSuccess>(1);
                Succeeded[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Succeeded = array2;
        }
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryRedeemGooglePlayInventoryItemsRequest</c>.</summary>
public sealed class InventoryRedeemGooglePlayInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Purchases</c>.</summary>
    public IReadOnlyList<InventoryGooglePlayProductPurchase>? Purchases { get; set; }

    internal static unsafe InventoryRedeemGooglePlayInventoryItemsRequest FromNative(PFInventoryRedeemGooglePlayInventoryItemsRequest* source)
    {
        var value = new InventoryRedeemGooglePlayInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->Purchases is not null && source->PurchasesCount != 0)
        {
            var list4 = new List<InventoryGooglePlayProductPurchase>((int)source->PurchasesCount);
            for (uint i4 = 0; i4 < source->PurchasesCount; i4++)
            {
                list4.Add(InventoryGooglePlayProductPurchase.FromNative(source->Purchases[i4]));
            }

            value.Purchases = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemGooglePlayInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->PurchasesCount = (uint)(Purchases is null ? 0 : Purchases.Count);
        if (Purchases is not null && Purchases.Count > 0)
        {
            PFInventoryGooglePlayProductPurchase** array4 = arena.PointerArray<PFInventoryGooglePlayProductPurchase>(Purchases.Count);
            for (int i4 = 0; i4 < Purchases.Count; i4++)
            {
                PFInventoryGooglePlayProductPurchase* item4 = arena.Alloc<PFInventoryGooglePlayProductPurchase>(1);
                Purchases[i4].WriteTo(item4, arena);
                array4[i4] = item4;
            }

            target->Purchases = array4;
        }
    }
}

/// <summary>Projects <c>PFInventoryRedeemGooglePlayInventoryItemsResponse</c>.</summary>
public sealed class InventoryRedeemGooglePlayInventoryItemsResponse
{
    /// <summary><c>Failed</c>.</summary>
    public IReadOnlyList<InventoryRedemptionFailure>? Failed { get; set; }

    /// <summary><c>Succeeded</c>.</summary>
    public IReadOnlyList<InventoryRedemptionSuccess>? Succeeded { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryRedeemGooglePlayInventoryItemsResponse FromNative(PFInventoryRedeemGooglePlayInventoryItemsResponse* source)
    {
        var value = new InventoryRedeemGooglePlayInventoryItemsResponse();
        if (source->Failed is not null && source->FailedCount != 0)
        {
            var list0 = new List<InventoryRedemptionFailure>((int)source->FailedCount);
            for (uint i0 = 0; i0 < source->FailedCount; i0++)
            {
                list0.Add(InventoryRedemptionFailure.FromNative(source->Failed[i0]));
            }

            value.Failed = list0;
        }
        if (source->Succeeded is not null && source->SucceededCount != 0)
        {
            var list2 = new List<InventoryRedemptionSuccess>((int)source->SucceededCount);
            for (uint i2 = 0; i2 < source->SucceededCount; i2++)
            {
                list2.Add(InventoryRedemptionSuccess.FromNative(source->Succeeded[i2]));
            }

            value.Succeeded = list2;
        }
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list4 = new List<string>((int)source->TransactionIdsCount);
            for (uint i4 = 0; i4 < source->TransactionIdsCount; i4++)
            {
                list4.Add(Utf8.ToString(source->TransactionIds[i4]) ?? string.Empty);
            }

            value.TransactionIds = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemGooglePlayInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->FailedCount = (uint)(Failed is null ? 0 : Failed.Count);
        if (Failed is not null && Failed.Count > 0)
        {
            PFInventoryRedemptionFailure** array0 = arena.PointerArray<PFInventoryRedemptionFailure>(Failed.Count);
            for (int i0 = 0; i0 < Failed.Count; i0++)
            {
                PFInventoryRedemptionFailure* item0 = arena.Alloc<PFInventoryRedemptionFailure>(1);
                Failed[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Failed = array0;
        }
        target->SucceededCount = (uint)(Succeeded is null ? 0 : Succeeded.Count);
        if (Succeeded is not null && Succeeded.Count > 0)
        {
            PFInventoryRedemptionSuccess** array2 = arena.PointerArray<PFInventoryRedemptionSuccess>(Succeeded.Count);
            for (int i2 = 0; i2 < Succeeded.Count; i2++)
            {
                PFInventoryRedemptionSuccess* item2 = arena.Alloc<PFInventoryRedemptionSuccess>(1);
                Succeeded[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Succeeded = array2;
        }
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryRedeemMicrosoftStoreInventoryItemsRequest</c>.</summary>
public sealed class InventoryRedeemMicrosoftStoreInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary>The Xbox user this request is made on behalf of.</summary>
    public User? User { get; set; }

    internal static unsafe InventoryRedeemMicrosoftStoreInventoryItemsRequest FromNative(PFInventoryRedeemMicrosoftStoreInventoryItemsRequest* source)
    {
        var value = new InventoryRedeemMicrosoftStoreInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemMicrosoftStoreInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->User = User is null ? IntPtr.Zero : User.Handle;
    }
}

/// <summary>Projects <c>PFInventoryRedeemMicrosoftStoreInventoryItemsResponse</c>.</summary>
public sealed class InventoryRedeemMicrosoftStoreInventoryItemsResponse
{
    /// <summary><c>Failed</c>.</summary>
    public IReadOnlyList<InventoryRedemptionFailure>? Failed { get; set; }

    /// <summary><c>Succeeded</c>.</summary>
    public IReadOnlyList<InventoryRedemptionSuccess>? Succeeded { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryRedeemMicrosoftStoreInventoryItemsResponse FromNative(PFInventoryRedeemMicrosoftStoreInventoryItemsResponse* source)
    {
        var value = new InventoryRedeemMicrosoftStoreInventoryItemsResponse();
        if (source->Failed is not null && source->FailedCount != 0)
        {
            var list0 = new List<InventoryRedemptionFailure>((int)source->FailedCount);
            for (uint i0 = 0; i0 < source->FailedCount; i0++)
            {
                list0.Add(InventoryRedemptionFailure.FromNative(source->Failed[i0]));
            }

            value.Failed = list0;
        }
        if (source->Succeeded is not null && source->SucceededCount != 0)
        {
            var list2 = new List<InventoryRedemptionSuccess>((int)source->SucceededCount);
            for (uint i2 = 0; i2 < source->SucceededCount; i2++)
            {
                list2.Add(InventoryRedemptionSuccess.FromNative(source->Succeeded[i2]));
            }

            value.Succeeded = list2;
        }
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list4 = new List<string>((int)source->TransactionIdsCount);
            for (uint i4 = 0; i4 < source->TransactionIdsCount; i4++)
            {
                list4.Add(Utf8.ToString(source->TransactionIds[i4]) ?? string.Empty);
            }

            value.TransactionIds = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemMicrosoftStoreInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->FailedCount = (uint)(Failed is null ? 0 : Failed.Count);
        if (Failed is not null && Failed.Count > 0)
        {
            PFInventoryRedemptionFailure** array0 = arena.PointerArray<PFInventoryRedemptionFailure>(Failed.Count);
            for (int i0 = 0; i0 < Failed.Count; i0++)
            {
                PFInventoryRedemptionFailure* item0 = arena.Alloc<PFInventoryRedemptionFailure>(1);
                Failed[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Failed = array0;
        }
        target->SucceededCount = (uint)(Succeeded is null ? 0 : Succeeded.Count);
        if (Succeeded is not null && Succeeded.Count > 0)
        {
            PFInventoryRedemptionSuccess** array2 = arena.PointerArray<PFInventoryRedemptionSuccess>(Succeeded.Count);
            for (int i2 = 0; i2 < Succeeded.Count; i2++)
            {
                PFInventoryRedemptionSuccess* item2 = arena.Alloc<PFInventoryRedemptionSuccess>(1);
                Succeeded[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Succeeded = array2;
        }
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryRedeemNintendoEShopInventoryItemsRequest</c>.</summary>
public sealed class InventoryRedeemNintendoEShopInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>NintendoServiceAccountIdToken</c>.</summary>
    public string? NintendoServiceAccountIdToken { get; set; }

    internal static unsafe InventoryRedeemNintendoEShopInventoryItemsRequest FromNative(PFInventoryRedeemNintendoEShopInventoryItemsRequest* source)
    {
        var value = new InventoryRedeemNintendoEShopInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.NintendoServiceAccountIdToken = Utf8.ToString(source->NintendoServiceAccountIdToken);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemNintendoEShopInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->NintendoServiceAccountIdToken = arena.String(NintendoServiceAccountIdToken);
    }
}

/// <summary>Projects <c>PFInventoryRedeemNintendoEShopInventoryItemsResponse</c>.</summary>
public sealed class InventoryRedeemNintendoEShopInventoryItemsResponse
{
    /// <summary><c>Failed</c>.</summary>
    public IReadOnlyList<InventoryRedemptionFailure>? Failed { get; set; }

    /// <summary><c>Succeeded</c>.</summary>
    public IReadOnlyList<InventoryRedemptionSuccess>? Succeeded { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryRedeemNintendoEShopInventoryItemsResponse FromNative(PFInventoryRedeemNintendoEShopInventoryItemsResponse* source)
    {
        var value = new InventoryRedeemNintendoEShopInventoryItemsResponse();
        if (source->Failed is not null && source->FailedCount != 0)
        {
            var list0 = new List<InventoryRedemptionFailure>((int)source->FailedCount);
            for (uint i0 = 0; i0 < source->FailedCount; i0++)
            {
                list0.Add(InventoryRedemptionFailure.FromNative(source->Failed[i0]));
            }

            value.Failed = list0;
        }
        if (source->Succeeded is not null && source->SucceededCount != 0)
        {
            var list2 = new List<InventoryRedemptionSuccess>((int)source->SucceededCount);
            for (uint i2 = 0; i2 < source->SucceededCount; i2++)
            {
                list2.Add(InventoryRedemptionSuccess.FromNative(source->Succeeded[i2]));
            }

            value.Succeeded = list2;
        }
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list4 = new List<string>((int)source->TransactionIdsCount);
            for (uint i4 = 0; i4 < source->TransactionIdsCount; i4++)
            {
                list4.Add(Utf8.ToString(source->TransactionIds[i4]) ?? string.Empty);
            }

            value.TransactionIds = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemNintendoEShopInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->FailedCount = (uint)(Failed is null ? 0 : Failed.Count);
        if (Failed is not null && Failed.Count > 0)
        {
            PFInventoryRedemptionFailure** array0 = arena.PointerArray<PFInventoryRedemptionFailure>(Failed.Count);
            for (int i0 = 0; i0 < Failed.Count; i0++)
            {
                PFInventoryRedemptionFailure* item0 = arena.Alloc<PFInventoryRedemptionFailure>(1);
                Failed[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Failed = array0;
        }
        target->SucceededCount = (uint)(Succeeded is null ? 0 : Succeeded.Count);
        if (Succeeded is not null && Succeeded.Count > 0)
        {
            PFInventoryRedemptionSuccess** array2 = arena.PointerArray<PFInventoryRedemptionSuccess>(Succeeded.Count);
            for (int i2 = 0; i2 < Succeeded.Count; i2++)
            {
                PFInventoryRedemptionSuccess* item2 = arena.Alloc<PFInventoryRedemptionSuccess>(1);
                Succeeded[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Succeeded = array2;
        }
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryRedeemPlayStationStoreInventoryItemsRequest</c>.</summary>
public sealed class InventoryRedeemPlayStationStoreInventoryItemsRequest
{
    /// <summary><c>AuthorizationCode</c>.</summary>
    public string? AuthorizationCode { get; set; }

    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>RedirectUri</c>.</summary>
    public string? RedirectUri { get; set; }

    /// <summary><c>ServiceLabel</c>.</summary>
    public string? ServiceLabel { get; set; }

    internal static unsafe InventoryRedeemPlayStationStoreInventoryItemsRequest FromNative(PFInventoryRedeemPlayStationStoreInventoryItemsRequest* source)
    {
        var value = new InventoryRedeemPlayStationStoreInventoryItemsRequest();
        value.AuthorizationCode = Utf8.ToString(source->AuthorizationCode);
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.RedirectUri = Utf8.ToString(source->RedirectUri);
        value.ServiceLabel = Utf8.ToString(source->ServiceLabel);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemPlayStationStoreInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->AuthorizationCode = arena.String(AuthorizationCode);
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        if (Entity is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native4, arena);
            target->Entity = native4;
        }
        target->RedirectUri = arena.String(RedirectUri);
        target->ServiceLabel = arena.String(ServiceLabel);
    }
}

/// <summary>Projects <c>PFInventoryRedeemPlayStationStoreInventoryItemsResponse</c>.</summary>
public sealed class InventoryRedeemPlayStationStoreInventoryItemsResponse
{
    /// <summary><c>Failed</c>.</summary>
    public IReadOnlyList<InventoryRedemptionFailure>? Failed { get; set; }

    /// <summary><c>Succeeded</c>.</summary>
    public IReadOnlyList<InventoryRedemptionSuccess>? Succeeded { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryRedeemPlayStationStoreInventoryItemsResponse FromNative(PFInventoryRedeemPlayStationStoreInventoryItemsResponse* source)
    {
        var value = new InventoryRedeemPlayStationStoreInventoryItemsResponse();
        if (source->Failed is not null && source->FailedCount != 0)
        {
            var list0 = new List<InventoryRedemptionFailure>((int)source->FailedCount);
            for (uint i0 = 0; i0 < source->FailedCount; i0++)
            {
                list0.Add(InventoryRedemptionFailure.FromNative(source->Failed[i0]));
            }

            value.Failed = list0;
        }
        if (source->Succeeded is not null && source->SucceededCount != 0)
        {
            var list2 = new List<InventoryRedemptionSuccess>((int)source->SucceededCount);
            for (uint i2 = 0; i2 < source->SucceededCount; i2++)
            {
                list2.Add(InventoryRedemptionSuccess.FromNative(source->Succeeded[i2]));
            }

            value.Succeeded = list2;
        }
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list4 = new List<string>((int)source->TransactionIdsCount);
            for (uint i4 = 0; i4 < source->TransactionIdsCount; i4++)
            {
                list4.Add(Utf8.ToString(source->TransactionIds[i4]) ?? string.Empty);
            }

            value.TransactionIds = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemPlayStationStoreInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->FailedCount = (uint)(Failed is null ? 0 : Failed.Count);
        if (Failed is not null && Failed.Count > 0)
        {
            PFInventoryRedemptionFailure** array0 = arena.PointerArray<PFInventoryRedemptionFailure>(Failed.Count);
            for (int i0 = 0; i0 < Failed.Count; i0++)
            {
                PFInventoryRedemptionFailure* item0 = arena.Alloc<PFInventoryRedemptionFailure>(1);
                Failed[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Failed = array0;
        }
        target->SucceededCount = (uint)(Succeeded is null ? 0 : Succeeded.Count);
        if (Succeeded is not null && Succeeded.Count > 0)
        {
            PFInventoryRedemptionSuccess** array2 = arena.PointerArray<PFInventoryRedemptionSuccess>(Succeeded.Count);
            for (int i2 = 0; i2 < Succeeded.Count; i2++)
            {
                PFInventoryRedemptionSuccess* item2 = arena.Alloc<PFInventoryRedemptionSuccess>(1);
                Succeeded[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Succeeded = array2;
        }
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryRedeemSteamInventoryItemsRequest</c>.</summary>
public sealed class InventoryRedeemSteamInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    internal static unsafe InventoryRedeemSteamInventoryItemsRequest FromNative(PFInventoryRedeemSteamInventoryItemsRequest* source)
    {
        var value = new InventoryRedeemSteamInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemSteamInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
    }
}

/// <summary>Projects <c>PFInventoryRedeemSteamInventoryItemsResponse</c>.</summary>
public sealed class InventoryRedeemSteamInventoryItemsResponse
{
    /// <summary><c>Failed</c>.</summary>
    public IReadOnlyList<InventoryRedemptionFailure>? Failed { get; set; }

    /// <summary><c>Succeeded</c>.</summary>
    public IReadOnlyList<InventoryRedemptionSuccess>? Succeeded { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryRedeemSteamInventoryItemsResponse FromNative(PFInventoryRedeemSteamInventoryItemsResponse* source)
    {
        var value = new InventoryRedeemSteamInventoryItemsResponse();
        if (source->Failed is not null && source->FailedCount != 0)
        {
            var list0 = new List<InventoryRedemptionFailure>((int)source->FailedCount);
            for (uint i0 = 0; i0 < source->FailedCount; i0++)
            {
                list0.Add(InventoryRedemptionFailure.FromNative(source->Failed[i0]));
            }

            value.Failed = list0;
        }
        if (source->Succeeded is not null && source->SucceededCount != 0)
        {
            var list2 = new List<InventoryRedemptionSuccess>((int)source->SucceededCount);
            for (uint i2 = 0; i2 < source->SucceededCount; i2++)
            {
                list2.Add(InventoryRedemptionSuccess.FromNative(source->Succeeded[i2]));
            }

            value.Succeeded = list2;
        }
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list4 = new List<string>((int)source->TransactionIdsCount);
            for (uint i4 = 0; i4 < source->TransactionIdsCount; i4++)
            {
                list4.Add(Utf8.ToString(source->TransactionIds[i4]) ?? string.Empty);
            }

            value.TransactionIds = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedeemSteamInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->FailedCount = (uint)(Failed is null ? 0 : Failed.Count);
        if (Failed is not null && Failed.Count > 0)
        {
            PFInventoryRedemptionFailure** array0 = arena.PointerArray<PFInventoryRedemptionFailure>(Failed.Count);
            for (int i0 = 0; i0 < Failed.Count; i0++)
            {
                PFInventoryRedemptionFailure* item0 = arena.Alloc<PFInventoryRedemptionFailure>(1);
                Failed[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Failed = array0;
        }
        target->SucceededCount = (uint)(Succeeded is null ? 0 : Succeeded.Count);
        if (Succeeded is not null && Succeeded.Count > 0)
        {
            PFInventoryRedemptionSuccess** array2 = arena.PointerArray<PFInventoryRedemptionSuccess>(Succeeded.Count);
            for (int i2 = 0; i2 < Succeeded.Count; i2++)
            {
                PFInventoryRedemptionSuccess* item2 = arena.Alloc<PFInventoryRedemptionSuccess>(1);
                Succeeded[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Succeeded = array2;
        }
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryRedemptionFailure</c>.</summary>
public sealed class InventoryRedemptionFailure
{
    /// <summary><c>FailureCode</c>.</summary>
    public string? FailureCode { get; set; }

    /// <summary><c>FailureDetails</c>.</summary>
    public string? FailureDetails { get; set; }

    /// <summary><c>MarketplaceAlternateId</c>.</summary>
    public string? MarketplaceAlternateId { get; set; }

    /// <summary><c>MarketplaceTransactionId</c>.</summary>
    public string? MarketplaceTransactionId { get; set; }

    internal static unsafe InventoryRedemptionFailure FromNative(PFInventoryRedemptionFailure* source)
    {
        var value = new InventoryRedemptionFailure();
        value.FailureCode = Utf8.ToString(source->FailureCode);
        value.FailureDetails = Utf8.ToString(source->FailureDetails);
        value.MarketplaceAlternateId = Utf8.ToString(source->MarketplaceAlternateId);
        value.MarketplaceTransactionId = Utf8.ToString(source->MarketplaceTransactionId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedemptionFailure* target, PlayFabArena arena)
    {
        *target = default;
        target->FailureCode = arena.String(FailureCode);
        target->FailureDetails = arena.String(FailureDetails);
        target->MarketplaceAlternateId = arena.String(MarketplaceAlternateId);
        target->MarketplaceTransactionId = arena.String(MarketplaceTransactionId);
    }
}

/// <summary>Projects <c>PFInventoryRedemptionSuccess</c>.</summary>
public sealed class InventoryRedemptionSuccess
{
    /// <summary><c>ExpirationTimestamp</c>.</summary>
    public DateTimeOffset? ExpirationTimestamp { get; set; }

    /// <summary><c>MarketplaceAlternateId</c>.</summary>
    public string? MarketplaceAlternateId { get; set; }

    /// <summary><c>MarketplaceTransactionId</c>.</summary>
    public string? MarketplaceTransactionId { get; set; }

    /// <summary><c>SuccessTimestamp</c>.</summary>
    public DateTimeOffset SuccessTimestamp { get; set; }

    internal static unsafe InventoryRedemptionSuccess FromNative(PFInventoryRedemptionSuccess* source)
    {
        var value = new InventoryRedemptionSuccess();
        value.ExpirationTimestamp = source->ExpirationTimestamp is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->ExpirationTimestamp);
        value.MarketplaceAlternateId = Utf8.ToString(source->MarketplaceAlternateId);
        value.MarketplaceTransactionId = Utf8.ToString(source->MarketplaceTransactionId);
        value.SuccessTimestamp = PlayFabTime.ToDateTimeOffset(source->SuccessTimestamp);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryRedemptionSuccess* target, PlayFabArena arena)
    {
        *target = default;
        target->ExpirationTimestamp = arena.Value(ExpirationTimestamp is null ? null : (long?)PlayFabTime.ToUnixTime(ExpirationTimestamp.Value));
        target->MarketplaceAlternateId = arena.String(MarketplaceAlternateId);
        target->MarketplaceTransactionId = arena.String(MarketplaceTransactionId);
        target->SuccessTimestamp = PlayFabTime.ToUnixTime(SuccessTimestamp);
    }
}

/// <summary>Projects <c>PFInventorySubtractInventoryItemsOperation</c>.</summary>
public sealed class InventorySubtractInventoryItemsOperation
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>DeleteEmptyStacks</c>.</summary>
    public bool DeleteEmptyStacks { get; set; }

    /// <summary><c>DurationInSeconds</c>.</summary>
    public double? DurationInSeconds { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    internal static unsafe InventorySubtractInventoryItemsOperation FromNative(PFInventorySubtractInventoryItemsOperation* source)
    {
        var value = new InventorySubtractInventoryItemsOperation();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.DeleteEmptyStacks = source->DeleteEmptyStacks != 0;
        value.DurationInSeconds = source->DurationInSeconds is null ? null : (double?)(*source->DurationInSeconds);
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFInventorySubtractInventoryItemsOperation* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->DeleteEmptyStacks = DeleteEmptyStacks ? (byte)1 : (byte)0;
        target->DurationInSeconds = arena.Value(DurationInSeconds);
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native3 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native3, arena);
            target->Item = native3;
        }
    }
}

/// <summary>Projects <c>PFInventorySubtractInventoryItemsRequest</c>.</summary>
public sealed class InventorySubtractInventoryItemsRequest
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DeleteEmptyStacks</c>.</summary>
    public bool DeleteEmptyStacks { get; set; }

    /// <summary><c>DurationInSeconds</c>.</summary>
    public double? DurationInSeconds { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItemReference? Item { get; set; }

    internal static unsafe InventorySubtractInventoryItemsRequest FromNative(PFInventorySubtractInventoryItemsRequest* source)
    {
        var value = new InventorySubtractInventoryItemsRequest();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.DeleteEmptyStacks = source->DeleteEmptyStacks != 0;
        value.DurationInSeconds = source->DurationInSeconds is null ? null : (double?)(*source->DurationInSeconds);
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.Item = source->Item is null ? null : InventoryInventoryItemReference.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFInventorySubtractInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        target->DeleteEmptyStacks = DeleteEmptyStacks ? (byte)1 : (byte)0;
        target->DurationInSeconds = arena.Value(DurationInSeconds);
        if (Entity is not null)
        {
            PFEntityKey* native6 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native6, arena);
            target->Entity = native6;
        }
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        if (Item is not null)
        {
            PFInventoryInventoryItemReference* native9 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            Item.WriteTo(native9, arena);
            target->Item = native9;
        }
    }
}

/// <summary>Projects <c>PFInventorySubtractInventoryItemsResponse</c>.</summary>
public sealed class InventorySubtractInventoryItemsResponse
{
    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventorySubtractInventoryItemsResponse FromNative(PFInventorySubtractInventoryItemsResponse* source)
    {
        var value = new InventorySubtractInventoryItemsResponse();
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list2 = new List<string>((int)source->TransactionIdsCount);
            for (uint i2 = 0; i2 < source->TransactionIdsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->TransactionIds[i2]) ?? string.Empty);
            }

            value.TransactionIds = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventorySubtractInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryTransaction</c>.</summary>
public sealed class InventoryTransaction
{
    /// <summary><c>ApiName</c>.</summary>
    public string? ApiName { get; set; }

    /// <summary><c>ClawbackDetails</c>.</summary>
    public InventoryTransactionClawbackDetails? ClawbackDetails { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ItemType</c>.</summary>
    public string? ItemType { get; set; }

    /// <summary><c>Operations</c>.</summary>
    public IReadOnlyList<InventoryTransactionOperation>? Operations { get; set; }

    /// <summary><c>OperationType</c>.</summary>
    public string? OperationType { get; set; }

    /// <summary><c>PurchaseDetails</c>.</summary>
    public InventoryTransactionPurchaseDetails? PurchaseDetails { get; set; }

    /// <summary><c>RedeemDetails</c>.</summary>
    public InventoryTransactionRedeemDetails? RedeemDetails { get; set; }

    /// <summary><c>Timestamp</c>.</summary>
    public DateTimeOffset Timestamp { get; set; }

    /// <summary><c>TransactionId</c>.</summary>
    public string? TransactionId { get; set; }

    /// <summary><c>TransferDetails</c>.</summary>
    public InventoryTransactionTransferDetails? TransferDetails { get; set; }

    internal static unsafe InventoryTransaction FromNative(PFInventoryTransaction* source)
    {
        var value = new InventoryTransaction();
        value.ApiName = Utf8.ToString(source->ApiName);
        value.ClawbackDetails = source->ClawbackDetails is null ? null : InventoryTransactionClawbackDetails.FromNative(source->ClawbackDetails);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.ItemType = Utf8.ToString(source->ItemType);
        if (source->Operations is not null && source->OperationsCount != 0)
        {
            var list5 = new List<InventoryTransactionOperation>((int)source->OperationsCount);
            for (uint i5 = 0; i5 < source->OperationsCount; i5++)
            {
                list5.Add(InventoryTransactionOperation.FromNative(source->Operations[i5]));
            }

            value.Operations = list5;
        }
        value.OperationType = Utf8.ToString(source->OperationType);
        value.PurchaseDetails = source->PurchaseDetails is null ? null : InventoryTransactionPurchaseDetails.FromNative(source->PurchaseDetails);
        value.RedeemDetails = source->RedeemDetails is null ? null : InventoryTransactionRedeemDetails.FromNative(source->RedeemDetails);
        value.Timestamp = PlayFabTime.ToDateTimeOffset(source->Timestamp);
        value.TransactionId = Utf8.ToString(source->TransactionId);
        value.TransferDetails = source->TransferDetails is null ? null : InventoryTransactionTransferDetails.FromNative(source->TransferDetails);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransaction* target, PlayFabArena arena)
    {
        *target = default;
        target->ApiName = arena.String(ApiName);
        if (ClawbackDetails is not null)
        {
            PFInventoryTransactionClawbackDetails* native1 = arena.Alloc<PFInventoryTransactionClawbackDetails>(1);
            ClawbackDetails.WriteTo(native1, arena);
            target->ClawbackDetails = native1;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        target->ItemType = arena.String(ItemType);
        target->OperationsCount = (uint)(Operations is null ? 0 : Operations.Count);
        if (Operations is not null && Operations.Count > 0)
        {
            PFInventoryTransactionOperation** array5 = arena.PointerArray<PFInventoryTransactionOperation>(Operations.Count);
            for (int i5 = 0; i5 < Operations.Count; i5++)
            {
                PFInventoryTransactionOperation* item5 = arena.Alloc<PFInventoryTransactionOperation>(1);
                Operations[i5].WriteTo(item5, arena);
                array5[i5] = item5;
            }

            target->Operations = array5;
        }
        target->OperationType = arena.String(OperationType);
        if (PurchaseDetails is not null)
        {
            PFInventoryTransactionPurchaseDetails* native8 = arena.Alloc<PFInventoryTransactionPurchaseDetails>(1);
            PurchaseDetails.WriteTo(native8, arena);
            target->PurchaseDetails = native8;
        }
        if (RedeemDetails is not null)
        {
            PFInventoryTransactionRedeemDetails* native9 = arena.Alloc<PFInventoryTransactionRedeemDetails>(1);
            RedeemDetails.WriteTo(native9, arena);
            target->RedeemDetails = native9;
        }
        target->Timestamp = PlayFabTime.ToUnixTime(Timestamp);
        target->TransactionId = arena.String(TransactionId);
        if (TransferDetails is not null)
        {
            PFInventoryTransactionTransferDetails* native12 = arena.Alloc<PFInventoryTransactionTransferDetails>(1);
            TransferDetails.WriteTo(native12, arena);
            target->TransferDetails = native12;
        }
    }
}

/// <summary>Projects <c>PFInventoryTransactionClawbackDetails</c>.</summary>
public sealed class InventoryTransactionClawbackDetails
{
    /// <summary><c>TransactionIdClawedback</c>.</summary>
    public string? TransactionIdClawedback { get; set; }

    internal static unsafe InventoryTransactionClawbackDetails FromNative(PFInventoryTransactionClawbackDetails* source)
    {
        var value = new InventoryTransactionClawbackDetails();
        value.TransactionIdClawedback = Utf8.ToString(source->TransactionIdClawedback);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransactionClawbackDetails* target, PlayFabArena arena)
    {
        *target = default;
        target->TransactionIdClawedback = arena.String(TransactionIdClawedback);
    }
}

/// <summary>Projects <c>PFInventoryTransactionOperation</c>.</summary>
public sealed class InventoryTransactionOperation
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>DurationInSeconds</c>.</summary>
    public double? DurationInSeconds { get; set; }

    /// <summary><c>ItemFriendlyId</c>.</summary>
    public string? ItemFriendlyId { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>ItemType</c>.</summary>
    public string? ItemType { get; set; }

    /// <summary><c>StackId</c>.</summary>
    public string? StackId { get; set; }

    /// <summary><c>Type</c>.</summary>
    public string? Type { get; set; }

    internal static unsafe InventoryTransactionOperation FromNative(PFInventoryTransactionOperation* source)
    {
        var value = new InventoryTransactionOperation();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.DurationInSeconds = source->DurationInSeconds is null ? null : (double?)(*source->DurationInSeconds);
        value.ItemFriendlyId = Utf8.ToString(source->ItemFriendlyId);
        value.ItemId = Utf8.ToString(source->ItemId);
        value.ItemType = Utf8.ToString(source->ItemType);
        value.StackId = Utf8.ToString(source->StackId);
        value.Type = Utf8.ToString(source->Type);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransactionOperation* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->DurationInSeconds = arena.Value(DurationInSeconds);
        target->ItemFriendlyId = arena.String(ItemFriendlyId);
        target->ItemId = arena.String(ItemId);
        target->ItemType = arena.String(ItemType);
        target->StackId = arena.String(StackId);
        target->Type = arena.String(Type);
    }
}

/// <summary>Projects <c>PFInventoryTransactionPurchaseDetails</c>.</summary>
public sealed class InventoryTransactionPurchaseDetails
{
    /// <summary><c>ItemFriendlyId</c>.</summary>
    public string? ItemFriendlyId { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>StoreFriendlyId</c>.</summary>
    public string? StoreFriendlyId { get; set; }

    /// <summary><c>StoreId</c>.</summary>
    public string? StoreId { get; set; }

    internal static unsafe InventoryTransactionPurchaseDetails FromNative(PFInventoryTransactionPurchaseDetails* source)
    {
        var value = new InventoryTransactionPurchaseDetails();
        value.ItemFriendlyId = Utf8.ToString(source->ItemFriendlyId);
        value.ItemId = Utf8.ToString(source->ItemId);
        value.StoreFriendlyId = Utf8.ToString(source->StoreFriendlyId);
        value.StoreId = Utf8.ToString(source->StoreId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransactionPurchaseDetails* target, PlayFabArena arena)
    {
        *target = default;
        target->ItemFriendlyId = arena.String(ItemFriendlyId);
        target->ItemId = arena.String(ItemId);
        target->StoreFriendlyId = arena.String(StoreFriendlyId);
        target->StoreId = arena.String(StoreId);
    }
}

/// <summary>Projects <c>PFInventoryTransactionRedeemDetails</c>.</summary>
public sealed class InventoryTransactionRedeemDetails
{
    /// <summary><c>Marketplace</c>.</summary>
    public string? Marketplace { get; set; }

    /// <summary><c>MarketplaceTransactionId</c>.</summary>
    public string? MarketplaceTransactionId { get; set; }

    /// <summary><c>OfferId</c>.</summary>
    public string? OfferId { get; set; }

    internal static unsafe InventoryTransactionRedeemDetails FromNative(PFInventoryTransactionRedeemDetails* source)
    {
        var value = new InventoryTransactionRedeemDetails();
        value.Marketplace = Utf8.ToString(source->Marketplace);
        value.MarketplaceTransactionId = Utf8.ToString(source->MarketplaceTransactionId);
        value.OfferId = Utf8.ToString(source->OfferId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransactionRedeemDetails* target, PlayFabArena arena)
    {
        *target = default;
        target->Marketplace = arena.String(Marketplace);
        target->MarketplaceTransactionId = arena.String(MarketplaceTransactionId);
        target->OfferId = arena.String(OfferId);
    }
}

/// <summary>Projects <c>PFInventoryTransactionTransferDetails</c>.</summary>
public sealed class InventoryTransactionTransferDetails
{
    /// <summary><c>GivingCollectionId</c>.</summary>
    public string? GivingCollectionId { get; set; }

    /// <summary><c>GivingEntity</c>.</summary>
    public EntityKey? GivingEntity { get; set; }

    /// <summary><c>ReceivingCollectionId</c>.</summary>
    public string? ReceivingCollectionId { get; set; }

    /// <summary><c>ReceivingEntity</c>.</summary>
    public EntityKey? ReceivingEntity { get; set; }

    /// <summary><c>TransferId</c>.</summary>
    public string? TransferId { get; set; }

    internal static unsafe InventoryTransactionTransferDetails FromNative(PFInventoryTransactionTransferDetails* source)
    {
        var value = new InventoryTransactionTransferDetails();
        value.GivingCollectionId = Utf8.ToString(source->GivingCollectionId);
        value.GivingEntity = source->GivingEntity is null ? null : EntityKey.FromNative(source->GivingEntity);
        value.ReceivingCollectionId = Utf8.ToString(source->ReceivingCollectionId);
        value.ReceivingEntity = source->ReceivingEntity is null ? null : EntityKey.FromNative(source->ReceivingEntity);
        value.TransferId = Utf8.ToString(source->TransferId);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransactionTransferDetails* target, PlayFabArena arena)
    {
        *target = default;
        target->GivingCollectionId = arena.String(GivingCollectionId);
        if (GivingEntity is not null)
        {
            PFEntityKey* native1 = arena.Alloc<PFEntityKey>(1);
            GivingEntity.WriteTo(native1, arena);
            target->GivingEntity = native1;
        }
        target->ReceivingCollectionId = arena.String(ReceivingCollectionId);
        if (ReceivingEntity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            ReceivingEntity.WriteTo(native3, arena);
            target->ReceivingEntity = native3;
        }
        target->TransferId = arena.String(TransferId);
    }
}

/// <summary>Projects <c>PFInventoryTransferInventoryItemsOperation</c>.</summary>
public sealed class InventoryTransferInventoryItemsOperation
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>DeleteEmptyStacks</c>.</summary>
    public bool DeleteEmptyStacks { get; set; }

    /// <summary><c>GivingItem</c>.</summary>
    public InventoryInventoryItemReference? GivingItem { get; set; }

    /// <summary><c>NewStackValues</c>.</summary>
    public InventoryInitialValues? NewStackValues { get; set; }

    /// <summary><c>ReceivingItem</c>.</summary>
    public InventoryInventoryItemReference? ReceivingItem { get; set; }

    internal static unsafe InventoryTransferInventoryItemsOperation FromNative(PFInventoryTransferInventoryItemsOperation* source)
    {
        var value = new InventoryTransferInventoryItemsOperation();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.DeleteEmptyStacks = source->DeleteEmptyStacks != 0;
        value.GivingItem = source->GivingItem is null ? null : InventoryInventoryItemReference.FromNative(source->GivingItem);
        value.NewStackValues = source->NewStackValues is null ? null : InventoryInitialValues.FromNative(source->NewStackValues);
        value.ReceivingItem = source->ReceivingItem is null ? null : InventoryInventoryItemReference.FromNative(source->ReceivingItem);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransferInventoryItemsOperation* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->DeleteEmptyStacks = DeleteEmptyStacks ? (byte)1 : (byte)0;
        if (GivingItem is not null)
        {
            PFInventoryInventoryItemReference* native2 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            GivingItem.WriteTo(native2, arena);
            target->GivingItem = native2;
        }
        if (NewStackValues is not null)
        {
            PFInventoryInitialValues* native3 = arena.Alloc<PFInventoryInitialValues>(1);
            NewStackValues.WriteTo(native3, arena);
            target->NewStackValues = native3;
        }
        if (ReceivingItem is not null)
        {
            PFInventoryInventoryItemReference* native4 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            ReceivingItem.WriteTo(native4, arena);
            target->ReceivingItem = native4;
        }
    }
}

/// <summary>Projects <c>PFInventoryTransferInventoryItemsRequest</c>.</summary>
public sealed class InventoryTransferInventoryItemsRequest
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DeleteEmptyStacks</c>.</summary>
    public bool DeleteEmptyStacks { get; set; }

    /// <summary><c>GivingCollectionId</c>.</summary>
    public string? GivingCollectionId { get; set; }

    /// <summary><c>GivingEntity</c>.</summary>
    public EntityKey? GivingEntity { get; set; }

    /// <summary><c>GivingETag</c>.</summary>
    public string? GivingETag { get; set; }

    /// <summary><c>GivingItem</c>.</summary>
    public InventoryInventoryItemReference? GivingItem { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>NewStackValues</c>.</summary>
    public InventoryInitialValues? NewStackValues { get; set; }

    /// <summary><c>ReceivingCollectionId</c>.</summary>
    public string? ReceivingCollectionId { get; set; }

    /// <summary><c>ReceivingEntity</c>.</summary>
    public EntityKey? ReceivingEntity { get; set; }

    /// <summary><c>ReceivingItem</c>.</summary>
    public InventoryInventoryItemReference? ReceivingItem { get; set; }

    internal static unsafe InventoryTransferInventoryItemsRequest FromNative(PFInventoryTransferInventoryItemsRequest* source)
    {
        var value = new InventoryTransferInventoryItemsRequest();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.DeleteEmptyStacks = source->DeleteEmptyStacks != 0;
        value.GivingCollectionId = Utf8.ToString(source->GivingCollectionId);
        value.GivingEntity = source->GivingEntity is null ? null : EntityKey.FromNative(source->GivingEntity);
        value.GivingETag = Utf8.ToString(source->GivingETag);
        value.GivingItem = source->GivingItem is null ? null : InventoryInventoryItemReference.FromNative(source->GivingItem);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.NewStackValues = source->NewStackValues is null ? null : InventoryInitialValues.FromNative(source->NewStackValues);
        value.ReceivingCollectionId = Utf8.ToString(source->ReceivingCollectionId);
        value.ReceivingEntity = source->ReceivingEntity is null ? null : EntityKey.FromNative(source->ReceivingEntity);
        value.ReceivingItem = source->ReceivingItem is null ? null : InventoryInventoryItemReference.FromNative(source->ReceivingItem);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransferInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->DeleteEmptyStacks = DeleteEmptyStacks ? (byte)1 : (byte)0;
        target->GivingCollectionId = arena.String(GivingCollectionId);
        if (GivingEntity is not null)
        {
            PFEntityKey* native5 = arena.Alloc<PFEntityKey>(1);
            GivingEntity.WriteTo(native5, arena);
            target->GivingEntity = native5;
        }
        target->GivingETag = arena.String(GivingETag);
        if (GivingItem is not null)
        {
            PFInventoryInventoryItemReference* native7 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            GivingItem.WriteTo(native7, arena);
            target->GivingItem = native7;
        }
        target->IdempotencyId = arena.String(IdempotencyId);
        if (NewStackValues is not null)
        {
            PFInventoryInitialValues* native9 = arena.Alloc<PFInventoryInitialValues>(1);
            NewStackValues.WriteTo(native9, arena);
            target->NewStackValues = native9;
        }
        target->ReceivingCollectionId = arena.String(ReceivingCollectionId);
        if (ReceivingEntity is not null)
        {
            PFEntityKey* native11 = arena.Alloc<PFEntityKey>(1);
            ReceivingEntity.WriteTo(native11, arena);
            target->ReceivingEntity = native11;
        }
        if (ReceivingItem is not null)
        {
            PFInventoryInventoryItemReference* native12 = arena.Alloc<PFInventoryInventoryItemReference>(1);
            ReceivingItem.WriteTo(native12, arena);
            target->ReceivingItem = native12;
        }
    }
}

/// <summary>Projects <c>PFInventoryTransferInventoryItemsResponse</c>.</summary>
public sealed class InventoryTransferInventoryItemsResponse
{
    /// <summary><c>GivingETag</c>.</summary>
    public string? GivingETag { get; set; }

    /// <summary><c>GivingTransactionIds</c>.</summary>
    public IReadOnlyList<string>? GivingTransactionIds { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>OperationStatus</c>.</summary>
    public string? OperationStatus { get; set; }

    /// <summary><c>OperationToken</c>.</summary>
    public string? OperationToken { get; set; }

    /// <summary><c>ReceivingTransactionIds</c>.</summary>
    public IReadOnlyList<string>? ReceivingTransactionIds { get; set; }

    internal static unsafe InventoryTransferInventoryItemsResponse FromNative(PFInventoryTransferInventoryItemsResponse* source)
    {
        var value = new InventoryTransferInventoryItemsResponse();
        value.GivingETag = Utf8.ToString(source->GivingETag);
        if (source->GivingTransactionIds is not null && source->GivingTransactionIdsCount != 0)
        {
            var list1 = new List<string>((int)source->GivingTransactionIdsCount);
            for (uint i1 = 0; i1 < source->GivingTransactionIdsCount; i1++)
            {
                list1.Add(Utf8.ToString(source->GivingTransactionIds[i1]) ?? string.Empty);
            }

            value.GivingTransactionIds = list1;
        }
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.OperationStatus = Utf8.ToString(source->OperationStatus);
        value.OperationToken = Utf8.ToString(source->OperationToken);
        if (source->ReceivingTransactionIds is not null && source->ReceivingTransactionIdsCount != 0)
        {
            var list6 = new List<string>((int)source->ReceivingTransactionIdsCount);
            for (uint i6 = 0; i6 < source->ReceivingTransactionIdsCount; i6++)
            {
                list6.Add(Utf8.ToString(source->ReceivingTransactionIds[i6]) ?? string.Empty);
            }

            value.ReceivingTransactionIds = list6;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryTransferInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->GivingETag = arena.String(GivingETag);
        target->GivingTransactionIdsCount = (uint)(GivingTransactionIds is null ? 0 : GivingTransactionIds.Count);
        target->GivingTransactionIds = arena.StringArray(GivingTransactionIds);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->OperationStatus = arena.String(OperationStatus);
        target->OperationToken = arena.String(OperationToken);
        target->ReceivingTransactionIdsCount = (uint)(ReceivingTransactionIds is null ? 0 : ReceivingTransactionIds.Count);
        target->ReceivingTransactionIds = arena.StringArray(ReceivingTransactionIds);
    }
}

/// <summary>Projects <c>PFInventoryUpdateInventoryItemsOperation</c>.</summary>
public sealed class InventoryUpdateInventoryItemsOperation
{
    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItem? Item { get; set; }

    internal static unsafe InventoryUpdateInventoryItemsOperation FromNative(PFInventoryUpdateInventoryItemsOperation* source)
    {
        var value = new InventoryUpdateInventoryItemsOperation();
        value.Item = source->Item is null ? null : InventoryInventoryItem.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryUpdateInventoryItemsOperation* target, PlayFabArena arena)
    {
        *target = default;
        if (Item is not null)
        {
            PFInventoryInventoryItem* native0 = arena.Alloc<PFInventoryInventoryItem>(1);
            Item.WriteTo(native0, arena);
            target->Item = native0;
        }
    }
}

/// <summary>Projects <c>PFInventoryUpdateInventoryItemsRequest</c>.</summary>
public sealed class InventoryUpdateInventoryItemsRequest
{
    /// <summary><c>CollectionId</c>.</summary>
    public string? CollectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>Item</c>.</summary>
    public InventoryInventoryItem? Item { get; set; }

    internal static unsafe InventoryUpdateInventoryItemsRequest FromNative(PFInventoryUpdateInventoryItemsRequest* source)
    {
        var value = new InventoryUpdateInventoryItemsRequest();
        value.CollectionId = Utf8.ToString(source->CollectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        value.Item = source->Item is null ? null : InventoryInventoryItem.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFInventoryUpdateInventoryItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CollectionId = arena.String(CollectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        if (Item is not null)
        {
            PFInventoryInventoryItem* native6 = arena.Alloc<PFInventoryInventoryItem>(1);
            Item.WriteTo(native6, arena);
            target->Item = native6;
        }
    }
}

/// <summary>Projects <c>PFInventoryUpdateInventoryItemsResponse</c>.</summary>
public sealed class InventoryUpdateInventoryItemsResponse
{
    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>IdempotencyId</c>.</summary>
    public string? IdempotencyId { get; set; }

    /// <summary><c>TransactionIds</c>.</summary>
    public IReadOnlyList<string>? TransactionIds { get; set; }

    internal static unsafe InventoryUpdateInventoryItemsResponse FromNative(PFInventoryUpdateInventoryItemsResponse* source)
    {
        var value = new InventoryUpdateInventoryItemsResponse();
        value.ETag = Utf8.ToString(source->ETag);
        value.IdempotencyId = Utf8.ToString(source->IdempotencyId);
        if (source->TransactionIds is not null && source->TransactionIdsCount != 0)
        {
            var list2 = new List<string>((int)source->TransactionIdsCount);
            for (uint i2 = 0; i2 < source->TransactionIdsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->TransactionIds[i2]) ?? string.Empty);
            }

            value.TransactionIds = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFInventoryUpdateInventoryItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ETag = arena.String(ETag);
        target->IdempotencyId = arena.String(IdempotencyId);
        target->TransactionIdsCount = (uint)(TransactionIds is null ? 0 : TransactionIds.Count);
        target->TransactionIds = arena.StringArray(TransactionIds);
    }
}
