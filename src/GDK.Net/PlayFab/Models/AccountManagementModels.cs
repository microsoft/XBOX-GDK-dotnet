// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFAccountManagement
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFAccountManagementUserFamilyType</c>.</summary>
public enum AccountManagementUserFamilyType : uint
{
    /// <summary>Projects <c>PFAccountManagementUserFamilyType::None</c>.</summary>
    None = 0,
    /// <summary>Projects <c>PFAccountManagementUserFamilyType::Xbox</c>.</summary>
    Xbox = 1,
    /// <summary>Projects <c>PFAccountManagementUserFamilyType::Steam</c>.</summary>
    Steam = 2,
}

/// <summary>Projects <c>PFAccountManagementAddOrUpdateContactEmailRequest</c>.</summary>
public sealed class AccountManagementAddOrUpdateContactEmailRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>EmailAddress</c>.</summary>
    public string? EmailAddress { get; set; }

    internal static unsafe AccountManagementAddOrUpdateContactEmailRequest FromNative(PFAccountManagementAddOrUpdateContactEmailRequest* source)
    {
        var value = new AccountManagementAddOrUpdateContactEmailRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.EmailAddress = Utf8.ToString(source->EmailAddress);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementAddOrUpdateContactEmailRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->EmailAddress = arena.String(EmailAddress);
    }
}

/// <summary>Projects <c>PFAccountManagementBanInfo</c>.</summary>
public sealed class AccountManagementBanInfo
{
    /// <summary><c>Active</c>.</summary>
    public bool Active { get; set; }

    /// <summary><c>BanId</c>.</summary>
    public string? BanId { get; set; }

    /// <summary><c>Created</c>.</summary>
    public DateTimeOffset? Created { get; set; }

    /// <summary><c>Expires</c>.</summary>
    public DateTimeOffset? Expires { get; set; }

    /// <summary><c>IPAddress</c>.</summary>
    public string? IPAddress { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>Reason</c>.</summary>
    public string? Reason { get; set; }

    /// <summary><c>UserFamilyType</c>.</summary>
    public string? UserFamilyType { get; set; }

    internal static unsafe AccountManagementBanInfo FromNative(PFAccountManagementBanInfo* source)
    {
        var value = new AccountManagementBanInfo();
        value.Active = source->Active != 0;
        value.BanId = Utf8.ToString(source->BanId);
        value.Created = source->Created is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->Created);
        value.Expires = source->Expires is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->Expires);
        value.IPAddress = Utf8.ToString(source->IPAddress);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.Reason = Utf8.ToString(source->Reason);
        value.UserFamilyType = Utf8.ToString(source->UserFamilyType);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementBanInfo* target, PlayFabArena arena)
    {
        *target = default;
        target->Active = Active ? (byte)1 : (byte)0;
        target->BanId = arena.String(BanId);
        target->Created = arena.Value(Created is null ? null : (long?)PlayFabTime.ToUnixTime(Created.Value));
        target->Expires = arena.Value(Expires is null ? null : (long?)PlayFabTime.ToUnixTime(Expires.Value));
        target->IPAddress = arena.String(IPAddress);
        target->PlayFabId = arena.String(PlayFabId);
        target->Reason = arena.String(Reason);
        target->UserFamilyType = arena.String(UserFamilyType);
    }
}

/// <summary>Projects <c>PFAccountManagementBanRequest</c>.</summary>
public sealed class AccountManagementBanRequest
{
    /// <summary><c>DurationInHours</c>.</summary>
    public uint? DurationInHours { get; set; }

    /// <summary><c>IPAddress</c>.</summary>
    public string? IPAddress { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>Reason</c>.</summary>
    public string? Reason { get; set; }

    /// <summary><c>UserFamilyType</c>.</summary>
    public AccountManagementUserFamilyType? UserFamilyType { get; set; }

    internal static unsafe AccountManagementBanRequest FromNative(PFAccountManagementBanRequest* source)
    {
        var value = new AccountManagementBanRequest();
        value.DurationInHours = source->DurationInHours is null ? null : (uint?)(*source->DurationInHours);
        value.IPAddress = Utf8.ToString(source->IPAddress);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.Reason = Utf8.ToString(source->Reason);
        value.UserFamilyType = source->UserFamilyType is null ? null : (AccountManagementUserFamilyType?)(*source->UserFamilyType);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementBanRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->DurationInHours = arena.Value(DurationInHours);
        target->IPAddress = arena.String(IPAddress);
        target->PlayFabId = arena.String(PlayFabId);
        target->Reason = arena.String(Reason);
        target->UserFamilyType = arena.Value((PFAccountManagementUserFamilyType?)UserFamilyType);
    }
}

/// <summary>Projects <c>PFAccountManagementBanUsersRequest</c>.</summary>
public sealed class AccountManagementBanUsersRequest
{
    /// <summary><c>Bans</c>.</summary>
    public IReadOnlyList<AccountManagementBanRequest>? Bans { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe AccountManagementBanUsersRequest FromNative(PFAccountManagementBanUsersRequest* source)
    {
        var value = new AccountManagementBanUsersRequest();
        if (source->Bans is not null && source->BansCount != 0)
        {
            var list0 = new List<AccountManagementBanRequest>((int)source->BansCount);
            for (uint i0 = 0; i0 < source->BansCount; i0++)
            {
                list0.Add(AccountManagementBanRequest.FromNative(source->Bans[i0]));
            }

            value.Bans = list0;
        }
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementBanUsersRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->BansCount = (uint)(Bans is null ? 0 : Bans.Count);
        if (Bans is not null && Bans.Count > 0)
        {
            PFAccountManagementBanRequest** array0 = arena.PointerArray<PFAccountManagementBanRequest>(Bans.Count);
            for (int i0 = 0; i0 < Bans.Count; i0++)
            {
                PFAccountManagementBanRequest* item0 = arena.Alloc<PFAccountManagementBanRequest>(1);
                Bans[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Bans = array0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementBanUsersResult</c>.</summary>
public sealed class AccountManagementBanUsersResult
{
    /// <summary><c>BanData</c>.</summary>
    public IReadOnlyList<AccountManagementBanInfo>? BanData { get; set; }

    internal static unsafe AccountManagementBanUsersResult FromNative(PFAccountManagementBanUsersResult* source)
    {
        var value = new AccountManagementBanUsersResult();
        if (source->BanData is not null && source->BanDataCount != 0)
        {
            var list0 = new List<AccountManagementBanInfo>((int)source->BanDataCount);
            for (uint i0 = 0; i0 < source->BanDataCount; i0++)
            {
                list0.Add(AccountManagementBanInfo.FromNative(source->BanData[i0]));
            }

            value.BanData = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementBanUsersResult* target, PlayFabArena arena)
    {
        *target = default;
        target->BanDataCount = (uint)(BanData is null ? 0 : BanData.Count);
        if (BanData is not null && BanData.Count > 0)
        {
            PFAccountManagementBanInfo** array0 = arena.PointerArray<PFAccountManagementBanInfo>(BanData.Count);
            for (int i0 = 0; i0 < BanData.Count; i0++)
            {
                PFAccountManagementBanInfo* item0 = arena.Alloc<PFAccountManagementBanInfo>(1);
                BanData[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->BanData = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementBattleNetAccountPlayFabIdPair</c>.</summary>
public sealed class AccountManagementBattleNetAccountPlayFabIdPair
{
    /// <summary><c>BattleNetAccountId</c>.</summary>
    public string? BattleNetAccountId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementBattleNetAccountPlayFabIdPair FromNative(PFAccountManagementBattleNetAccountPlayFabIdPair* source)
    {
        var value = new AccountManagementBattleNetAccountPlayFabIdPair();
        value.BattleNetAccountId = Utf8.ToString(source->BattleNetAccountId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementBattleNetAccountPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->BattleNetAccountId = arena.String(BattleNetAccountId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementClientLinkBattleNetAccountRequest</c>.</summary>
public sealed class AccountManagementClientLinkBattleNetAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>IdentityToken</c>.</summary>
    public string? IdentityToken { get; set; }

    internal static unsafe AccountManagementClientLinkBattleNetAccountRequest FromNative(PFAccountManagementClientLinkBattleNetAccountRequest* source)
    {
        var value = new AccountManagementClientLinkBattleNetAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.IdentityToken = Utf8.ToString(source->IdentityToken);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementClientLinkBattleNetAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->IdentityToken = arena.String(IdentityToken);
    }
}

/// <summary>Projects <c>PFAccountManagementClientLinkXboxAccountRequest</c>.</summary>
public sealed class AccountManagementClientLinkXboxAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary>The Xbox user this request is made on behalf of.</summary>
    public User? User { get; set; }

    internal static unsafe AccountManagementClientLinkXboxAccountRequest FromNative(PFAccountManagementClientLinkXboxAccountRequest* source)
    {
        var value = new AccountManagementClientLinkXboxAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementClientLinkXboxAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->User = User is null ? IntPtr.Zero : User.Handle;
    }
}

/// <summary>Projects <c>PFAccountManagementClientUnlinkBattleNetAccountRequest</c>.</summary>
public sealed class AccountManagementClientUnlinkBattleNetAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe AccountManagementClientUnlinkBattleNetAccountRequest FromNative(PFAccountManagementClientUnlinkBattleNetAccountRequest* source)
    {
        var value = new AccountManagementClientUnlinkBattleNetAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementClientUnlinkBattleNetAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementClientUnlinkXboxAccountRequest</c>.</summary>
public sealed class AccountManagementClientUnlinkXboxAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe AccountManagementClientUnlinkXboxAccountRequest FromNative(PFAccountManagementClientUnlinkXboxAccountRequest* source)
    {
        var value = new AccountManagementClientUnlinkXboxAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementClientUnlinkXboxAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementClientUpdateAvatarUrlRequest</c>.</summary>
public sealed class AccountManagementClientUpdateAvatarUrlRequest
{
    /// <summary><c>ImageUrl</c>.</summary>
    public string? ImageUrl { get; set; }

    internal static unsafe AccountManagementClientUpdateAvatarUrlRequest FromNative(PFAccountManagementClientUpdateAvatarUrlRequest* source)
    {
        var value = new AccountManagementClientUpdateAvatarUrlRequest();
        value.ImageUrl = Utf8.ToString(source->ImageUrl);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementClientUpdateAvatarUrlRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ImageUrl = arena.String(ImageUrl);
    }
}

/// <summary>Projects <c>PFAccountManagementDeletePlayerRequest</c>.</summary>
public sealed class AccountManagementDeletePlayerRequest
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementDeletePlayerRequest FromNative(PFAccountManagementDeletePlayerRequest* source)
    {
        var value = new AccountManagementDeletePlayerRequest();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementDeletePlayerRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementFacebookInstantGamesPlayFabIdPair</c>.</summary>
public sealed class AccountManagementFacebookInstantGamesPlayFabIdPair
{
    /// <summary><c>FacebookInstantGamesId</c>.</summary>
    public string? FacebookInstantGamesId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementFacebookInstantGamesPlayFabIdPair FromNative(PFAccountManagementFacebookInstantGamesPlayFabIdPair* source)
    {
        var value = new AccountManagementFacebookInstantGamesPlayFabIdPair();
        value.FacebookInstantGamesId = Utf8.ToString(source->FacebookInstantGamesId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementFacebookInstantGamesPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->FacebookInstantGamesId = arena.String(FacebookInstantGamesId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementFacebookPlayFabIdPair</c>.</summary>
public sealed class AccountManagementFacebookPlayFabIdPair
{
    /// <summary><c>FacebookId</c>.</summary>
    public string? FacebookId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementFacebookPlayFabIdPair FromNative(PFAccountManagementFacebookPlayFabIdPair* source)
    {
        var value = new AccountManagementFacebookPlayFabIdPair();
        value.FacebookId = Utf8.ToString(source->FacebookId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementFacebookPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->FacebookId = arena.String(FacebookId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementGameCenterPlayFabIdPair</c>.</summary>
public sealed class AccountManagementGameCenterPlayFabIdPair
{
    /// <summary><c>GameCenterId</c>.</summary>
    public string? GameCenterId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementGameCenterPlayFabIdPair FromNative(PFAccountManagementGameCenterPlayFabIdPair* source)
    {
        var value = new AccountManagementGameCenterPlayFabIdPair();
        value.GameCenterId = Utf8.ToString(source->GameCenterId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGameCenterPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->GameCenterId = arena.String(GameCenterId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementGetAccountInfoRequest</c>.</summary>
public sealed class AccountManagementGetAccountInfoRequest
{
    /// <summary><c>Email</c>.</summary>
    public string? Email { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>TitleDisplayName</c>.</summary>
    public string? TitleDisplayName { get; set; }

    /// <summary><c>Username</c>.</summary>
    public string? Username { get; set; }

    internal static unsafe AccountManagementGetAccountInfoRequest FromNative(PFAccountManagementGetAccountInfoRequest* source)
    {
        var value = new AccountManagementGetAccountInfoRequest();
        value.Email = Utf8.ToString(source->Email);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.TitleDisplayName = Utf8.ToString(source->TitleDisplayName);
        value.Username = Utf8.ToString(source->Username);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetAccountInfoRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Email = arena.String(Email);
        target->PlayFabId = arena.String(PlayFabId);
        target->TitleDisplayName = arena.String(TitleDisplayName);
        target->Username = arena.String(Username);
    }
}

/// <summary>Projects <c>PFAccountManagementGetAccountInfoResult</c>.</summary>
public sealed class AccountManagementGetAccountInfoResult
{
    /// <summary><c>AccountInfo</c>.</summary>
    public UserAccountInfo? AccountInfo { get; set; }

    internal static unsafe AccountManagementGetAccountInfoResult FromNative(PFAccountManagementGetAccountInfoResult* source)
    {
        var value = new AccountManagementGetAccountInfoResult();
        value.AccountInfo = source->AccountInfo is null ? null : UserAccountInfo.FromNative(source->AccountInfo);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetAccountInfoResult* target, PlayFabArena arena)
    {
        *target = default;
        if (AccountInfo is not null)
        {
            PFUserAccountInfo* native0 = arena.Alloc<PFUserAccountInfo>(1);
            AccountInfo.WriteTo(native0, arena);
            target->AccountInfo = native0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest
{
    /// <summary><c>BattleNetAccountIds</c>.</summary>
    public IReadOnlyList<string>? BattleNetAccountIds { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest FromNative(PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest();
        if (source->BattleNetAccountIds is not null && source->BattleNetAccountIdsCount != 0)
        {
            var list0 = new List<string>((int)source->BattleNetAccountIdsCount);
            for (uint i0 = 0; i0 < source->BattleNetAccountIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->BattleNetAccountIds[i0]) ?? string.Empty);
            }

            value.BattleNetAccountIds = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->BattleNetAccountIdsCount = (uint)(BattleNetAccountIds is null ? 0 : BattleNetAccountIds.Count);
        target->BattleNetAccountIds = arena.StringArray(BattleNetAccountIds);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementBattleNetAccountPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult FromNative(PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementBattleNetAccountPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementBattleNetAccountPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementBattleNetAccountPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementBattleNetAccountPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementBattleNetAccountPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementBattleNetAccountPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromFacebookIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromFacebookIDsRequest
{
    /// <summary><c>FacebookIDs</c>.</summary>
    public IReadOnlyList<string>? FacebookIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromFacebookIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromFacebookIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromFacebookIDsRequest();
        if (source->FacebookIDs is not null && source->FacebookIDsCount != 0)
        {
            var list0 = new List<string>((int)source->FacebookIDsCount);
            for (uint i0 = 0; i0 < source->FacebookIDsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->FacebookIDs[i0]) ?? string.Empty);
            }

            value.FacebookIDs = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromFacebookIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->FacebookIDsCount = (uint)(FacebookIDs is null ? 0 : FacebookIDs.Count);
        target->FacebookIDs = arena.StringArray(FacebookIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromFacebookIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromFacebookIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementFacebookPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromFacebookIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromFacebookIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromFacebookIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementFacebookPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementFacebookPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromFacebookIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementFacebookPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementFacebookPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementFacebookPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementFacebookPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest
{
    /// <summary><c>FacebookInstantGamesIds</c>.</summary>
    public IReadOnlyList<string>? FacebookInstantGamesIds { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest FromNative(PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest();
        if (source->FacebookInstantGamesIds is not null && source->FacebookInstantGamesIdsCount != 0)
        {
            var list0 = new List<string>((int)source->FacebookInstantGamesIdsCount);
            for (uint i0 = 0; i0 < source->FacebookInstantGamesIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->FacebookInstantGamesIds[i0]) ?? string.Empty);
            }

            value.FacebookInstantGamesIds = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->FacebookInstantGamesIdsCount = (uint)(FacebookInstantGamesIds is null ? 0 : FacebookInstantGamesIds.Count);
        target->FacebookInstantGamesIds = arena.StringArray(FacebookInstantGamesIds);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementFacebookInstantGamesPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult FromNative(PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementFacebookInstantGamesPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementFacebookInstantGamesPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementFacebookInstantGamesPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementFacebookInstantGamesPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementFacebookInstantGamesPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementFacebookInstantGamesPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromGameCenterIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromGameCenterIDsRequest
{
    /// <summary><c>GameCenterIDs</c>.</summary>
    public IReadOnlyList<string>? GameCenterIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromGameCenterIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromGameCenterIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromGameCenterIDsRequest();
        if (source->GameCenterIDs is not null && source->GameCenterIDsCount != 0)
        {
            var list0 = new List<string>((int)source->GameCenterIDsCount);
            for (uint i0 = 0; i0 < source->GameCenterIDsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->GameCenterIDs[i0]) ?? string.Empty);
            }

            value.GameCenterIDs = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromGameCenterIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->GameCenterIDsCount = (uint)(GameCenterIDs is null ? 0 : GameCenterIDs.Count);
        target->GameCenterIDs = arena.StringArray(GameCenterIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromGameCenterIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromGameCenterIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementGameCenterPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromGameCenterIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromGameCenterIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromGameCenterIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementGameCenterPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementGameCenterPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromGameCenterIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementGameCenterPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementGameCenterPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementGameCenterPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementGameCenterPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromGoogleIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromGoogleIDsRequest
{
    /// <summary><c>GoogleIDs</c>.</summary>
    public IReadOnlyList<string>? GoogleIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromGoogleIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromGoogleIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromGoogleIDsRequest();
        if (source->GoogleIDs is not null && source->GoogleIDsCount != 0)
        {
            var list0 = new List<string>((int)source->GoogleIDsCount);
            for (uint i0 = 0; i0 < source->GoogleIDsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->GoogleIDs[i0]) ?? string.Empty);
            }

            value.GoogleIDs = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromGoogleIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->GoogleIDsCount = (uint)(GoogleIDs is null ? 0 : GoogleIDs.Count);
        target->GoogleIDs = arena.StringArray(GoogleIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromGoogleIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromGoogleIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementGooglePlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromGoogleIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromGoogleIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromGoogleIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementGooglePlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementGooglePlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromGoogleIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementGooglePlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementGooglePlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementGooglePlayFabIdPair* item0 = arena.Alloc<PFAccountManagementGooglePlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest
{
    /// <summary><c>GooglePlayGamesPlayerIDs</c>.</summary>
    public IReadOnlyList<string>? GooglePlayGamesPlayerIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest();
        if (source->GooglePlayGamesPlayerIDs is not null && source->GooglePlayGamesPlayerIDsCount != 0)
        {
            var list0 = new List<string>((int)source->GooglePlayGamesPlayerIDsCount);
            for (uint i0 = 0; i0 < source->GooglePlayGamesPlayerIDsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->GooglePlayGamesPlayerIDs[i0]) ?? string.Empty);
            }

            value.GooglePlayGamesPlayerIDs = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->GooglePlayGamesPlayerIDsCount = (uint)(GooglePlayGamesPlayerIDs is null ? 0 : GooglePlayGamesPlayerIDs.Count);
        target->GooglePlayGamesPlayerIDs = arena.StringArray(GooglePlayGamesPlayerIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementGooglePlayGamesPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementGooglePlayGamesPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementGooglePlayGamesPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementGooglePlayGamesPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementGooglePlayGamesPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementGooglePlayGamesPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementGooglePlayGamesPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromKongregateIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromKongregateIDsRequest
{
    /// <summary><c>KongregateIDs</c>.</summary>
    public IReadOnlyList<string>? KongregateIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromKongregateIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromKongregateIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromKongregateIDsRequest();
        if (source->KongregateIDs is not null && source->KongregateIDsCount != 0)
        {
            var list0 = new List<string>((int)source->KongregateIDsCount);
            for (uint i0 = 0; i0 < source->KongregateIDsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->KongregateIDs[i0]) ?? string.Empty);
            }

            value.KongregateIDs = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromKongregateIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->KongregateIDsCount = (uint)(KongregateIDs is null ? 0 : KongregateIDs.Count);
        target->KongregateIDs = arena.StringArray(KongregateIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromKongregateIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromKongregateIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementKongregatePlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromKongregateIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromKongregateIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromKongregateIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementKongregatePlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementKongregatePlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromKongregateIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementKongregatePlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementKongregatePlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementKongregatePlayFabIdPair* item0 = arena.Alloc<PFAccountManagementKongregatePlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest
{
    /// <summary><c>NintendoAccountIds</c>.</summary>
    public IReadOnlyList<string>? NintendoAccountIds { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest FromNative(PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest();
        if (source->NintendoAccountIds is not null && source->NintendoAccountIdsCount != 0)
        {
            var list0 = new List<string>((int)source->NintendoAccountIdsCount);
            for (uint i0 = 0; i0 < source->NintendoAccountIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->NintendoAccountIds[i0]) ?? string.Empty);
            }

            value.NintendoAccountIds = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->NintendoAccountIdsCount = (uint)(NintendoAccountIds is null ? 0 : NintendoAccountIds.Count);
        target->NintendoAccountIds = arena.StringArray(NintendoAccountIds);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementNintendoServiceAccountPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult FromNative(PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementNintendoServiceAccountPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementNintendoServiceAccountPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementNintendoServiceAccountPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementNintendoServiceAccountPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementNintendoServiceAccountPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementNintendoServiceAccountPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest
{
    /// <summary><c>NintendoSwitchDeviceIds</c>.</summary>
    public IReadOnlyList<string>? NintendoSwitchDeviceIds { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest FromNative(PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest();
        if (source->NintendoSwitchDeviceIds is not null && source->NintendoSwitchDeviceIdsCount != 0)
        {
            var list0 = new List<string>((int)source->NintendoSwitchDeviceIdsCount);
            for (uint i0 = 0; i0 < source->NintendoSwitchDeviceIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->NintendoSwitchDeviceIds[i0]) ?? string.Empty);
            }

            value.NintendoSwitchDeviceIds = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->NintendoSwitchDeviceIdsCount = (uint)(NintendoSwitchDeviceIds is null ? 0 : NintendoSwitchDeviceIds.Count);
        target->NintendoSwitchDeviceIds = arena.StringArray(NintendoSwitchDeviceIds);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementNintendoSwitchPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult FromNative(PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementNintendoSwitchPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementNintendoSwitchPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementNintendoSwitchPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementNintendoSwitchPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementNintendoSwitchPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementNintendoSwitchPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromPSNAccountIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromPSNAccountIDsRequest
{
    /// <summary><c>IssuerId</c>.</summary>
    public int? IssuerId { get; set; }

    /// <summary><c>PSNAccountIDs</c>.</summary>
    public IReadOnlyList<string>? PSNAccountIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromPSNAccountIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromPSNAccountIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromPSNAccountIDsRequest();
        value.IssuerId = source->IssuerId is null ? null : (int?)(*source->IssuerId);
        if (source->PSNAccountIDs is not null && source->PSNAccountIDsCount != 0)
        {
            var list1 = new List<string>((int)source->PSNAccountIDsCount);
            for (uint i1 = 0; i1 < source->PSNAccountIDsCount; i1++)
            {
                list1.Add(Utf8.ToString(source->PSNAccountIDs[i1]) ?? string.Empty);
            }

            value.PSNAccountIDs = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromPSNAccountIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->IssuerId = arena.Value(IssuerId);
        target->PSNAccountIDsCount = (uint)(PSNAccountIDs is null ? 0 : PSNAccountIDs.Count);
        target->PSNAccountIDs = arena.StringArray(PSNAccountIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromPSNAccountIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromPSNAccountIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementPSNAccountPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromPSNAccountIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromPSNAccountIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromPSNAccountIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementPSNAccountPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementPSNAccountPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromPSNAccountIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementPSNAccountPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementPSNAccountPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementPSNAccountPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementPSNAccountPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest
{
    /// <summary><c>IssuerId</c>.</summary>
    public int? IssuerId { get; set; }

    /// <summary><c>PSNOnlineIDs</c>.</summary>
    public IReadOnlyList<string>? PSNOnlineIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest();
        value.IssuerId = source->IssuerId is null ? null : (int?)(*source->IssuerId);
        if (source->PSNOnlineIDs is not null && source->PSNOnlineIDsCount != 0)
        {
            var list1 = new List<string>((int)source->PSNOnlineIDsCount);
            for (uint i1 = 0; i1 < source->PSNOnlineIDsCount; i1++)
            {
                list1.Add(Utf8.ToString(source->PSNOnlineIDs[i1]) ?? string.Empty);
            }

            value.PSNOnlineIDs = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->IssuerId = arena.Value(IssuerId);
        target->PSNOnlineIDsCount = (uint)(PSNOnlineIDs is null ? 0 : PSNOnlineIDs.Count);
        target->PSNOnlineIDs = arena.StringArray(PSNOnlineIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromPSNOnlineIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementPSNOnlinePlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromPSNOnlineIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromPSNOnlineIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementPSNOnlinePlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementPSNOnlinePlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementPSNOnlinePlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementPSNOnlinePlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementPSNOnlinePlayFabIdPair* item0 = arena.Alloc<PFAccountManagementPSNOnlinePlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromSteamIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromSteamIDsRequest
{
    /// <summary><c>SteamStringIDs</c>.</summary>
    public IReadOnlyList<string>? SteamStringIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromSteamIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromSteamIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromSteamIDsRequest();
        if (source->SteamStringIDs is not null && source->SteamStringIDsCount != 0)
        {
            var list0 = new List<string>((int)source->SteamStringIDsCount);
            for (uint i0 = 0; i0 < source->SteamStringIDsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->SteamStringIDs[i0]) ?? string.Empty);
            }

            value.SteamStringIDs = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromSteamIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->SteamStringIDsCount = (uint)(SteamStringIDs is null ? 0 : SteamStringIDs.Count);
        target->SteamStringIDs = arena.StringArray(SteamStringIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromSteamIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromSteamIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementSteamPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromSteamIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromSteamIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromSteamIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementSteamPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementSteamPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromSteamIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementSteamPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementSteamPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementSteamPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementSteamPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromSteamNamesRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromSteamNamesRequest
{
    /// <summary><c>SteamNames</c>.</summary>
    public IReadOnlyList<string>? SteamNames { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromSteamNamesRequest FromNative(PFAccountManagementGetPlayFabIDsFromSteamNamesRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromSteamNamesRequest();
        if (source->SteamNames is not null && source->SteamNamesCount != 0)
        {
            var list0 = new List<string>((int)source->SteamNamesCount);
            for (uint i0 = 0; i0 < source->SteamNamesCount; i0++)
            {
                list0.Add(Utf8.ToString(source->SteamNames[i0]) ?? string.Empty);
            }

            value.SteamNames = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromSteamNamesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->SteamNamesCount = (uint)(SteamNames is null ? 0 : SteamNames.Count);
        target->SteamNames = arena.StringArray(SteamNames);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromSteamNamesResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromSteamNamesResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementSteamNamePlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromSteamNamesResult FromNative(PFAccountManagementGetPlayFabIDsFromSteamNamesResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromSteamNamesResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementSteamNamePlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementSteamNamePlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromSteamNamesResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementSteamNamePlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementSteamNamePlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementSteamNamePlayFabIdPair* item0 = arena.Alloc<PFAccountManagementSteamNamePlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromTwitchIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromTwitchIDsRequest
{
    /// <summary><c>TwitchIds</c>.</summary>
    public IReadOnlyList<string>? TwitchIds { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromTwitchIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromTwitchIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromTwitchIDsRequest();
        if (source->TwitchIds is not null && source->TwitchIdsCount != 0)
        {
            var list0 = new List<string>((int)source->TwitchIdsCount);
            for (uint i0 = 0; i0 < source->TwitchIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->TwitchIds[i0]) ?? string.Empty);
            }

            value.TwitchIds = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromTwitchIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->TwitchIdsCount = (uint)(TwitchIds is null ? 0 : TwitchIds.Count);
        target->TwitchIds = arena.StringArray(TwitchIds);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromTwitchIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromTwitchIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementTwitchPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromTwitchIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromTwitchIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromTwitchIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementTwitchPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementTwitchPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromTwitchIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementTwitchPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementTwitchPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementTwitchPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementTwitchPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromXboxLiveIDsRequest</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromXboxLiveIDsRequest
{
    /// <summary><c>Sandbox</c>.</summary>
    public string? Sandbox { get; set; }

    /// <summary><c>XboxLiveAccountIDs</c>.</summary>
    public IReadOnlyList<string>? XboxLiveAccountIDs { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromXboxLiveIDsRequest FromNative(PFAccountManagementGetPlayFabIDsFromXboxLiveIDsRequest* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromXboxLiveIDsRequest();
        value.Sandbox = Utf8.ToString(source->Sandbox);
        if (source->XboxLiveAccountIDs is not null && source->XboxLiveAccountIDsCount != 0)
        {
            var list1 = new List<string>((int)source->XboxLiveAccountIDsCount);
            for (uint i1 = 0; i1 < source->XboxLiveAccountIDsCount; i1++)
            {
                list1.Add(Utf8.ToString(source->XboxLiveAccountIDs[i1]) ?? string.Empty);
            }

            value.XboxLiveAccountIDs = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromXboxLiveIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Sandbox = arena.String(Sandbox);
        target->XboxLiveAccountIDsCount = (uint)(XboxLiveAccountIDs is null ? 0 : XboxLiveAccountIDs.Count);
        target->XboxLiveAccountIDs = arena.StringArray(XboxLiveAccountIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayFabIDsFromXboxLiveIDsResult</c>.</summary>
public sealed class AccountManagementGetPlayFabIDsFromXboxLiveIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementXboxLiveAccountPlayFabIdPair>? Data { get; set; }

    internal static unsafe AccountManagementGetPlayFabIDsFromXboxLiveIDsResult FromNative(PFAccountManagementGetPlayFabIDsFromXboxLiveIDsResult* source)
    {
        var value = new AccountManagementGetPlayFabIDsFromXboxLiveIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementXboxLiveAccountPlayFabIdPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementXboxLiveAccountPlayFabIdPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayFabIDsFromXboxLiveIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementXboxLiveAccountPlayFabIdPair** array0 = arena.PointerArray<PFAccountManagementXboxLiveAccountPlayFabIdPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementXboxLiveAccountPlayFabIdPair* item0 = arena.Alloc<PFAccountManagementXboxLiveAccountPlayFabIdPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayerCombinedInfoRequest</c>.</summary>
public sealed class AccountManagementGetPlayerCombinedInfoRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>InfoRequestParameters</c>.</summary>
    public GetPlayerCombinedInfoRequestParams? InfoRequestParameters { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementGetPlayerCombinedInfoRequest FromNative(PFAccountManagementGetPlayerCombinedInfoRequest* source)
    {
        var value = new AccountManagementGetPlayerCombinedInfoRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.InfoRequestParameters = source->InfoRequestParameters is null ? null : GetPlayerCombinedInfoRequestParams.FromNative(source->InfoRequestParameters);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayerCombinedInfoRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (InfoRequestParameters is not null)
        {
            PFGetPlayerCombinedInfoRequestParams* native2 = arena.Alloc<PFGetPlayerCombinedInfoRequestParams>(1);
            InfoRequestParameters.WriteTo(native2, arena);
            target->InfoRequestParameters = native2;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayerCombinedInfoResult</c>.</summary>
public sealed class AccountManagementGetPlayerCombinedInfoResult
{
    /// <summary><c>InfoResultPayload</c>.</summary>
    public GetPlayerCombinedInfoResultPayload? InfoResultPayload { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementGetPlayerCombinedInfoResult FromNative(PFAccountManagementGetPlayerCombinedInfoResult* source)
    {
        var value = new AccountManagementGetPlayerCombinedInfoResult();
        value.InfoResultPayload = source->InfoResultPayload is null ? null : GetPlayerCombinedInfoResultPayload.FromNative(source->InfoResultPayload);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayerCombinedInfoResult* target, PlayFabArena arena)
    {
        *target = default;
        if (InfoResultPayload is not null)
        {
            PFGetPlayerCombinedInfoResultPayload* native0 = arena.Alloc<PFGetPlayerCombinedInfoResultPayload>(1);
            InfoResultPayload.WriteTo(native0, arena);
            target->InfoResultPayload = native0;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayerProfileRequest</c>.</summary>
public sealed class AccountManagementGetPlayerProfileRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>ProfileConstraints</c>.</summary>
    public PlayerProfileViewConstraints? ProfileConstraints { get; set; }

    internal static unsafe AccountManagementGetPlayerProfileRequest FromNative(PFAccountManagementGetPlayerProfileRequest* source)
    {
        var value = new AccountManagementGetPlayerProfileRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.ProfileConstraints = source->ProfileConstraints is null ? null : PlayerProfileViewConstraints.FromNative(source->ProfileConstraints);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayerProfileRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
        if (ProfileConstraints is not null)
        {
            PFPlayerProfileViewConstraints* native3 = arena.Alloc<PFPlayerProfileViewConstraints>(1);
            ProfileConstraints.WriteTo(native3, arena);
            target->ProfileConstraints = native3;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetPlayerProfileResult</c>.</summary>
public sealed class AccountManagementGetPlayerProfileResult
{
    /// <summary><c>PlayerProfile</c>.</summary>
    public PlayerProfileModel? PlayerProfile { get; set; }

    internal static unsafe AccountManagementGetPlayerProfileResult FromNative(PFAccountManagementGetPlayerProfileResult* source)
    {
        var value = new AccountManagementGetPlayerProfileResult();
        value.PlayerProfile = source->PlayerProfile is null ? null : PlayerProfileModel.FromNative(source->PlayerProfile);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetPlayerProfileResult* target, PlayFabArena arena)
    {
        *target = default;
        if (PlayerProfile is not null)
        {
            PFPlayerProfileModel* native0 = arena.Alloc<PFPlayerProfileModel>(1);
            PlayerProfile.WriteTo(native0, arena);
            target->PlayerProfile = native0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetServerCustomIDsFromPlayFabIDsRequest</c>.</summary>
public sealed class AccountManagementGetServerCustomIDsFromPlayFabIDsRequest
{
    /// <summary><c>PlayFabIDs</c>.</summary>
    public IReadOnlyList<string>? PlayFabIDs { get; set; }

    internal static unsafe AccountManagementGetServerCustomIDsFromPlayFabIDsRequest FromNative(PFAccountManagementGetServerCustomIDsFromPlayFabIDsRequest* source)
    {
        var value = new AccountManagementGetServerCustomIDsFromPlayFabIDsRequest();
        if (source->PlayFabIDs is not null && source->PlayFabIDsCount != 0)
        {
            var list0 = new List<string>((int)source->PlayFabIDsCount);
            for (uint i0 = 0; i0 < source->PlayFabIDsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->PlayFabIDs[i0]) ?? string.Empty);
            }

            value.PlayFabIDs = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetServerCustomIDsFromPlayFabIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabIDsCount = (uint)(PlayFabIDs is null ? 0 : PlayFabIDs.Count);
        target->PlayFabIDs = arena.StringArray(PlayFabIDs);
    }
}

/// <summary>Projects <c>PFAccountManagementGetServerCustomIDsFromPlayFabIDsResult</c>.</summary>
public sealed class AccountManagementGetServerCustomIDsFromPlayFabIDsResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyList<AccountManagementServerCustomIDPlayFabIDPair>? Data { get; set; }

    internal static unsafe AccountManagementGetServerCustomIDsFromPlayFabIDsResult FromNative(PFAccountManagementGetServerCustomIDsFromPlayFabIDsResult* source)
    {
        var value = new AccountManagementGetServerCustomIDsFromPlayFabIDsResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var list0 = new List<AccountManagementServerCustomIDPlayFabIDPair>((int)source->DataCount);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                list0.Add(AccountManagementServerCustomIDPlayFabIDPair.FromNative(source->Data[i0]));
            }

            value.Data = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetServerCustomIDsFromPlayFabIDsResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFAccountManagementServerCustomIDPlayFabIDPair** array0 = arena.PointerArray<PFAccountManagementServerCustomIDPlayFabIDPair>(Data.Count);
            for (int i0 = 0; i0 < Data.Count; i0++)
            {
                PFAccountManagementServerCustomIDPlayFabIDPair* item0 = arena.Alloc<PFAccountManagementServerCustomIDPlayFabIDPair>(1);
                Data[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Data = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetTitlePlayersFromProviderIDsResponse</c>.</summary>
public sealed class AccountManagementGetTitlePlayersFromProviderIDsResponse
{
    /// <summary><c>TitlePlayerAccounts</c>.</summary>
    public IReadOnlyDictionary<string, EntityLineage>? TitlePlayerAccounts { get; set; }

    internal static unsafe AccountManagementGetTitlePlayersFromProviderIDsResponse FromNative(PFAccountManagementGetTitlePlayersFromProviderIDsResponse* source)
    {
        var value = new AccountManagementGetTitlePlayersFromProviderIDsResponse();
        if (source->TitlePlayerAccounts is not null && source->TitlePlayerAccountsCount != 0)
        {
            var map0 = new Dictionary<string, EntityLineage>((int)source->TitlePlayerAccountsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->TitlePlayerAccountsCount; i0++)
            {
                map0[Utf8.ToString(source->TitlePlayerAccounts[i0].Key) ?? string.Empty] = EntityLineage.FromNative(source->TitlePlayerAccounts[i0].Value);
            }

            value.TitlePlayerAccounts = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetTitlePlayersFromProviderIDsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->TitlePlayerAccountsCount = (uint)(TitlePlayerAccounts is null ? 0 : TitlePlayerAccounts.Count);
        if (TitlePlayerAccounts is not null && TitlePlayerAccounts.Count > 0)
        {
            PFEntityLineageDictionaryEntry* entries0 = arena.Alloc<PFEntityLineageDictionaryEntry>(TitlePlayerAccounts.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, EntityLineage> pair0 in TitlePlayerAccounts)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                if (pair0.Value is not null)
                {
                    PFEntityLineage* item0 = arena.Alloc<PFEntityLineage>(1);
                    pair0.Value.WriteTo(item0, arena);
                    entries0[next0].Value = item0;
                }
                next0++;
            }

            target->TitlePlayerAccounts = entries0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetTitlePlayersFromXboxLiveIDsRequest</c>.</summary>
public sealed class AccountManagementGetTitlePlayersFromXboxLiveIDsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Sandbox</c>.</summary>
    public string? Sandbox { get; set; }

    /// <summary><c>TitleId</c>.</summary>
    public string? TitleId { get; set; }

    /// <summary><c>XboxLiveIds</c>.</summary>
    public IReadOnlyList<string>? XboxLiveIds { get; set; }

    internal static unsafe AccountManagementGetTitlePlayersFromXboxLiveIDsRequest FromNative(PFAccountManagementGetTitlePlayersFromXboxLiveIDsRequest* source)
    {
        var value = new AccountManagementGetTitlePlayersFromXboxLiveIDsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Sandbox = Utf8.ToString(source->Sandbox);
        value.TitleId = Utf8.ToString(source->TitleId);
        if (source->XboxLiveIds is not null && source->XboxLiveIdsCount != 0)
        {
            var list4 = new List<string>((int)source->XboxLiveIdsCount);
            for (uint i4 = 0; i4 < source->XboxLiveIdsCount; i4++)
            {
                list4.Add(Utf8.ToString(source->XboxLiveIds[i4]) ?? string.Empty);
            }

            value.XboxLiveIds = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetTitlePlayersFromXboxLiveIDsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->Sandbox = arena.String(Sandbox);
        target->TitleId = arena.String(TitleId);
        target->XboxLiveIdsCount = (uint)(XboxLiveIds is null ? 0 : XboxLiveIds.Count);
        target->XboxLiveIds = arena.StringArray(XboxLiveIds);
    }
}

/// <summary>Projects <c>PFAccountManagementGetUserAccountInfoRequest</c>.</summary>
public sealed class AccountManagementGetUserAccountInfoRequest
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementGetUserAccountInfoRequest FromNative(PFAccountManagementGetUserAccountInfoRequest* source)
    {
        var value = new AccountManagementGetUserAccountInfoRequest();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetUserAccountInfoRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementGetUserAccountInfoResult</c>.</summary>
public sealed class AccountManagementGetUserAccountInfoResult
{
    /// <summary><c>UserInfo</c>.</summary>
    public UserAccountInfo? UserInfo { get; set; }

    internal static unsafe AccountManagementGetUserAccountInfoResult FromNative(PFAccountManagementGetUserAccountInfoResult* source)
    {
        var value = new AccountManagementGetUserAccountInfoResult();
        value.UserInfo = source->UserInfo is null ? null : UserAccountInfo.FromNative(source->UserInfo);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetUserAccountInfoResult* target, PlayFabArena arena)
    {
        *target = default;
        if (UserInfo is not null)
        {
            PFUserAccountInfo* native0 = arena.Alloc<PFUserAccountInfo>(1);
            UserInfo.WriteTo(native0, arena);
            target->UserInfo = native0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGetUserBansRequest</c>.</summary>
public sealed class AccountManagementGetUserBansRequest
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementGetUserBansRequest FromNative(PFAccountManagementGetUserBansRequest* source)
    {
        var value = new AccountManagementGetUserBansRequest();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetUserBansRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementGetUserBansResult</c>.</summary>
public sealed class AccountManagementGetUserBansResult
{
    /// <summary><c>BanData</c>.</summary>
    public IReadOnlyList<AccountManagementBanInfo>? BanData { get; set; }

    internal static unsafe AccountManagementGetUserBansResult FromNative(PFAccountManagementGetUserBansResult* source)
    {
        var value = new AccountManagementGetUserBansResult();
        if (source->BanData is not null && source->BanDataCount != 0)
        {
            var list0 = new List<AccountManagementBanInfo>((int)source->BanDataCount);
            for (uint i0 = 0; i0 < source->BanDataCount; i0++)
            {
                list0.Add(AccountManagementBanInfo.FromNative(source->BanData[i0]));
            }

            value.BanData = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGetUserBansResult* target, PlayFabArena arena)
    {
        *target = default;
        target->BanDataCount = (uint)(BanData is null ? 0 : BanData.Count);
        if (BanData is not null && BanData.Count > 0)
        {
            PFAccountManagementBanInfo** array0 = arena.PointerArray<PFAccountManagementBanInfo>(BanData.Count);
            for (int i0 = 0; i0 < BanData.Count; i0++)
            {
                PFAccountManagementBanInfo* item0 = arena.Alloc<PFAccountManagementBanInfo>(1);
                BanData[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->BanData = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementGooglePlayFabIdPair</c>.</summary>
public sealed class AccountManagementGooglePlayFabIdPair
{
    /// <summary><c>GoogleId</c>.</summary>
    public string? GoogleId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementGooglePlayFabIdPair FromNative(PFAccountManagementGooglePlayFabIdPair* source)
    {
        var value = new AccountManagementGooglePlayFabIdPair();
        value.GoogleId = Utf8.ToString(source->GoogleId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGooglePlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->GoogleId = arena.String(GoogleId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementGooglePlayGamesPlayFabIdPair</c>.</summary>
public sealed class AccountManagementGooglePlayGamesPlayFabIdPair
{
    /// <summary><c>GooglePlayGamesPlayerId</c>.</summary>
    public string? GooglePlayGamesPlayerId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementGooglePlayGamesPlayFabIdPair FromNative(PFAccountManagementGooglePlayGamesPlayFabIdPair* source)
    {
        var value = new AccountManagementGooglePlayGamesPlayFabIdPair();
        value.GooglePlayGamesPlayerId = Utf8.ToString(source->GooglePlayGamesPlayerId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementGooglePlayGamesPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->GooglePlayGamesPlayerId = arena.String(GooglePlayGamesPlayerId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementKongregatePlayFabIdPair</c>.</summary>
public sealed class AccountManagementKongregatePlayFabIdPair
{
    /// <summary><c>KongregateId</c>.</summary>
    public string? KongregateId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementKongregatePlayFabIdPair FromNative(PFAccountManagementKongregatePlayFabIdPair* source)
    {
        var value = new AccountManagementKongregatePlayFabIdPair();
        value.KongregateId = Utf8.ToString(source->KongregateId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementKongregatePlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->KongregateId = arena.String(KongregateId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementLinkCustomIDRequest</c>.</summary>
public sealed class AccountManagementLinkCustomIDRequest
{
    /// <summary><c>CustomId</c>.</summary>
    public string? CustomId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    internal static unsafe AccountManagementLinkCustomIDRequest FromNative(PFAccountManagementLinkCustomIDRequest* source)
    {
        var value = new AccountManagementLinkCustomIDRequest();
        value.CustomId = Utf8.ToString(source->CustomId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementLinkCustomIDRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomId = arena.String(CustomId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
    }
}

/// <summary>Projects <c>PFAccountManagementLinkNintendoServiceAccountSubjectRequest</c>.</summary>
public sealed class AccountManagementLinkNintendoServiceAccountSubjectRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>Subject</c>.</summary>
    public string? Subject { get; set; }

    internal static unsafe AccountManagementLinkNintendoServiceAccountSubjectRequest FromNative(PFAccountManagementLinkNintendoServiceAccountSubjectRequest* source)
    {
        var value = new AccountManagementLinkNintendoServiceAccountSubjectRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.Subject = Utf8.ToString(source->Subject);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementLinkNintendoServiceAccountSubjectRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->PlayFabId = arena.String(PlayFabId);
        target->Subject = arena.String(Subject);
    }
}

/// <summary>Projects <c>PFAccountManagementLinkOpenIdConnectRequest</c>.</summary>
public sealed class AccountManagementLinkOpenIdConnectRequest
{
    /// <summary><c>ConnectionId</c>.</summary>
    public string? ConnectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>IdToken</c>.</summary>
    public string? IdToken { get; set; }

    internal static unsafe AccountManagementLinkOpenIdConnectRequest FromNative(PFAccountManagementLinkOpenIdConnectRequest* source)
    {
        var value = new AccountManagementLinkOpenIdConnectRequest();
        value.ConnectionId = Utf8.ToString(source->ConnectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.IdToken = Utf8.ToString(source->IdToken);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementLinkOpenIdConnectRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ConnectionId = arena.String(ConnectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->IdToken = arena.String(IdToken);
    }
}

/// <summary>Projects <c>PFAccountManagementLinkPSNIdRequest</c>.</summary>
public sealed class AccountManagementLinkPSNIdRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>IssuerId</c>.</summary>
    public int? IssuerId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PSNUserId</c>.</summary>
    public string? PSNUserId { get; set; }

    internal static unsafe AccountManagementLinkPSNIdRequest FromNative(PFAccountManagementLinkPSNIdRequest* source)
    {
        var value = new AccountManagementLinkPSNIdRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.IssuerId = source->IssuerId is null ? null : (int?)(*source->IssuerId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.PSNUserId = Utf8.ToString(source->PSNUserId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementLinkPSNIdRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->IssuerId = arena.Value(IssuerId);
        target->PlayFabId = arena.String(PlayFabId);
        target->PSNUserId = arena.String(PSNUserId);
    }
}

/// <summary>Projects <c>PFAccountManagementLinkServerCustomIdRequest</c>.</summary>
public sealed class AccountManagementLinkServerCustomIdRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>ServerCustomId</c>.</summary>
    public string? ServerCustomId { get; set; }

    internal static unsafe AccountManagementLinkServerCustomIdRequest FromNative(PFAccountManagementLinkServerCustomIdRequest* source)
    {
        var value = new AccountManagementLinkServerCustomIdRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.ServerCustomId = Utf8.ToString(source->ServerCustomId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementLinkServerCustomIdRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->PlayFabId = arena.String(PlayFabId);
        target->ServerCustomId = arena.String(ServerCustomId);
    }
}

/// <summary>Projects <c>PFAccountManagementLinkSteamAccountRequest</c>.</summary>
public sealed class AccountManagementLinkSteamAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>SteamTicket</c>.</summary>
    public string? SteamTicket { get; set; }

    /// <summary><c>TicketIsServiceSpecific</c>.</summary>
    public bool? TicketIsServiceSpecific { get; set; }

    internal static unsafe AccountManagementLinkSteamAccountRequest FromNative(PFAccountManagementLinkSteamAccountRequest* source)
    {
        var value = new AccountManagementLinkSteamAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.SteamTicket = Utf8.ToString(source->SteamTicket);
        value.TicketIsServiceSpecific = source->TicketIsServiceSpecific is null ? null : (bool?)(*source->TicketIsServiceSpecific != 0);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementLinkSteamAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->SteamTicket = arena.String(SteamTicket);
        target->TicketIsServiceSpecific = arena.Value(TicketIsServiceSpecific is null ? null : (TicketIsServiceSpecific.Value ? (byte?)1 : (byte?)0));
    }
}

/// <summary>Projects <c>PFAccountManagementLinkSteamIdRequest</c>.</summary>
public sealed class AccountManagementLinkSteamIdRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>SteamId</c>.</summary>
    public string? SteamId { get; set; }

    internal static unsafe AccountManagementLinkSteamIdRequest FromNative(PFAccountManagementLinkSteamIdRequest* source)
    {
        var value = new AccountManagementLinkSteamIdRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.SteamId = Utf8.ToString(source->SteamId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementLinkSteamIdRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->PlayFabId = arena.String(PlayFabId);
        target->SteamId = arena.String(SteamId);
    }
}

/// <summary>Projects <c>PFAccountManagementNintendoServiceAccountPlayFabIdPair</c>.</summary>
public sealed class AccountManagementNintendoServiceAccountPlayFabIdPair
{
    /// <summary><c>NintendoServiceAccountId</c>.</summary>
    public string? NintendoServiceAccountId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementNintendoServiceAccountPlayFabIdPair FromNative(PFAccountManagementNintendoServiceAccountPlayFabIdPair* source)
    {
        var value = new AccountManagementNintendoServiceAccountPlayFabIdPair();
        value.NintendoServiceAccountId = Utf8.ToString(source->NintendoServiceAccountId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementNintendoServiceAccountPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->NintendoServiceAccountId = arena.String(NintendoServiceAccountId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementNintendoSwitchPlayFabIdPair</c>.</summary>
public sealed class AccountManagementNintendoSwitchPlayFabIdPair
{
    /// <summary><c>NintendoSwitchDeviceId</c>.</summary>
    public string? NintendoSwitchDeviceId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementNintendoSwitchPlayFabIdPair FromNative(PFAccountManagementNintendoSwitchPlayFabIdPair* source)
    {
        var value = new AccountManagementNintendoSwitchPlayFabIdPair();
        value.NintendoSwitchDeviceId = Utf8.ToString(source->NintendoSwitchDeviceId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementNintendoSwitchPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->NintendoSwitchDeviceId = arena.String(NintendoSwitchDeviceId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementPSNAccountPlayFabIdPair</c>.</summary>
public sealed class AccountManagementPSNAccountPlayFabIdPair
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PSNAccountId</c>.</summary>
    public string? PSNAccountId { get; set; }

    internal static unsafe AccountManagementPSNAccountPlayFabIdPair FromNative(PFAccountManagementPSNAccountPlayFabIdPair* source)
    {
        var value = new AccountManagementPSNAccountPlayFabIdPair();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.PSNAccountId = Utf8.ToString(source->PSNAccountId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementPSNAccountPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->PSNAccountId = arena.String(PSNAccountId);
    }
}

/// <summary>Projects <c>PFAccountManagementPSNOnlinePlayFabIdPair</c>.</summary>
public sealed class AccountManagementPSNOnlinePlayFabIdPair
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PSNOnlineId</c>.</summary>
    public string? PSNOnlineId { get; set; }

    internal static unsafe AccountManagementPSNOnlinePlayFabIdPair FromNative(PFAccountManagementPSNOnlinePlayFabIdPair* source)
    {
        var value = new AccountManagementPSNOnlinePlayFabIdPair();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.PSNOnlineId = Utf8.ToString(source->PSNOnlineId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementPSNOnlinePlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->PSNOnlineId = arena.String(PSNOnlineId);
    }
}

/// <summary>Projects <c>PFAccountManagementRemoveContactEmailRequest</c>.</summary>
public sealed class AccountManagementRemoveContactEmailRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe AccountManagementRemoveContactEmailRequest FromNative(PFAccountManagementRemoveContactEmailRequest* source)
    {
        var value = new AccountManagementRemoveContactEmailRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementRemoveContactEmailRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementReportPlayerClientRequest</c>.</summary>
public sealed class AccountManagementReportPlayerClientRequest
{
    /// <summary><c>Comment</c>.</summary>
    public string? Comment { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ReporteeId</c>.</summary>
    public string? ReporteeId { get; set; }

    internal static unsafe AccountManagementReportPlayerClientRequest FromNative(PFAccountManagementReportPlayerClientRequest* source)
    {
        var value = new AccountManagementReportPlayerClientRequest();
        value.Comment = Utf8.ToString(source->Comment);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.ReporteeId = Utf8.ToString(source->ReporteeId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementReportPlayerClientRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Comment = arena.String(Comment);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->ReporteeId = arena.String(ReporteeId);
    }
}

/// <summary>Projects <c>PFAccountManagementReportPlayerClientResult</c>.</summary>
public sealed class AccountManagementReportPlayerClientResult
{
    /// <summary><c>SubmissionsRemaining</c>.</summary>
    public int SubmissionsRemaining { get; set; }

    internal static unsafe AccountManagementReportPlayerClientResult FromNative(PFAccountManagementReportPlayerClientResult* source)
    {
        var value = new AccountManagementReportPlayerClientResult();
        value.SubmissionsRemaining = source->SubmissionsRemaining;
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementReportPlayerClientResult* target, PlayFabArena arena)
    {
        *target = default;
        target->SubmissionsRemaining = SubmissionsRemaining;
    }
}

/// <summary>Projects <c>PFAccountManagementRevokeAllBansForUserRequest</c>.</summary>
public sealed class AccountManagementRevokeAllBansForUserRequest
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementRevokeAllBansForUserRequest FromNative(PFAccountManagementRevokeAllBansForUserRequest* source)
    {
        var value = new AccountManagementRevokeAllBansForUserRequest();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementRevokeAllBansForUserRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementRevokeAllBansForUserResult</c>.</summary>
public sealed class AccountManagementRevokeAllBansForUserResult
{
    /// <summary><c>BanData</c>.</summary>
    public IReadOnlyList<AccountManagementBanInfo>? BanData { get; set; }

    internal static unsafe AccountManagementRevokeAllBansForUserResult FromNative(PFAccountManagementRevokeAllBansForUserResult* source)
    {
        var value = new AccountManagementRevokeAllBansForUserResult();
        if (source->BanData is not null && source->BanDataCount != 0)
        {
            var list0 = new List<AccountManagementBanInfo>((int)source->BanDataCount);
            for (uint i0 = 0; i0 < source->BanDataCount; i0++)
            {
                list0.Add(AccountManagementBanInfo.FromNative(source->BanData[i0]));
            }

            value.BanData = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementRevokeAllBansForUserResult* target, PlayFabArena arena)
    {
        *target = default;
        target->BanDataCount = (uint)(BanData is null ? 0 : BanData.Count);
        if (BanData is not null && BanData.Count > 0)
        {
            PFAccountManagementBanInfo** array0 = arena.PointerArray<PFAccountManagementBanInfo>(BanData.Count);
            for (int i0 = 0; i0 < BanData.Count; i0++)
            {
                PFAccountManagementBanInfo* item0 = arena.Alloc<PFAccountManagementBanInfo>(1);
                BanData[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->BanData = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementRevokeBansRequest</c>.</summary>
public sealed class AccountManagementRevokeBansRequest
{
    /// <summary><c>BanIds</c>.</summary>
    public IReadOnlyList<string>? BanIds { get; set; }

    internal static unsafe AccountManagementRevokeBansRequest FromNative(PFAccountManagementRevokeBansRequest* source)
    {
        var value = new AccountManagementRevokeBansRequest();
        if (source->BanIds is not null && source->BanIdsCount != 0)
        {
            var list0 = new List<string>((int)source->BanIdsCount);
            for (uint i0 = 0; i0 < source->BanIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->BanIds[i0]) ?? string.Empty);
            }

            value.BanIds = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementRevokeBansRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->BanIdsCount = (uint)(BanIds is null ? 0 : BanIds.Count);
        target->BanIds = arena.StringArray(BanIds);
    }
}

/// <summary>Projects <c>PFAccountManagementRevokeBansResult</c>.</summary>
public sealed class AccountManagementRevokeBansResult
{
    /// <summary><c>BanData</c>.</summary>
    public IReadOnlyList<AccountManagementBanInfo>? BanData { get; set; }

    internal static unsafe AccountManagementRevokeBansResult FromNative(PFAccountManagementRevokeBansResult* source)
    {
        var value = new AccountManagementRevokeBansResult();
        if (source->BanData is not null && source->BanDataCount != 0)
        {
            var list0 = new List<AccountManagementBanInfo>((int)source->BanDataCount);
            for (uint i0 = 0; i0 < source->BanDataCount; i0++)
            {
                list0.Add(AccountManagementBanInfo.FromNative(source->BanData[i0]));
            }

            value.BanData = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementRevokeBansResult* target, PlayFabArena arena)
    {
        *target = default;
        target->BanDataCount = (uint)(BanData is null ? 0 : BanData.Count);
        if (BanData is not null && BanData.Count > 0)
        {
            PFAccountManagementBanInfo** array0 = arena.PointerArray<PFAccountManagementBanInfo>(BanData.Count);
            for (int i0 = 0; i0 < BanData.Count; i0++)
            {
                PFAccountManagementBanInfo* item0 = arena.Alloc<PFAccountManagementBanInfo>(1);
                BanData[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->BanData = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementSendCustomAccountRecoveryEmailRequest</c>.</summary>
public sealed class AccountManagementSendCustomAccountRecoveryEmailRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Email</c>.</summary>
    public string? Email { get; set; }

    /// <summary><c>EmailTemplateId</c>.</summary>
    public string? EmailTemplateId { get; set; }

    /// <summary><c>Username</c>.</summary>
    public string? Username { get; set; }

    internal static unsafe AccountManagementSendCustomAccountRecoveryEmailRequest FromNative(PFAccountManagementSendCustomAccountRecoveryEmailRequest* source)
    {
        var value = new AccountManagementSendCustomAccountRecoveryEmailRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Email = Utf8.ToString(source->Email);
        value.EmailTemplateId = Utf8.ToString(source->EmailTemplateId);
        value.Username = Utf8.ToString(source->Username);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementSendCustomAccountRecoveryEmailRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->Email = arena.String(Email);
        target->EmailTemplateId = arena.String(EmailTemplateId);
        target->Username = arena.String(Username);
    }
}

/// <summary>Projects <c>PFAccountManagementSendEmailFromTemplateRequest</c>.</summary>
public sealed class AccountManagementSendEmailFromTemplateRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>EmailTemplateId</c>.</summary>
    public string? EmailTemplateId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementSendEmailFromTemplateRequest FromNative(PFAccountManagementSendEmailFromTemplateRequest* source)
    {
        var value = new AccountManagementSendEmailFromTemplateRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.EmailTemplateId = Utf8.ToString(source->EmailTemplateId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementSendEmailFromTemplateRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->EmailTemplateId = arena.String(EmailTemplateId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerCustomIDPlayFabIDPair</c>.</summary>
public sealed class AccountManagementServerCustomIDPlayFabIDPair
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>ServerCustomId</c>.</summary>
    public string? ServerCustomId { get; set; }

    internal static unsafe AccountManagementServerCustomIDPlayFabIDPair FromNative(PFAccountManagementServerCustomIDPlayFabIDPair* source)
    {
        var value = new AccountManagementServerCustomIDPlayFabIDPair();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.ServerCustomId = Utf8.ToString(source->ServerCustomId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerCustomIDPlayFabIDPair* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->ServerCustomId = arena.String(ServerCustomId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerLinkBattleNetAccountRequest</c>.</summary>
public sealed class AccountManagementServerLinkBattleNetAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>IdentityToken</c>.</summary>
    public string? IdentityToken { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerLinkBattleNetAccountRequest FromNative(PFAccountManagementServerLinkBattleNetAccountRequest* source)
    {
        var value = new AccountManagementServerLinkBattleNetAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.IdentityToken = Utf8.ToString(source->IdentityToken);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerLinkBattleNetAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->IdentityToken = arena.String(IdentityToken);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerLinkNintendoServiceAccountRequest</c>.</summary>
public sealed class AccountManagementServerLinkNintendoServiceAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>IdentityToken</c>.</summary>
    public string? IdentityToken { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerLinkNintendoServiceAccountRequest FromNative(PFAccountManagementServerLinkNintendoServiceAccountRequest* source)
    {
        var value = new AccountManagementServerLinkNintendoServiceAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.IdentityToken = Utf8.ToString(source->IdentityToken);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerLinkNintendoServiceAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->IdentityToken = arena.String(IdentityToken);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerLinkNintendoSwitchDeviceIdRequest</c>.</summary>
public sealed class AccountManagementServerLinkNintendoSwitchDeviceIdRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>NintendoSwitchDeviceId</c>.</summary>
    public string? NintendoSwitchDeviceId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerLinkNintendoSwitchDeviceIdRequest FromNative(PFAccountManagementServerLinkNintendoSwitchDeviceIdRequest* source)
    {
        var value = new AccountManagementServerLinkNintendoSwitchDeviceIdRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.NintendoSwitchDeviceId = Utf8.ToString(source->NintendoSwitchDeviceId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerLinkNintendoSwitchDeviceIdRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->NintendoSwitchDeviceId = arena.String(NintendoSwitchDeviceId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerLinkPSNAccountRequest</c>.</summary>
public sealed class AccountManagementServerLinkPSNAccountRequest
{
    /// <summary><c>AuthCode</c>.</summary>
    public string? AuthCode { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>IssuerId</c>.</summary>
    public int? IssuerId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>RedirectUri</c>.</summary>
    public string? RedirectUri { get; set; }

    internal static unsafe AccountManagementServerLinkPSNAccountRequest FromNative(PFAccountManagementServerLinkPSNAccountRequest* source)
    {
        var value = new AccountManagementServerLinkPSNAccountRequest();
        value.AuthCode = Utf8.ToString(source->AuthCode);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.IssuerId = source->IssuerId is null ? null : (int?)(*source->IssuerId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.RedirectUri = Utf8.ToString(source->RedirectUri);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerLinkPSNAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->AuthCode = arena.String(AuthCode);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->IssuerId = arena.Value(IssuerId);
        target->PlayFabId = arena.String(PlayFabId);
        target->RedirectUri = arena.String(RedirectUri);
    }
}

/// <summary>Projects <c>PFAccountManagementServerLinkXboxAccountRequest</c>.</summary>
public sealed class AccountManagementServerLinkXboxAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ForceLink</c>.</summary>
    public bool? ForceLink { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>XboxToken</c>.</summary>
    public string? XboxToken { get; set; }

    internal static unsafe AccountManagementServerLinkXboxAccountRequest FromNative(PFAccountManagementServerLinkXboxAccountRequest* source)
    {
        var value = new AccountManagementServerLinkXboxAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ForceLink = source->ForceLink is null ? null : (bool?)(*source->ForceLink != 0);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.XboxToken = Utf8.ToString(source->XboxToken);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerLinkXboxAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ForceLink = arena.Value(ForceLink is null ? null : (ForceLink.Value ? (byte?)1 : (byte?)0));
        target->PlayFabId = arena.String(PlayFabId);
        target->XboxToken = arena.String(XboxToken);
    }
}

/// <summary>Projects <c>PFAccountManagementServerUnlinkBattleNetAccountRequest</c>.</summary>
public sealed class AccountManagementServerUnlinkBattleNetAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerUnlinkBattleNetAccountRequest FromNative(PFAccountManagementServerUnlinkBattleNetAccountRequest* source)
    {
        var value = new AccountManagementServerUnlinkBattleNetAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerUnlinkBattleNetAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerUnlinkNintendoServiceAccountRequest</c>.</summary>
public sealed class AccountManagementServerUnlinkNintendoServiceAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerUnlinkNintendoServiceAccountRequest FromNative(PFAccountManagementServerUnlinkNintendoServiceAccountRequest* source)
    {
        var value = new AccountManagementServerUnlinkNintendoServiceAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerUnlinkNintendoServiceAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerUnlinkNintendoSwitchDeviceIdRequest</c>.</summary>
public sealed class AccountManagementServerUnlinkNintendoSwitchDeviceIdRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>NintendoSwitchDeviceId</c>.</summary>
    public string? NintendoSwitchDeviceId { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerUnlinkNintendoSwitchDeviceIdRequest FromNative(PFAccountManagementServerUnlinkNintendoSwitchDeviceIdRequest* source)
    {
        var value = new AccountManagementServerUnlinkNintendoSwitchDeviceIdRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.NintendoSwitchDeviceId = Utf8.ToString(source->NintendoSwitchDeviceId);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerUnlinkNintendoSwitchDeviceIdRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->NintendoSwitchDeviceId = arena.String(NintendoSwitchDeviceId);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerUnlinkPSNAccountRequest</c>.</summary>
public sealed class AccountManagementServerUnlinkPSNAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerUnlinkPSNAccountRequest FromNative(PFAccountManagementServerUnlinkPSNAccountRequest* source)
    {
        var value = new AccountManagementServerUnlinkPSNAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerUnlinkPSNAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerUnlinkXboxAccountRequest</c>.</summary>
public sealed class AccountManagementServerUnlinkXboxAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerUnlinkXboxAccountRequest FromNative(PFAccountManagementServerUnlinkXboxAccountRequest* source)
    {
        var value = new AccountManagementServerUnlinkXboxAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerUnlinkXboxAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementServerUpdateAvatarUrlRequest</c>.</summary>
public sealed class AccountManagementServerUpdateAvatarUrlRequest
{
    /// <summary><c>ImageUrl</c>.</summary>
    public string? ImageUrl { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementServerUpdateAvatarUrlRequest FromNative(PFAccountManagementServerUpdateAvatarUrlRequest* source)
    {
        var value = new AccountManagementServerUpdateAvatarUrlRequest();
        value.ImageUrl = Utf8.ToString(source->ImageUrl);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementServerUpdateAvatarUrlRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ImageUrl = arena.String(ImageUrl);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementSetDisplayNameRequest</c>.</summary>
public sealed class AccountManagementSetDisplayNameRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DisplayName</c>.</summary>
    public string? DisplayName { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ExpectedVersion</c>.</summary>
    public int? ExpectedVersion { get; set; }

    internal static unsafe AccountManagementSetDisplayNameRequest FromNative(PFAccountManagementSetDisplayNameRequest* source)
    {
        var value = new AccountManagementSetDisplayNameRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.DisplayName = Utf8.ToString(source->DisplayName);
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ExpectedVersion = source->ExpectedVersion is null ? null : (int?)(*source->ExpectedVersion);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementSetDisplayNameRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DisplayName = arena.String(DisplayName);
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->ExpectedVersion = arena.Value(ExpectedVersion);
    }
}

/// <summary>Projects <c>PFAccountManagementSetDisplayNameResponse</c>.</summary>
public sealed class AccountManagementSetDisplayNameResponse
{
    /// <summary><c>OperationResult</c>.</summary>
    public OperationTypes? OperationResult { get; set; }

    /// <summary><c>VersionNumber</c>.</summary>
    public int? VersionNumber { get; set; }

    internal static unsafe AccountManagementSetDisplayNameResponse FromNative(PFAccountManagementSetDisplayNameResponse* source)
    {
        var value = new AccountManagementSetDisplayNameResponse();
        value.OperationResult = source->OperationResult is null ? null : (OperationTypes?)(*source->OperationResult);
        value.VersionNumber = source->VersionNumber is null ? null : (int?)(*source->VersionNumber);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementSetDisplayNameResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->OperationResult = arena.Value((PFOperationTypes?)OperationResult);
        target->VersionNumber = arena.Value(VersionNumber);
    }
}

/// <summary>Projects <c>PFAccountManagementSteamNamePlayFabIdPair</c>.</summary>
public sealed class AccountManagementSteamNamePlayFabIdPair
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>SteamName</c>.</summary>
    public string? SteamName { get; set; }

    internal static unsafe AccountManagementSteamNamePlayFabIdPair FromNative(PFAccountManagementSteamNamePlayFabIdPair* source)
    {
        var value = new AccountManagementSteamNamePlayFabIdPair();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.SteamName = Utf8.ToString(source->SteamName);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementSteamNamePlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->SteamName = arena.String(SteamName);
    }
}

/// <summary>Projects <c>PFAccountManagementSteamPlayFabIdPair</c>.</summary>
public sealed class AccountManagementSteamPlayFabIdPair
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>SteamStringId</c>.</summary>
    public string? SteamStringId { get; set; }

    internal static unsafe AccountManagementSteamPlayFabIdPair FromNative(PFAccountManagementSteamPlayFabIdPair* source)
    {
        var value = new AccountManagementSteamPlayFabIdPair();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.SteamStringId = Utf8.ToString(source->SteamStringId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementSteamPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->SteamStringId = arena.String(SteamStringId);
    }
}

/// <summary>Projects <c>PFAccountManagementTwitchPlayFabIdPair</c>.</summary>
public sealed class AccountManagementTwitchPlayFabIdPair
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>TwitchId</c>.</summary>
    public string? TwitchId { get; set; }

    internal static unsafe AccountManagementTwitchPlayFabIdPair FromNative(PFAccountManagementTwitchPlayFabIdPair* source)
    {
        var value = new AccountManagementTwitchPlayFabIdPair();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.TwitchId = Utf8.ToString(source->TwitchId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementTwitchPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->TwitchId = arena.String(TwitchId);
    }
}

/// <summary>Projects <c>PFAccountManagementUnlinkCustomIDRequest</c>.</summary>
public sealed class AccountManagementUnlinkCustomIDRequest
{
    /// <summary><c>CustomId</c>.</summary>
    public string? CustomId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe AccountManagementUnlinkCustomIDRequest FromNative(PFAccountManagementUnlinkCustomIDRequest* source)
    {
        var value = new AccountManagementUnlinkCustomIDRequest();
        value.CustomId = Utf8.ToString(source->CustomId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUnlinkCustomIDRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomId = arena.String(CustomId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementUnlinkOpenIdConnectRequest</c>.</summary>
public sealed class AccountManagementUnlinkOpenIdConnectRequest
{
    /// <summary><c>ConnectionId</c>.</summary>
    public string? ConnectionId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe AccountManagementUnlinkOpenIdConnectRequest FromNative(PFAccountManagementUnlinkOpenIdConnectRequest* source)
    {
        var value = new AccountManagementUnlinkOpenIdConnectRequest();
        value.ConnectionId = Utf8.ToString(source->ConnectionId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUnlinkOpenIdConnectRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ConnectionId = arena.String(ConnectionId);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementUnlinkServerCustomIdRequest</c>.</summary>
public sealed class AccountManagementUnlinkServerCustomIdRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>ServerCustomId</c>.</summary>
    public string? ServerCustomId { get; set; }

    internal static unsafe AccountManagementUnlinkServerCustomIdRequest FromNative(PFAccountManagementUnlinkServerCustomIdRequest* source)
    {
        var value = new AccountManagementUnlinkServerCustomIdRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.ServerCustomId = Utf8.ToString(source->ServerCustomId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUnlinkServerCustomIdRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
        target->ServerCustomId = arena.String(ServerCustomId);
    }
}

/// <summary>Projects <c>PFAccountManagementUnlinkSteamAccountRequest</c>.</summary>
public sealed class AccountManagementUnlinkSteamAccountRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe AccountManagementUnlinkSteamAccountRequest FromNative(PFAccountManagementUnlinkSteamAccountRequest* source)
    {
        var value = new AccountManagementUnlinkSteamAccountRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUnlinkSteamAccountRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementUnlinkSteamIdRequest</c>.</summary>
public sealed class AccountManagementUnlinkSteamIdRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe AccountManagementUnlinkSteamIdRequest FromNative(PFAccountManagementUnlinkSteamIdRequest* source)
    {
        var value = new AccountManagementUnlinkSteamIdRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUnlinkSteamIdRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFAccountManagementUpdateBanRequest</c>.</summary>
public sealed class AccountManagementUpdateBanRequest
{
    /// <summary><c>Active</c>.</summary>
    public bool? Active { get; set; }

    /// <summary><c>BanId</c>.</summary>
    public string? BanId { get; set; }

    /// <summary><c>Expires</c>.</summary>
    public DateTimeOffset? Expires { get; set; }

    /// <summary><c>IPAddress</c>.</summary>
    public string? IPAddress { get; set; }

    /// <summary><c>Permanent</c>.</summary>
    public bool? Permanent { get; set; }

    /// <summary><c>Reason</c>.</summary>
    public string? Reason { get; set; }

    /// <summary><c>UserFamilyType</c>.</summary>
    public AccountManagementUserFamilyType? UserFamilyType { get; set; }

    internal static unsafe AccountManagementUpdateBanRequest FromNative(PFAccountManagementUpdateBanRequest* source)
    {
        var value = new AccountManagementUpdateBanRequest();
        value.Active = source->Active is null ? null : (bool?)(*source->Active != 0);
        value.BanId = Utf8.ToString(source->BanId);
        value.Expires = source->Expires is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->Expires);
        value.IPAddress = Utf8.ToString(source->IPAddress);
        value.Permanent = source->Permanent is null ? null : (bool?)(*source->Permanent != 0);
        value.Reason = Utf8.ToString(source->Reason);
        value.UserFamilyType = source->UserFamilyType is null ? null : (AccountManagementUserFamilyType?)(*source->UserFamilyType);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUpdateBanRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->Active = arena.Value(Active is null ? null : (Active.Value ? (byte?)1 : (byte?)0));
        target->BanId = arena.String(BanId);
        target->Expires = arena.Value(Expires is null ? null : (long?)PlayFabTime.ToUnixTime(Expires.Value));
        target->IPAddress = arena.String(IPAddress);
        target->Permanent = arena.Value(Permanent is null ? null : (Permanent.Value ? (byte?)1 : (byte?)0));
        target->Reason = arena.String(Reason);
        target->UserFamilyType = arena.Value((PFAccountManagementUserFamilyType?)UserFamilyType);
    }
}

/// <summary>Projects <c>PFAccountManagementUpdateBansRequest</c>.</summary>
public sealed class AccountManagementUpdateBansRequest
{
    /// <summary><c>Bans</c>.</summary>
    public IReadOnlyList<AccountManagementUpdateBanRequest>? Bans { get; set; }

    internal static unsafe AccountManagementUpdateBansRequest FromNative(PFAccountManagementUpdateBansRequest* source)
    {
        var value = new AccountManagementUpdateBansRequest();
        if (source->Bans is not null && source->BansCount != 0)
        {
            var list0 = new List<AccountManagementUpdateBanRequest>((int)source->BansCount);
            for (uint i0 = 0; i0 < source->BansCount; i0++)
            {
                list0.Add(AccountManagementUpdateBanRequest.FromNative(source->Bans[i0]));
            }

            value.Bans = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUpdateBansRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->BansCount = (uint)(Bans is null ? 0 : Bans.Count);
        if (Bans is not null && Bans.Count > 0)
        {
            PFAccountManagementUpdateBanRequest** array0 = arena.PointerArray<PFAccountManagementUpdateBanRequest>(Bans.Count);
            for (int i0 = 0; i0 < Bans.Count; i0++)
            {
                PFAccountManagementUpdateBanRequest* item0 = arena.Alloc<PFAccountManagementUpdateBanRequest>(1);
                Bans[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Bans = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementUpdateBansResult</c>.</summary>
public sealed class AccountManagementUpdateBansResult
{
    /// <summary><c>BanData</c>.</summary>
    public IReadOnlyList<AccountManagementBanInfo>? BanData { get; set; }

    internal static unsafe AccountManagementUpdateBansResult FromNative(PFAccountManagementUpdateBansResult* source)
    {
        var value = new AccountManagementUpdateBansResult();
        if (source->BanData is not null && source->BanDataCount != 0)
        {
            var list0 = new List<AccountManagementBanInfo>((int)source->BanDataCount);
            for (uint i0 = 0; i0 < source->BanDataCount; i0++)
            {
                list0.Add(AccountManagementBanInfo.FromNative(source->BanData[i0]));
            }

            value.BanData = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUpdateBansResult* target, PlayFabArena arena)
    {
        *target = default;
        target->BanDataCount = (uint)(BanData is null ? 0 : BanData.Count);
        if (BanData is not null && BanData.Count > 0)
        {
            PFAccountManagementBanInfo** array0 = arena.PointerArray<PFAccountManagementBanInfo>(BanData.Count);
            for (int i0 = 0; i0 < BanData.Count; i0++)
            {
                PFAccountManagementBanInfo* item0 = arena.Alloc<PFAccountManagementBanInfo>(1);
                BanData[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->BanData = array0;
        }
    }
}

/// <summary>Projects <c>PFAccountManagementUpdateUserTitleDisplayNameRequest</c>.</summary>
public sealed class AccountManagementUpdateUserTitleDisplayNameRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DisplayName</c>.</summary>
    public string? DisplayName { get; set; }

    internal static unsafe AccountManagementUpdateUserTitleDisplayNameRequest FromNative(PFAccountManagementUpdateUserTitleDisplayNameRequest* source)
    {
        var value = new AccountManagementUpdateUserTitleDisplayNameRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.DisplayName = Utf8.ToString(source->DisplayName);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUpdateUserTitleDisplayNameRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DisplayName = arena.String(DisplayName);
    }
}

/// <summary>Projects <c>PFAccountManagementUpdateUserTitleDisplayNameResult</c>.</summary>
public sealed class AccountManagementUpdateUserTitleDisplayNameResult
{
    /// <summary><c>DisplayName</c>.</summary>
    public string? DisplayName { get; set; }

    internal static unsafe AccountManagementUpdateUserTitleDisplayNameResult FromNative(PFAccountManagementUpdateUserTitleDisplayNameResult* source)
    {
        var value = new AccountManagementUpdateUserTitleDisplayNameResult();
        value.DisplayName = Utf8.ToString(source->DisplayName);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementUpdateUserTitleDisplayNameResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DisplayName = arena.String(DisplayName);
    }
}

/// <summary>Projects <c>PFAccountManagementXboxLiveAccountPlayFabIdPair</c>.</summary>
public sealed class AccountManagementXboxLiveAccountPlayFabIdPair
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>XboxLiveAccountId</c>.</summary>
    public string? XboxLiveAccountId { get; set; }

    internal static unsafe AccountManagementXboxLiveAccountPlayFabIdPair FromNative(PFAccountManagementXboxLiveAccountPlayFabIdPair* source)
    {
        var value = new AccountManagementXboxLiveAccountPlayFabIdPair();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.XboxLiveAccountId = Utf8.ToString(source->XboxLiveAccountId);
        return value;
    }

    internal unsafe void WriteTo(PFAccountManagementXboxLiveAccountPlayFabIdPair* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->XboxLiveAccountId = arena.String(XboxLiveAccountId);
    }
}
