// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFPlayerDataManagement
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFPlayerDataManagementClientDeletePlayerCustomPropertiesRequest</c>.</summary>
public sealed class PlayerDataManagementClientDeletePlayerCustomPropertiesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ExpectedPropertiesVersion</c>.</summary>
    public int? ExpectedPropertiesVersion { get; set; }

    /// <summary><c>PropertyNames</c>.</summary>
    public IReadOnlyList<string>? PropertyNames { get; set; }

    internal static unsafe PlayerDataManagementClientDeletePlayerCustomPropertiesRequest FromNative(PFPlayerDataManagementClientDeletePlayerCustomPropertiesRequest* source)
    {
        var value = new PlayerDataManagementClientDeletePlayerCustomPropertiesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ExpectedPropertiesVersion = source->ExpectedPropertiesVersion is null ? null : (int?)(*source->ExpectedPropertiesVersion);
        if (source->PropertyNames is not null && source->PropertyNamesCount != 0)
        {
            var list3 = new List<string>((int)source->PropertyNamesCount);
            for (uint i3 = 0; i3 < source->PropertyNamesCount; i3++)
            {
                list3.Add(Utf8.ToString(source->PropertyNames[i3]) ?? string.Empty);
            }

            value.PropertyNames = list3;
        }
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientDeletePlayerCustomPropertiesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ExpectedPropertiesVersion = arena.Value(ExpectedPropertiesVersion);
        target->PropertyNamesCount = (uint)(PropertyNames is null ? 0 : PropertyNames.Count);
        target->PropertyNames = arena.StringArray(PropertyNames);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientDeletePlayerCustomPropertiesResult</c>.</summary>
public sealed class PlayerDataManagementClientDeletePlayerCustomPropertiesResult
{
    /// <summary><c>DeletedProperties</c>.</summary>
    public IReadOnlyList<PlayerDataManagementDeletedPropertyDetails>? DeletedProperties { get; set; }

    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    internal static unsafe PlayerDataManagementClientDeletePlayerCustomPropertiesResult FromNative(PFPlayerDataManagementClientDeletePlayerCustomPropertiesResult* source)
    {
        var value = new PlayerDataManagementClientDeletePlayerCustomPropertiesResult();
        if (source->DeletedProperties is not null && source->DeletedPropertiesCount != 0)
        {
            var list0 = new List<PlayerDataManagementDeletedPropertyDetails>((int)source->DeletedPropertiesCount);
            for (uint i0 = 0; i0 < source->DeletedPropertiesCount; i0++)
            {
                list0.Add(PlayerDataManagementDeletedPropertyDetails.FromNative(source->DeletedProperties[i0]));
            }

            value.DeletedProperties = list0;
        }
        value.PropertiesVersion = source->PropertiesVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientDeletePlayerCustomPropertiesResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DeletedPropertiesCount = (uint)(DeletedProperties is null ? 0 : DeletedProperties.Count);
        if (DeletedProperties is not null && DeletedProperties.Count > 0)
        {
            PFPlayerDataManagementDeletedPropertyDetails** array0 = arena.PointerArray<PFPlayerDataManagementDeletedPropertyDetails>(DeletedProperties.Count);
            for (int i0 = 0; i0 < DeletedProperties.Count; i0++)
            {
                PFPlayerDataManagementDeletedPropertyDetails* item0 = arena.Alloc<PFPlayerDataManagementDeletedPropertyDetails>(1);
                DeletedProperties[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->DeletedProperties = array0;
        }
        target->PropertiesVersion = PropertiesVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientGetPlayerCustomPropertyRequest</c>.</summary>
public sealed class PlayerDataManagementClientGetPlayerCustomPropertyRequest
{
    /// <summary><c>PropertyName</c>.</summary>
    public string? PropertyName { get; set; }

    internal static unsafe PlayerDataManagementClientGetPlayerCustomPropertyRequest FromNative(PFPlayerDataManagementClientGetPlayerCustomPropertyRequest* source)
    {
        var value = new PlayerDataManagementClientGetPlayerCustomPropertyRequest();
        value.PropertyName = Utf8.ToString(source->PropertyName);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientGetPlayerCustomPropertyRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PropertyName = arena.String(PropertyName);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientGetPlayerCustomPropertyResult</c>.</summary>
public sealed class PlayerDataManagementClientGetPlayerCustomPropertyResult
{
    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    /// <summary><c>Property</c>.</summary>
    public PlayerDataManagementCustomPropertyDetails? Property { get; set; }

    internal static unsafe PlayerDataManagementClientGetPlayerCustomPropertyResult FromNative(PFPlayerDataManagementClientGetPlayerCustomPropertyResult* source)
    {
        var value = new PlayerDataManagementClientGetPlayerCustomPropertyResult();
        value.PropertiesVersion = source->PropertiesVersion;
        value.Property = source->Property is null ? null : PlayerDataManagementCustomPropertyDetails.FromNative(source->Property);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientGetPlayerCustomPropertyResult* target, PlayFabArena arena)
    {
        *target = default;
        target->PropertiesVersion = PropertiesVersion;
        if (Property is not null)
        {
            PFPlayerDataManagementCustomPropertyDetails* native1 = arena.Alloc<PFPlayerDataManagementCustomPropertyDetails>(1);
            Property.WriteTo(native1, arena);
            target->Property = native1;
        }
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientGetUserDataResult</c>.</summary>
public sealed class PlayerDataManagementClientGetUserDataResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyDictionary<string, UserDataRecord>? Data { get; set; }

    /// <summary><c>DataVersion</c>.</summary>
    public uint DataVersion { get; set; }

    internal static unsafe PlayerDataManagementClientGetUserDataResult FromNative(PFPlayerDataManagementClientGetUserDataResult* source)
    {
        var value = new PlayerDataManagementClientGetUserDataResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var map0 = new Dictionary<string, UserDataRecord>((int)source->DataCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                map0[Utf8.ToString(source->Data[i0].Key) ?? string.Empty] = UserDataRecord.FromNative(source->Data[i0].Value);
            }

            value.Data = map0;
        }
        value.DataVersion = source->DataVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientGetUserDataResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFUserDataRecordDictionaryEntry* entries0 = arena.Alloc<PFUserDataRecordDictionaryEntry>(Data.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, UserDataRecord> pair0 in Data)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                if (pair0.Value is not null)
                {
                    PFUserDataRecord* item0 = arena.Alloc<PFUserDataRecord>(1);
                    pair0.Value.WriteTo(item0, arena);
                    entries0[next0].Value = item0;
                }
                next0++;
            }

            target->Data = entries0;
        }
        target->DataVersion = DataVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientListPlayerCustomPropertiesResult</c>.</summary>
public sealed class PlayerDataManagementClientListPlayerCustomPropertiesResult
{
    /// <summary><c>Properties</c>.</summary>
    public IReadOnlyList<PlayerDataManagementCustomPropertyDetails>? Properties { get; set; }

    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    internal static unsafe PlayerDataManagementClientListPlayerCustomPropertiesResult FromNative(PFPlayerDataManagementClientListPlayerCustomPropertiesResult* source)
    {
        var value = new PlayerDataManagementClientListPlayerCustomPropertiesResult();
        if (source->Properties is not null && source->PropertiesCount != 0)
        {
            var list0 = new List<PlayerDataManagementCustomPropertyDetails>((int)source->PropertiesCount);
            for (uint i0 = 0; i0 < source->PropertiesCount; i0++)
            {
                list0.Add(PlayerDataManagementCustomPropertyDetails.FromNative(source->Properties[i0]));
            }

            value.Properties = list0;
        }
        value.PropertiesVersion = source->PropertiesVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientListPlayerCustomPropertiesResult* target, PlayFabArena arena)
    {
        *target = default;
        target->PropertiesCount = (uint)(Properties is null ? 0 : Properties.Count);
        if (Properties is not null && Properties.Count > 0)
        {
            PFPlayerDataManagementCustomPropertyDetails** array0 = arena.PointerArray<PFPlayerDataManagementCustomPropertyDetails>(Properties.Count);
            for (int i0 = 0; i0 < Properties.Count; i0++)
            {
                PFPlayerDataManagementCustomPropertyDetails* item0 = arena.Alloc<PFPlayerDataManagementCustomPropertyDetails>(1);
                Properties[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Properties = array0;
        }
        target->PropertiesVersion = PropertiesVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientUpdatePlayerCustomPropertiesRequest</c>.</summary>
public sealed class PlayerDataManagementClientUpdatePlayerCustomPropertiesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ExpectedPropertiesVersion</c>.</summary>
    public int? ExpectedPropertiesVersion { get; set; }

    /// <summary><c>Properties</c>.</summary>
    public IReadOnlyList<PlayerDataManagementUpdateProperty>? Properties { get; set; }

    internal static unsafe PlayerDataManagementClientUpdatePlayerCustomPropertiesRequest FromNative(PFPlayerDataManagementClientUpdatePlayerCustomPropertiesRequest* source)
    {
        var value = new PlayerDataManagementClientUpdatePlayerCustomPropertiesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ExpectedPropertiesVersion = source->ExpectedPropertiesVersion is null ? null : (int?)(*source->ExpectedPropertiesVersion);
        if (source->Properties is not null && source->PropertiesCount != 0)
        {
            var list3 = new List<PlayerDataManagementUpdateProperty>((int)source->PropertiesCount);
            for (uint i3 = 0; i3 < source->PropertiesCount; i3++)
            {
                list3.Add(PlayerDataManagementUpdateProperty.FromNative(source->Properties[i3]));
            }

            value.Properties = list3;
        }
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientUpdatePlayerCustomPropertiesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ExpectedPropertiesVersion = arena.Value(ExpectedPropertiesVersion);
        target->PropertiesCount = (uint)(Properties is null ? 0 : Properties.Count);
        if (Properties is not null && Properties.Count > 0)
        {
            PFPlayerDataManagementUpdateProperty** array3 = arena.PointerArray<PFPlayerDataManagementUpdateProperty>(Properties.Count);
            for (int i3 = 0; i3 < Properties.Count; i3++)
            {
                PFPlayerDataManagementUpdateProperty* item3 = arena.Alloc<PFPlayerDataManagementUpdateProperty>(1);
                Properties[i3].WriteTo(item3, arena);
                array3[i3] = item3;
            }

            target->Properties = array3;
        }
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientUpdatePlayerCustomPropertiesResult</c>.</summary>
public sealed class PlayerDataManagementClientUpdatePlayerCustomPropertiesResult
{
    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    internal static unsafe PlayerDataManagementClientUpdatePlayerCustomPropertiesResult FromNative(PFPlayerDataManagementClientUpdatePlayerCustomPropertiesResult* source)
    {
        var value = new PlayerDataManagementClientUpdatePlayerCustomPropertiesResult();
        value.PropertiesVersion = source->PropertiesVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientUpdatePlayerCustomPropertiesResult* target, PlayFabArena arena)
    {
        *target = default;
        target->PropertiesVersion = PropertiesVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementClientUpdateUserDataRequest</c>.</summary>
public sealed class PlayerDataManagementClientUpdateUserDataRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Data</c>.</summary>
    public IReadOnlyDictionary<string, string>? Data { get; set; }

    /// <summary><c>KeysToRemove</c>.</summary>
    public IReadOnlyList<string>? KeysToRemove { get; set; }

    /// <summary><c>Permission</c>.</summary>
    public UserDataPermission? Permission { get; set; }

    internal static unsafe PlayerDataManagementClientUpdateUserDataRequest FromNative(PFPlayerDataManagementClientUpdateUserDataRequest* source)
    {
        var value = new PlayerDataManagementClientUpdateUserDataRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        if (source->Data is not null && source->DataCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->DataCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->DataCount; i2++)
            {
                map2[Utf8.ToString(source->Data[i2].Key) ?? string.Empty] = Utf8.ToString(source->Data[i2].Value) ?? string.Empty;
            }

            value.Data = map2;
        }
        if (source->KeysToRemove is not null && source->KeysToRemoveCount != 0)
        {
            var list4 = new List<string>((int)source->KeysToRemoveCount);
            for (uint i4 = 0; i4 < source->KeysToRemoveCount; i4++)
            {
                list4.Add(Utf8.ToString(source->KeysToRemove[i4]) ?? string.Empty);
            }

            value.KeysToRemove = list4;
        }
        value.Permission = source->Permission is null ? null : (UserDataPermission?)(*source->Permission);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementClientUpdateUserDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(Data.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in Data)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->Data = entries2;
        }
        target->KeysToRemoveCount = (uint)(KeysToRemove is null ? 0 : KeysToRemove.Count);
        target->KeysToRemove = arena.StringArray(KeysToRemove);
        target->Permission = arena.Value((PFUserDataPermission?)Permission);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementCustomPropertyDetails</c>.</summary>
public sealed class PlayerDataManagementCustomPropertyDetails
{
    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    /// <summary><c>Value</c>.</summary>
    public string? Value { get; set; }

    internal static unsafe PlayerDataManagementCustomPropertyDetails FromNative(PFPlayerDataManagementCustomPropertyDetails* source)
    {
        var value = new PlayerDataManagementCustomPropertyDetails();
        value.Name = Utf8.ToString(source->Name);
        value.Value = Utf8.ToString(source->Value.StringValue);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementCustomPropertyDetails* target, PlayFabArena arena)
    {
        *target = default;
        target->Name = arena.String(Name);
        target->Value.StringValue = arena.String(Value);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementDeletedPropertyDetails</c>.</summary>
public sealed class PlayerDataManagementDeletedPropertyDetails
{
    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    /// <summary><c>WasDeleted</c>.</summary>
    public bool WasDeleted { get; set; }

    internal static unsafe PlayerDataManagementDeletedPropertyDetails FromNative(PFPlayerDataManagementDeletedPropertyDetails* source)
    {
        var value = new PlayerDataManagementDeletedPropertyDetails();
        value.Name = Utf8.ToString(source->Name);
        value.WasDeleted = source->WasDeleted != 0;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementDeletedPropertyDetails* target, PlayFabArena arena)
    {
        *target = default;
        target->Name = arena.String(Name);
        target->WasDeleted = WasDeleted ? (byte)1 : (byte)0;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementGetUserDataRequest</c>.</summary>
public sealed class PlayerDataManagementGetUserDataRequest
{
    /// <summary><c>IfChangedFromDataVersion</c>.</summary>
    public uint? IfChangedFromDataVersion { get; set; }

    /// <summary><c>Keys</c>.</summary>
    public IReadOnlyList<string>? Keys { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe PlayerDataManagementGetUserDataRequest FromNative(PFPlayerDataManagementGetUserDataRequest* source)
    {
        var value = new PlayerDataManagementGetUserDataRequest();
        value.IfChangedFromDataVersion = source->IfChangedFromDataVersion is null ? null : (uint?)(*source->IfChangedFromDataVersion);
        if (source->Keys is not null && source->KeysCount != 0)
        {
            var list1 = new List<string>((int)source->KeysCount);
            for (uint i1 = 0; i1 < source->KeysCount; i1++)
            {
                list1.Add(Utf8.ToString(source->Keys[i1]) ?? string.Empty);
            }

            value.Keys = list1;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementGetUserDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->IfChangedFromDataVersion = arena.Value(IfChangedFromDataVersion);
        target->KeysCount = (uint)(Keys is null ? 0 : Keys.Count);
        target->Keys = arena.StringArray(Keys);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementListPlayerCustomPropertiesRequest</c>.</summary>
public sealed class PlayerDataManagementListPlayerCustomPropertiesRequest
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe PlayerDataManagementListPlayerCustomPropertiesRequest FromNative(PFPlayerDataManagementListPlayerCustomPropertiesRequest* source)
    {
        var value = new PlayerDataManagementListPlayerCustomPropertiesRequest();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementListPlayerCustomPropertiesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerDeletePlayerCustomPropertiesRequest</c>.</summary>
public sealed class PlayerDataManagementServerDeletePlayerCustomPropertiesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ExpectedPropertiesVersion</c>.</summary>
    public int? ExpectedPropertiesVersion { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PropertyNames</c>.</summary>
    public IReadOnlyList<string>? PropertyNames { get; set; }

    internal static unsafe PlayerDataManagementServerDeletePlayerCustomPropertiesRequest FromNative(PFPlayerDataManagementServerDeletePlayerCustomPropertiesRequest* source)
    {
        var value = new PlayerDataManagementServerDeletePlayerCustomPropertiesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ExpectedPropertiesVersion = source->ExpectedPropertiesVersion is null ? null : (int?)(*source->ExpectedPropertiesVersion);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        if (source->PropertyNames is not null && source->PropertyNamesCount != 0)
        {
            var list4 = new List<string>((int)source->PropertyNamesCount);
            for (uint i4 = 0; i4 < source->PropertyNamesCount; i4++)
            {
                list4.Add(Utf8.ToString(source->PropertyNames[i4]) ?? string.Empty);
            }

            value.PropertyNames = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerDeletePlayerCustomPropertiesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ExpectedPropertiesVersion = arena.Value(ExpectedPropertiesVersion);
        target->PlayFabId = arena.String(PlayFabId);
        target->PropertyNamesCount = (uint)(PropertyNames is null ? 0 : PropertyNames.Count);
        target->PropertyNames = arena.StringArray(PropertyNames);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerDeletePlayerCustomPropertiesResult</c>.</summary>
public sealed class PlayerDataManagementServerDeletePlayerCustomPropertiesResult
{
    /// <summary><c>DeletedProperties</c>.</summary>
    public IReadOnlyList<PlayerDataManagementDeletedPropertyDetails>? DeletedProperties { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    internal static unsafe PlayerDataManagementServerDeletePlayerCustomPropertiesResult FromNative(PFPlayerDataManagementServerDeletePlayerCustomPropertiesResult* source)
    {
        var value = new PlayerDataManagementServerDeletePlayerCustomPropertiesResult();
        if (source->DeletedProperties is not null && source->DeletedPropertiesCount != 0)
        {
            var list0 = new List<PlayerDataManagementDeletedPropertyDetails>((int)source->DeletedPropertiesCount);
            for (uint i0 = 0; i0 < source->DeletedPropertiesCount; i0++)
            {
                list0.Add(PlayerDataManagementDeletedPropertyDetails.FromNative(source->DeletedProperties[i0]));
            }

            value.DeletedProperties = list0;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.PropertiesVersion = source->PropertiesVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerDeletePlayerCustomPropertiesResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DeletedPropertiesCount = (uint)(DeletedProperties is null ? 0 : DeletedProperties.Count);
        if (DeletedProperties is not null && DeletedProperties.Count > 0)
        {
            PFPlayerDataManagementDeletedPropertyDetails** array0 = arena.PointerArray<PFPlayerDataManagementDeletedPropertyDetails>(DeletedProperties.Count);
            for (int i0 = 0; i0 < DeletedProperties.Count; i0++)
            {
                PFPlayerDataManagementDeletedPropertyDetails* item0 = arena.Alloc<PFPlayerDataManagementDeletedPropertyDetails>(1);
                DeletedProperties[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->DeletedProperties = array0;
        }
        target->PlayFabId = arena.String(PlayFabId);
        target->PropertiesVersion = PropertiesVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerGetPlayerCustomPropertyRequest</c>.</summary>
public sealed class PlayerDataManagementServerGetPlayerCustomPropertyRequest
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PropertyName</c>.</summary>
    public string? PropertyName { get; set; }

    internal static unsafe PlayerDataManagementServerGetPlayerCustomPropertyRequest FromNative(PFPlayerDataManagementServerGetPlayerCustomPropertyRequest* source)
    {
        var value = new PlayerDataManagementServerGetPlayerCustomPropertyRequest();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.PropertyName = Utf8.ToString(source->PropertyName);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerGetPlayerCustomPropertyRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->PropertyName = arena.String(PropertyName);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerGetPlayerCustomPropertyResult</c>.</summary>
public sealed class PlayerDataManagementServerGetPlayerCustomPropertyResult
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    /// <summary><c>Property</c>.</summary>
    public PlayerDataManagementCustomPropertyDetails? Property { get; set; }

    internal static unsafe PlayerDataManagementServerGetPlayerCustomPropertyResult FromNative(PFPlayerDataManagementServerGetPlayerCustomPropertyResult* source)
    {
        var value = new PlayerDataManagementServerGetPlayerCustomPropertyResult();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.PropertiesVersion = source->PropertiesVersion;
        value.Property = source->Property is null ? null : PlayerDataManagementCustomPropertyDetails.FromNative(source->Property);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerGetPlayerCustomPropertyResult* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->PropertiesVersion = PropertiesVersion;
        if (Property is not null)
        {
            PFPlayerDataManagementCustomPropertyDetails* native2 = arena.Alloc<PFPlayerDataManagementCustomPropertyDetails>(1);
            Property.WriteTo(native2, arena);
            target->Property = native2;
        }
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerGetUserDataResult</c>.</summary>
public sealed class PlayerDataManagementServerGetUserDataResult
{
    /// <summary><c>Data</c>.</summary>
    public IReadOnlyDictionary<string, UserDataRecord>? Data { get; set; }

    /// <summary><c>DataVersion</c>.</summary>
    public uint DataVersion { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe PlayerDataManagementServerGetUserDataResult FromNative(PFPlayerDataManagementServerGetUserDataResult* source)
    {
        var value = new PlayerDataManagementServerGetUserDataResult();
        if (source->Data is not null && source->DataCount != 0)
        {
            var map0 = new Dictionary<string, UserDataRecord>((int)source->DataCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->DataCount; i0++)
            {
                map0[Utf8.ToString(source->Data[i0].Key) ?? string.Empty] = UserDataRecord.FromNative(source->Data[i0].Value);
            }

            value.Data = map0;
        }
        value.DataVersion = source->DataVersion;
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerGetUserDataResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFUserDataRecordDictionaryEntry* entries0 = arena.Alloc<PFUserDataRecordDictionaryEntry>(Data.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, UserDataRecord> pair0 in Data)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                if (pair0.Value is not null)
                {
                    PFUserDataRecord* item0 = arena.Alloc<PFUserDataRecord>(1);
                    pair0.Value.WriteTo(item0, arena);
                    entries0[next0].Value = item0;
                }
                next0++;
            }

            target->Data = entries0;
        }
        target->DataVersion = DataVersion;
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerListPlayerCustomPropertiesResult</c>.</summary>
public sealed class PlayerDataManagementServerListPlayerCustomPropertiesResult
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>Properties</c>.</summary>
    public IReadOnlyList<PlayerDataManagementCustomPropertyDetails>? Properties { get; set; }

    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    internal static unsafe PlayerDataManagementServerListPlayerCustomPropertiesResult FromNative(PFPlayerDataManagementServerListPlayerCustomPropertiesResult* source)
    {
        var value = new PlayerDataManagementServerListPlayerCustomPropertiesResult();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        if (source->Properties is not null && source->PropertiesCount != 0)
        {
            var list1 = new List<PlayerDataManagementCustomPropertyDetails>((int)source->PropertiesCount);
            for (uint i1 = 0; i1 < source->PropertiesCount; i1++)
            {
                list1.Add(PlayerDataManagementCustomPropertyDetails.FromNative(source->Properties[i1]));
            }

            value.Properties = list1;
        }
        value.PropertiesVersion = source->PropertiesVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerListPlayerCustomPropertiesResult* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->PropertiesCount = (uint)(Properties is null ? 0 : Properties.Count);
        if (Properties is not null && Properties.Count > 0)
        {
            PFPlayerDataManagementCustomPropertyDetails** array1 = arena.PointerArray<PFPlayerDataManagementCustomPropertyDetails>(Properties.Count);
            for (int i1 = 0; i1 < Properties.Count; i1++)
            {
                PFPlayerDataManagementCustomPropertyDetails* item1 = arena.Alloc<PFPlayerDataManagementCustomPropertyDetails>(1);
                Properties[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->Properties = array1;
        }
        target->PropertiesVersion = PropertiesVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerUpdatePlayerCustomPropertiesRequest</c>.</summary>
public sealed class PlayerDataManagementServerUpdatePlayerCustomPropertiesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>ExpectedPropertiesVersion</c>.</summary>
    public int? ExpectedPropertiesVersion { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>Properties</c>.</summary>
    public IReadOnlyList<PlayerDataManagementUpdateProperty>? Properties { get; set; }

    internal static unsafe PlayerDataManagementServerUpdatePlayerCustomPropertiesRequest FromNative(PFPlayerDataManagementServerUpdatePlayerCustomPropertiesRequest* source)
    {
        var value = new PlayerDataManagementServerUpdatePlayerCustomPropertiesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.ExpectedPropertiesVersion = source->ExpectedPropertiesVersion is null ? null : (int?)(*source->ExpectedPropertiesVersion);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        if (source->Properties is not null && source->PropertiesCount != 0)
        {
            var list4 = new List<PlayerDataManagementUpdateProperty>((int)source->PropertiesCount);
            for (uint i4 = 0; i4 < source->PropertiesCount; i4++)
            {
                list4.Add(PlayerDataManagementUpdateProperty.FromNative(source->Properties[i4]));
            }

            value.Properties = list4;
        }
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerUpdatePlayerCustomPropertiesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ExpectedPropertiesVersion = arena.Value(ExpectedPropertiesVersion);
        target->PlayFabId = arena.String(PlayFabId);
        target->PropertiesCount = (uint)(Properties is null ? 0 : Properties.Count);
        if (Properties is not null && Properties.Count > 0)
        {
            PFPlayerDataManagementUpdateProperty** array4 = arena.PointerArray<PFPlayerDataManagementUpdateProperty>(Properties.Count);
            for (int i4 = 0; i4 < Properties.Count; i4++)
            {
                PFPlayerDataManagementUpdateProperty* item4 = arena.Alloc<PFPlayerDataManagementUpdateProperty>(1);
                Properties[i4].WriteTo(item4, arena);
                array4[i4] = item4;
            }

            target->Properties = array4;
        }
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerUpdatePlayerCustomPropertiesResult</c>.</summary>
public sealed class PlayerDataManagementServerUpdatePlayerCustomPropertiesResult
{
    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    /// <summary><c>PropertiesVersion</c>.</summary>
    public int PropertiesVersion { get; set; }

    internal static unsafe PlayerDataManagementServerUpdatePlayerCustomPropertiesResult FromNative(PFPlayerDataManagementServerUpdatePlayerCustomPropertiesResult* source)
    {
        var value = new PlayerDataManagementServerUpdatePlayerCustomPropertiesResult();
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        value.PropertiesVersion = source->PropertiesVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerUpdatePlayerCustomPropertiesResult* target, PlayFabArena arena)
    {
        *target = default;
        target->PlayFabId = arena.String(PlayFabId);
        target->PropertiesVersion = PropertiesVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementServerUpdateUserDataRequest</c>.</summary>
public sealed class PlayerDataManagementServerUpdateUserDataRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Data</c>.</summary>
    public IReadOnlyDictionary<string, string>? Data { get; set; }

    /// <summary><c>KeysToRemove</c>.</summary>
    public IReadOnlyList<string>? KeysToRemove { get; set; }

    /// <summary><c>Permission</c>.</summary>
    public UserDataPermission? Permission { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe PlayerDataManagementServerUpdateUserDataRequest FromNative(PFPlayerDataManagementServerUpdateUserDataRequest* source)
    {
        var value = new PlayerDataManagementServerUpdateUserDataRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        if (source->Data is not null && source->DataCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->DataCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->DataCount; i2++)
            {
                map2[Utf8.ToString(source->Data[i2].Key) ?? string.Empty] = Utf8.ToString(source->Data[i2].Value) ?? string.Empty;
            }

            value.Data = map2;
        }
        if (source->KeysToRemove is not null && source->KeysToRemoveCount != 0)
        {
            var list4 = new List<string>((int)source->KeysToRemoveCount);
            for (uint i4 = 0; i4 < source->KeysToRemoveCount; i4++)
            {
                list4.Add(Utf8.ToString(source->KeysToRemove[i4]) ?? string.Empty);
            }

            value.KeysToRemove = list4;
        }
        value.Permission = source->Permission is null ? null : (UserDataPermission?)(*source->Permission);
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementServerUpdateUserDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(Data.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in Data)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->Data = entries2;
        }
        target->KeysToRemoveCount = (uint)(KeysToRemove is null ? 0 : KeysToRemove.Count);
        target->KeysToRemove = arena.StringArray(KeysToRemove);
        target->Permission = arena.Value((PFUserDataPermission?)Permission);
        target->PlayFabId = arena.String(PlayFabId);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementUpdateProperty</c>.</summary>
public sealed class PlayerDataManagementUpdateProperty
{
    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    /// <summary><c>Value</c>.</summary>
    public string? Value { get; set; }

    internal static unsafe PlayerDataManagementUpdateProperty FromNative(PFPlayerDataManagementUpdateProperty* source)
    {
        var value = new PlayerDataManagementUpdateProperty();
        value.Name = Utf8.ToString(source->Name);
        value.Value = Utf8.ToString(source->Value.StringValue);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementUpdateProperty* target, PlayFabArena arena)
    {
        *target = default;
        target->Name = arena.String(Name);
        target->Value.StringValue = arena.String(Value);
    }
}

/// <summary>Projects <c>PFPlayerDataManagementUpdateUserDataResult</c>.</summary>
public sealed class PlayerDataManagementUpdateUserDataResult
{
    /// <summary><c>DataVersion</c>.</summary>
    public uint DataVersion { get; set; }

    internal static unsafe PlayerDataManagementUpdateUserDataResult FromNative(PFPlayerDataManagementUpdateUserDataResult* source)
    {
        var value = new PlayerDataManagementUpdateUserDataResult();
        value.DataVersion = source->DataVersion;
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementUpdateUserDataResult* target, PlayFabArena arena)
    {
        *target = default;
        target->DataVersion = DataVersion;
    }
}

/// <summary>Projects <c>PFPlayerDataManagementUpdateUserInternalDataRequest</c>.</summary>
public sealed class PlayerDataManagementUpdateUserInternalDataRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Data</c>.</summary>
    public IReadOnlyDictionary<string, string>? Data { get; set; }

    /// <summary><c>KeysToRemove</c>.</summary>
    public IReadOnlyList<string>? KeysToRemove { get; set; }

    /// <summary><c>PlayFabId</c>.</summary>
    public string? PlayFabId { get; set; }

    internal static unsafe PlayerDataManagementUpdateUserInternalDataRequest FromNative(PFPlayerDataManagementUpdateUserInternalDataRequest* source)
    {
        var value = new PlayerDataManagementUpdateUserInternalDataRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        if (source->Data is not null && source->DataCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->DataCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->DataCount; i2++)
            {
                map2[Utf8.ToString(source->Data[i2].Key) ?? string.Empty] = Utf8.ToString(source->Data[i2].Value) ?? string.Empty;
            }

            value.Data = map2;
        }
        if (source->KeysToRemove is not null && source->KeysToRemoveCount != 0)
        {
            var list4 = new List<string>((int)source->KeysToRemoveCount);
            for (uint i4 = 0; i4 < source->KeysToRemoveCount; i4++)
            {
                list4.Add(Utf8.ToString(source->KeysToRemove[i4]) ?? string.Empty);
            }

            value.KeysToRemove = list4;
        }
        value.PlayFabId = Utf8.ToString(source->PlayFabId);
        return value;
    }

    internal unsafe void WriteTo(PFPlayerDataManagementUpdateUserInternalDataRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DataCount = (uint)(Data is null ? 0 : Data.Count);
        if (Data is not null && Data.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(Data.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in Data)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->Data = entries2;
        }
        target->KeysToRemoveCount = (uint)(KeysToRemove is null ? 0 : KeysToRemove.Count);
        target->KeysToRemove = arena.StringArray(KeysToRemove);
        target->PlayFabId = arena.String(PlayFabId);
    }
}
