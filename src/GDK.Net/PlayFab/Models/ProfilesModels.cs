// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFProfiles
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFProfilesEffectType</c>.</summary>
public enum ProfilesEffectType : uint
{
    /// <summary>Projects <c>PFProfilesEffectType::Allow</c>.</summary>
    Allow = 0,
    /// <summary>Projects <c>PFProfilesEffectType::Deny</c>.</summary>
    Deny = 1,
}

/// <summary>Projects <c>PFEntityStatisticValue</c>.</summary>
public sealed class EntityStatisticValue
{
    /// <summary><c>Metadata</c>.</summary>
    public string? Metadata { get; set; }

    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    /// <summary><c>Scores</c>.</summary>
    public IReadOnlyList<string>? Scores { get; set; }

    /// <summary><c>Version</c>.</summary>
    public int Version { get; set; }

    internal static unsafe EntityStatisticValue FromNative(PFEntityStatisticValue* source)
    {
        var value = new EntityStatisticValue();
        value.Metadata = Utf8.ToString(source->Metadata);
        value.Name = Utf8.ToString(source->Name);
        if (source->Scores is not null && source->ScoresCount != 0)
        {
            var list2 = new List<string>((int)source->ScoresCount);
            for (uint i2 = 0; i2 < source->ScoresCount; i2++)
            {
                list2.Add(Utf8.ToString(source->Scores[i2]) ?? string.Empty);
            }

            value.Scores = list2;
        }
        value.Version = source->Version;
        return value;
    }

    internal unsafe void WriteTo(PFEntityStatisticValue* target, PlayFabArena arena)
    {
        *target = default;
        target->Metadata = arena.String(Metadata);
        target->Name = arena.String(Name);
        target->ScoresCount = (uint)(Scores is null ? 0 : Scores.Count);
        target->Scores = arena.StringArray(Scores);
        target->Version = Version;
    }
}

/// <summary>Projects <c>PFProfilesEntityDataObject</c>.</summary>
public sealed class ProfilesEntityDataObject
{
    /// <summary><c>DataObject</c>.</summary>
    public string? DataObject { get; set; }

    /// <summary><c>EscapedDataObject</c>.</summary>
    public string? EscapedDataObject { get; set; }

    /// <summary><c>ObjectName</c>.</summary>
    public string? ObjectName { get; set; }

    internal static unsafe ProfilesEntityDataObject FromNative(PFProfilesEntityDataObject* source)
    {
        var value = new ProfilesEntityDataObject();
        value.DataObject = Utf8.ToString(source->DataObject.StringValue);
        value.EscapedDataObject = Utf8.ToString(source->EscapedDataObject);
        value.ObjectName = Utf8.ToString(source->ObjectName);
        return value;
    }

    internal unsafe void WriteTo(PFProfilesEntityDataObject* target, PlayFabArena arena)
    {
        *target = default;
        target->DataObject.StringValue = arena.String(DataObject);
        target->EscapedDataObject = arena.String(EscapedDataObject);
        target->ObjectName = arena.String(ObjectName);
    }
}

/// <summary>Projects <c>PFProfilesEntityPermissionStatement</c>.</summary>
public sealed class ProfilesEntityPermissionStatement
{
    /// <summary><c>Action</c>.</summary>
    public string? Action { get; set; }

    /// <summary><c>Comment</c>.</summary>
    public string? Comment { get; set; }

    /// <summary><c>Condition</c>.</summary>
    public string? Condition { get; set; }

    /// <summary><c>Effect</c>.</summary>
    public ProfilesEffectType Effect { get; set; }

    /// <summary><c>Principal</c>.</summary>
    public string? Principal { get; set; }

    /// <summary><c>Resource</c>.</summary>
    public string? Resource { get; set; }

    internal static unsafe ProfilesEntityPermissionStatement FromNative(PFProfilesEntityPermissionStatement* source)
    {
        var value = new ProfilesEntityPermissionStatement();
        value.Action = Utf8.ToString(source->Action);
        value.Comment = Utf8.ToString(source->Comment);
        value.Condition = Utf8.ToString(source->Condition.StringValue);
        value.Effect = (ProfilesEffectType)source->Effect;
        value.Principal = Utf8.ToString(source->Principal.StringValue);
        value.Resource = Utf8.ToString(source->Resource);
        return value;
    }

    internal unsafe void WriteTo(PFProfilesEntityPermissionStatement* target, PlayFabArena arena)
    {
        *target = default;
        target->Action = arena.String(Action);
        target->Comment = arena.String(Comment);
        target->Condition.StringValue = arena.String(Condition);
        target->Effect = (PFProfilesEffectType)Effect;
        target->Principal.StringValue = arena.String(Principal);
        target->Resource = arena.String(Resource);
    }
}

/// <summary>Projects <c>PFProfilesEntityProfileBody</c>.</summary>
public sealed class ProfilesEntityProfileBody
{
    /// <summary><c>AvatarUrl</c>.</summary>
    public string? AvatarUrl { get; set; }

    /// <summary><c>Created</c>.</summary>
    public DateTimeOffset Created { get; set; }

    /// <summary><c>DisplayName</c>.</summary>
    public string? DisplayName { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>EntityChain</c>.</summary>
    public string? EntityChain { get; set; }

    /// <summary><c>ExperimentVariants</c>.</summary>
    public IReadOnlyList<string>? ExperimentVariants { get; set; }

    /// <summary><c>Files</c>.</summary>
    public IReadOnlyDictionary<string, ProfilesEntityProfileFileMetadata>? Files { get; set; }

    /// <summary><c>Language</c>.</summary>
    public string? Language { get; set; }

    /// <summary><c>Lineage</c>.</summary>
    public EntityLineage? Lineage { get; set; }

    /// <summary><c>Objects</c>.</summary>
    public IReadOnlyDictionary<string, ProfilesEntityDataObject>? Objects { get; set; }

    /// <summary><c>Permissions</c>.</summary>
    public IReadOnlyList<ProfilesEntityPermissionStatement>? Permissions { get; set; }

    /// <summary><c>Statistics</c>.</summary>
    public IReadOnlyDictionary<string, EntityStatisticValue>? Statistics { get; set; }

    /// <summary><c>VersionNumber</c>.</summary>
    public int VersionNumber { get; set; }

    internal static unsafe ProfilesEntityProfileBody FromNative(PFProfilesEntityProfileBody* source)
    {
        var value = new ProfilesEntityProfileBody();
        value.AvatarUrl = Utf8.ToString(source->AvatarUrl);
        value.Created = PlayFabTime.ToDateTimeOffset(source->Created);
        value.DisplayName = Utf8.ToString(source->DisplayName);
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.EntityChain = Utf8.ToString(source->EntityChain);
        if (source->ExperimentVariants is not null && source->ExperimentVariantsCount != 0)
        {
            var list5 = new List<string>((int)source->ExperimentVariantsCount);
            for (uint i5 = 0; i5 < source->ExperimentVariantsCount; i5++)
            {
                list5.Add(Utf8.ToString(source->ExperimentVariants[i5]) ?? string.Empty);
            }

            value.ExperimentVariants = list5;
        }
        if (source->Files is not null && source->FilesCount != 0)
        {
            var map7 = new Dictionary<string, ProfilesEntityProfileFileMetadata>((int)source->FilesCount, StringComparer.Ordinal);
            for (uint i7 = 0; i7 < source->FilesCount; i7++)
            {
                map7[Utf8.ToString(source->Files[i7].Key) ?? string.Empty] = ProfilesEntityProfileFileMetadata.FromNative(source->Files[i7].Value);
            }

            value.Files = map7;
        }
        value.Language = Utf8.ToString(source->Language);
        value.Lineage = source->Lineage is null ? null : EntityLineage.FromNative(source->Lineage);
        if (source->Objects is not null && source->ObjectsCount != 0)
        {
            var map11 = new Dictionary<string, ProfilesEntityDataObject>((int)source->ObjectsCount, StringComparer.Ordinal);
            for (uint i11 = 0; i11 < source->ObjectsCount; i11++)
            {
                map11[Utf8.ToString(source->Objects[i11].Key) ?? string.Empty] = ProfilesEntityDataObject.FromNative(source->Objects[i11].Value);
            }

            value.Objects = map11;
        }
        if (source->Permissions is not null && source->PermissionsCount != 0)
        {
            var list13 = new List<ProfilesEntityPermissionStatement>((int)source->PermissionsCount);
            for (uint i13 = 0; i13 < source->PermissionsCount; i13++)
            {
                list13.Add(ProfilesEntityPermissionStatement.FromNative(source->Permissions[i13]));
            }

            value.Permissions = list13;
        }
        if (source->Statistics is not null && source->StatisticsCount != 0)
        {
            var map15 = new Dictionary<string, EntityStatisticValue>((int)source->StatisticsCount, StringComparer.Ordinal);
            for (uint i15 = 0; i15 < source->StatisticsCount; i15++)
            {
                map15[Utf8.ToString(source->Statistics[i15].Key) ?? string.Empty] = EntityStatisticValue.FromNative(source->Statistics[i15].Value);
            }

            value.Statistics = map15;
        }
        value.VersionNumber = source->VersionNumber;
        return value;
    }

    internal unsafe void WriteTo(PFProfilesEntityProfileBody* target, PlayFabArena arena)
    {
        *target = default;
        target->AvatarUrl = arena.String(AvatarUrl);
        target->Created = PlayFabTime.ToUnixTime(Created);
        target->DisplayName = arena.String(DisplayName);
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->EntityChain = arena.String(EntityChain);
        target->ExperimentVariantsCount = (uint)(ExperimentVariants is null ? 0 : ExperimentVariants.Count);
        target->ExperimentVariants = arena.StringArray(ExperimentVariants);
        target->FilesCount = (uint)(Files is null ? 0 : Files.Count);
        if (Files is not null && Files.Count > 0)
        {
            PFProfilesEntityProfileFileMetadataDictionaryEntry* entries7 = arena.Alloc<PFProfilesEntityProfileFileMetadataDictionaryEntry>(Files.Count);
            int next7 = 0;
            foreach (KeyValuePair<string, ProfilesEntityProfileFileMetadata> pair7 in Files)
            {
                entries7[next7].Key = arena.String(pair7.Key);
                if (pair7.Value is not null)
                {
                    PFProfilesEntityProfileFileMetadata* item7 = arena.Alloc<PFProfilesEntityProfileFileMetadata>(1);
                    pair7.Value.WriteTo(item7, arena);
                    entries7[next7].Value = item7;
                }
                next7++;
            }

            target->Files = entries7;
        }
        target->Language = arena.String(Language);
        if (Lineage is not null)
        {
            PFEntityLineage* native10 = arena.Alloc<PFEntityLineage>(1);
            Lineage.WriteTo(native10, arena);
            target->Lineage = native10;
        }
        target->ObjectsCount = (uint)(Objects is null ? 0 : Objects.Count);
        if (Objects is not null && Objects.Count > 0)
        {
            PFProfilesEntityDataObjectDictionaryEntry* entries11 = arena.Alloc<PFProfilesEntityDataObjectDictionaryEntry>(Objects.Count);
            int next11 = 0;
            foreach (KeyValuePair<string, ProfilesEntityDataObject> pair11 in Objects)
            {
                entries11[next11].Key = arena.String(pair11.Key);
                if (pair11.Value is not null)
                {
                    PFProfilesEntityDataObject* item11 = arena.Alloc<PFProfilesEntityDataObject>(1);
                    pair11.Value.WriteTo(item11, arena);
                    entries11[next11].Value = item11;
                }
                next11++;
            }

            target->Objects = entries11;
        }
        target->PermissionsCount = (uint)(Permissions is null ? 0 : Permissions.Count);
        if (Permissions is not null && Permissions.Count > 0)
        {
            PFProfilesEntityPermissionStatement** array13 = arena.PointerArray<PFProfilesEntityPermissionStatement>(Permissions.Count);
            for (int i13 = 0; i13 < Permissions.Count; i13++)
            {
                PFProfilesEntityPermissionStatement* item13 = arena.Alloc<PFProfilesEntityPermissionStatement>(1);
                Permissions[i13].WriteTo(item13, arena);
                array13[i13] = item13;
            }

            target->Permissions = array13;
        }
        target->StatisticsCount = (uint)(Statistics is null ? 0 : Statistics.Count);
        if (Statistics is not null && Statistics.Count > 0)
        {
            PFEntityStatisticValueDictionaryEntry* entries15 = arena.Alloc<PFEntityStatisticValueDictionaryEntry>(Statistics.Count);
            int next15 = 0;
            foreach (KeyValuePair<string, EntityStatisticValue> pair15 in Statistics)
            {
                entries15[next15].Key = arena.String(pair15.Key);
                if (pair15.Value is not null)
                {
                    PFEntityStatisticValue* item15 = arena.Alloc<PFEntityStatisticValue>(1);
                    pair15.Value.WriteTo(item15, arena);
                    entries15[next15].Value = item15;
                }
                next15++;
            }

            target->Statistics = entries15;
        }
        target->VersionNumber = VersionNumber;
    }
}

/// <summary>Projects <c>PFProfilesEntityProfileFileMetadata</c>.</summary>
public sealed class ProfilesEntityProfileFileMetadata
{
    /// <summary><c>Checksum</c>.</summary>
    public string? Checksum { get; set; }

    /// <summary><c>FileName</c>.</summary>
    public string? FileName { get; set; }

    /// <summary><c>LastModified</c>.</summary>
    public DateTimeOffset LastModified { get; set; }

    /// <summary><c>Size</c>.</summary>
    public int Size { get; set; }

    internal static unsafe ProfilesEntityProfileFileMetadata FromNative(PFProfilesEntityProfileFileMetadata* source)
    {
        var value = new ProfilesEntityProfileFileMetadata();
        value.Checksum = Utf8.ToString(source->Checksum);
        value.FileName = Utf8.ToString(source->FileName);
        value.LastModified = PlayFabTime.ToDateTimeOffset(source->LastModified);
        value.Size = source->Size;
        return value;
    }

    internal unsafe void WriteTo(PFProfilesEntityProfileFileMetadata* target, PlayFabArena arena)
    {
        *target = default;
        target->Checksum = arena.String(Checksum);
        target->FileName = arena.String(FileName);
        target->LastModified = PlayFabTime.ToUnixTime(LastModified);
        target->Size = Size;
    }
}

/// <summary>Projects <c>PFProfilesGetEntityProfileRequest</c>.</summary>
public sealed class ProfilesGetEntityProfileRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DataAsObject</c>.</summary>
    public bool? DataAsObject { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    internal static unsafe ProfilesGetEntityProfileRequest FromNative(PFProfilesGetEntityProfileRequest* source)
    {
        var value = new ProfilesGetEntityProfileRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.DataAsObject = source->DataAsObject is null ? null : (bool?)(*source->DataAsObject != 0);
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFProfilesGetEntityProfileRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DataAsObject = arena.Value(DataAsObject is null ? null : (DataAsObject.Value ? (byte?)1 : (byte?)0));
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
    }
}

/// <summary>Projects <c>PFProfilesGetEntityProfileResponse</c>.</summary>
public sealed class ProfilesGetEntityProfileResponse
{
    /// <summary><c>Profile</c>.</summary>
    public ProfilesEntityProfileBody? Profile { get; set; }

    internal static unsafe ProfilesGetEntityProfileResponse FromNative(PFProfilesGetEntityProfileResponse* source)
    {
        var value = new ProfilesGetEntityProfileResponse();
        value.Profile = source->Profile is null ? null : ProfilesEntityProfileBody.FromNative(source->Profile);
        return value;
    }

    internal unsafe void WriteTo(PFProfilesGetEntityProfileResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Profile is not null)
        {
            PFProfilesEntityProfileBody* native0 = arena.Alloc<PFProfilesEntityProfileBody>(1);
            Profile.WriteTo(native0, arena);
            target->Profile = native0;
        }
    }
}

/// <summary>Projects <c>PFProfilesGetEntityProfilesRequest</c>.</summary>
public sealed class ProfilesGetEntityProfilesRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>DataAsObject</c>.</summary>
    public bool? DataAsObject { get; set; }

    /// <summary><c>Entities</c>.</summary>
    public IReadOnlyList<EntityKey>? Entities { get; set; }

    internal static unsafe ProfilesGetEntityProfilesRequest FromNative(PFProfilesGetEntityProfilesRequest* source)
    {
        var value = new ProfilesGetEntityProfilesRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.DataAsObject = source->DataAsObject is null ? null : (bool?)(*source->DataAsObject != 0);
        if (source->Entities is not null && source->EntitiesCount != 0)
        {
            var list3 = new List<EntityKey>((int)source->EntitiesCount);
            for (uint i3 = 0; i3 < source->EntitiesCount; i3++)
            {
                list3.Add(EntityKey.FromNative(source->Entities[i3]));
            }

            value.Entities = list3;
        }
        return value;
    }

    internal unsafe void WriteTo(PFProfilesGetEntityProfilesRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->DataAsObject = arena.Value(DataAsObject is null ? null : (DataAsObject.Value ? (byte?)1 : (byte?)0));
        target->EntitiesCount = (uint)(Entities is null ? 0 : Entities.Count);
        if (Entities is not null && Entities.Count > 0)
        {
            PFEntityKey** array3 = arena.PointerArray<PFEntityKey>(Entities.Count);
            for (int i3 = 0; i3 < Entities.Count; i3++)
            {
                PFEntityKey* item3 = arena.Alloc<PFEntityKey>(1);
                Entities[i3].WriteTo(item3, arena);
                array3[i3] = item3;
            }

            target->Entities = array3;
        }
    }
}

/// <summary>Projects <c>PFProfilesGetEntityProfilesResponse</c>.</summary>
public sealed class ProfilesGetEntityProfilesResponse
{
    /// <summary><c>Profiles</c>.</summary>
    public IReadOnlyList<ProfilesEntityProfileBody>? Profiles { get; set; }

    internal static unsafe ProfilesGetEntityProfilesResponse FromNative(PFProfilesGetEntityProfilesResponse* source)
    {
        var value = new ProfilesGetEntityProfilesResponse();
        if (source->Profiles is not null && source->ProfilesCount != 0)
        {
            var list0 = new List<ProfilesEntityProfileBody>((int)source->ProfilesCount);
            for (uint i0 = 0; i0 < source->ProfilesCount; i0++)
            {
                list0.Add(ProfilesEntityProfileBody.FromNative(source->Profiles[i0]));
            }

            value.Profiles = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFProfilesGetEntityProfilesResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ProfilesCount = (uint)(Profiles is null ? 0 : Profiles.Count);
        if (Profiles is not null && Profiles.Count > 0)
        {
            PFProfilesEntityProfileBody** array0 = arena.PointerArray<PFProfilesEntityProfileBody>(Profiles.Count);
            for (int i0 = 0; i0 < Profiles.Count; i0++)
            {
                PFProfilesEntityProfileBody* item0 = arena.Alloc<PFProfilesEntityProfileBody>(1);
                Profiles[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Profiles = array0;
        }
    }
}

/// <summary>Projects <c>PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest</c>.</summary>
public sealed class ProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>MasterPlayerAccountIds</c>.</summary>
    public IReadOnlyList<string>? MasterPlayerAccountIds { get; set; }

    /// <summary><c>TitleId</c>.</summary>
    public string? TitleId { get; set; }

    internal static unsafe ProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest FromNative(PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest* source)
    {
        var value = new ProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        if (source->MasterPlayerAccountIds is not null && source->MasterPlayerAccountIdsCount != 0)
        {
            var list2 = new List<string>((int)source->MasterPlayerAccountIdsCount);
            for (uint i2 = 0; i2 < source->MasterPlayerAccountIdsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->MasterPlayerAccountIds[i2]) ?? string.Empty);
            }

            value.MasterPlayerAccountIds = list2;
        }
        value.TitleId = Utf8.ToString(source->TitleId);
        return value;
    }

    internal unsafe void WriteTo(PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->MasterPlayerAccountIdsCount = (uint)(MasterPlayerAccountIds is null ? 0 : MasterPlayerAccountIds.Count);
        target->MasterPlayerAccountIds = arena.StringArray(MasterPlayerAccountIds);
        target->TitleId = arena.String(TitleId);
    }
}

/// <summary>Projects <c>PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse</c>.</summary>
public sealed class ProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse
{
    /// <summary><c>TitleId</c>.</summary>
    public string? TitleId { get; set; }

    /// <summary><c>TitlePlayerAccounts</c>.</summary>
    public IReadOnlyDictionary<string, EntityKey>? TitlePlayerAccounts { get; set; }

    internal static unsafe ProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse FromNative(PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse* source)
    {
        var value = new ProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse();
        value.TitleId = Utf8.ToString(source->TitleId);
        if (source->TitlePlayerAccounts is not null && source->TitlePlayerAccountsCount != 0)
        {
            var map1 = new Dictionary<string, EntityKey>((int)source->TitlePlayerAccountsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->TitlePlayerAccountsCount; i1++)
            {
                map1[Utf8.ToString(source->TitlePlayerAccounts[i1].Key) ?? string.Empty] = EntityKey.FromNative(source->TitlePlayerAccounts[i1].Value);
            }

            value.TitlePlayerAccounts = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->TitleId = arena.String(TitleId);
        target->TitlePlayerAccountsCount = (uint)(TitlePlayerAccounts is null ? 0 : TitlePlayerAccounts.Count);
        if (TitlePlayerAccounts is not null && TitlePlayerAccounts.Count > 0)
        {
            PFEntityKeyDictionaryEntry* entries1 = arena.Alloc<PFEntityKeyDictionaryEntry>(TitlePlayerAccounts.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, EntityKey> pair1 in TitlePlayerAccounts)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                if (pair1.Value is not null)
                {
                    PFEntityKey* item1 = arena.Alloc<PFEntityKey>(1);
                    pair1.Value.WriteTo(item1, arena);
                    entries1[next1].Value = item1;
                }
                next1++;
            }

            target->TitlePlayerAccounts = entries1;
        }
    }
}

/// <summary>Projects <c>PFProfilesSetEntityProfilePolicyRequest</c>.</summary>
public sealed class ProfilesSetEntityProfilePolicyRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Statements</c>.</summary>
    public IReadOnlyList<ProfilesEntityPermissionStatement>? Statements { get; set; }

    internal static unsafe ProfilesSetEntityProfilePolicyRequest FromNative(PFProfilesSetEntityProfilePolicyRequest* source)
    {
        var value = new ProfilesSetEntityProfilePolicyRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->Statements is not null && source->StatementsCount != 0)
        {
            var list3 = new List<ProfilesEntityPermissionStatement>((int)source->StatementsCount);
            for (uint i3 = 0; i3 < source->StatementsCount; i3++)
            {
                list3.Add(ProfilesEntityPermissionStatement.FromNative(source->Statements[i3]));
            }

            value.Statements = list3;
        }
        return value;
    }

    internal unsafe void WriteTo(PFProfilesSetEntityProfilePolicyRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->StatementsCount = (uint)(Statements is null ? 0 : Statements.Count);
        if (Statements is not null && Statements.Count > 0)
        {
            PFProfilesEntityPermissionStatement** array3 = arena.PointerArray<PFProfilesEntityPermissionStatement>(Statements.Count);
            for (int i3 = 0; i3 < Statements.Count; i3++)
            {
                PFProfilesEntityPermissionStatement* item3 = arena.Alloc<PFProfilesEntityPermissionStatement>(1);
                Statements[i3].WriteTo(item3, arena);
                array3[i3] = item3;
            }

            target->Statements = array3;
        }
    }
}

/// <summary>Projects <c>PFProfilesSetEntityProfilePolicyResponse</c>.</summary>
public sealed class ProfilesSetEntityProfilePolicyResponse
{
    /// <summary><c>Permissions</c>.</summary>
    public IReadOnlyList<ProfilesEntityPermissionStatement>? Permissions { get; set; }

    internal static unsafe ProfilesSetEntityProfilePolicyResponse FromNative(PFProfilesSetEntityProfilePolicyResponse* source)
    {
        var value = new ProfilesSetEntityProfilePolicyResponse();
        if (source->Permissions is not null && source->PermissionsCount != 0)
        {
            var list0 = new List<ProfilesEntityPermissionStatement>((int)source->PermissionsCount);
            for (uint i0 = 0; i0 < source->PermissionsCount; i0++)
            {
                list0.Add(ProfilesEntityPermissionStatement.FromNative(source->Permissions[i0]));
            }

            value.Permissions = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFProfilesSetEntityProfilePolicyResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->PermissionsCount = (uint)(Permissions is null ? 0 : Permissions.Count);
        if (Permissions is not null && Permissions.Count > 0)
        {
            PFProfilesEntityPermissionStatement** array0 = arena.PointerArray<PFProfilesEntityPermissionStatement>(Permissions.Count);
            for (int i0 = 0; i0 < Permissions.Count; i0++)
            {
                PFProfilesEntityPermissionStatement* item0 = arena.Alloc<PFProfilesEntityPermissionStatement>(1);
                Permissions[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Permissions = array0;
        }
    }
}

/// <summary>Projects <c>PFProfilesSetProfileLanguageRequest</c>.</summary>
public sealed class ProfilesSetProfileLanguageRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ExpectedVersion</c>.</summary>
    public int? ExpectedVersion { get; set; }

    /// <summary><c>Language</c>.</summary>
    public string? Language { get; set; }

    internal static unsafe ProfilesSetProfileLanguageRequest FromNative(PFProfilesSetProfileLanguageRequest* source)
    {
        var value = new ProfilesSetProfileLanguageRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ExpectedVersion = source->ExpectedVersion is null ? null : (int?)(*source->ExpectedVersion);
        value.Language = Utf8.ToString(source->Language);
        return value;
    }

    internal unsafe void WriteTo(PFProfilesSetProfileLanguageRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
        target->ExpectedVersion = arena.Value(ExpectedVersion);
        target->Language = arena.String(Language);
    }
}

/// <summary>Projects <c>PFProfilesSetProfileLanguageResponse</c>.</summary>
public sealed class ProfilesSetProfileLanguageResponse
{
    /// <summary><c>OperationResult</c>.</summary>
    public OperationTypes? OperationResult { get; set; }

    /// <summary><c>VersionNumber</c>.</summary>
    public int? VersionNumber { get; set; }

    internal static unsafe ProfilesSetProfileLanguageResponse FromNative(PFProfilesSetProfileLanguageResponse* source)
    {
        var value = new ProfilesSetProfileLanguageResponse();
        value.OperationResult = source->OperationResult is null ? null : (OperationTypes?)(*source->OperationResult);
        value.VersionNumber = source->VersionNumber is null ? null : (int?)(*source->VersionNumber);
        return value;
    }

    internal unsafe void WriteTo(PFProfilesSetProfileLanguageResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->OperationResult = arena.Value((PFOperationTypes?)OperationResult);
        target->VersionNumber = arena.Value(VersionNumber);
    }
}
