// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFCatalog
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>Projects <c>PFCatalogPublishResult</c>.</summary>
public enum CatalogPublishResult : uint
{
    /// <summary>Projects <c>PFCatalogPublishResult::Unknown</c>.</summary>
    Unknown = 0,
    /// <summary>Projects <c>PFCatalogPublishResult::Pending</c>.</summary>
    Pending = 1,
    /// <summary>Projects <c>PFCatalogPublishResult::Succeeded</c>.</summary>
    Succeeded = 2,
    /// <summary>Projects <c>PFCatalogPublishResult::Failed</c>.</summary>
    Failed = 3,
    /// <summary>Projects <c>PFCatalogPublishResult::Canceled</c>.</summary>
    Canceled = 4,
}

/// <summary>Projects <c>PFCatalogModerationStatus</c>.</summary>
public enum CatalogModerationStatus : uint
{
    /// <summary>Projects <c>PFCatalogModerationStatus::Unknown</c>.</summary>
    Unknown = 0,
    /// <summary>Projects <c>PFCatalogModerationStatus::AwaitingModeration</c>.</summary>
    AwaitingModeration = 1,
    /// <summary>Projects <c>PFCatalogModerationStatus::Approved</c>.</summary>
    Approved = 2,
    /// <summary>Projects <c>PFCatalogModerationStatus::Rejected</c>.</summary>
    Rejected = 3,
}

/// <summary>Projects <c>PFCatalogHelpfulnessVote</c>.</summary>
public enum CatalogHelpfulnessVote : uint
{
    /// <summary>Projects <c>PFCatalogHelpfulnessVote::None</c>.</summary>
    None = 0,
    /// <summary>Projects <c>PFCatalogHelpfulnessVote::UnHelpful</c>.</summary>
    UnHelpful = 1,
    /// <summary>Projects <c>PFCatalogHelpfulnessVote::Helpful</c>.</summary>
    Helpful = 2,
}

/// <summary>Projects <c>PFCatalogDisplayPropertyType</c>.</summary>
public enum CatalogDisplayPropertyType : uint
{
    /// <summary>Projects <c>PFCatalogDisplayPropertyType::None</c>.</summary>
    None = 0,
    /// <summary>Projects <c>PFCatalogDisplayPropertyType::QueryDateTime</c>.</summary>
    QueryDateTime = 1,
    /// <summary>Projects <c>PFCatalogDisplayPropertyType::QueryDouble</c>.</summary>
    QueryDouble = 2,
    /// <summary>Projects <c>PFCatalogDisplayPropertyType::QueryString</c>.</summary>
    QueryString = 3,
    /// <summary>Projects <c>PFCatalogDisplayPropertyType::SearchString</c>.</summary>
    SearchString = 4,
}

/// <summary>Projects <c>PFCatalogConcernCategory</c>.</summary>
public enum CatalogConcernCategory : uint
{
    /// <summary>Projects <c>PFCatalogConcernCategory::None</c>.</summary>
    None = 0,
    /// <summary>Projects <c>PFCatalogConcernCategory::OffensiveContent</c>.</summary>
    OffensiveContent = 1,
    /// <summary>Projects <c>PFCatalogConcernCategory::ChildExploitation</c>.</summary>
    ChildExploitation = 2,
    /// <summary>Projects <c>PFCatalogConcernCategory::MalwareOrVirus</c>.</summary>
    MalwareOrVirus = 3,
    /// <summary>Projects <c>PFCatalogConcernCategory::PrivacyConcerns</c>.</summary>
    PrivacyConcerns = 4,
    /// <summary>Projects <c>PFCatalogConcernCategory::MisleadingApp</c>.</summary>
    MisleadingApp = 5,
    /// <summary>Projects <c>PFCatalogConcernCategory::PoorPerformance</c>.</summary>
    PoorPerformance = 6,
    /// <summary>Projects <c>PFCatalogConcernCategory::ReviewResponse</c>.</summary>
    ReviewResponse = 7,
    /// <summary>Projects <c>PFCatalogConcernCategory::SpamAdvertising</c>.</summary>
    SpamAdvertising = 8,
    /// <summary>Projects <c>PFCatalogConcernCategory::Profanity</c>.</summary>
    Profanity = 9,
}

/// <summary>Projects <c>PFCatalogCatalogAlternateId</c>.</summary>
public sealed class CatalogCatalogAlternateId
{
    /// <summary><c>Type</c>.</summary>
    public string? Type { get; set; }

    /// <summary><c>Value</c>.</summary>
    public string? Value { get; set; }

    internal static unsafe CatalogCatalogAlternateId FromNative(PFCatalogCatalogAlternateId* source)
    {
        var value = new CatalogCatalogAlternateId();
        value.Type = Utf8.ToString(source->Type);
        value.Value = Utf8.ToString(source->Value);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogAlternateId* target, PlayFabArena arena)
    {
        *target = default;
        target->Type = arena.String(Type);
        target->Value = arena.String(Value);
    }
}

/// <summary>Projects <c>PFCatalogCatalogConfig</c>.</summary>
public sealed class CatalogCatalogConfig
{
    /// <summary><c>AdminEntities</c>.</summary>
    public IReadOnlyList<EntityKey>? AdminEntities { get; set; }

    /// <summary><c>Catalog</c>.</summary>
    public CatalogCatalogSpecificConfig? Catalog { get; set; }

    /// <summary><c>DeepLinkFormats</c>.</summary>
    public IReadOnlyList<CatalogDeepLinkFormat>? DeepLinkFormats { get; set; }

    /// <summary><c>DisplayPropertyIndexInfos</c>.</summary>
    public IReadOnlyList<CatalogDisplayPropertyIndexInfo>? DisplayPropertyIndexInfos { get; set; }

    /// <summary><c>File</c>.</summary>
    public CatalogFileConfig? File { get; set; }

    /// <summary><c>Image</c>.</summary>
    public CatalogImageConfig? Image { get; set; }

    /// <summary><c>IsCatalogEnabled</c>.</summary>
    public bool IsCatalogEnabled { get; set; }

    /// <summary><c>Platforms</c>.</summary>
    public IReadOnlyList<string>? Platforms { get; set; }

    /// <summary><c>Review</c>.</summary>
    public CatalogReviewConfig? Review { get; set; }

    /// <summary><c>ReviewerEntities</c>.</summary>
    public IReadOnlyList<EntityKey>? ReviewerEntities { get; set; }

    /// <summary><c>UserGeneratedContent</c>.</summary>
    public CatalogUserGeneratedContentSpecificConfig? UserGeneratedContent { get; set; }

    internal static unsafe CatalogCatalogConfig FromNative(PFCatalogCatalogConfig* source)
    {
        var value = new CatalogCatalogConfig();
        if (source->AdminEntities is not null && source->AdminEntitiesCount != 0)
        {
            var list0 = new List<EntityKey>((int)source->AdminEntitiesCount);
            for (uint i0 = 0; i0 < source->AdminEntitiesCount; i0++)
            {
                list0.Add(EntityKey.FromNative(source->AdminEntities[i0]));
            }

            value.AdminEntities = list0;
        }
        value.Catalog = source->Catalog is null ? null : CatalogCatalogSpecificConfig.FromNative(source->Catalog);
        if (source->DeepLinkFormats is not null && source->DeepLinkFormatsCount != 0)
        {
            var list3 = new List<CatalogDeepLinkFormat>((int)source->DeepLinkFormatsCount);
            for (uint i3 = 0; i3 < source->DeepLinkFormatsCount; i3++)
            {
                list3.Add(CatalogDeepLinkFormat.FromNative(source->DeepLinkFormats[i3]));
            }

            value.DeepLinkFormats = list3;
        }
        if (source->DisplayPropertyIndexInfos is not null && source->DisplayPropertyIndexInfosCount != 0)
        {
            var list5 = new List<CatalogDisplayPropertyIndexInfo>((int)source->DisplayPropertyIndexInfosCount);
            for (uint i5 = 0; i5 < source->DisplayPropertyIndexInfosCount; i5++)
            {
                list5.Add(CatalogDisplayPropertyIndexInfo.FromNative(source->DisplayPropertyIndexInfos[i5]));
            }

            value.DisplayPropertyIndexInfos = list5;
        }
        value.File = source->File is null ? null : CatalogFileConfig.FromNative(source->File);
        value.Image = source->Image is null ? null : CatalogImageConfig.FromNative(source->Image);
        value.IsCatalogEnabled = source->IsCatalogEnabled != 0;
        if (source->Platforms is not null && source->PlatformsCount != 0)
        {
            var list10 = new List<string>((int)source->PlatformsCount);
            for (uint i10 = 0; i10 < source->PlatformsCount; i10++)
            {
                list10.Add(Utf8.ToString(source->Platforms[i10]) ?? string.Empty);
            }

            value.Platforms = list10;
        }
        value.Review = source->Review is null ? null : CatalogReviewConfig.FromNative(source->Review);
        if (source->ReviewerEntities is not null && source->ReviewerEntitiesCount != 0)
        {
            var list13 = new List<EntityKey>((int)source->ReviewerEntitiesCount);
            for (uint i13 = 0; i13 < source->ReviewerEntitiesCount; i13++)
            {
                list13.Add(EntityKey.FromNative(source->ReviewerEntities[i13]));
            }

            value.ReviewerEntities = list13;
        }
        value.UserGeneratedContent = source->UserGeneratedContent is null ? null : CatalogUserGeneratedContentSpecificConfig.FromNative(source->UserGeneratedContent);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogConfig* target, PlayFabArena arena)
    {
        *target = default;
        target->AdminEntitiesCount = (uint)(AdminEntities is null ? 0 : AdminEntities.Count);
        if (AdminEntities is not null && AdminEntities.Count > 0)
        {
            PFEntityKey** array0 = arena.PointerArray<PFEntityKey>(AdminEntities.Count);
            for (int i0 = 0; i0 < AdminEntities.Count; i0++)
            {
                PFEntityKey* item0 = arena.Alloc<PFEntityKey>(1);
                AdminEntities[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->AdminEntities = array0;
        }
        if (Catalog is not null)
        {
            PFCatalogCatalogSpecificConfig* native2 = arena.Alloc<PFCatalogCatalogSpecificConfig>(1);
            Catalog.WriteTo(native2, arena);
            target->Catalog = native2;
        }
        target->DeepLinkFormatsCount = (uint)(DeepLinkFormats is null ? 0 : DeepLinkFormats.Count);
        if (DeepLinkFormats is not null && DeepLinkFormats.Count > 0)
        {
            PFCatalogDeepLinkFormat** array3 = arena.PointerArray<PFCatalogDeepLinkFormat>(DeepLinkFormats.Count);
            for (int i3 = 0; i3 < DeepLinkFormats.Count; i3++)
            {
                PFCatalogDeepLinkFormat* item3 = arena.Alloc<PFCatalogDeepLinkFormat>(1);
                DeepLinkFormats[i3].WriteTo(item3, arena);
                array3[i3] = item3;
            }

            target->DeepLinkFormats = array3;
        }
        target->DisplayPropertyIndexInfosCount = (uint)(DisplayPropertyIndexInfos is null ? 0 : DisplayPropertyIndexInfos.Count);
        if (DisplayPropertyIndexInfos is not null && DisplayPropertyIndexInfos.Count > 0)
        {
            PFCatalogDisplayPropertyIndexInfo** array5 = arena.PointerArray<PFCatalogDisplayPropertyIndexInfo>(DisplayPropertyIndexInfos.Count);
            for (int i5 = 0; i5 < DisplayPropertyIndexInfos.Count; i5++)
            {
                PFCatalogDisplayPropertyIndexInfo* item5 = arena.Alloc<PFCatalogDisplayPropertyIndexInfo>(1);
                DisplayPropertyIndexInfos[i5].WriteTo(item5, arena);
                array5[i5] = item5;
            }

            target->DisplayPropertyIndexInfos = array5;
        }
        if (File is not null)
        {
            PFCatalogFileConfig* native7 = arena.Alloc<PFCatalogFileConfig>(1);
            File.WriteTo(native7, arena);
            target->File = native7;
        }
        if (Image is not null)
        {
            PFCatalogImageConfig* native8 = arena.Alloc<PFCatalogImageConfig>(1);
            Image.WriteTo(native8, arena);
            target->Image = native8;
        }
        target->IsCatalogEnabled = IsCatalogEnabled ? (byte)1 : (byte)0;
        target->PlatformsCount = (uint)(Platforms is null ? 0 : Platforms.Count);
        target->Platforms = arena.StringArray(Platforms);
        if (Review is not null)
        {
            PFCatalogReviewConfig* native12 = arena.Alloc<PFCatalogReviewConfig>(1);
            Review.WriteTo(native12, arena);
            target->Review = native12;
        }
        target->ReviewerEntitiesCount = (uint)(ReviewerEntities is null ? 0 : ReviewerEntities.Count);
        if (ReviewerEntities is not null && ReviewerEntities.Count > 0)
        {
            PFEntityKey** array13 = arena.PointerArray<PFEntityKey>(ReviewerEntities.Count);
            for (int i13 = 0; i13 < ReviewerEntities.Count; i13++)
            {
                PFEntityKey* item13 = arena.Alloc<PFEntityKey>(1);
                ReviewerEntities[i13].WriteTo(item13, arena);
                array13[i13] = item13;
            }

            target->ReviewerEntities = array13;
        }
        if (UserGeneratedContent is not null)
        {
            PFCatalogUserGeneratedContentSpecificConfig* native15 = arena.Alloc<PFCatalogUserGeneratedContentSpecificConfig>(1);
            UserGeneratedContent.WriteTo(native15, arena);
            target->UserGeneratedContent = native15;
        }
    }
}

/// <summary>Projects <c>PFCatalogCatalogItem</c>.</summary>
public sealed class CatalogCatalogItem
{
    /// <summary><c>AlternateIds</c>.</summary>
    public IReadOnlyList<CatalogCatalogAlternateId>? AlternateIds { get; set; }

    /// <summary><c>Contents</c>.</summary>
    public IReadOnlyList<CatalogContent>? Contents { get; set; }

    /// <summary><c>ContentType</c>.</summary>
    public string? ContentType { get; set; }

    /// <summary><c>CreationDate</c>.</summary>
    public DateTimeOffset? CreationDate { get; set; }

    /// <summary><c>CreatorEntity</c>.</summary>
    public EntityKey? CreatorEntity { get; set; }

    /// <summary><c>DeepLinks</c>.</summary>
    public IReadOnlyList<CatalogDeepLink>? DeepLinks { get; set; }

    /// <summary><c>DefaultStackId</c>.</summary>
    public string? DefaultStackId { get; set; }

    /// <summary><c>Description</c>.</summary>
    public IReadOnlyDictionary<string, string>? Description { get; set; }

    /// <summary><c>DisplayProperties</c>.</summary>
    public string? DisplayProperties { get; set; }

    /// <summary><c>DisplayVersion</c>.</summary>
    public string? DisplayVersion { get; set; }

    /// <summary><c>EndDate</c>.</summary>
    public DateTimeOffset? EndDate { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Images</c>.</summary>
    public IReadOnlyList<CatalogImage>? Images { get; set; }

    /// <summary><c>IsHidden</c>.</summary>
    public bool? IsHidden { get; set; }

    /// <summary><c>ItemReferences</c>.</summary>
    public IReadOnlyList<CatalogCatalogItemReference>? ItemReferences { get; set; }

    /// <summary><c>Keywords</c>.</summary>
    public IReadOnlyDictionary<string, CatalogKeywordSet>? Keywords { get; set; }

    /// <summary><c>LastModifiedDate</c>.</summary>
    public DateTimeOffset? LastModifiedDate { get; set; }

    /// <summary><c>Moderation</c>.</summary>
    public CatalogModerationState? Moderation { get; set; }

    /// <summary><c>Platforms</c>.</summary>
    public IReadOnlyList<string>? Platforms { get; set; }

    /// <summary><c>PriceOptions</c>.</summary>
    public CatalogCatalogPriceOptions? PriceOptions { get; set; }

    /// <summary><c>Rating</c>.</summary>
    public CatalogRating? Rating { get; set; }

    /// <summary><c>RealMoneyPriceDetails</c>.</summary>
    public CatalogRealMoneyPriceDetails? RealMoneyPriceDetails { get; set; }

    /// <summary><c>StartDate</c>.</summary>
    public DateTimeOffset? StartDate { get; set; }

    /// <summary><c>StoreDetails</c>.</summary>
    public CatalogStoreDetails? StoreDetails { get; set; }

    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    /// <summary><c>Title</c>.</summary>
    public IReadOnlyDictionary<string, string>? Title { get; set; }

    /// <summary><c>Type</c>.</summary>
    public string? Type { get; set; }

    internal static unsafe CatalogCatalogItem FromNative(PFCatalogCatalogItem* source)
    {
        var value = new CatalogCatalogItem();
        if (source->AlternateIds is not null && source->AlternateIdsCount != 0)
        {
            var list0 = new List<CatalogCatalogAlternateId>((int)source->AlternateIdsCount);
            for (uint i0 = 0; i0 < source->AlternateIdsCount; i0++)
            {
                list0.Add(CatalogCatalogAlternateId.FromNative(source->AlternateIds[i0]));
            }

            value.AlternateIds = list0;
        }
        if (source->Contents is not null && source->ContentsCount != 0)
        {
            var list2 = new List<CatalogContent>((int)source->ContentsCount);
            for (uint i2 = 0; i2 < source->ContentsCount; i2++)
            {
                list2.Add(CatalogContent.FromNative(source->Contents[i2]));
            }

            value.Contents = list2;
        }
        value.ContentType = Utf8.ToString(source->ContentType);
        value.CreationDate = source->CreationDate is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->CreationDate);
        value.CreatorEntity = source->CreatorEntity is null ? null : EntityKey.FromNative(source->CreatorEntity);
        if (source->DeepLinks is not null && source->DeepLinksCount != 0)
        {
            var list7 = new List<CatalogDeepLink>((int)source->DeepLinksCount);
            for (uint i7 = 0; i7 < source->DeepLinksCount; i7++)
            {
                list7.Add(CatalogDeepLink.FromNative(source->DeepLinks[i7]));
            }

            value.DeepLinks = list7;
        }
        value.DefaultStackId = Utf8.ToString(source->DefaultStackId);
        if (source->Description is not null && source->DescriptionCount != 0)
        {
            var map10 = new Dictionary<string, string>((int)source->DescriptionCount, StringComparer.Ordinal);
            for (uint i10 = 0; i10 < source->DescriptionCount; i10++)
            {
                map10[Utf8.ToString(source->Description[i10].Key) ?? string.Empty] = Utf8.ToString(source->Description[i10].Value) ?? string.Empty;
            }

            value.Description = map10;
        }
        value.DisplayProperties = Utf8.ToString(source->DisplayProperties.StringValue);
        value.DisplayVersion = Utf8.ToString(source->DisplayVersion);
        value.EndDate = source->EndDate is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->EndDate);
        value.ETag = Utf8.ToString(source->ETag);
        value.Id = Utf8.ToString(source->Id);
        if (source->Images is not null && source->ImagesCount != 0)
        {
            var list17 = new List<CatalogImage>((int)source->ImagesCount);
            for (uint i17 = 0; i17 < source->ImagesCount; i17++)
            {
                list17.Add(CatalogImage.FromNative(source->Images[i17]));
            }

            value.Images = list17;
        }
        value.IsHidden = source->IsHidden is null ? null : (bool?)(*source->IsHidden != 0);
        if (source->ItemReferences is not null && source->ItemReferencesCount != 0)
        {
            var list20 = new List<CatalogCatalogItemReference>((int)source->ItemReferencesCount);
            for (uint i20 = 0; i20 < source->ItemReferencesCount; i20++)
            {
                list20.Add(CatalogCatalogItemReference.FromNative(source->ItemReferences[i20]));
            }

            value.ItemReferences = list20;
        }
        if (source->Keywords is not null && source->KeywordsCount != 0)
        {
            var map22 = new Dictionary<string, CatalogKeywordSet>((int)source->KeywordsCount, StringComparer.Ordinal);
            for (uint i22 = 0; i22 < source->KeywordsCount; i22++)
            {
                map22[Utf8.ToString(source->Keywords[i22].Key) ?? string.Empty] = CatalogKeywordSet.FromNative(source->Keywords[i22].Value);
            }

            value.Keywords = map22;
        }
        value.LastModifiedDate = source->LastModifiedDate is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->LastModifiedDate);
        value.Moderation = source->Moderation is null ? null : CatalogModerationState.FromNative(source->Moderation);
        if (source->Platforms is not null && source->PlatformsCount != 0)
        {
            var list26 = new List<string>((int)source->PlatformsCount);
            for (uint i26 = 0; i26 < source->PlatformsCount; i26++)
            {
                list26.Add(Utf8.ToString(source->Platforms[i26]) ?? string.Empty);
            }

            value.Platforms = list26;
        }
        value.PriceOptions = source->PriceOptions is null ? null : CatalogCatalogPriceOptions.FromNative(source->PriceOptions);
        value.Rating = source->Rating is null ? null : CatalogRating.FromNative(source->Rating);
        value.RealMoneyPriceDetails = source->RealMoneyPriceDetails is null ? null : CatalogRealMoneyPriceDetails.FromNative(source->RealMoneyPriceDetails);
        value.StartDate = source->StartDate is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->StartDate);
        value.StoreDetails = source->StoreDetails is null ? null : CatalogStoreDetails.FromNative(source->StoreDetails);
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list33 = new List<string>((int)source->TagsCount);
            for (uint i33 = 0; i33 < source->TagsCount; i33++)
            {
                list33.Add(Utf8.ToString(source->Tags[i33]) ?? string.Empty);
            }

            value.Tags = list33;
        }
        if (source->Title is not null && source->TitleCount != 0)
        {
            var map35 = new Dictionary<string, string>((int)source->TitleCount, StringComparer.Ordinal);
            for (uint i35 = 0; i35 < source->TitleCount; i35++)
            {
                map35[Utf8.ToString(source->Title[i35].Key) ?? string.Empty] = Utf8.ToString(source->Title[i35].Value) ?? string.Empty;
            }

            value.Title = map35;
        }
        value.Type = Utf8.ToString(source->Type);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogItem* target, PlayFabArena arena)
    {
        *target = default;
        target->AlternateIdsCount = (uint)(AlternateIds is null ? 0 : AlternateIds.Count);
        if (AlternateIds is not null && AlternateIds.Count > 0)
        {
            PFCatalogCatalogAlternateId** array0 = arena.PointerArray<PFCatalogCatalogAlternateId>(AlternateIds.Count);
            for (int i0 = 0; i0 < AlternateIds.Count; i0++)
            {
                PFCatalogCatalogAlternateId* item0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
                AlternateIds[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->AlternateIds = array0;
        }
        target->ContentsCount = (uint)(Contents is null ? 0 : Contents.Count);
        if (Contents is not null && Contents.Count > 0)
        {
            PFCatalogContent** array2 = arena.PointerArray<PFCatalogContent>(Contents.Count);
            for (int i2 = 0; i2 < Contents.Count; i2++)
            {
                PFCatalogContent* item2 = arena.Alloc<PFCatalogContent>(1);
                Contents[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Contents = array2;
        }
        target->ContentType = arena.String(ContentType);
        target->CreationDate = arena.Value(CreationDate is null ? null : (long?)PlayFabTime.ToUnixTime(CreationDate.Value));
        if (CreatorEntity is not null)
        {
            PFEntityKey* native6 = arena.Alloc<PFEntityKey>(1);
            CreatorEntity.WriteTo(native6, arena);
            target->CreatorEntity = native6;
        }
        target->DeepLinksCount = (uint)(DeepLinks is null ? 0 : DeepLinks.Count);
        if (DeepLinks is not null && DeepLinks.Count > 0)
        {
            PFCatalogDeepLink** array7 = arena.PointerArray<PFCatalogDeepLink>(DeepLinks.Count);
            for (int i7 = 0; i7 < DeepLinks.Count; i7++)
            {
                PFCatalogDeepLink* item7 = arena.Alloc<PFCatalogDeepLink>(1);
                DeepLinks[i7].WriteTo(item7, arena);
                array7[i7] = item7;
            }

            target->DeepLinks = array7;
        }
        target->DefaultStackId = arena.String(DefaultStackId);
        target->DescriptionCount = (uint)(Description is null ? 0 : Description.Count);
        if (Description is not null && Description.Count > 0)
        {
            PFStringDictionaryEntry* entries10 = arena.Alloc<PFStringDictionaryEntry>(Description.Count);
            int next10 = 0;
            foreach (KeyValuePair<string, string> pair10 in Description)
            {
                entries10[next10].Key = arena.String(pair10.Key);
                entries10[next10].Value = arena.String(pair10.Value);
                next10++;
            }

            target->Description = entries10;
        }
        target->DisplayProperties.StringValue = arena.String(DisplayProperties);
        target->DisplayVersion = arena.String(DisplayVersion);
        target->EndDate = arena.Value(EndDate is null ? null : (long?)PlayFabTime.ToUnixTime(EndDate.Value));
        target->ETag = arena.String(ETag);
        target->Id = arena.String(Id);
        target->ImagesCount = (uint)(Images is null ? 0 : Images.Count);
        if (Images is not null && Images.Count > 0)
        {
            PFCatalogImage** array17 = arena.PointerArray<PFCatalogImage>(Images.Count);
            for (int i17 = 0; i17 < Images.Count; i17++)
            {
                PFCatalogImage* item17 = arena.Alloc<PFCatalogImage>(1);
                Images[i17].WriteTo(item17, arena);
                array17[i17] = item17;
            }

            target->Images = array17;
        }
        target->IsHidden = arena.Value(IsHidden is null ? null : (IsHidden.Value ? (byte?)1 : (byte?)0));
        target->ItemReferencesCount = (uint)(ItemReferences is null ? 0 : ItemReferences.Count);
        if (ItemReferences is not null && ItemReferences.Count > 0)
        {
            PFCatalogCatalogItemReference** array20 = arena.PointerArray<PFCatalogCatalogItemReference>(ItemReferences.Count);
            for (int i20 = 0; i20 < ItemReferences.Count; i20++)
            {
                PFCatalogCatalogItemReference* item20 = arena.Alloc<PFCatalogCatalogItemReference>(1);
                ItemReferences[i20].WriteTo(item20, arena);
                array20[i20] = item20;
            }

            target->ItemReferences = array20;
        }
        target->KeywordsCount = (uint)(Keywords is null ? 0 : Keywords.Count);
        if (Keywords is not null && Keywords.Count > 0)
        {
            PFCatalogKeywordSetDictionaryEntry* entries22 = arena.Alloc<PFCatalogKeywordSetDictionaryEntry>(Keywords.Count);
            int next22 = 0;
            foreach (KeyValuePair<string, CatalogKeywordSet> pair22 in Keywords)
            {
                entries22[next22].Key = arena.String(pair22.Key);
                if (pair22.Value is not null)
                {
                    PFCatalogKeywordSet* item22 = arena.Alloc<PFCatalogKeywordSet>(1);
                    pair22.Value.WriteTo(item22, arena);
                    entries22[next22].Value = item22;
                }
                next22++;
            }

            target->Keywords = entries22;
        }
        target->LastModifiedDate = arena.Value(LastModifiedDate is null ? null : (long?)PlayFabTime.ToUnixTime(LastModifiedDate.Value));
        if (Moderation is not null)
        {
            PFCatalogModerationState* native25 = arena.Alloc<PFCatalogModerationState>(1);
            Moderation.WriteTo(native25, arena);
            target->Moderation = native25;
        }
        target->PlatformsCount = (uint)(Platforms is null ? 0 : Platforms.Count);
        target->Platforms = arena.StringArray(Platforms);
        if (PriceOptions is not null)
        {
            PFCatalogCatalogPriceOptions* native28 = arena.Alloc<PFCatalogCatalogPriceOptions>(1);
            PriceOptions.WriteTo(native28, arena);
            target->PriceOptions = native28;
        }
        if (Rating is not null)
        {
            PFCatalogRating* native29 = arena.Alloc<PFCatalogRating>(1);
            Rating.WriteTo(native29, arena);
            target->Rating = native29;
        }
        if (RealMoneyPriceDetails is not null)
        {
            PFCatalogRealMoneyPriceDetails* native30 = arena.Alloc<PFCatalogRealMoneyPriceDetails>(1);
            RealMoneyPriceDetails.WriteTo(native30, arena);
            target->RealMoneyPriceDetails = native30;
        }
        target->StartDate = arena.Value(StartDate is null ? null : (long?)PlayFabTime.ToUnixTime(StartDate.Value));
        if (StoreDetails is not null)
        {
            PFCatalogStoreDetails* native32 = arena.Alloc<PFCatalogStoreDetails>(1);
            StoreDetails.WriteTo(native32, arena);
            target->StoreDetails = native32;
        }
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
        target->TitleCount = (uint)(Title is null ? 0 : Title.Count);
        if (Title is not null && Title.Count > 0)
        {
            PFStringDictionaryEntry* entries35 = arena.Alloc<PFStringDictionaryEntry>(Title.Count);
            int next35 = 0;
            foreach (KeyValuePair<string, string> pair35 in Title)
            {
                entries35[next35].Key = arena.String(pair35.Key);
                entries35[next35].Value = arena.String(pair35.Value);
                next35++;
            }

            target->Title = entries35;
        }
        target->Type = arena.String(Type);
    }
}

/// <summary>Projects <c>PFCatalogCatalogItemReference</c>.</summary>
public sealed class CatalogCatalogItemReference
{
    /// <summary><c>Amount</c>.</summary>
    public int? Amount { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>PriceOptions</c>.</summary>
    public CatalogCatalogPriceOptions? PriceOptions { get; set; }

    internal static unsafe CatalogCatalogItemReference FromNative(PFCatalogCatalogItemReference* source)
    {
        var value = new CatalogCatalogItemReference();
        value.Amount = source->Amount is null ? null : (int?)(*source->Amount);
        value.Id = Utf8.ToString(source->Id);
        value.PriceOptions = source->PriceOptions is null ? null : CatalogCatalogPriceOptions.FromNative(source->PriceOptions);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogItemReference* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = arena.Value(Amount);
        target->Id = arena.String(Id);
        if (PriceOptions is not null)
        {
            PFCatalogCatalogPriceOptions* native2 = arena.Alloc<PFCatalogCatalogPriceOptions>(1);
            PriceOptions.WriteTo(native2, arena);
            target->PriceOptions = native2;
        }
    }
}

/// <summary>Projects <c>PFCatalogCatalogPrice</c>.</summary>
public sealed class CatalogCatalogPrice
{
    /// <summary><c>Amounts</c>.</summary>
    public IReadOnlyList<CatalogCatalogPriceAmount>? Amounts { get; set; }

    /// <summary><c>UnitAmount</c>.</summary>
    public int? UnitAmount { get; set; }

    /// <summary><c>UnitDurationInSeconds</c>.</summary>
    public double? UnitDurationInSeconds { get; set; }

    internal static unsafe CatalogCatalogPrice FromNative(PFCatalogCatalogPrice* source)
    {
        var value = new CatalogCatalogPrice();
        if (source->Amounts is not null && source->AmountsCount != 0)
        {
            var list0 = new List<CatalogCatalogPriceAmount>((int)source->AmountsCount);
            for (uint i0 = 0; i0 < source->AmountsCount; i0++)
            {
                list0.Add(CatalogCatalogPriceAmount.FromNative(source->Amounts[i0]));
            }

            value.Amounts = list0;
        }
        value.UnitAmount = source->UnitAmount is null ? null : (int?)(*source->UnitAmount);
        value.UnitDurationInSeconds = source->UnitDurationInSeconds is null ? null : (double?)(*source->UnitDurationInSeconds);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogPrice* target, PlayFabArena arena)
    {
        *target = default;
        target->AmountsCount = (uint)(Amounts is null ? 0 : Amounts.Count);
        if (Amounts is not null && Amounts.Count > 0)
        {
            PFCatalogCatalogPriceAmount** array0 = arena.PointerArray<PFCatalogCatalogPriceAmount>(Amounts.Count);
            for (int i0 = 0; i0 < Amounts.Count; i0++)
            {
                PFCatalogCatalogPriceAmount* item0 = arena.Alloc<PFCatalogCatalogPriceAmount>(1);
                Amounts[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Amounts = array0;
        }
        target->UnitAmount = arena.Value(UnitAmount);
        target->UnitDurationInSeconds = arena.Value(UnitDurationInSeconds);
    }
}

/// <summary>Projects <c>PFCatalogCatalogPriceAmount</c>.</summary>
public sealed class CatalogCatalogPriceAmount
{
    /// <summary><c>Amount</c>.</summary>
    public int Amount { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    internal static unsafe CatalogCatalogPriceAmount FromNative(PFCatalogCatalogPriceAmount* source)
    {
        var value = new CatalogCatalogPriceAmount();
        value.Amount = source->Amount;
        value.ItemId = Utf8.ToString(source->ItemId);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogPriceAmount* target, PlayFabArena arena)
    {
        *target = default;
        target->Amount = Amount;
        target->ItemId = arena.String(ItemId);
    }
}

/// <summary>Projects <c>PFCatalogCatalogPriceAmountOverride</c>.</summary>
public sealed class CatalogCatalogPriceAmountOverride
{
    /// <summary><c>FixedValue</c>.</summary>
    public int? FixedValue { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>Multiplier</c>.</summary>
    public double? Multiplier { get; set; }

    internal static unsafe CatalogCatalogPriceAmountOverride FromNative(PFCatalogCatalogPriceAmountOverride* source)
    {
        var value = new CatalogCatalogPriceAmountOverride();
        value.FixedValue = source->FixedValue is null ? null : (int?)(*source->FixedValue);
        value.ItemId = Utf8.ToString(source->ItemId);
        value.Multiplier = source->Multiplier is null ? null : (double?)(*source->Multiplier);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogPriceAmountOverride* target, PlayFabArena arena)
    {
        *target = default;
        target->FixedValue = arena.Value(FixedValue);
        target->ItemId = arena.String(ItemId);
        target->Multiplier = arena.Value(Multiplier);
    }
}

/// <summary>Projects <c>PFCatalogCatalogPriceOptions</c>.</summary>
public sealed class CatalogCatalogPriceOptions
{
    /// <summary><c>Prices</c>.</summary>
    public IReadOnlyList<CatalogCatalogPrice>? Prices { get; set; }

    internal static unsafe CatalogCatalogPriceOptions FromNative(PFCatalogCatalogPriceOptions* source)
    {
        var value = new CatalogCatalogPriceOptions();
        if (source->Prices is not null && source->PricesCount != 0)
        {
            var list0 = new List<CatalogCatalogPrice>((int)source->PricesCount);
            for (uint i0 = 0; i0 < source->PricesCount; i0++)
            {
                list0.Add(CatalogCatalogPrice.FromNative(source->Prices[i0]));
            }

            value.Prices = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogPriceOptions* target, PlayFabArena arena)
    {
        *target = default;
        target->PricesCount = (uint)(Prices is null ? 0 : Prices.Count);
        if (Prices is not null && Prices.Count > 0)
        {
            PFCatalogCatalogPrice** array0 = arena.PointerArray<PFCatalogCatalogPrice>(Prices.Count);
            for (int i0 = 0; i0 < Prices.Count; i0++)
            {
                PFCatalogCatalogPrice* item0 = arena.Alloc<PFCatalogCatalogPrice>(1);
                Prices[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Prices = array0;
        }
    }
}

/// <summary>Projects <c>PFCatalogCatalogPriceOptionsOverride</c>.</summary>
public sealed class CatalogCatalogPriceOptionsOverride
{
    /// <summary><c>Prices</c>.</summary>
    public IReadOnlyList<CatalogCatalogPriceOverride>? Prices { get; set; }

    internal static unsafe CatalogCatalogPriceOptionsOverride FromNative(PFCatalogCatalogPriceOptionsOverride* source)
    {
        var value = new CatalogCatalogPriceOptionsOverride();
        if (source->Prices is not null && source->PricesCount != 0)
        {
            var list0 = new List<CatalogCatalogPriceOverride>((int)source->PricesCount);
            for (uint i0 = 0; i0 < source->PricesCount; i0++)
            {
                list0.Add(CatalogCatalogPriceOverride.FromNative(source->Prices[i0]));
            }

            value.Prices = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogPriceOptionsOverride* target, PlayFabArena arena)
    {
        *target = default;
        target->PricesCount = (uint)(Prices is null ? 0 : Prices.Count);
        if (Prices is not null && Prices.Count > 0)
        {
            PFCatalogCatalogPriceOverride** array0 = arena.PointerArray<PFCatalogCatalogPriceOverride>(Prices.Count);
            for (int i0 = 0; i0 < Prices.Count; i0++)
            {
                PFCatalogCatalogPriceOverride* item0 = arena.Alloc<PFCatalogCatalogPriceOverride>(1);
                Prices[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Prices = array0;
        }
    }
}

/// <summary>Projects <c>PFCatalogCatalogPriceOverride</c>.</summary>
public sealed class CatalogCatalogPriceOverride
{
    /// <summary><c>Amounts</c>.</summary>
    public IReadOnlyList<CatalogCatalogPriceAmountOverride>? Amounts { get; set; }

    internal static unsafe CatalogCatalogPriceOverride FromNative(PFCatalogCatalogPriceOverride* source)
    {
        var value = new CatalogCatalogPriceOverride();
        if (source->Amounts is not null && source->AmountsCount != 0)
        {
            var list0 = new List<CatalogCatalogPriceAmountOverride>((int)source->AmountsCount);
            for (uint i0 = 0; i0 < source->AmountsCount; i0++)
            {
                list0.Add(CatalogCatalogPriceAmountOverride.FromNative(source->Amounts[i0]));
            }

            value.Amounts = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogPriceOverride* target, PlayFabArena arena)
    {
        *target = default;
        target->AmountsCount = (uint)(Amounts is null ? 0 : Amounts.Count);
        if (Amounts is not null && Amounts.Count > 0)
        {
            PFCatalogCatalogPriceAmountOverride** array0 = arena.PointerArray<PFCatalogCatalogPriceAmountOverride>(Amounts.Count);
            for (int i0 = 0; i0 < Amounts.Count; i0++)
            {
                PFCatalogCatalogPriceAmountOverride* item0 = arena.Alloc<PFCatalogCatalogPriceAmountOverride>(1);
                Amounts[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Amounts = array0;
        }
    }
}

/// <summary>Projects <c>PFCatalogCatalogSpecificConfig</c>.</summary>
public sealed class CatalogCatalogSpecificConfig
{
    /// <summary><c>ContentTypes</c>.</summary>
    public IReadOnlyList<string>? ContentTypes { get; set; }

    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    internal static unsafe CatalogCatalogSpecificConfig FromNative(PFCatalogCatalogSpecificConfig* source)
    {
        var value = new CatalogCatalogSpecificConfig();
        if (source->ContentTypes is not null && source->ContentTypesCount != 0)
        {
            var list0 = new List<string>((int)source->ContentTypesCount);
            for (uint i0 = 0; i0 < source->ContentTypesCount; i0++)
            {
                list0.Add(Utf8.ToString(source->ContentTypes[i0]) ?? string.Empty);
            }

            value.ContentTypes = list0;
        }
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list2 = new List<string>((int)source->TagsCount);
            for (uint i2 = 0; i2 < source->TagsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->Tags[i2]) ?? string.Empty);
            }

            value.Tags = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCatalogSpecificConfig* target, PlayFabArena arena)
    {
        *target = default;
        target->ContentTypesCount = (uint)(ContentTypes is null ? 0 : ContentTypes.Count);
        target->ContentTypes = arena.StringArray(ContentTypes);
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
    }
}

/// <summary>Projects <c>PFCatalogCategoryRatingConfig</c>.</summary>
public sealed class CatalogCategoryRatingConfig
{
    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    internal static unsafe CatalogCategoryRatingConfig FromNative(PFCatalogCategoryRatingConfig* source)
    {
        var value = new CatalogCategoryRatingConfig();
        value.Name = Utf8.ToString(source->Name);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCategoryRatingConfig* target, PlayFabArena arena)
    {
        *target = default;
        target->Name = arena.String(Name);
    }
}

/// <summary>Projects <c>PFCatalogContent</c>.</summary>
public sealed class CatalogContent
{
    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>MaxClientVersion</c>.</summary>
    public string? MaxClientVersion { get; set; }

    /// <summary><c>MinClientVersion</c>.</summary>
    public string? MinClientVersion { get; set; }

    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    /// <summary><c>Type</c>.</summary>
    public string? Type { get; set; }

    /// <summary><c>Url</c>.</summary>
    public string? Url { get; set; }

    internal static unsafe CatalogContent FromNative(PFCatalogContent* source)
    {
        var value = new CatalogContent();
        value.Id = Utf8.ToString(source->Id);
        value.MaxClientVersion = Utf8.ToString(source->MaxClientVersion);
        value.MinClientVersion = Utf8.ToString(source->MinClientVersion);
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list3 = new List<string>((int)source->TagsCount);
            for (uint i3 = 0; i3 < source->TagsCount; i3++)
            {
                list3.Add(Utf8.ToString(source->Tags[i3]) ?? string.Empty);
            }

            value.Tags = list3;
        }
        value.Type = Utf8.ToString(source->Type);
        value.Url = Utf8.ToString(source->Url);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogContent* target, PlayFabArena arena)
    {
        *target = default;
        target->Id = arena.String(Id);
        target->MaxClientVersion = arena.String(MaxClientVersion);
        target->MinClientVersion = arena.String(MinClientVersion);
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
        target->Type = arena.String(Type);
        target->Url = arena.String(Url);
    }
}

/// <summary>Projects <c>PFCatalogCreateDraftItemRequest</c>.</summary>
public sealed class CatalogCreateDraftItemRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Item</c>.</summary>
    public CatalogCatalogItem? Item { get; set; }

    /// <summary><c>Publish</c>.</summary>
    public bool Publish { get; set; }

    internal static unsafe CatalogCreateDraftItemRequest FromNative(PFCatalogCreateDraftItemRequest* source)
    {
        var value = new CatalogCreateDraftItemRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Item = source->Item is null ? null : CatalogCatalogItem.FromNative(source->Item);
        value.Publish = source->Publish != 0;
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCreateDraftItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Item is not null)
        {
            PFCatalogCatalogItem* native2 = arena.Alloc<PFCatalogCatalogItem>(1);
            Item.WriteTo(native2, arena);
            target->Item = native2;
        }
        target->Publish = Publish ? (byte)1 : (byte)0;
    }
}

/// <summary>Projects <c>PFCatalogCreateDraftItemResponse</c>.</summary>
public sealed class CatalogCreateDraftItemResponse
{
    /// <summary><c>Item</c>.</summary>
    public CatalogCatalogItem? Item { get; set; }

    internal static unsafe CatalogCreateDraftItemResponse FromNative(PFCatalogCreateDraftItemResponse* source)
    {
        var value = new CatalogCreateDraftItemResponse();
        value.Item = source->Item is null ? null : CatalogCatalogItem.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCreateDraftItemResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Item is not null)
        {
            PFCatalogCatalogItem* native0 = arena.Alloc<PFCatalogCatalogItem>(1);
            Item.WriteTo(native0, arena);
            target->Item = native0;
        }
    }
}

/// <summary>Projects <c>PFCatalogCreateUploadUrlsRequest</c>.</summary>
public sealed class CatalogCreateUploadUrlsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Files</c>.</summary>
    public IReadOnlyList<CatalogUploadInfo>? Files { get; set; }

    internal static unsafe CatalogCreateUploadUrlsRequest FromNative(PFCatalogCreateUploadUrlsRequest* source)
    {
        var value = new CatalogCreateUploadUrlsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        if (source->Files is not null && source->FilesCount != 0)
        {
            var list2 = new List<CatalogUploadInfo>((int)source->FilesCount);
            for (uint i2 = 0; i2 < source->FilesCount; i2++)
            {
                list2.Add(CatalogUploadInfo.FromNative(source->Files[i2]));
            }

            value.Files = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCreateUploadUrlsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->FilesCount = (uint)(Files is null ? 0 : Files.Count);
        if (Files is not null && Files.Count > 0)
        {
            PFCatalogUploadInfo** array2 = arena.PointerArray<PFCatalogUploadInfo>(Files.Count);
            for (int i2 = 0; i2 < Files.Count; i2++)
            {
                PFCatalogUploadInfo* item2 = arena.Alloc<PFCatalogUploadInfo>(1);
                Files[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Files = array2;
        }
    }
}

/// <summary>Projects <c>PFCatalogCreateUploadUrlsResponse</c>.</summary>
public sealed class CatalogCreateUploadUrlsResponse
{
    /// <summary><c>UploadUrls</c>.</summary>
    public IReadOnlyList<CatalogUploadUrlMetadata>? UploadUrls { get; set; }

    internal static unsafe CatalogCreateUploadUrlsResponse FromNative(PFCatalogCreateUploadUrlsResponse* source)
    {
        var value = new CatalogCreateUploadUrlsResponse();
        if (source->UploadUrls is not null && source->UploadUrlsCount != 0)
        {
            var list0 = new List<CatalogUploadUrlMetadata>((int)source->UploadUrlsCount);
            for (uint i0 = 0; i0 < source->UploadUrlsCount; i0++)
            {
                list0.Add(CatalogUploadUrlMetadata.FromNative(source->UploadUrls[i0]));
            }

            value.UploadUrls = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogCreateUploadUrlsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->UploadUrlsCount = (uint)(UploadUrls is null ? 0 : UploadUrls.Count);
        if (UploadUrls is not null && UploadUrls.Count > 0)
        {
            PFCatalogUploadUrlMetadata** array0 = arena.PointerArray<PFCatalogUploadUrlMetadata>(UploadUrls.Count);
            for (int i0 = 0; i0 < UploadUrls.Count; i0++)
            {
                PFCatalogUploadUrlMetadata* item0 = arena.Alloc<PFCatalogUploadUrlMetadata>(1);
                UploadUrls[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->UploadUrls = array0;
        }
    }
}

/// <summary>Projects <c>PFCatalogDeepLink</c>.</summary>
public sealed class CatalogDeepLink
{
    /// <summary><c>Platform</c>.</summary>
    public string? Platform { get; set; }

    /// <summary><c>Url</c>.</summary>
    public string? Url { get; set; }

    internal static unsafe CatalogDeepLink FromNative(PFCatalogDeepLink* source)
    {
        var value = new CatalogDeepLink();
        value.Platform = Utf8.ToString(source->Platform);
        value.Url = Utf8.ToString(source->Url);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogDeepLink* target, PlayFabArena arena)
    {
        *target = default;
        target->Platform = arena.String(Platform);
        target->Url = arena.String(Url);
    }
}

/// <summary>Projects <c>PFCatalogDeepLinkFormat</c>.</summary>
public sealed class CatalogDeepLinkFormat
{
    /// <summary><c>Format</c>.</summary>
    public string? Format { get; set; }

    /// <summary><c>Platform</c>.</summary>
    public string? Platform { get; set; }

    internal static unsafe CatalogDeepLinkFormat FromNative(PFCatalogDeepLinkFormat* source)
    {
        var value = new CatalogDeepLinkFormat();
        value.Format = Utf8.ToString(source->Format);
        value.Platform = Utf8.ToString(source->Platform);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogDeepLinkFormat* target, PlayFabArena arena)
    {
        *target = default;
        target->Format = arena.String(Format);
        target->Platform = arena.String(Platform);
    }
}

/// <summary>Projects <c>PFCatalogDeleteEntityItemReviewsRequest</c>.</summary>
public sealed class CatalogDeleteEntityItemReviewsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    internal static unsafe CatalogDeleteEntityItemReviewsRequest FromNative(PFCatalogDeleteEntityItemReviewsRequest* source)
    {
        var value = new CatalogDeleteEntityItemReviewsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogDeleteEntityItemReviewsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Entity is not null)
        {
            PFEntityKey* native2 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native2, arena);
            target->Entity = native2;
        }
    }
}

/// <summary>Projects <c>PFCatalogDeleteItemRequest</c>.</summary>
public sealed class CatalogDeleteItemRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogDeleteItemRequest FromNative(PFCatalogDeleteItemRequest* source)
    {
        var value = new CatalogDeleteItemRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogDeleteItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogDisplayPropertyIndexInfo</c>.</summary>
public sealed class CatalogDisplayPropertyIndexInfo
{
    /// <summary><c>Name</c>.</summary>
    public string? Name { get; set; }

    /// <summary><c>Type</c>.</summary>
    public CatalogDisplayPropertyType? Type { get; set; }

    internal static unsafe CatalogDisplayPropertyIndexInfo FromNative(PFCatalogDisplayPropertyIndexInfo* source)
    {
        var value = new CatalogDisplayPropertyIndexInfo();
        value.Name = Utf8.ToString(source->Name);
        value.Type = source->Type is null ? null : (CatalogDisplayPropertyType?)(*source->Type);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogDisplayPropertyIndexInfo* target, PlayFabArena arena)
    {
        *target = default;
        target->Name = arena.String(Name);
        target->Type = arena.Value((PFCatalogDisplayPropertyType?)Type);
    }
}

/// <summary>Projects <c>PFCatalogFileConfig</c>.</summary>
public sealed class CatalogFileConfig
{
    /// <summary><c>ContentTypes</c>.</summary>
    public IReadOnlyList<string>? ContentTypes { get; set; }

    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    internal static unsafe CatalogFileConfig FromNative(PFCatalogFileConfig* source)
    {
        var value = new CatalogFileConfig();
        if (source->ContentTypes is not null && source->ContentTypesCount != 0)
        {
            var list0 = new List<string>((int)source->ContentTypesCount);
            for (uint i0 = 0; i0 < source->ContentTypesCount; i0++)
            {
                list0.Add(Utf8.ToString(source->ContentTypes[i0]) ?? string.Empty);
            }

            value.ContentTypes = list0;
        }
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list2 = new List<string>((int)source->TagsCount);
            for (uint i2 = 0; i2 < source->TagsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->Tags[i2]) ?? string.Empty);
            }

            value.Tags = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogFileConfig* target, PlayFabArena arena)
    {
        *target = default;
        target->ContentTypesCount = (uint)(ContentTypes is null ? 0 : ContentTypes.Count);
        target->ContentTypes = arena.StringArray(ContentTypes);
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
    }
}

/// <summary>Projects <c>PFCatalogFilterOptions</c>.</summary>
public sealed class CatalogFilterOptions
{
    /// <summary><c>Filter</c>.</summary>
    public string? Filter { get; set; }

    /// <summary><c>IncludeAllItems</c>.</summary>
    public bool? IncludeAllItems { get; set; }

    internal static unsafe CatalogFilterOptions FromNative(PFCatalogFilterOptions* source)
    {
        var value = new CatalogFilterOptions();
        value.Filter = Utf8.ToString(source->Filter);
        value.IncludeAllItems = source->IncludeAllItems is null ? null : (bool?)(*source->IncludeAllItems != 0);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogFilterOptions* target, PlayFabArena arena)
    {
        *target = default;
        target->Filter = arena.String(Filter);
        target->IncludeAllItems = arena.Value(IncludeAllItems is null ? null : (IncludeAllItems.Value ? (byte?)1 : (byte?)0));
    }
}

/// <summary>Projects <c>PFCatalogGetCatalogConfigRequest</c>.</summary>
public sealed class CatalogGetCatalogConfigRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe CatalogGetCatalogConfigRequest FromNative(PFCatalogGetCatalogConfigRequest* source)
    {
        var value = new CatalogGetCatalogConfigRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetCatalogConfigRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetCatalogConfigResponse</c>.</summary>
public sealed class CatalogGetCatalogConfigResponse
{
    /// <summary><c>Config</c>.</summary>
    public CatalogCatalogConfig? Config { get; set; }

    internal static unsafe CatalogGetCatalogConfigResponse FromNative(PFCatalogGetCatalogConfigResponse* source)
    {
        var value = new CatalogGetCatalogConfigResponse();
        value.Config = source->Config is null ? null : CatalogCatalogConfig.FromNative(source->Config);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetCatalogConfigResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Config is not null)
        {
            PFCatalogCatalogConfig* native0 = arena.Alloc<PFCatalogCatalogConfig>(1);
            Config.WriteTo(native0, arena);
            target->Config = native0;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetDraftItemRequest</c>.</summary>
public sealed class CatalogGetDraftItemRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogGetDraftItemRequest FromNative(PFCatalogGetDraftItemRequest* source)
    {
        var value = new CatalogGetDraftItemRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetDraftItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogGetDraftItemResponse</c>.</summary>
public sealed class CatalogGetDraftItemResponse
{
    /// <summary><c>Item</c>.</summary>
    public CatalogCatalogItem? Item { get; set; }

    internal static unsafe CatalogGetDraftItemResponse FromNative(PFCatalogGetDraftItemResponse* source)
    {
        var value = new CatalogGetDraftItemResponse();
        value.Item = source->Item is null ? null : CatalogCatalogItem.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetDraftItemResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Item is not null)
        {
            PFCatalogCatalogItem* native0 = arena.Alloc<PFCatalogCatalogItem>(1);
            Item.WriteTo(native0, arena);
            target->Item = native0;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetDraftItemsRequest</c>.</summary>
public sealed class CatalogGetDraftItemsRequest
{
    /// <summary><c>AlternateIds</c>.</summary>
    public IReadOnlyList<CatalogCatalogAlternateId>? AlternateIds { get; set; }

    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int? Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Ids</c>.</summary>
    public IReadOnlyList<string>? Ids { get; set; }

    internal static unsafe CatalogGetDraftItemsRequest FromNative(PFCatalogGetDraftItemsRequest* source)
    {
        var value = new CatalogGetDraftItemsRequest();
        if (source->AlternateIds is not null && source->AlternateIdsCount != 0)
        {
            var list0 = new List<CatalogCatalogAlternateId>((int)source->AlternateIdsCount);
            for (uint i0 = 0; i0 < source->AlternateIdsCount; i0++)
            {
                list0.Add(CatalogCatalogAlternateId.FromNative(source->AlternateIds[i0]));
            }

            value.AlternateIds = list0;
        }
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count is null ? null : (int?)(*source->Count);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map4 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i4 = 0; i4 < source->CustomTagsCount; i4++)
            {
                map4[Utf8.ToString(source->CustomTags[i4].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i4].Value) ?? string.Empty;
            }

            value.CustomTags = map4;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->Ids is not null && source->IdsCount != 0)
        {
            var list7 = new List<string>((int)source->IdsCount);
            for (uint i7 = 0; i7 < source->IdsCount; i7++)
            {
                list7.Add(Utf8.ToString(source->Ids[i7]) ?? string.Empty);
            }

            value.Ids = list7;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetDraftItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->AlternateIdsCount = (uint)(AlternateIds is null ? 0 : AlternateIds.Count);
        if (AlternateIds is not null && AlternateIds.Count > 0)
        {
            PFCatalogCatalogAlternateId** array0 = arena.PointerArray<PFCatalogCatalogAlternateId>(AlternateIds.Count);
            for (int i0 = 0; i0 < AlternateIds.Count; i0++)
            {
                PFCatalogCatalogAlternateId* item0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
                AlternateIds[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->AlternateIds = array0;
        }
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = arena.Value(Count);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries4 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next4 = 0;
            foreach (KeyValuePair<string, string> pair4 in CustomTags)
            {
                entries4[next4].Key = arena.String(pair4.Key);
                entries4[next4].Value = arena.String(pair4.Value);
                next4++;
            }

            target->CustomTags = entries4;
        }
        if (Entity is not null)
        {
            PFEntityKey* native6 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native6, arena);
            target->Entity = native6;
        }
        target->IdsCount = (uint)(Ids is null ? 0 : Ids.Count);
        target->Ids = arena.StringArray(Ids);
    }
}

/// <summary>Projects <c>PFCatalogGetDraftItemsResponse</c>.</summary>
public sealed class CatalogGetDraftItemsResponse
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Items</c>.</summary>
    public IReadOnlyList<CatalogCatalogItem>? Items { get; set; }

    internal static unsafe CatalogGetDraftItemsResponse FromNative(PFCatalogGetDraftItemsResponse* source)
    {
        var value = new CatalogGetDraftItemsResponse();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        if (source->Items is not null && source->ItemsCount != 0)
        {
            var list1 = new List<CatalogCatalogItem>((int)source->ItemsCount);
            for (uint i1 = 0; i1 < source->ItemsCount; i1++)
            {
                list1.Add(CatalogCatalogItem.FromNative(source->Items[i1]));
            }

            value.Items = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetDraftItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->ItemsCount = (uint)(Items is null ? 0 : Items.Count);
        if (Items is not null && Items.Count > 0)
        {
            PFCatalogCatalogItem** array1 = arena.PointerArray<PFCatalogCatalogItem>(Items.Count);
            for (int i1 = 0; i1 < Items.Count; i1++)
            {
                PFCatalogCatalogItem* item1 = arena.Alloc<PFCatalogCatalogItem>(1);
                Items[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->Items = array1;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetEntityDraftItemsRequest</c>.</summary>
public sealed class CatalogGetEntityDraftItemsRequest
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Filter</c>.</summary>
    public string? Filter { get; set; }

    internal static unsafe CatalogGetEntityDraftItemsRequest FromNative(PFCatalogGetEntityDraftItemsRequest* source)
    {
        var value = new CatalogGetEntityDraftItemsRequest();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count;
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Filter = Utf8.ToString(source->Filter);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetEntityDraftItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = Count;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        if (Entity is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native4, arena);
            target->Entity = native4;
        }
        target->Filter = arena.String(Filter);
    }
}

/// <summary>Projects <c>PFCatalogGetEntityDraftItemsResponse</c>.</summary>
public sealed class CatalogGetEntityDraftItemsResponse
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Items</c>.</summary>
    public IReadOnlyList<CatalogCatalogItem>? Items { get; set; }

    internal static unsafe CatalogGetEntityDraftItemsResponse FromNative(PFCatalogGetEntityDraftItemsResponse* source)
    {
        var value = new CatalogGetEntityDraftItemsResponse();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        if (source->Items is not null && source->ItemsCount != 0)
        {
            var list1 = new List<CatalogCatalogItem>((int)source->ItemsCount);
            for (uint i1 = 0; i1 < source->ItemsCount; i1++)
            {
                list1.Add(CatalogCatalogItem.FromNative(source->Items[i1]));
            }

            value.Items = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetEntityDraftItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->ItemsCount = (uint)(Items is null ? 0 : Items.Count);
        if (Items is not null && Items.Count > 0)
        {
            PFCatalogCatalogItem** array1 = arena.PointerArray<PFCatalogCatalogItem>(Items.Count);
            for (int i1 = 0; i1 < Items.Count; i1++)
            {
                PFCatalogCatalogItem* item1 = arena.Alloc<PFCatalogCatalogItem>(1);
                Items[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->Items = array1;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetEntityItemReviewRequest</c>.</summary>
public sealed class CatalogGetEntityItemReviewRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogGetEntityItemReviewRequest FromNative(PFCatalogGetEntityItemReviewRequest* source)
    {
        var value = new CatalogGetEntityItemReviewRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetEntityItemReviewRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogGetEntityItemReviewResponse</c>.</summary>
public sealed class CatalogGetEntityItemReviewResponse
{
    /// <summary><c>Review</c>.</summary>
    public CatalogReview? Review { get; set; }

    internal static unsafe CatalogGetEntityItemReviewResponse FromNative(PFCatalogGetEntityItemReviewResponse* source)
    {
        var value = new CatalogGetEntityItemReviewResponse();
        value.Review = source->Review is null ? null : CatalogReview.FromNative(source->Review);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetEntityItemReviewResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Review is not null)
        {
            PFCatalogReview* native0 = arena.Alloc<PFCatalogReview>(1);
            Review.WriteTo(native0, arena);
            target->Review = native0;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetItemContainersRequest</c>.</summary>
public sealed class CatalogGetItemContainersRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogGetItemContainersRequest FromNative(PFCatalogGetItemContainersRequest* source)
    {
        var value = new CatalogGetItemContainersRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count;
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map3 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i3 = 0; i3 < source->CustomTagsCount; i3++)
            {
                map3[Utf8.ToString(source->CustomTags[i3].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i3].Value) ?? string.Empty;
            }

            value.CustomTags = map3;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemContainersRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = Count;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries3 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next3 = 0;
            foreach (KeyValuePair<string, string> pair3 in CustomTags)
            {
                entries3[next3].Key = arena.String(pair3.Key);
                entries3[next3].Value = arena.String(pair3.Value);
                next3++;
            }

            target->CustomTags = entries3;
        }
        if (Entity is not null)
        {
            PFEntityKey* native5 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native5, arena);
            target->Entity = native5;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogGetItemContainersResponse</c>.</summary>
public sealed class CatalogGetItemContainersResponse
{
    /// <summary><c>Containers</c>.</summary>
    public IReadOnlyList<CatalogCatalogItem>? Containers { get; set; }

    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    internal static unsafe CatalogGetItemContainersResponse FromNative(PFCatalogGetItemContainersResponse* source)
    {
        var value = new CatalogGetItemContainersResponse();
        if (source->Containers is not null && source->ContainersCount != 0)
        {
            var list0 = new List<CatalogCatalogItem>((int)source->ContainersCount);
            for (uint i0 = 0; i0 < source->ContainersCount; i0++)
            {
                list0.Add(CatalogCatalogItem.FromNative(source->Containers[i0]));
            }

            value.Containers = list0;
        }
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemContainersResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ContainersCount = (uint)(Containers is null ? 0 : Containers.Count);
        if (Containers is not null && Containers.Count > 0)
        {
            PFCatalogCatalogItem** array0 = arena.PointerArray<PFCatalogCatalogItem>(Containers.Count);
            for (int i0 = 0; i0 < Containers.Count; i0++)
            {
                PFCatalogCatalogItem* item0 = arena.Alloc<PFCatalogCatalogItem>(1);
                Containers[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Containers = array0;
        }
        target->ContinuationToken = arena.String(ContinuationToken);
    }
}

/// <summary>Projects <c>PFCatalogGetItemModerationStateRequest</c>.</summary>
public sealed class CatalogGetItemModerationStateRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogGetItemModerationStateRequest FromNative(PFCatalogGetItemModerationStateRequest* source)
    {
        var value = new CatalogGetItemModerationStateRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemModerationStateRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogGetItemModerationStateResponse</c>.</summary>
public sealed class CatalogGetItemModerationStateResponse
{
    /// <summary><c>State</c>.</summary>
    public CatalogModerationState? State { get; set; }

    internal static unsafe CatalogGetItemModerationStateResponse FromNative(PFCatalogGetItemModerationStateResponse* source)
    {
        var value = new CatalogGetItemModerationStateResponse();
        value.State = source->State is null ? null : CatalogModerationState.FromNative(source->State);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemModerationStateResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (State is not null)
        {
            PFCatalogModerationState* native0 = arena.Alloc<PFCatalogModerationState>(1);
            State.WriteTo(native0, arena);
            target->State = native0;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetItemPublishStatusRequest</c>.</summary>
public sealed class CatalogGetItemPublishStatusRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogGetItemPublishStatusRequest FromNative(PFCatalogGetItemPublishStatusRequest* source)
    {
        var value = new CatalogGetItemPublishStatusRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemPublishStatusRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogGetItemPublishStatusResponse</c>.</summary>
public sealed class CatalogGetItemPublishStatusResponse
{
    /// <summary><c>Result</c>.</summary>
    public CatalogPublishResult? Result { get; set; }

    /// <summary><c>StatusMessage</c>.</summary>
    public string? StatusMessage { get; set; }

    internal static unsafe CatalogGetItemPublishStatusResponse FromNative(PFCatalogGetItemPublishStatusResponse* source)
    {
        var value = new CatalogGetItemPublishStatusResponse();
        value.Result = source->Result is null ? null : (CatalogPublishResult?)(*source->Result);
        value.StatusMessage = Utf8.ToString(source->StatusMessage);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemPublishStatusResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->Result = arena.Value((PFCatalogPublishResult?)Result);
        target->StatusMessage = arena.String(StatusMessage);
    }
}

/// <summary>Projects <c>PFCatalogGetItemRequest</c>.</summary>
public sealed class CatalogGetItemRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogGetItemRequest FromNative(PFCatalogGetItemRequest* source)
    {
        var value = new CatalogGetItemRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogGetItemResponse</c>.</summary>
public sealed class CatalogGetItemResponse
{
    /// <summary><c>Item</c>.</summary>
    public CatalogCatalogItem? Item { get; set; }

    internal static unsafe CatalogGetItemResponse FromNative(PFCatalogGetItemResponse* source)
    {
        var value = new CatalogGetItemResponse();
        value.Item = source->Item is null ? null : CatalogCatalogItem.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Item is not null)
        {
            PFCatalogCatalogItem* native0 = arena.Alloc<PFCatalogCatalogItem>(1);
            Item.WriteTo(native0, arena);
            target->Item = native0;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetItemReviewSummaryRequest</c>.</summary>
public sealed class CatalogGetItemReviewSummaryRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogGetItemReviewSummaryRequest FromNative(PFCatalogGetItemReviewSummaryRequest* source)
    {
        var value = new CatalogGetItemReviewSummaryRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemReviewSummaryRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogGetItemReviewSummaryResponse</c>.</summary>
public sealed class CatalogGetItemReviewSummaryResponse
{
    /// <summary><c>LeastFavorableReview</c>.</summary>
    public CatalogReview? LeastFavorableReview { get; set; }

    /// <summary><c>MostFavorableReview</c>.</summary>
    public CatalogReview? MostFavorableReview { get; set; }

    /// <summary><c>Rating</c>.</summary>
    public CatalogRating? Rating { get; set; }

    /// <summary><c>ReviewsCount</c>.</summary>
    public int ReviewsCount { get; set; }

    internal static unsafe CatalogGetItemReviewSummaryResponse FromNative(PFCatalogGetItemReviewSummaryResponse* source)
    {
        var value = new CatalogGetItemReviewSummaryResponse();
        value.LeastFavorableReview = source->LeastFavorableReview is null ? null : CatalogReview.FromNative(source->LeastFavorableReview);
        value.MostFavorableReview = source->MostFavorableReview is null ? null : CatalogReview.FromNative(source->MostFavorableReview);
        value.Rating = source->Rating is null ? null : CatalogRating.FromNative(source->Rating);
        value.ReviewsCount = source->ReviewsCount;
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemReviewSummaryResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (LeastFavorableReview is not null)
        {
            PFCatalogReview* native0 = arena.Alloc<PFCatalogReview>(1);
            LeastFavorableReview.WriteTo(native0, arena);
            target->LeastFavorableReview = native0;
        }
        if (MostFavorableReview is not null)
        {
            PFCatalogReview* native1 = arena.Alloc<PFCatalogReview>(1);
            MostFavorableReview.WriteTo(native1, arena);
            target->MostFavorableReview = native1;
        }
        if (Rating is not null)
        {
            PFCatalogRating* native2 = arena.Alloc<PFCatalogRating>(1);
            Rating.WriteTo(native2, arena);
            target->Rating = native2;
        }
        target->ReviewsCount = ReviewsCount;
    }
}

/// <summary>Projects <c>PFCatalogGetItemReviewsRequest</c>.</summary>
public sealed class CatalogGetItemReviewsRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>OrderBy</c>.</summary>
    public string? OrderBy { get; set; }

    internal static unsafe CatalogGetItemReviewsRequest FromNative(PFCatalogGetItemReviewsRequest* source)
    {
        var value = new CatalogGetItemReviewsRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count;
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map3 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i3 = 0; i3 < source->CustomTagsCount; i3++)
            {
                map3[Utf8.ToString(source->CustomTags[i3].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i3].Value) ?? string.Empty;
            }

            value.CustomTags = map3;
        }
        value.Id = Utf8.ToString(source->Id);
        value.OrderBy = Utf8.ToString(source->OrderBy);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemReviewsRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = Count;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries3 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next3 = 0;
            foreach (KeyValuePair<string, string> pair3 in CustomTags)
            {
                entries3[next3].Key = arena.String(pair3.Key);
                entries3[next3].Value = arena.String(pair3.Value);
                next3++;
            }

            target->CustomTags = entries3;
        }
        target->Id = arena.String(Id);
        target->OrderBy = arena.String(OrderBy);
    }
}

/// <summary>Projects <c>PFCatalogGetItemReviewsResponse</c>.</summary>
public sealed class CatalogGetItemReviewsResponse
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Reviews</c>.</summary>
    public IReadOnlyList<CatalogReview>? Reviews { get; set; }

    internal static unsafe CatalogGetItemReviewsResponse FromNative(PFCatalogGetItemReviewsResponse* source)
    {
        var value = new CatalogGetItemReviewsResponse();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        if (source->Reviews is not null && source->ReviewsCount != 0)
        {
            var list1 = new List<CatalogReview>((int)source->ReviewsCount);
            for (uint i1 = 0; i1 < source->ReviewsCount; i1++)
            {
                list1.Add(CatalogReview.FromNative(source->Reviews[i1]));
            }

            value.Reviews = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemReviewsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->ReviewsCount = (uint)(Reviews is null ? 0 : Reviews.Count);
        if (Reviews is not null && Reviews.Count > 0)
        {
            PFCatalogReview** array1 = arena.PointerArray<PFCatalogReview>(Reviews.Count);
            for (int i1 = 0; i1 < Reviews.Count; i1++)
            {
                PFCatalogReview* item1 = arena.Alloc<PFCatalogReview>(1);
                Reviews[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->Reviews = array1;
        }
    }
}

/// <summary>Projects <c>PFCatalogGetItemsRequest</c>.</summary>
public sealed class CatalogGetItemsRequest
{
    /// <summary><c>AlternateIds</c>.</summary>
    public IReadOnlyList<CatalogCatalogAlternateId>? AlternateIds { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Ids</c>.</summary>
    public IReadOnlyList<string>? Ids { get; set; }

    internal static unsafe CatalogGetItemsRequest FromNative(PFCatalogGetItemsRequest* source)
    {
        var value = new CatalogGetItemsRequest();
        if (source->AlternateIds is not null && source->AlternateIdsCount != 0)
        {
            var list0 = new List<CatalogCatalogAlternateId>((int)source->AlternateIdsCount);
            for (uint i0 = 0; i0 < source->AlternateIdsCount; i0++)
            {
                list0.Add(CatalogCatalogAlternateId.FromNative(source->AlternateIds[i0]));
            }

            value.AlternateIds = list0;
        }
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        if (source->Ids is not null && source->IdsCount != 0)
        {
            var list5 = new List<string>((int)source->IdsCount);
            for (uint i5 = 0; i5 < source->IdsCount; i5++)
            {
                list5.Add(Utf8.ToString(source->Ids[i5]) ?? string.Empty);
            }

            value.Ids = list5;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->AlternateIdsCount = (uint)(AlternateIds is null ? 0 : AlternateIds.Count);
        if (AlternateIds is not null && AlternateIds.Count > 0)
        {
            PFCatalogCatalogAlternateId** array0 = arena.PointerArray<PFCatalogCatalogAlternateId>(AlternateIds.Count);
            for (int i0 = 0; i0 < AlternateIds.Count; i0++)
            {
                PFCatalogCatalogAlternateId* item0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
                AlternateIds[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->AlternateIds = array0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        if (Entity is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native4, arena);
            target->Entity = native4;
        }
        target->IdsCount = (uint)(Ids is null ? 0 : Ids.Count);
        target->Ids = arena.StringArray(Ids);
    }
}

/// <summary>Projects <c>PFCatalogGetItemsResponse</c>.</summary>
public sealed class CatalogGetItemsResponse
{
    /// <summary><c>Items</c>.</summary>
    public IReadOnlyList<CatalogCatalogItem>? Items { get; set; }

    internal static unsafe CatalogGetItemsResponse FromNative(PFCatalogGetItemsResponse* source)
    {
        var value = new CatalogGetItemsResponse();
        if (source->Items is not null && source->ItemsCount != 0)
        {
            var list0 = new List<CatalogCatalogItem>((int)source->ItemsCount);
            for (uint i0 = 0; i0 < source->ItemsCount; i0++)
            {
                list0.Add(CatalogCatalogItem.FromNative(source->Items[i0]));
            }

            value.Items = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogGetItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ItemsCount = (uint)(Items is null ? 0 : Items.Count);
        if (Items is not null && Items.Count > 0)
        {
            PFCatalogCatalogItem** array0 = arena.PointerArray<PFCatalogCatalogItem>(Items.Count);
            for (int i0 = 0; i0 < Items.Count; i0++)
            {
                PFCatalogCatalogItem* item0 = arena.Alloc<PFCatalogCatalogItem>(1);
                Items[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->Items = array0;
        }
    }
}

/// <summary>Projects <c>PFCatalogImage</c>.</summary>
public sealed class CatalogImage
{
    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Tag</c>.</summary>
    public string? Tag { get; set; }

    /// <summary><c>Type</c>.</summary>
    public string? Type { get; set; }

    /// <summary><c>Url</c>.</summary>
    public string? Url { get; set; }

    internal static unsafe CatalogImage FromNative(PFCatalogImage* source)
    {
        var value = new CatalogImage();
        value.Id = Utf8.ToString(source->Id);
        value.Tag = Utf8.ToString(source->Tag);
        value.Type = Utf8.ToString(source->Type);
        value.Url = Utf8.ToString(source->Url);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogImage* target, PlayFabArena arena)
    {
        *target = default;
        target->Id = arena.String(Id);
        target->Tag = arena.String(Tag);
        target->Type = arena.String(Type);
        target->Url = arena.String(Url);
    }
}

/// <summary>Projects <c>PFCatalogImageConfig</c>.</summary>
public sealed class CatalogImageConfig
{
    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    internal static unsafe CatalogImageConfig FromNative(PFCatalogImageConfig* source)
    {
        var value = new CatalogImageConfig();
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list0 = new List<string>((int)source->TagsCount);
            for (uint i0 = 0; i0 < source->TagsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->Tags[i0]) ?? string.Empty);
            }

            value.Tags = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogImageConfig* target, PlayFabArena arena)
    {
        *target = default;
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
    }
}

/// <summary>Projects <c>PFCatalogKeywordSet</c>.</summary>
public sealed class CatalogKeywordSet
{
    /// <summary><c>Values</c>.</summary>
    public IReadOnlyList<string>? Values { get; set; }

    internal static unsafe CatalogKeywordSet FromNative(PFCatalogKeywordSet* source)
    {
        var value = new CatalogKeywordSet();
        if (source->Values is not null && source->ValuesCount != 0)
        {
            var list0 = new List<string>((int)source->ValuesCount);
            for (uint i0 = 0; i0 < source->ValuesCount; i0++)
            {
                list0.Add(Utf8.ToString(source->Values[i0]) ?? string.Empty);
            }

            value.Values = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogKeywordSet* target, PlayFabArena arena)
    {
        *target = default;
        target->ValuesCount = (uint)(Values is null ? 0 : Values.Count);
        target->Values = arena.StringArray(Values);
    }
}

/// <summary>Projects <c>PFCatalogModerationState</c>.</summary>
public sealed class CatalogModerationState
{
    /// <summary><c>LastModifiedDate</c>.</summary>
    public DateTimeOffset? LastModifiedDate { get; set; }

    /// <summary><c>Reason</c>.</summary>
    public string? Reason { get; set; }

    /// <summary><c>Status</c>.</summary>
    public CatalogModerationStatus? Status { get; set; }

    internal static unsafe CatalogModerationState FromNative(PFCatalogModerationState* source)
    {
        var value = new CatalogModerationState();
        value.LastModifiedDate = source->LastModifiedDate is null ? null : (DateTimeOffset?)PlayFabTime.ToDateTimeOffset(*source->LastModifiedDate);
        value.Reason = Utf8.ToString(source->Reason);
        value.Status = source->Status is null ? null : (CatalogModerationStatus?)(*source->Status);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogModerationState* target, PlayFabArena arena)
    {
        *target = default;
        target->LastModifiedDate = arena.Value(LastModifiedDate is null ? null : (long?)PlayFabTime.ToUnixTime(LastModifiedDate.Value));
        target->Reason = arena.String(Reason);
        target->Status = arena.Value((PFCatalogModerationStatus?)Status);
    }
}

/// <summary>Projects <c>PFCatalogPermissions</c>.</summary>
public sealed class CatalogPermissions
{
    /// <summary><c>SegmentIds</c>.</summary>
    public IReadOnlyList<string>? SegmentIds { get; set; }

    internal static unsafe CatalogPermissions FromNative(PFCatalogPermissions* source)
    {
        var value = new CatalogPermissions();
        if (source->SegmentIds is not null && source->SegmentIdsCount != 0)
        {
            var list0 = new List<string>((int)source->SegmentIdsCount);
            for (uint i0 = 0; i0 < source->SegmentIdsCount; i0++)
            {
                list0.Add(Utf8.ToString(source->SegmentIds[i0]) ?? string.Empty);
            }

            value.SegmentIds = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogPermissions* target, PlayFabArena arena)
    {
        *target = default;
        target->SegmentIdsCount = (uint)(SegmentIds is null ? 0 : SegmentIds.Count);
        target->SegmentIds = arena.StringArray(SegmentIds);
    }
}

/// <summary>Projects <c>PFCatalogPublishDraftItemRequest</c>.</summary>
public sealed class CatalogPublishDraftItemRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ETag</c>.</summary>
    public string? ETag { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogPublishDraftItemRequest FromNative(PFCatalogPublishDraftItemRequest* source)
    {
        var value = new CatalogPublishDraftItemRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ETag = Utf8.ToString(source->ETag);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogPublishDraftItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->ETag = arena.String(ETag);
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogRating</c>.</summary>
public sealed class CatalogRating
{
    /// <summary><c>Average</c>.</summary>
    public float? Average { get; set; }

    /// <summary><c>Count1Star</c>.</summary>
    public int? Count1Star { get; set; }

    /// <summary><c>Count2Star</c>.</summary>
    public int? Count2Star { get; set; }

    /// <summary><c>Count3Star</c>.</summary>
    public int? Count3Star { get; set; }

    /// <summary><c>Count4Star</c>.</summary>
    public int? Count4Star { get; set; }

    /// <summary><c>Count5Star</c>.</summary>
    public int? Count5Star { get; set; }

    /// <summary><c>TotalCount</c>.</summary>
    public int? TotalCount { get; set; }

    internal static unsafe CatalogRating FromNative(PFCatalogRating* source)
    {
        var value = new CatalogRating();
        value.Average = source->Average is null ? null : (float?)(*source->Average);
        value.Count1Star = source->Count1Star is null ? null : (int?)(*source->Count1Star);
        value.Count2Star = source->Count2Star is null ? null : (int?)(*source->Count2Star);
        value.Count3Star = source->Count3Star is null ? null : (int?)(*source->Count3Star);
        value.Count4Star = source->Count4Star is null ? null : (int?)(*source->Count4Star);
        value.Count5Star = source->Count5Star is null ? null : (int?)(*source->Count5Star);
        value.TotalCount = source->TotalCount is null ? null : (int?)(*source->TotalCount);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogRating* target, PlayFabArena arena)
    {
        *target = default;
        target->Average = arena.Value(Average);
        target->Count1Star = arena.Value(Count1Star);
        target->Count2Star = arena.Value(Count2Star);
        target->Count3Star = arena.Value(Count3Star);
        target->Count4Star = arena.Value(Count4Star);
        target->Count5Star = arena.Value(Count5Star);
        target->TotalCount = arena.Value(TotalCount);
    }
}

/// <summary>Projects <c>PFCatalogRealMoneyPriceDetails</c>.</summary>
public sealed class CatalogRealMoneyPriceDetails
{
    /// <summary><c>AppleAppStorePrices</c>.</summary>
    public IReadOnlyDictionary<string, int>? AppleAppStorePrices { get; set; }

    /// <summary><c>GooglePlayPrices</c>.</summary>
    public IReadOnlyDictionary<string, int>? GooglePlayPrices { get; set; }

    /// <summary><c>MicrosoftStorePrices</c>.</summary>
    public IReadOnlyDictionary<string, int>? MicrosoftStorePrices { get; set; }

    /// <summary><c>NintendoEShopPrices</c>.</summary>
    public IReadOnlyDictionary<string, int>? NintendoEShopPrices { get; set; }

    /// <summary><c>PlayStationStorePrices</c>.</summary>
    public IReadOnlyDictionary<string, int>? PlayStationStorePrices { get; set; }

    /// <summary><c>SteamPrices</c>.</summary>
    public IReadOnlyDictionary<string, int>? SteamPrices { get; set; }

    internal static unsafe CatalogRealMoneyPriceDetails FromNative(PFCatalogRealMoneyPriceDetails* source)
    {
        var value = new CatalogRealMoneyPriceDetails();
        if (source->AppleAppStorePrices is not null && source->AppleAppStorePricesCount != 0)
        {
            var map0 = new Dictionary<string, int>((int)source->AppleAppStorePricesCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->AppleAppStorePricesCount; i0++)
            {
                map0[Utf8.ToString(source->AppleAppStorePrices[i0].Key) ?? string.Empty] = source->AppleAppStorePrices[i0].Value;
            }

            value.AppleAppStorePrices = map0;
        }
        if (source->GooglePlayPrices is not null && source->GooglePlayPricesCount != 0)
        {
            var map2 = new Dictionary<string, int>((int)source->GooglePlayPricesCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->GooglePlayPricesCount; i2++)
            {
                map2[Utf8.ToString(source->GooglePlayPrices[i2].Key) ?? string.Empty] = source->GooglePlayPrices[i2].Value;
            }

            value.GooglePlayPrices = map2;
        }
        if (source->MicrosoftStorePrices is not null && source->MicrosoftStorePricesCount != 0)
        {
            var map4 = new Dictionary<string, int>((int)source->MicrosoftStorePricesCount, StringComparer.Ordinal);
            for (uint i4 = 0; i4 < source->MicrosoftStorePricesCount; i4++)
            {
                map4[Utf8.ToString(source->MicrosoftStorePrices[i4].Key) ?? string.Empty] = source->MicrosoftStorePrices[i4].Value;
            }

            value.MicrosoftStorePrices = map4;
        }
        if (source->NintendoEShopPrices is not null && source->NintendoEShopPricesCount != 0)
        {
            var map6 = new Dictionary<string, int>((int)source->NintendoEShopPricesCount, StringComparer.Ordinal);
            for (uint i6 = 0; i6 < source->NintendoEShopPricesCount; i6++)
            {
                map6[Utf8.ToString(source->NintendoEShopPrices[i6].Key) ?? string.Empty] = source->NintendoEShopPrices[i6].Value;
            }

            value.NintendoEShopPrices = map6;
        }
        if (source->PlayStationStorePrices is not null && source->PlayStationStorePricesCount != 0)
        {
            var map8 = new Dictionary<string, int>((int)source->PlayStationStorePricesCount, StringComparer.Ordinal);
            for (uint i8 = 0; i8 < source->PlayStationStorePricesCount; i8++)
            {
                map8[Utf8.ToString(source->PlayStationStorePrices[i8].Key) ?? string.Empty] = source->PlayStationStorePrices[i8].Value;
            }

            value.PlayStationStorePrices = map8;
        }
        if (source->SteamPrices is not null && source->SteamPricesCount != 0)
        {
            var map10 = new Dictionary<string, int>((int)source->SteamPricesCount, StringComparer.Ordinal);
            for (uint i10 = 0; i10 < source->SteamPricesCount; i10++)
            {
                map10[Utf8.ToString(source->SteamPrices[i10].Key) ?? string.Empty] = source->SteamPrices[i10].Value;
            }

            value.SteamPrices = map10;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogRealMoneyPriceDetails* target, PlayFabArena arena)
    {
        *target = default;
        target->AppleAppStorePricesCount = (uint)(AppleAppStorePrices is null ? 0 : AppleAppStorePrices.Count);
        if (AppleAppStorePrices is not null && AppleAppStorePrices.Count > 0)
        {
            PFInt32DictionaryEntry* entries0 = arena.Alloc<PFInt32DictionaryEntry>(AppleAppStorePrices.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, int> pair0 in AppleAppStorePrices)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = pair0.Value;
                next0++;
            }

            target->AppleAppStorePrices = entries0;
        }
        target->GooglePlayPricesCount = (uint)(GooglePlayPrices is null ? 0 : GooglePlayPrices.Count);
        if (GooglePlayPrices is not null && GooglePlayPrices.Count > 0)
        {
            PFInt32DictionaryEntry* entries2 = arena.Alloc<PFInt32DictionaryEntry>(GooglePlayPrices.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, int> pair2 in GooglePlayPrices)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = pair2.Value;
                next2++;
            }

            target->GooglePlayPrices = entries2;
        }
        target->MicrosoftStorePricesCount = (uint)(MicrosoftStorePrices is null ? 0 : MicrosoftStorePrices.Count);
        if (MicrosoftStorePrices is not null && MicrosoftStorePrices.Count > 0)
        {
            PFInt32DictionaryEntry* entries4 = arena.Alloc<PFInt32DictionaryEntry>(MicrosoftStorePrices.Count);
            int next4 = 0;
            foreach (KeyValuePair<string, int> pair4 in MicrosoftStorePrices)
            {
                entries4[next4].Key = arena.String(pair4.Key);
                entries4[next4].Value = pair4.Value;
                next4++;
            }

            target->MicrosoftStorePrices = entries4;
        }
        target->NintendoEShopPricesCount = (uint)(NintendoEShopPrices is null ? 0 : NintendoEShopPrices.Count);
        if (NintendoEShopPrices is not null && NintendoEShopPrices.Count > 0)
        {
            PFInt32DictionaryEntry* entries6 = arena.Alloc<PFInt32DictionaryEntry>(NintendoEShopPrices.Count);
            int next6 = 0;
            foreach (KeyValuePair<string, int> pair6 in NintendoEShopPrices)
            {
                entries6[next6].Key = arena.String(pair6.Key);
                entries6[next6].Value = pair6.Value;
                next6++;
            }

            target->NintendoEShopPrices = entries6;
        }
        target->PlayStationStorePricesCount = (uint)(PlayStationStorePrices is null ? 0 : PlayStationStorePrices.Count);
        if (PlayStationStorePrices is not null && PlayStationStorePrices.Count > 0)
        {
            PFInt32DictionaryEntry* entries8 = arena.Alloc<PFInt32DictionaryEntry>(PlayStationStorePrices.Count);
            int next8 = 0;
            foreach (KeyValuePair<string, int> pair8 in PlayStationStorePrices)
            {
                entries8[next8].Key = arena.String(pair8.Key);
                entries8[next8].Value = pair8.Value;
                next8++;
            }

            target->PlayStationStorePrices = entries8;
        }
        target->SteamPricesCount = (uint)(SteamPrices is null ? 0 : SteamPrices.Count);
        if (SteamPrices is not null && SteamPrices.Count > 0)
        {
            PFInt32DictionaryEntry* entries10 = arena.Alloc<PFInt32DictionaryEntry>(SteamPrices.Count);
            int next10 = 0;
            foreach (KeyValuePair<string, int> pair10 in SteamPrices)
            {
                entries10[next10].Key = arena.String(pair10.Key);
                entries10[next10].Value = pair10.Value;
                next10++;
            }

            target->SteamPrices = entries10;
        }
    }
}

/// <summary>Projects <c>PFCatalogReportItemRequest</c>.</summary>
public sealed class CatalogReportItemRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>ConcernCategory</c>.</summary>
    public CatalogConcernCategory? ConcernCategory { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Reason</c>.</summary>
    public string? Reason { get; set; }

    internal static unsafe CatalogReportItemRequest FromNative(PFCatalogReportItemRequest* source)
    {
        var value = new CatalogReportItemRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        value.ConcernCategory = source->ConcernCategory is null ? null : (CatalogConcernCategory?)(*source->ConcernCategory);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        value.Reason = Utf8.ToString(source->Reason);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogReportItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->ConcernCategory = arena.Value((PFCatalogConcernCategory?)ConcernCategory);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        if (Entity is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native4, arena);
            target->Entity = native4;
        }
        target->Id = arena.String(Id);
        target->Reason = arena.String(Reason);
    }
}

/// <summary>Projects <c>PFCatalogReportItemReviewRequest</c>.</summary>
public sealed class CatalogReportItemReviewRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>ConcernCategory</c>.</summary>
    public CatalogConcernCategory? ConcernCategory { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>Reason</c>.</summary>
    public string? Reason { get; set; }

    /// <summary><c>ReviewId</c>.</summary>
    public string? ReviewId { get; set; }

    internal static unsafe CatalogReportItemReviewRequest FromNative(PFCatalogReportItemReviewRequest* source)
    {
        var value = new CatalogReportItemReviewRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        value.ConcernCategory = source->ConcernCategory is null ? null : (CatalogConcernCategory?)(*source->ConcernCategory);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ItemId = Utf8.ToString(source->ItemId);
        value.Reason = Utf8.ToString(source->Reason);
        value.ReviewId = Utf8.ToString(source->ReviewId);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogReportItemReviewRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->ConcernCategory = arena.Value((PFCatalogConcernCategory?)ConcernCategory);
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        if (Entity is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native4, arena);
            target->Entity = native4;
        }
        target->ItemId = arena.String(ItemId);
        target->Reason = arena.String(Reason);
        target->ReviewId = arena.String(ReviewId);
    }
}

/// <summary>Projects <c>PFCatalogReview</c>.</summary>
public sealed class CatalogReview
{
    /// <summary><c>CategoryRatings</c>.</summary>
    public IReadOnlyDictionary<string, int>? CategoryRatings { get; set; }

    /// <summary><c>HelpfulNegative</c>.</summary>
    public int HelpfulNegative { get; set; }

    /// <summary><c>HelpfulPositive</c>.</summary>
    public int HelpfulPositive { get; set; }

    /// <summary><c>IsInstalled</c>.</summary>
    public bool IsInstalled { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>ItemVersion</c>.</summary>
    public string? ItemVersion { get; set; }

    /// <summary><c>Locale</c>.</summary>
    public string? Locale { get; set; }

    /// <summary><c>Rating</c>.</summary>
    public int Rating { get; set; }

    /// <summary><c>ReviewerEntity</c>.</summary>
    public EntityKey? ReviewerEntity { get; set; }

    /// <summary><c>ReviewId</c>.</summary>
    public string? ReviewId { get; set; }

    /// <summary><c>ReviewText</c>.</summary>
    public string? ReviewText { get; set; }

    /// <summary><c>Submitted</c>.</summary>
    public DateTimeOffset Submitted { get; set; }

    /// <summary><c>Title</c>.</summary>
    public string? Title { get; set; }

    internal static unsafe CatalogReview FromNative(PFCatalogReview* source)
    {
        var value = new CatalogReview();
        if (source->CategoryRatings is not null && source->CategoryRatingsCount != 0)
        {
            var map0 = new Dictionary<string, int>((int)source->CategoryRatingsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CategoryRatingsCount; i0++)
            {
                map0[Utf8.ToString(source->CategoryRatings[i0].Key) ?? string.Empty] = source->CategoryRatings[i0].Value;
            }

            value.CategoryRatings = map0;
        }
        value.HelpfulNegative = source->HelpfulNegative;
        value.HelpfulPositive = source->HelpfulPositive;
        value.IsInstalled = source->IsInstalled != 0;
        value.ItemId = Utf8.ToString(source->ItemId);
        value.ItemVersion = Utf8.ToString(source->ItemVersion);
        value.Locale = Utf8.ToString(source->Locale);
        value.Rating = source->Rating;
        value.ReviewerEntity = source->ReviewerEntity is null ? null : EntityKey.FromNative(source->ReviewerEntity);
        value.ReviewId = Utf8.ToString(source->ReviewId);
        value.ReviewText = Utf8.ToString(source->ReviewText);
        value.Submitted = PlayFabTime.ToDateTimeOffset(source->Submitted);
        value.Title = Utf8.ToString(source->Title);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogReview* target, PlayFabArena arena)
    {
        *target = default;
        target->CategoryRatingsCount = (uint)(CategoryRatings is null ? 0 : CategoryRatings.Count);
        if (CategoryRatings is not null && CategoryRatings.Count > 0)
        {
            PFInt32DictionaryEntry* entries0 = arena.Alloc<PFInt32DictionaryEntry>(CategoryRatings.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, int> pair0 in CategoryRatings)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = pair0.Value;
                next0++;
            }

            target->CategoryRatings = entries0;
        }
        target->HelpfulNegative = HelpfulNegative;
        target->HelpfulPositive = HelpfulPositive;
        target->IsInstalled = IsInstalled ? (byte)1 : (byte)0;
        target->ItemId = arena.String(ItemId);
        target->ItemVersion = arena.String(ItemVersion);
        target->Locale = arena.String(Locale);
        target->Rating = Rating;
        if (ReviewerEntity is not null)
        {
            PFEntityKey* native9 = arena.Alloc<PFEntityKey>(1);
            ReviewerEntity.WriteTo(native9, arena);
            target->ReviewerEntity = native9;
        }
        target->ReviewId = arena.String(ReviewId);
        target->ReviewText = arena.String(ReviewText);
        target->Submitted = PlayFabTime.ToUnixTime(Submitted);
        target->Title = arena.String(Title);
    }
}

/// <summary>Projects <c>PFCatalogReviewConfig</c>.</summary>
public sealed class CatalogReviewConfig
{
    /// <summary><c>CategoryRatings</c>.</summary>
    public IReadOnlyList<CatalogCategoryRatingConfig>? CategoryRatings { get; set; }

    internal static unsafe CatalogReviewConfig FromNative(PFCatalogReviewConfig* source)
    {
        var value = new CatalogReviewConfig();
        if (source->CategoryRatings is not null && source->CategoryRatingsCount != 0)
        {
            var list0 = new List<CatalogCategoryRatingConfig>((int)source->CategoryRatingsCount);
            for (uint i0 = 0; i0 < source->CategoryRatingsCount; i0++)
            {
                list0.Add(CatalogCategoryRatingConfig.FromNative(source->CategoryRatings[i0]));
            }

            value.CategoryRatings = list0;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogReviewConfig* target, PlayFabArena arena)
    {
        *target = default;
        target->CategoryRatingsCount = (uint)(CategoryRatings is null ? 0 : CategoryRatings.Count);
        if (CategoryRatings is not null && CategoryRatings.Count > 0)
        {
            PFCatalogCategoryRatingConfig** array0 = arena.PointerArray<PFCatalogCategoryRatingConfig>(CategoryRatings.Count);
            for (int i0 = 0; i0 < CategoryRatings.Count; i0++)
            {
                PFCatalogCategoryRatingConfig* item0 = arena.Alloc<PFCatalogCategoryRatingConfig>(1);
                CategoryRatings[i0].WriteTo(item0, arena);
                array0[i0] = item0;
            }

            target->CategoryRatings = array0;
        }
    }
}

/// <summary>Projects <c>PFCatalogReviewItemRequest</c>.</summary>
public sealed class CatalogReviewItemRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Review</c>.</summary>
    public CatalogReview? Review { get; set; }

    internal static unsafe CatalogReviewItemRequest FromNative(PFCatalogReviewItemRequest* source)
    {
        var value = new CatalogReviewItemRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Id = Utf8.ToString(source->Id);
        value.Review = source->Review is null ? null : CatalogReview.FromNative(source->Review);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogReviewItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->Id = arena.String(Id);
        if (Review is not null)
        {
            PFCatalogReview* native5 = arena.Alloc<PFCatalogReview>(1);
            Review.WriteTo(native5, arena);
            target->Review = native5;
        }
    }
}

/// <summary>Projects <c>PFCatalogReviewTakedown</c>.</summary>
public sealed class CatalogReviewTakedown
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>ReviewId</c>.</summary>
    public string? ReviewId { get; set; }

    internal static unsafe CatalogReviewTakedown FromNative(PFCatalogReviewTakedown* source)
    {
        var value = new CatalogReviewTakedown();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        value.ItemId = Utf8.ToString(source->ItemId);
        value.ReviewId = Utf8.ToString(source->ReviewId);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogReviewTakedown* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->ItemId = arena.String(ItemId);
        target->ReviewId = arena.String(ReviewId);
    }
}

/// <summary>Projects <c>PFCatalogSearchItemsRequest</c>.</summary>
public sealed class CatalogSearchItemsRequest
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Count</c>.</summary>
    public int Count { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>Filter</c>.</summary>
    public string? Filter { get; set; }

    /// <summary><c>Language</c>.</summary>
    public string? Language { get; set; }

    /// <summary><c>OrderBy</c>.</summary>
    public string? OrderBy { get; set; }

    /// <summary><c>Search</c>.</summary>
    public string? Search { get; set; }

    /// <summary><c>Select</c>.</summary>
    public string? Select { get; set; }

    /// <summary><c>Store</c>.</summary>
    public CatalogStoreReference? Store { get; set; }

    internal static unsafe CatalogSearchItemsRequest FromNative(PFCatalogSearchItemsRequest* source)
    {
        var value = new CatalogSearchItemsRequest();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        value.Count = source->Count;
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map2 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i2 = 0; i2 < source->CustomTagsCount; i2++)
            {
                map2[Utf8.ToString(source->CustomTags[i2].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i2].Value) ?? string.Empty;
            }

            value.CustomTags = map2;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.Filter = Utf8.ToString(source->Filter);
        value.Language = Utf8.ToString(source->Language);
        value.OrderBy = Utf8.ToString(source->OrderBy);
        value.Search = Utf8.ToString(source->Search);
        value.Select = Utf8.ToString(source->Select);
        value.Store = source->Store is null ? null : CatalogStoreReference.FromNative(source->Store);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogSearchItemsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->Count = Count;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries2 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next2 = 0;
            foreach (KeyValuePair<string, string> pair2 in CustomTags)
            {
                entries2[next2].Key = arena.String(pair2.Key);
                entries2[next2].Value = arena.String(pair2.Value);
                next2++;
            }

            target->CustomTags = entries2;
        }
        if (Entity is not null)
        {
            PFEntityKey* native4 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native4, arena);
            target->Entity = native4;
        }
        target->Filter = arena.String(Filter);
        target->Language = arena.String(Language);
        target->OrderBy = arena.String(OrderBy);
        target->Search = arena.String(Search);
        target->Select = arena.String(Select);
        if (Store is not null)
        {
            PFCatalogStoreReference* native10 = arena.Alloc<PFCatalogStoreReference>(1);
            Store.WriteTo(native10, arena);
            target->Store = native10;
        }
    }
}

/// <summary>Projects <c>PFCatalogSearchItemsResponse</c>.</summary>
public sealed class CatalogSearchItemsResponse
{
    /// <summary><c>ContinuationToken</c>.</summary>
    public string? ContinuationToken { get; set; }

    /// <summary><c>Items</c>.</summary>
    public IReadOnlyList<CatalogCatalogItem>? Items { get; set; }

    internal static unsafe CatalogSearchItemsResponse FromNative(PFCatalogSearchItemsResponse* source)
    {
        var value = new CatalogSearchItemsResponse();
        value.ContinuationToken = Utf8.ToString(source->ContinuationToken);
        if (source->Items is not null && source->ItemsCount != 0)
        {
            var list1 = new List<CatalogCatalogItem>((int)source->ItemsCount);
            for (uint i1 = 0; i1 < source->ItemsCount; i1++)
            {
                list1.Add(CatalogCatalogItem.FromNative(source->Items[i1]));
            }

            value.Items = list1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogSearchItemsResponse* target, PlayFabArena arena)
    {
        *target = default;
        target->ContinuationToken = arena.String(ContinuationToken);
        target->ItemsCount = (uint)(Items is null ? 0 : Items.Count);
        if (Items is not null && Items.Count > 0)
        {
            PFCatalogCatalogItem** array1 = arena.PointerArray<PFCatalogCatalogItem>(Items.Count);
            for (int i1 = 0; i1 < Items.Count; i1++)
            {
                PFCatalogCatalogItem* item1 = arena.Alloc<PFCatalogCatalogItem>(1);
                Items[i1].WriteTo(item1, arena);
                array1[i1] = item1;
            }

            target->Items = array1;
        }
    }
}

/// <summary>Projects <c>PFCatalogSetItemModerationStateRequest</c>.</summary>
public sealed class CatalogSetItemModerationStateRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Reason</c>.</summary>
    public string? Reason { get; set; }

    /// <summary><c>Status</c>.</summary>
    public CatalogModerationStatus? Status { get; set; }

    internal static unsafe CatalogSetItemModerationStateRequest FromNative(PFCatalogSetItemModerationStateRequest* source)
    {
        var value = new CatalogSetItemModerationStateRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Id = Utf8.ToString(source->Id);
        value.Reason = Utf8.ToString(source->Reason);
        value.Status = source->Status is null ? null : (CatalogModerationStatus?)(*source->Status);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogSetItemModerationStateRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        target->Id = arena.String(Id);
        target->Reason = arena.String(Reason);
        target->Status = arena.Value((PFCatalogModerationStatus?)Status);
    }
}

/// <summary>Projects <c>PFCatalogStoreDetails</c>.</summary>
public sealed class CatalogStoreDetails
{
    /// <summary><c>FilterOptions</c>.</summary>
    public CatalogFilterOptions? FilterOptions { get; set; }

    /// <summary><c>Permissions</c>.</summary>
    public CatalogPermissions? Permissions { get; set; }

    /// <summary><c>PriceOptionsOverride</c>.</summary>
    public CatalogCatalogPriceOptionsOverride? PriceOptionsOverride { get; set; }

    internal static unsafe CatalogStoreDetails FromNative(PFCatalogStoreDetails* source)
    {
        var value = new CatalogStoreDetails();
        value.FilterOptions = source->FilterOptions is null ? null : CatalogFilterOptions.FromNative(source->FilterOptions);
        value.Permissions = source->Permissions is null ? null : CatalogPermissions.FromNative(source->Permissions);
        value.PriceOptionsOverride = source->PriceOptionsOverride is null ? null : CatalogCatalogPriceOptionsOverride.FromNative(source->PriceOptionsOverride);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogStoreDetails* target, PlayFabArena arena)
    {
        *target = default;
        if (FilterOptions is not null)
        {
            PFCatalogFilterOptions* native0 = arena.Alloc<PFCatalogFilterOptions>(1);
            FilterOptions.WriteTo(native0, arena);
            target->FilterOptions = native0;
        }
        if (Permissions is not null)
        {
            PFCatalogPermissions* native1 = arena.Alloc<PFCatalogPermissions>(1);
            Permissions.WriteTo(native1, arena);
            target->Permissions = native1;
        }
        if (PriceOptionsOverride is not null)
        {
            PFCatalogCatalogPriceOptionsOverride* native2 = arena.Alloc<PFCatalogCatalogPriceOptionsOverride>(1);
            PriceOptionsOverride.WriteTo(native2, arena);
            target->PriceOptionsOverride = native2;
        }
    }
}

/// <summary>Projects <c>PFCatalogStoreReference</c>.</summary>
public sealed class CatalogStoreReference
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    internal static unsafe CatalogStoreReference FromNative(PFCatalogStoreReference* source)
    {
        var value = new CatalogStoreReference();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        value.Id = Utf8.ToString(source->Id);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogStoreReference* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->Id = arena.String(Id);
    }
}

/// <summary>Projects <c>PFCatalogSubmitItemReviewVoteRequest</c>.</summary>
public sealed class CatalogSubmitItemReviewVoteRequest
{
    /// <summary><c>AlternateId</c>.</summary>
    public CatalogCatalogAlternateId? AlternateId { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Entity</c>.</summary>
    public EntityKey? Entity { get; set; }

    /// <summary><c>ItemId</c>.</summary>
    public string? ItemId { get; set; }

    /// <summary><c>ReviewId</c>.</summary>
    public string? ReviewId { get; set; }

    /// <summary><c>Vote</c>.</summary>
    public CatalogHelpfulnessVote? Vote { get; set; }

    internal static unsafe CatalogSubmitItemReviewVoteRequest FromNative(PFCatalogSubmitItemReviewVoteRequest* source)
    {
        var value = new CatalogSubmitItemReviewVoteRequest();
        value.AlternateId = source->AlternateId is null ? null : CatalogCatalogAlternateId.FromNative(source->AlternateId);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        value.Entity = source->Entity is null ? null : EntityKey.FromNative(source->Entity);
        value.ItemId = Utf8.ToString(source->ItemId);
        value.ReviewId = Utf8.ToString(source->ReviewId);
        value.Vote = source->Vote is null ? null : (CatalogHelpfulnessVote?)(*source->Vote);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogSubmitItemReviewVoteRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (AlternateId is not null)
        {
            PFCatalogCatalogAlternateId* native0 = arena.Alloc<PFCatalogCatalogAlternateId>(1);
            AlternateId.WriteTo(native0, arena);
            target->AlternateId = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
        if (Entity is not null)
        {
            PFEntityKey* native3 = arena.Alloc<PFEntityKey>(1);
            Entity.WriteTo(native3, arena);
            target->Entity = native3;
        }
        target->ItemId = arena.String(ItemId);
        target->ReviewId = arena.String(ReviewId);
        target->Vote = arena.Value((PFCatalogHelpfulnessVote?)Vote);
    }
}

/// <summary>Projects <c>PFCatalogTakedownItemReviewsRequest</c>.</summary>
public sealed class CatalogTakedownItemReviewsRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Reviews</c>.</summary>
    public IReadOnlyList<CatalogReviewTakedown>? Reviews { get; set; }

    internal static unsafe CatalogTakedownItemReviewsRequest FromNative(PFCatalogTakedownItemReviewsRequest* source)
    {
        var value = new CatalogTakedownItemReviewsRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        if (source->Reviews is not null && source->ReviewsCount != 0)
        {
            var list2 = new List<CatalogReviewTakedown>((int)source->ReviewsCount);
            for (uint i2 = 0; i2 < source->ReviewsCount; i2++)
            {
                list2.Add(CatalogReviewTakedown.FromNative(source->Reviews[i2]));
            }

            value.Reviews = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogTakedownItemReviewsRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        target->ReviewsCount = (uint)(Reviews is null ? 0 : Reviews.Count);
        if (Reviews is not null && Reviews.Count > 0)
        {
            PFCatalogReviewTakedown** array2 = arena.PointerArray<PFCatalogReviewTakedown>(Reviews.Count);
            for (int i2 = 0; i2 < Reviews.Count; i2++)
            {
                PFCatalogReviewTakedown* item2 = arena.Alloc<PFCatalogReviewTakedown>(1);
                Reviews[i2].WriteTo(item2, arena);
                array2[i2] = item2;
            }

            target->Reviews = array2;
        }
    }
}

/// <summary>Projects <c>PFCatalogUpdateCatalogConfigRequest</c>.</summary>
public sealed class CatalogUpdateCatalogConfigRequest
{
    /// <summary><c>Config</c>.</summary>
    public CatalogCatalogConfig? Config { get; set; }

    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    internal static unsafe CatalogUpdateCatalogConfigRequest FromNative(PFCatalogUpdateCatalogConfigRequest* source)
    {
        var value = new CatalogUpdateCatalogConfigRequest();
        value.Config = source->Config is null ? null : CatalogCatalogConfig.FromNative(source->Config);
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map1 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i1 = 0; i1 < source->CustomTagsCount; i1++)
            {
                map1[Utf8.ToString(source->CustomTags[i1].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i1].Value) ?? string.Empty;
            }

            value.CustomTags = map1;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogUpdateCatalogConfigRequest* target, PlayFabArena arena)
    {
        *target = default;
        if (Config is not null)
        {
            PFCatalogCatalogConfig* native0 = arena.Alloc<PFCatalogCatalogConfig>(1);
            Config.WriteTo(native0, arena);
            target->Config = native0;
        }
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries1 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next1 = 0;
            foreach (KeyValuePair<string, string> pair1 in CustomTags)
            {
                entries1[next1].Key = arena.String(pair1.Key);
                entries1[next1].Value = arena.String(pair1.Value);
                next1++;
            }

            target->CustomTags = entries1;
        }
    }
}

/// <summary>Projects <c>PFCatalogUpdateDraftItemRequest</c>.</summary>
public sealed class CatalogUpdateDraftItemRequest
{
    /// <summary><c>CustomTags</c>.</summary>
    public IReadOnlyDictionary<string, string>? CustomTags { get; set; }

    /// <summary><c>Item</c>.</summary>
    public CatalogCatalogItem? Item { get; set; }

    /// <summary><c>Publish</c>.</summary>
    public bool Publish { get; set; }

    internal static unsafe CatalogUpdateDraftItemRequest FromNative(PFCatalogUpdateDraftItemRequest* source)
    {
        var value = new CatalogUpdateDraftItemRequest();
        if (source->CustomTags is not null && source->CustomTagsCount != 0)
        {
            var map0 = new Dictionary<string, string>((int)source->CustomTagsCount, StringComparer.Ordinal);
            for (uint i0 = 0; i0 < source->CustomTagsCount; i0++)
            {
                map0[Utf8.ToString(source->CustomTags[i0].Key) ?? string.Empty] = Utf8.ToString(source->CustomTags[i0].Value) ?? string.Empty;
            }

            value.CustomTags = map0;
        }
        value.Item = source->Item is null ? null : CatalogCatalogItem.FromNative(source->Item);
        value.Publish = source->Publish != 0;
        return value;
    }

    internal unsafe void WriteTo(PFCatalogUpdateDraftItemRequest* target, PlayFabArena arena)
    {
        *target = default;
        target->CustomTagsCount = (uint)(CustomTags is null ? 0 : CustomTags.Count);
        if (CustomTags is not null && CustomTags.Count > 0)
        {
            PFStringDictionaryEntry* entries0 = arena.Alloc<PFStringDictionaryEntry>(CustomTags.Count);
            int next0 = 0;
            foreach (KeyValuePair<string, string> pair0 in CustomTags)
            {
                entries0[next0].Key = arena.String(pair0.Key);
                entries0[next0].Value = arena.String(pair0.Value);
                next0++;
            }

            target->CustomTags = entries0;
        }
        if (Item is not null)
        {
            PFCatalogCatalogItem* native2 = arena.Alloc<PFCatalogCatalogItem>(1);
            Item.WriteTo(native2, arena);
            target->Item = native2;
        }
        target->Publish = Publish ? (byte)1 : (byte)0;
    }
}

/// <summary>Projects <c>PFCatalogUpdateDraftItemResponse</c>.</summary>
public sealed class CatalogUpdateDraftItemResponse
{
    /// <summary><c>Item</c>.</summary>
    public CatalogCatalogItem? Item { get; set; }

    internal static unsafe CatalogUpdateDraftItemResponse FromNative(PFCatalogUpdateDraftItemResponse* source)
    {
        var value = new CatalogUpdateDraftItemResponse();
        value.Item = source->Item is null ? null : CatalogCatalogItem.FromNative(source->Item);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogUpdateDraftItemResponse* target, PlayFabArena arena)
    {
        *target = default;
        if (Item is not null)
        {
            PFCatalogCatalogItem* native0 = arena.Alloc<PFCatalogCatalogItem>(1);
            Item.WriteTo(native0, arena);
            target->Item = native0;
        }
    }
}

/// <summary>Projects <c>PFCatalogUploadInfo</c>.</summary>
public sealed class CatalogUploadInfo
{
    /// <summary><c>FileName</c>.</summary>
    public string? FileName { get; set; }

    internal static unsafe CatalogUploadInfo FromNative(PFCatalogUploadInfo* source)
    {
        var value = new CatalogUploadInfo();
        value.FileName = Utf8.ToString(source->FileName);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogUploadInfo* target, PlayFabArena arena)
    {
        *target = default;
        target->FileName = arena.String(FileName);
    }
}

/// <summary>Projects <c>PFCatalogUploadUrlMetadata</c>.</summary>
public sealed class CatalogUploadUrlMetadata
{
    /// <summary><c>FileName</c>.</summary>
    public string? FileName { get; set; }

    /// <summary><c>Id</c>.</summary>
    public string? Id { get; set; }

    /// <summary><c>Url</c>.</summary>
    public string? Url { get; set; }

    internal static unsafe CatalogUploadUrlMetadata FromNative(PFCatalogUploadUrlMetadata* source)
    {
        var value = new CatalogUploadUrlMetadata();
        value.FileName = Utf8.ToString(source->FileName);
        value.Id = Utf8.ToString(source->Id);
        value.Url = Utf8.ToString(source->Url);
        return value;
    }

    internal unsafe void WriteTo(PFCatalogUploadUrlMetadata* target, PlayFabArena arena)
    {
        *target = default;
        target->FileName = arena.String(FileName);
        target->Id = arena.String(Id);
        target->Url = arena.String(Url);
    }
}

/// <summary>Projects <c>PFCatalogUserGeneratedContentSpecificConfig</c>.</summary>
public sealed class CatalogUserGeneratedContentSpecificConfig
{
    /// <summary><c>ContentTypes</c>.</summary>
    public IReadOnlyList<string>? ContentTypes { get; set; }

    /// <summary><c>Tags</c>.</summary>
    public IReadOnlyList<string>? Tags { get; set; }

    internal static unsafe CatalogUserGeneratedContentSpecificConfig FromNative(PFCatalogUserGeneratedContentSpecificConfig* source)
    {
        var value = new CatalogUserGeneratedContentSpecificConfig();
        if (source->ContentTypes is not null && source->ContentTypesCount != 0)
        {
            var list0 = new List<string>((int)source->ContentTypesCount);
            for (uint i0 = 0; i0 < source->ContentTypesCount; i0++)
            {
                list0.Add(Utf8.ToString(source->ContentTypes[i0]) ?? string.Empty);
            }

            value.ContentTypes = list0;
        }
        if (source->Tags is not null && source->TagsCount != 0)
        {
            var list2 = new List<string>((int)source->TagsCount);
            for (uint i2 = 0; i2 < source->TagsCount; i2++)
            {
                list2.Add(Utf8.ToString(source->Tags[i2]) ?? string.Empty);
            }

            value.Tags = list2;
        }
        return value;
    }

    internal unsafe void WriteTo(PFCatalogUserGeneratedContentSpecificConfig* target, PlayFabArena arena)
    {
        *target = default;
        target->ContentTypesCount = (uint)(ContentTypes is null ? 0 : ContentTypes.Count);
        target->ContentTypes = arena.StringArray(ContentTypes);
        target->TagsCount = (uint)(Tags is null ? 0 : Tags.Count);
        target->Tags = arena.StringArray(Tags);
    }
}
