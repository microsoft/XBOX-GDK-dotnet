// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFProfiles
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Threading;
using System.Threading.Tasks;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>The PlayFab Profiles service (<c>PFProfiles.h</c>).</summary>
public static unsafe class Profiles
{
    /// <summary>Calls <c>PFProfilesGetProfileAsync</c>.</summary>
    public static Task<ProfilesGetEntityProfileResponse> GetProfileAsync(
        PlayFabEntity entity,
        ProfilesGetEntityProfileRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFProfilesGetEntityProfileRequest* nativeRequest = arena.Alloc<PFProfilesGetEntityProfileRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFProfilesGetProfileAsync(context, (PFProfilesGetEntityProfileRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out ProfilesGetEntityProfileResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFProfilesGetProfileGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFProfilesGetEntityProfileResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFProfilesGetProfileGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = ProfilesGetEntityProfileResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFProfilesGetProfilesAsync</c>.</summary>
    public static Task<ProfilesGetEntityProfilesResponse> GetProfilesAsync(
        PlayFabEntity entity,
        ProfilesGetEntityProfilesRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFProfilesGetEntityProfilesRequest* nativeRequest = arena.Alloc<PFProfilesGetEntityProfilesRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFProfilesGetProfilesAsync(context, (PFProfilesGetEntityProfilesRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out ProfilesGetEntityProfilesResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFProfilesGetProfilesGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFProfilesGetEntityProfilesResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFProfilesGetProfilesGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = ProfilesGetEntityProfilesResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsAsync</c>.</summary>
    public static Task<ProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse> GetTitlePlayersFromMasterPlayerAccountIdsAsync(
        PlayFabEntity entity,
        ProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest* nativeRequest = arena.Alloc<PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsAsync(context, (PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out ProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = ProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFProfilesSetProfileLanguageAsync</c>.</summary>
    public static Task<ProfilesSetProfileLanguageResponse> SetProfileLanguageAsync(
        PlayFabEntity entity,
        ProfilesSetProfileLanguageRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFProfilesSetProfileLanguageRequest* nativeRequest = arena.Alloc<PFProfilesSetProfileLanguageRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFProfilesSetProfileLanguageAsync(context, (PFProfilesSetProfileLanguageRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out ProfilesSetProfileLanguageResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFProfilesSetProfileLanguageGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFProfilesSetProfileLanguageResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFProfilesSetProfileLanguageGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = ProfilesSetProfileLanguageResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFProfilesSetProfilePolicyAsync</c>.</summary>
    public static Task<ProfilesSetEntityProfilePolicyResponse> SetProfilePolicyAsync(
        PlayFabEntity entity,
        ProfilesSetEntityProfilePolicyRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFProfilesSetEntityProfilePolicyRequest* nativeRequest = arena.Alloc<PFProfilesSetEntityProfilePolicyRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFProfilesSetProfilePolicyAsync(context, (PFProfilesSetEntityProfilePolicyRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out ProfilesSetEntityProfilePolicyResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFProfilesSetProfilePolicyGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFProfilesSetEntityProfilePolicyResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFProfilesSetProfilePolicyGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = ProfilesSetEntityProfilePolicyResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }
}
