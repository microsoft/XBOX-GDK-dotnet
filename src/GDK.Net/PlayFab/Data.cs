// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFData
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Threading;
using System.Threading.Tasks;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>The PlayFab Data service (<c>PFData.h</c>).</summary>
public static unsafe class Data
{
    /// <summary>Calls <c>PFDataAbortFileUploadsAsync</c>.</summary>
    public static Task<DataAbortFileUploadsResponse> AbortFileUploadsAsync(
        PlayFabEntity entity,
        DataAbortFileUploadsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFDataAbortFileUploadsRequest* nativeRequest = arena.Alloc<PFDataAbortFileUploadsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFDataAbortFileUploadsAsync(context, (PFDataAbortFileUploadsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out DataAbortFileUploadsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFDataAbortFileUploadsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFDataAbortFileUploadsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFDataAbortFileUploadsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = DataAbortFileUploadsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFDataDeleteFilesAsync</c>.</summary>
    public static Task<DataDeleteFilesResponse> DeleteFilesAsync(
        PlayFabEntity entity,
        DataDeleteFilesRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFDataDeleteFilesRequest* nativeRequest = arena.Alloc<PFDataDeleteFilesRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFDataDeleteFilesAsync(context, (PFDataDeleteFilesRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out DataDeleteFilesResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFDataDeleteFilesGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFDataDeleteFilesResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFDataDeleteFilesGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = DataDeleteFilesResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFDataFinalizeFileUploadsAsync</c>.</summary>
    public static Task<DataFinalizeFileUploadsResponse> FinalizeFileUploadsAsync(
        PlayFabEntity entity,
        DataFinalizeFileUploadsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFDataFinalizeFileUploadsRequest* nativeRequest = arena.Alloc<PFDataFinalizeFileUploadsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFDataFinalizeFileUploadsAsync(context, (PFDataFinalizeFileUploadsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out DataFinalizeFileUploadsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFDataFinalizeFileUploadsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFDataFinalizeFileUploadsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFDataFinalizeFileUploadsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = DataFinalizeFileUploadsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFDataGetFilesAsync</c>.</summary>
    public static Task<DataGetFilesResponse> GetFilesAsync(
        PlayFabEntity entity,
        DataGetFilesRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFDataGetFilesRequest* nativeRequest = arena.Alloc<PFDataGetFilesRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFDataGetFilesAsync(context, (PFDataGetFilesRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out DataGetFilesResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFDataGetFilesGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFDataGetFilesResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFDataGetFilesGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = DataGetFilesResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFDataGetObjectsAsync</c>.</summary>
    public static Task<DataGetObjectsResponse> GetObjectsAsync(
        PlayFabEntity entity,
        DataGetObjectsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFDataGetObjectsRequest* nativeRequest = arena.Alloc<PFDataGetObjectsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFDataGetObjectsAsync(context, (PFDataGetObjectsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out DataGetObjectsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFDataGetObjectsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFDataGetObjectsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFDataGetObjectsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = DataGetObjectsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFDataInitiateFileUploadsAsync</c>.</summary>
    public static Task<DataInitiateFileUploadsResponse> InitiateFileUploadsAsync(
        PlayFabEntity entity,
        DataInitiateFileUploadsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFDataInitiateFileUploadsRequest* nativeRequest = arena.Alloc<PFDataInitiateFileUploadsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFDataInitiateFileUploadsAsync(context, (PFDataInitiateFileUploadsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out DataInitiateFileUploadsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFDataInitiateFileUploadsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFDataInitiateFileUploadsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFDataInitiateFileUploadsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = DataInitiateFileUploadsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFDataSetObjectsAsync</c>.</summary>
    public static Task<DataSetObjectsResponse> SetObjectsAsync(
        PlayFabEntity entity,
        DataSetObjectsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFDataSetObjectsRequest* nativeRequest = arena.Alloc<PFDataSetObjectsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFDataSetObjectsAsync(context, (PFDataSetObjectsRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out DataSetObjectsResponse value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFDataSetObjectsGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFDataSetObjectsResponse* result;
                        nuint used;
                        hr = NativePlayFab.PFDataSetObjectsGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = DataSetObjectsResponse.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }
}
