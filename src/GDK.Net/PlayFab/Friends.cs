// <auto-generated>
//   Generated by eng/playfab/emit_services.py from the Microsoft GDK 260404 PlayFab headers.
//   Source header family: PFFriends
//   Do not edit by hand: re-run `pwsh -NoProfile -File eng/generate-playfab.ps1`.
// </auto-generated>


#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Threading;
using System.Threading.Tasks;
using GDK.Net.Interop;
using GDK.Net.Users;

namespace GDK.Net.PlayFab;

/// <summary>The PlayFab Friends service (<c>PFFriends.h</c>).</summary>
public static unsafe class Friends
{
    /// <summary>Calls <c>PFFriendsClientAddFriendAsync</c>.</summary>
    public static Task<FriendsAddFriendResult> ClientAddFriendAsync(
        PlayFabEntity entity,
        FriendsClientAddFriendRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsClientAddFriendRequest* nativeRequest = arena.Alloc<PFFriendsClientAddFriendRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsClientAddFriendAsync(context, (PFFriendsClientAddFriendRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out FriendsAddFriendResult value) =>
                {
                    value = null!;
                    PFFriendsAddFriendResult native = default;
                    int hr = NativePlayFab.PFFriendsClientAddFriendGetResult((XAsyncBlock*)block, &native);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    value = FriendsAddFriendResult.FromNative(&native);
                    return HResult.SOk;
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFFriendsClientGetFriendsListAsync</c>.</summary>
    public static Task<FriendsGetFriendsListResult> ClientGetFriendsListAsync(
        PlayFabEntity entity,
        FriendsClientGetFriendsListRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsClientGetFriendsListRequest* nativeRequest = arena.Alloc<PFFriendsClientGetFriendsListRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsClientGetFriendsListAsync(context, (PFFriendsClientGetFriendsListRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out FriendsGetFriendsListResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFFriendsClientGetFriendsListGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFFriendsGetFriendsListResult* result;
                        nuint used;
                        hr = NativePlayFab.PFFriendsClientGetFriendsListGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = FriendsGetFriendsListResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFFriendsClientRemoveFriendAsync</c>.</summary>
    public static Task ClientRemoveFriendAsync(
        PlayFabEntity entity,
        FriendsClientRemoveFriendRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsClientRemoveFriendRequest* nativeRequest = arena.Alloc<PFFriendsClientRemoveFriendRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsClientRemoveFriendAsync(context, (PFFriendsClientRemoveFriendRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFFriendsClientSetFriendTagsAsync</c>.</summary>
    public static Task ClientSetFriendTagsAsync(
        PlayFabEntity entity,
        FriendsClientSetFriendTagsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsClientSetFriendTagsRequest* nativeRequest = arena.Alloc<PFFriendsClientSetFriendTagsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsClientSetFriendTagsAsync(context, (PFFriendsClientSetFriendTagsRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFFriendsServerAddFriendAsync</c>.</summary>
    public static Task ServerAddFriendAsync(
        PlayFabEntity entity,
        FriendsServerAddFriendRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsServerAddFriendRequest* nativeRequest = arena.Alloc<PFFriendsServerAddFriendRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsServerAddFriendAsync(context, (PFFriendsServerAddFriendRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFFriendsServerGetFriendsListAsync</c>.</summary>
    public static Task<FriendsGetFriendsListResult> ServerGetFriendsListAsync(
        PlayFabEntity entity,
        FriendsServerGetFriendsListRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsServerGetFriendsListRequest* nativeRequest = arena.Alloc<PFFriendsServerGetFriendsListRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsServerGetFriendsListAsync(context, (PFFriendsServerGetFriendsListRequest*)requestPointer, (XAsyncBlock*)block),
                static (IntPtr block, out FriendsGetFriendsListResult value) =>
                {
                    value = null!;
                    nuint size;
                    int hr = NativePlayFab.PFFriendsServerGetFriendsListGetResultSize((XAsyncBlock*)block, &size);
                    if (HResult.Failed(hr))
                    {
                        return hr;
                    }

                    IntPtr buffer = Marshal.AllocHGlobal(checked((int)size));
                    try
                    {
                        PFFriendsGetFriendsListResult* result;
                        nuint used;
                        hr = NativePlayFab.PFFriendsServerGetFriendsListGetResult((XAsyncBlock*)block, size, (void*)buffer, &result, &used);
                        if (HResult.Failed(hr))
                        {
                            return hr;
                        }

                        value = FriendsGetFriendsListResult.FromNative(result);
                        return HResult.SOk;
                    }
                    finally
                    {
                        Marshal.FreeHGlobal(buffer);
                    }
                },
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFFriendsServerRemoveFriendAsync</c>.</summary>
    public static Task ServerRemoveFriendAsync(
        PlayFabEntity entity,
        FriendsServerRemoveFriendRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsServerRemoveFriendRequest* nativeRequest = arena.Alloc<PFFriendsServerRemoveFriendRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsServerRemoveFriendAsync(context, (PFFriendsServerRemoveFriendRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }

    /// <summary>Calls <c>PFFriendsServerSetFriendTagsAsync</c>.</summary>
    public static Task ServerSetFriendTagsAsync(
        PlayFabEntity entity,
        FriendsServerSetFriendTagsRequest request,
        CancellationToken cancellationToken = default)
    {
        if (entity is null)
        {
            throw new ArgumentNullException(nameof(entity));
        }

        if (request is null)
        {
            throw new ArgumentNullException(nameof(request));
        }

        IntPtr context = entity.Handle;
        var arena = new PlayFabArena();
        try
        {
            PFFriendsServerSetFriendTagsRequest* nativeRequest = arena.Alloc<PFFriendsServerSetFriendTagsRequest>(1);
            request.WriteTo(nativeRequest, arena);
            IntPtr requestPointer = (IntPtr)nativeRequest;

            return PlayFabCall.InvokeAsync(
                arena,
                block => NativePlayFab.PFFriendsServerSetFriendTagsAsync(context, (PFFriendsServerSetFriendTagsRequest*)requestPointer, (XAsyncBlock*)block),
                static block => Native.XAsyncGetStatus((XAsyncBlock*)block, 0),
                cancellationToken);
        }
        catch
        {
            arena.Dispose();
            throw;
        }
    }
}
